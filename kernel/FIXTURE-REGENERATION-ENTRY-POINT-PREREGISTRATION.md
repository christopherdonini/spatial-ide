# Preregistration — fixture regeneration entry point

Authority: `DECISIONS-PENDING.md`, RULED 2026-09-24 — question round 17, item 6, applying entry 121's option (c).
Scope: `kernel/tests/support/mod.rs`, `kernel/tests/scale_pass.rs`, `kernel/tests/regenerate_fixture.rs`, `engine/src/fixture.rs`, `kernel/FIXTURES.md`; declared line budget, <= 150 non-generated.
Change: a new `#[ignore]`d test, `regenerate_parcels_5gb_fixture` (`kernel/tests/regenerate_fixture.rs`), regenerates `parcels-5gb.parquet`-shaped bytes at an explicit output path (env var `SPATIAL_REGEN_OUT`) outside the measurement harness — no watchdog, no canary, no silence ceiling — reusing the scale pass's exact generation spec (moved to `kernel/tests/support/mod.rs` as `spec_5gb()`, a pure move, so both the scale pass and this entry point share one definition) and the same writer, `spatial_engine::fixture::write_geoparquet_cancellable`. It refuses to overwrite an existing file, matching `scale_pass.rs`'s `generate()`. `kernel/FIXTURES.md`'s Regenerate row and its note move to the new command.
Tests+mutation: `regenerate_parcels_5gb_fixture` (refusal-to-overwrite path, tested via a small spec/tempfile, not the full 5 GB write) and `spec_5gb_matches_the_scale_pass` (equality assertion that the shared spec's fields equal the exact literals `kernel/FIXTURES.md` documents). Mutation for each recorded by a `RECORDED MUTATION` comment in `kernel/tests/regenerate_fixture.rs`.
Out-of-scope: no ADR, no security posture, no wire/SKP change, no stated guarantee changed; `kernel/SCALE-PASS-PREREGISTRATION.md` is not amended and the scale pass's measured behaviour does not change (`spec_5gb()`'s field values are unchanged by the move, proven by `spec_5gb_matches_the_scale_pass`).
