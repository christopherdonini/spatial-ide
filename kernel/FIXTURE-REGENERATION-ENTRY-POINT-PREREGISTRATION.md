# Preregistration — fixture regeneration entry point

Authority: `DECISIONS-PENDING.md`, RULED 2026-09-24 — question round 17, item 6, applying entry 121's option (c).
Scope: `kernel/tests/support/mod.rs`, `kernel/tests/scale_pass.rs`, `kernel/tests/regenerate_fixture.rs`, `engine/src/fixture.rs`, `kernel/FIXTURES.md`; declared line budget, <= 150 non-generated.
Change: a new `#[ignore]`d test, `regenerate_parcels_5gb_fixture` (`kernel/tests/regenerate_fixture.rs`), regenerates `parcels-5gb.parquet`-shaped bytes at an explicit output path (env var `SPATIAL_REGEN_OUT`) outside the measurement harness — no watchdog, no canary, no silence ceiling — reusing the scale pass's exact generation spec (moved to `kernel/tests/support/mod.rs` as `spec_5gb()`, a pure move, so both the scale pass and this entry point share one definition) and the same writer, `spatial_engine::fixture::write_geoparquet_cancellable`. It refuses to overwrite an existing file, matching `scale_pass.rs`'s `generate()`. `kernel/FIXTURES.md`'s Regenerate row and its note move to the new command.
Tests+mutation: `regenerate_parcels_5gb_fixture` (refusal-to-overwrite path, tested via a small spec/tempfile, not the full 5 GB write) and `spec_5gb_matches_the_scale_pass` (equality assertion that the shared spec's fields equal the exact literals `kernel/FIXTURES.md` documents). Mutation for each recorded by a `RECORDED MUTATION` comment in `kernel/tests/regenerate_fixture.rs`.
Out-of-scope: no ADR, no security posture, no wire/SKP change, no stated guarantee changed; `kernel/SCALE-PASS-PREREGISTRATION.md` is not amended and the scale pass's measured behaviour does not change (`spec_5gb()`'s field values are unchanged by the move, proven by `spec_5gb_matches_the_scale_pass`).

Amendment (class 6, budget deviation, Scope not edited): the declared figure was <= 150 non-generated lines. The landed diff (`git diff --stat 3e55d49 4157dfe`, excluding this preregistration file) is 212 insertions + 52 deletions = 264 changed lines across 5 files. Reason: `regenerate_fixture.rs` is a new file (140 lines, three tests each carrying doc comments naming the diagnosis and its mutation) and `scale_pass.rs`'s edit is a deletion-heavy pure move (50 lines: removing the local `spec_5gb()` and its six constants) rather than a net-small change; neither was foreseen at the five-line stage. Per §21c, this overrun closes the single-gate route; the piece stays in this five-line form per the same clause, and re-enters full (architect + reviewer) gating rather than the single reviewer gate. This is disclosed to the PR and the dispatching custodian rather than resolved by this worker, who has no architect-agent access from this worktree.

## Results

- Code commit: `4157dfe`. Diff `git diff --stat 3e55d49 4157dfe`.
- Byte-identity proof: `SPATIAL_REGEN_OUT=D:/spatial-ide/scratch/regen-proof/parcels-5gb.parquet cargo test --release -p spatial-kernel --test regenerate_fixture -- --ignored --exact regenerate_parcels_5gb_fixture --nocapture`, run to completion (`finished in 117.93s`), scratch file deleted afterward. Size 5,004,376,705 B (matches `kernel/FIXTURES.md`'s Size row). SHA-256 `5ae955c5fb7ee4d3f10436df271e19361d84f0845fbaa69dc60516f1b60c1788` (matches `kernel/FIXTURES.md`'s SHA-256 row). `target/slice-evidence/scale-pass/parcels-5gb.parquet` and `C:\Users\Public\spatial-ide-fixtures\parcels-5gb.parquet` both untouched, confirmed by size (5,004,376,705 B each) after the run.
- Checks: `cargo test -p spatial-kernel` (debug) rc 0 (clean rerun; two unrelated pre-existing timing flakes seen once each on earlier runs — `cancelling_mid_publish_leaves_no_bundle_and_no_staging_directory` (kernel/tests/publish.rs) and `cancel_reaches_the_producer_directly_and_is_observed_on_its_own_clock` (kernel/tests/skp_admission.rs) — both pass in isolation and are untouched by this diff). `node --test scripts/plan/*.test.mjs scripts/hooks/*.test.mjs` rc 0 (256 pass). `node scripts/plan/verify-quotes.mjs --show-cites` rc 0 PASS. `node scripts/plan/verify-cites.mjs` rc 0 PASS. `node scripts/plan/verify-test-claims.mjs` rc 0 PASS. `node scripts/plan/verify-mutation.mjs --base origin/main --head HEAD` rc 0 PASS, 3/3 new tests named. `node scripts/plan/verify.mjs --offline` rc 0 PASS.
