# N8 reopen reset -- five-line short form (`AUTONOMY.md` §21d), full gating

Committed before any change. Full gating (reviewer and architect): the piece touches the owner-side session-ended state, a property under test (`frontends/shell/OWNER-INVALIDATION-PREREGISTRATION.md`), which `AUTONOMY.md` §21a routes to both gates; the five-line form is kept on the round-16 item-1 shape (`DECISIONS-PENDING.md`, RULED 2026-09-18, item 1).

```
Authority: the human's word of 2026-09-22 (state/directives/2026-09-22-part-n-n8-and-sequencing.md, message 1; DECISIONS-PENDING.md RULED 2026-09-22); Part N run 1's N8 deviation (frontends/shell/MANUAL-WALKTHROUGH.md, Part N run); PLAN node briefa-p6-n8-reopen-reset
Scope: frontends/shell/src/App.tsx, the canvas or admission file the Zoom-to-layer investigation proves at fault, their unit tests, and frontends/shell/e2e/source-changed.mjs (a reopen route); <= 8 files; declared line budget <= 250 non-generated (insertions + deletions)
Change: a successful admission, including a same-file reopen after a source change, starts a fresh generation with the ended session's block and hover latch cleared, ordinary identification and Zoom to layer working; a failed reopen and any late callback or result from the ended generation neither revive the old generation nor mark the new one ended; a second source change after the reopen ends the new session; the misleading remount comment in App.tsx corrected
Tests+mutation: unit tests over the reset and the late-result guards, and an end-to-end reopen route driven through the real app (window.__SPATIAL_E2E__, not a helper with invented setters) asserting the four behaviours in Change; one recorded mutation per new test that fails it by name
Out-of-scope: no wire (protocol/skp untouched), no ADR text, no security surface, no renderer redesign, no watcher, no governance change; the owner-side guarantee is restored as stated, not changed
```

Budget: 244 of 250 non-generated lines across 3 files (git diff --numstat origin/main...HEAD, the form excluded); E2E evidence: frontends/shell/e2e/out/source-changed-1790108677871.json (reopen route, launched:true), frontends/shell/e2e/out/source-changed-1790108697242.json (default pre route, launched:false, same session); commit 804a64a.

Deviation before correction round 1 (template section 10 classes 2 and 6; reviewer and architect attempt 1 FAIL, state/gate-log.json records 105 and 106): Scope widens to frontends/shell/src/canvas/pick.ts for its stale remount comment (the same false mechanism); the declared budget rises to <= 400 non-generated lines across <= 4 files, the form excluded; full gating unchanged.

Correction round 1 (records 105-106): budget after this round 357 of 400 across 4 files (git diff --numstat origin/main...HEAD, the form excluded); the earlier Budget line (244 of 250, commit b90d5be) is superseded by this line, and its E2E runs were made from the uncommitted tree later committed as 7978334 and 804a64a, against the main checkout's shell binary (shared target); this round's E2E reports frontends/shell/e2e/out/source-changed-1790110828682.json (reopen route, launched:true) and frontends/shell/e2e/out/source-changed-1790110847061.json (default pre route, launched:false, same session) ran at commit 7734b29 with the same shared target; the claim this piece refutes is recorded at frontends/shell/OWNER-INVALIDATION-PREREGISTRATION.md:191-192 (append-only, not edited); commit 7734b29.

Closing commit under AI_DEVELOPMENT.md section A (reviewer attempt 2 FAIL on one mechanical item, state/gate-log.json record 107; architect attempt 2 PASS with two comment-text must-fixes, record 108; the root-cause note and this commit's verification in state/CUT-STATE.md, 2026-09-22): R1's recorded mutation observed (it fails by name, by exception) and its false exemption replaced; the two must-fixes and the reviewer's nits applied as comment text; the guard assertion now counts within reportViewportOutcome only, its mutation re-observed; the P3b record's cite frontends/shell/OWNER-INVALIDATION-PREREGISTRATION.md:916 names App.test.ts:1165-1166, true at main and found at :1179-1180 on this branch, shifted by this piece's insertions above it (that record is append-only and unedited); the refuted mechanism pinned frontends/shell/OWNER-INVALIDATION-PREREGISTRATION.md:191-192 @ 38e522f38217 sha256:a7df28523314c5776ece29966d4dbe37477a06047e2021b5aa1da93a51c16676.

Deviation before the late-result integration test (template section 10 classes 2 and 6; the human's word of 2026-09-22 that a source-text assertion alone does not meet the original request): Scope adds one new test file, frontends/shell/src/App.lateResult.test.tsx, which mounts the real App in jsdom with mocks only at the transport and render boundaries; the declared budget rises to <= 700 non-generated lines across <= 5 files, the form excluded; no product behaviour change is intended; full gating unchanged, a fresh gate count for this addition.

Late-result integration test: frontends/shell/src/App.lateResult.test.tsx at commit 8f0025d; mutations observed resolved-arm guard removed, rejected-arm guard removed (observed against both an engine.source_changed and an engine.connections_exhausted rejection), rejected-arm endSession call removed (the control test); budget after this addition 688 of 700 across 5 files (git diff --numstat origin/main...HEAD, the form excluded).

Deviation before the late-result correction round (template section 10 classes 2 and 6; reviewer and architect FAIL, state/gate-log.json records 109-110; authority state/directives/2026-09-22-n8-late-result-test.md and state/directives/2026-09-22-part-n-n8-and-sequencing.md, message 1, whose bar is that no late result may contaminate the new session): the Change widens to guard the scan-state and residency-status writes a late old-generation result can reach in both residency arms (the baseline issue path and the candidate session callbacks in App.tsx), keyed on the admitted dataset handle as the existing guards are; the late-result test mocks the SKP client, the stream manager, the candidate session and WorkingCanvas (boundaries App does not own), honours each producer's real stop contract, and covers the shipped candidate arm; the declared budget rises to <= 1000 non-generated lines across <= 5 files, the form excluded; full gating unchanged.

Late-result correction round (records 109-110): product guard App.tsx:1194 (onResidencyStatusChange), App.tsx:1207 (applyScanEvent), App.tsx:1323 (issueViewportQuery's .then), all at commit 74f0911; tests frontends/shell/src/App.lateResult.test.tsx at commit 654f171, mutations observed at 654f171: App.lateResult.test.tsx:344,377,400,425 (baseline, re-observed), :445 (baseline pre-cleanup, new), :535,550,570 (candidate control/success/rejection, new), :596 (candidate onResidencyStatusChange/applyScanEvent, two mutations, new); E2E reopen route frontends/shell/e2e/out/source-changed-1790119514349.json (launched) and default route frontends/shell/e2e/out/source-changed-1790119581809.json at 654f171; budget 1000 of 1000 across 5 files (git diff --numstat origin/main...HEAD, the form excluded); the earlier Late-result line's commit 8f0025d named the file state, its mutations ran on the pre-trim tree.

Landing after reviewer and architect attempt 2 PASS on the late-result round (state/gate-log.json records 111-112): comment and record text only at a687d0f (the header names what is guarded and the pre-existing residuals left for the human, an in-flight filter Apply in both arms and the dev-gated baseline arm's canvas-refusal, failure-terminal and resident-ceiling callbacks; two quotation marks dropped; one mutation record annotated). Budget deviation (template section 10 class 6, Scope not edited): 1009 of 1000 non-generated lines across 5 files, the nine lines over being that comment text. The late-result round's E2E runs were made at 16cc5cc, which differs from 654f171 by one test comment line; product and E2E files identical. Its test lines are cited at branch commits; if PR #100 is not merged with its commits, cite the tests by their titles in App.lateResult.test.tsx.

Deviation before the residual correction (template section 10 classes 2 and 6; the human's ruling of 2026-09-23, state/directives/2026-09-23-n8-residual-and-sequence.md, section 1, and DECISIONS-PENDING.md RULED 2026-09-23): the Change widens to guard, with the same admitted-handle key, the old generation's filter Apply completion (both arms) and the dev-gated baseline arm's canvas-refusal, failure-terminal and resident-ceiling callbacks; App.lateResult.test.tsx gains the full sequence through the real App wiring (the old generation ended by a source change, a successful reopen, then the old success, error and filter completion) asserting the new session's filter, warning, hover, scan and Cancel, and residency state, with positive controls for the new generation's own events; the declared budget rises to <= 1400 non-generated lines across <= 5 files, the form excluded; full gating, a fresh gate count for this correction.

Residual correction: guards App.tsx:848,854 (handleApplyFilter, both arms via issueQueryRef), :1242,1246 (candidate-arm dev queryWithFilter hook), :1297 (onFailureTerminal, baseline), :1380,1386 (baseline-arm dev queryWithFilter hook), :1595 (onCanvasRefusal, baseline), :1610 (onResidentCeilingExceeded, baseline), all at commit f2682c2; tests in App.lateResult.test.tsx by title A's ended -> reopen sequence: the late filter-Apply completion and the dev-gated baseline arm's canvas-refusal/failure-terminal/resident-ceiling callbacks do not contaminate B -- B's own equivalents still work (baseline arm); the real FilterPanel Apply button's own late completion, delivered in the pre-cleanup window after ended -> reopen, does not contaminate B (baseline arm); A's ended -> reopen sequence: the late filter-Apply completion (reissueUnrestricted) does not contaminate B -- B's own Apply still works (candidate arm); at commit e0adddc; mutations observed at e0adddc (six mutations: the two dev-hook filter-completion guards, the three baseline callback guards, and the real FilterPanel button's own handleApplyFilter guard, each reverted after observing); the three RECORDED MUTATION headers that paraphrased instead of quoting their own test's title verbatim were corrected, comment text only, at 36533a5 (verify:mutation FAIL then PASS); E2E reopen frontends/shell/e2e/out/source-changed-1790139538369.json (launched:true, R1-R4 PASS) and default frontends/shell/e2e/out/source-changed-1790139559209.json (launched:false, same session) at 36533a5; openPath is confirmed the same admission function AdmissionPanel.tsx's own Open button click runs (AdmissionPanel.tsx:290-296,306-315); budget 1305 of 1400 non-generated lines across 5 files (git diff --numstat origin/main...HEAD, the form excluded).

Deviation before the residual correction round 1 (template section 10 class 6, Scope not edited; reviewer and architect attempt 1 FAIL on the residual correction): the Change is unchanged -- the old Apply completion includes its error path, whose recovery and throttled re-issues are guarded with the same key; the declared budget rises to <= 1900 non-generated lines across <= 5 files, measured against the piece base 8380f34 (git diff --numstat 8380f34...HEAD, the form excluded), since PR #100 merged part of this branch to main.

Residual correction round 1: guards in handleApplyFilter's requestViewport closure, the candidate-arm dev queryWithFilter hook's requestViewport closure, onStreamOpened, onDeliveryCompleted, onBatchRows (all at commit e7a35ba); tests "the real FilterPanel Apply button's own late ERROR (refusal-recovery re-issue), delivered after ended -> reopen, does not reach B's own manager (baseline arm)", "the human's full ended -> reopen -> old SUCCESS sequence through reportViewportOutcome does not clear B's standing refusal (baseline arm)", "the human's full ended -> reopen -> old engine.source_changed REJECTION sequence through reportViewportOutcome does not write or end B (baseline arm)", "A's late onBatchRows/onDeliveryCompleted, delivered well after A's own effect cleanup has run, do not overwrite B's own in-flight scan state (baseline arm, item 7 probe)", "A's late ERROR (reissueUnrestricted's own refusal-recovery re-issue) through the candidate arm's own dev hook, delivered after ended -> reopen, does not reach B's own session (candidate arm)", "the human's full ended -> reopen -> old SUCCESS sequence through reportViewportOutcome does not clear B's standing refusal (candidate arm)", "the human's full ended -> reopen -> old engine.source_changed REJECTION sequence through reportViewportOutcome does not write or end B (candidate arm)" at commit 53b63fc; mutations observed at 53b63fc; E2E reopen frontends/shell/e2e/out/source-changed-1790142456009.json and default frontends/shell/e2e/out/source-changed-1790142503338.json, both launched, at commit 53b63fc; budget 1832 of 1900 across 5 files against 8380f34 (911 across 2 files against origin/main at 53b63fc); the earlier residual line's 1305 was measured against 8380f34.

Correction (self-caught, same round, before any gate): the closing line's two REJECTION-case test titles' OBSERVED text was copied from an earlier test's own recorded bytes rather than run against these tests; re-run and corrected at commit 9f2e8ef, both now reading "expected 'engine.source_changed' to be 'engine.no_covering_bbox' // Object.is equality"; the closing line's other citations (guards, the two SUCCESS-case texts, E2E, budget) are unaffected and were independently verified live before that line was written.
