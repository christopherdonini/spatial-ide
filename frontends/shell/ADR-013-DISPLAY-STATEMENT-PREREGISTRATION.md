# The equirectangular display statement in the describe summary -- five-line short form (`AUTONOMY.md` §21d)

Committed before any code, per the short-form discipline.

```
Authority: DECISIONS-PENDING.md RULED 2026-09-23 (late), entry 119 item (5) -- the human's word, verbatim in state/directives/2026-09-23-entry-119-and-map-studio.md; the gap recorded at entry 119 and first raised at entry 113 (3); AUTONOMY.md §21d
Scope: frontends/shell/src/admission/describeSummaryText.ts, frontends/shell/src/admission/describeSummaryText.test.ts, frontends/shell/src/admission/DescribeSummary.tsx, frontends/shell/src/skp/__tests__/fixtures.test.ts, frontends/shell/MANUAL-WALKTHROUGH.md (Part N, row N1 only); declared line budget <= 80 non-generated (insertions + deletions), measured by git diff --numstat origin/main...HEAD with this form excluded
Change: one pure function appended at the end of describeSummaryText.ts -- displayConventionLine(crs) returning crs.display_convention verbatim, or null when it is null -- and DescribeSummary.tsx rendering a "Display convention" row as the last row of its list, only when that result is non-null (appended below the existing rows, so no line cite into either file moves); DescribeSummary is what the admission panel already shows at open for every admitted dataset (frontends/shell/src/admission/AdmissionPanel.tsx:411), so the statement appears at open and in the describe summary with no new status surface; Part N's row N1 states the new row's expected text in place of its sentence that no component renders the field, so N1 can be re-sighted; the row label's wording is the human's to settle at that re-sight
Tests+mutation: "displayConventionLine renders crs.display_convention verbatim, and null when the dataset has none" -- mutation: the non-null return value replaced with a fixed paraphrase; plus one seam assertion in fixtures.test.ts that the real describe fixture's display_convention reaches displayConventionLine unchanged -- mutation: the function returning null unconditionally; the sentence is never retyped as a TypeScript literal (frontends/shell/src/skp/types.ts:93-99) -- the unit test uses a plainly-marked placeholder, as the existing sessionStatementLine test does
Out-of-scope: no ADR status line or amendment text (the acceptance is the human's click on a later PR); no security posture (ADR-020/ADR-009/ADR-021); no wire, control-plane or data-plane field, literal or message (protocol/skp/** untouched -- display_convention already exists on skp/0.3's DescribeResponse, frontends/shell/src/skp/types.ts:100); no stated guarantee or invariant changed; nothing added to the canvas status stack or over the map (the RULED 2026-09-23 (late) block, shell direction item (d)). Rendering an already-wired field is new user-visible behaviour, which crosses §21c's "no new user-visible behaviour" bound by construction (disclosed, not denied) -- so the architect gate is taken alongside the reviewer, as for PR #93, and the architect also states whether ADR-013 Amendment 1 item 5 is true of the build after this piece
```
