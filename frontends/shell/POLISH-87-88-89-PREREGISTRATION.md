# Preregistration — "filter-and-hover polish" (the human's directive of 2026-09-13): entries 87, 88, 89

*Drafted 2026-09-13 by the architect agent on the custodian's brief, from DECISIONS-PENDING entries 87 (with both appended findings; the 2026-09-13 diagnosis is the binding one), 88, 89, 75 and files read at `main`. Committed by the custodian on `polish/filter-and-hover` BEFORE any code. Append-only; an amendment written after any result has been seen must say so in its first line. The architect's three questions for the human are DECISIONS-PENDING entry 91; §2.3 (ii), §3 and §4.2 do not merge before the human's word on them.*

## §0. Disclosure

No pilot, no spike, no measurement informs this document. Every `file:line` is a read of `main` (e9900a3 or later) on 2026-09-13; a worker re-derives every cite it touches. §2's sub-cause is a **hypothesis with a discriminator**; the first thing the piece does is confirm it. If the discriminator points elsewhere, the worker STOPS and reports — amended, never quietly re-aimed. §3 and §4 change operator-visible behaviour and are **PROPOSED-PENDING-SIGHT**: neither merges before the human's word (§3 on entry 75 (3), §4 on the threshold value).

## §1. What this document may not claim

No timing figure, no p50/p95, no docs/08 row, no ADR-018 vocabulary (ADR-018 `:82-88`'s class-(a) discipline only). No SKP, MCP or wire change, and **no twelfth `skp.filter_*` code** (ADR-021 item 8, `ADR-021:93-99`). No new status sentence and no wording change to an existing one (the human owns wording). No ADR reopened, amended, or cited as settled; ADR-011 is not cited. No ADR-014 question is answered: the lease/permit **release ordering** reserved at `engine/src/pool.rs:72-78` is untouched, as is the pool's declared never-block/never-queue contract (`pool.rs:251-255`). ADR-006 class 1 (ephemeral) throughout. No macOS/Linux claim.

## §2. Entry 87 — a refused per-tile mint is reported truthfully and retried, never silently discarded

**2.1 Hypothesis.** Per-tile filter admission leases `LeaseClass::Maintenance` (`engine/src/predicate.rs:118-122`), whose capacity is 1 (`engine/src/pool.rs:99`), while the shell issues up to `MAX_IN_FLIGHT_TILE_STREAMS = 3` (`frontends/shell/src/canvas/tileGridConstants.ts:40`) concurrent `viewport_query` calls; `acquire` refuses rather than waits (`pool.rs:262-275`). A lost lease is mapped to `FilterError::RejectedByBinder` (`predicate.rs:118-122`, its doc `:240-242`) → `skp.filter_rejected_by_binder` (`kernel/src/skp.rs:749-753`), and the shell's bare `catch` (`frontends/shell/src/streaming/tileViewportStreamManager.ts:663-669`) deletes the tile's state with no log, no trace, no status, no requeue. The tile stays in the cover, never resident: `isFillComplete()` stays false.

**2.2 Discriminator, FIRST (test 1).** One run with a log line at that `catch` naming `err.skpError.code` and its `fields` (`SkpCallError`, `frontends/shell/src/skp/client.ts:24-31`): the refusals must be `skp.filter_rejected_by_binder` whose `detail` names the *lease*, not DuckDB's binder (the `detail` text is minted at `predicate.rs:120`). A `detail` naming a real binder refusal, or another code, stops the engine half for an amendment; the shell half (2.3 (i)/(iii)) proceeds either way.

**2.3 The rule — BOTH sites, split on the docs/02 module boundary (the engine owns its pool; the kernel only maps typed errors).**
 (i) **Shell, unconditional (docs/01 principle 8).** The `catch` logs one `logSessionEvent` line per refusal — `tile-stream-mint-refused <tileKey>: <code> <fields>` — and the two epoch-abandon branches (`tileViewportStreamManager.ts:671-680`) log theirs. A typed refusal is never discarded unnamed again.
 (ii) **Engine, the honest fix (principle 8: the reported cause must be the cause) — on the human's word (entry 91 (a)).** Per-request predicate admission stops sharing the whole-file-pass budget: a third declared lease class for admission work, sized so the admissions the binding can present concurrently cannot collide (≥ `MAX_STREAM_CONNECTIONS`), with `MAX_PHYSICAL_CONNECTIONS` (`pool.rs:102`) re-derived and the new ceiling declared at its site with the quantity it bounds (ADR-010 rule 6, `ADR-010:70`). A residual lease failure returns `EngineError::ConnectionsExhausted { class, capacity }` — **never** `RejectedByBinder`. **No wire change and no new code:** that variant already maps to `engine.connections_exhausted` (`kernel/src/skp.rs:677-680`) under SKP-V0's declared `engine.` + variant-name rule (`protocol/skp/SKP-V0.md:266`); ADR-021's eleven-code list is untouched. Nothing blocks, waits or queues in the engine (principle 7; `pool.rs:251-255` unchanged). The design is the candidate decision of the ADR-033 draft filed beside this file.
 (iii) **Shell, bounded requeue (principle 7, "never block the canvas" made honest).** On a code in a declared retryable set — initially `engine.connections_exhausted` alone, and until (ii) lands also `skp.filter_rejected_by_binder` when its `detail` names the lease — the tile returns to the queue as `queued` under its current epoch, so `drainQueueIfRoom` (`tileViewportStreamManager.ts:639-647`) retries it when a slot frees. Bounded: **at most one retry per drain per tile, and only while the tile is still in the current cover**; a tile that has left the view is dropped by the existing supersede path. Any other code keeps today's drop, now logged. No constant, ceiling, wording, enumeration or eviction change in the shell.

**2.4 Tests.** (1) The discriminator, recorded. (2) `tileViewportStreamManager.test.ts`: a `viewportQuery` rejecting with a retryable code → one log line and the tile back in the queue, issued by the next drain; the same tile refused twice → no more than one requeue per drain and no spin; any other code → dropped with one log line; both epoch branches log. (3) Engine (only if 2.3 (ii) lands): a Rust test in which N concurrent `AdmittedPredicate::admit` calls over one dataset, N = the declared admission ceiling, all succeed; and a test that a lease failure surfaces as `ConnectionsExhausted`, never as `RejectedByBinder`. (4) **`e2e/regression.mjs`: the suspended FIND′ assertion is REINSTATED unchanged** (`FILTER-84-85-PREREGISTRATION.md` §6 Amendment 1 (b); the suspension sites are `regression.mjs:1950-1964`) — under the filter and after the zoom-out, no PREMISE BROKEN record, every covering tile reaches a terminal, and the status settles to the within-budget sentence. Mutation: remove the requeue → PREMISE BROKEN reappears and the assertion fails by name. (5) K7 unchanged and PASS; `npm run verify` green; `engine/tests/connection_reuse.rs` and `pool.rs`'s class-bound tests updated only where the new class requires it.

**2.5 Declared.** Unchanged: every shell constant and ceiling; enumeration; eviction; the `fits` latch; all wording; `MAX_STREAM_CONNECTIONS`; the release ordering reserved to ADR-014. Changed and declared: one new engine lease class and its ceiling (if (ii) lands); the typed cause of a lease failure; a logged, once-per-drain requeue. **Invalidators:** a discriminator naming a real binder refusal (engine half stops); any change to the pool's blocking contract; any new `skp.*` code. **Falsification:** any test above failing; a requeue spin; K7 regressing; FIND′ passing on the mutated build.

## §3. Entry 88 — the blink per notch: standing-until-settle, only as a declared labelled state

**3.1 What the blink is.** Each wheel notch is its own camera burst: the mid-gesture rule clears a standing id (`frontends/shell/src/canvas/pickResolution.ts:131-143`, D2) and the settle re-pick restores it after `HOVER_REPICK_SETTLE_MS` (`hoverRepickConstants.ts:34`). A standing *refusal* already does not blink (`pickResolution.ts:138-140`), so only the id case is felt.

**3.2 The rule, and the honest shape.** "The last readout stays displayed, not re-asserted, until the settle re-pick replaces or clears it" displays an id at a camera nothing has confirmed. Unlabelled, that is the hazard ADR-010 rule 5 names by name — *"staleness is signalled, never silently served"* (`ADR-010:68`) — and docs/01 principle 8. Therefore: **stale-and-unlabelled is refused here.** Two admissible shapes only: **(a)** the readout stands with a *visible declared distinction* (a dimmed/marked `.hover-readout` state whose text or class differs from a confirmed id) until the settle replaces or clears it — which **is** entry 75 (3)'s "unconfirmed state", said plainly, and is the human's to open; or **(b)** today's clear stands. This piece builds (a) **only on the human's ruling reopening entry 75 (3)** (entry 91 (b)); the wording of any new marking is the human's. Filed as **Amendment 6 to `HOVER-REPICK-PREREGISTRATION.md`** (amending D2 and D6's caller), never an edit of that file.

**3.3 K6 (i)–(v) under (a).** (i), (iii), (iv), (v) hold unchanged, provided the marked state is textually distinguishable so `hoverReadoutId` (`regression.mjs:1141-1144`) cannot read it as a confirmed id. **(ii) changes and is the only change:** "no readout is ever an id without a confirming trace" becomes "no **confirmed** readout is ever an id without a confirming trace since the mark; a marked stale readout is not a confirmed readout" — a K6 contract change, reviewer-gated, and block-on-sight 13 still applies (the replacement keeps a case whose correct answer is a *different* id or an absence). New unit cases in `pickResolution.test.ts` for the mid-gesture decision's third return (mark, don't clear), and one `WorkingCanvas.test.ts` case that the marked state is always replaced or cleared at settle.

**3.4 Declared.** Unchanged: the settle mechanism, D1/D3/D4/D5/D11, both constants, the emission choke point, the threshold. **Invalidators:** any stale readout displayed without a visible distinction; the human declining to reopen 75 (3) (the piece ends at §3.2 (b), nothing built). **Falsification:** a marked readout that outlives its settle; K6 (iv) no longer failing on a retained id.

## §4. Entry 89 — the refusal threshold, and the hovering cursor

**4.1 The constant, today.** `SUB_PIXEL_PICK_REFUSAL_THRESHOLD_PX = 2` (`pickResolution.ts:45`), compared as `averageFeatureExtentWorldUnits * pixelsPerWorldUnit < THRESHOLD` (`:87-89`) against the **average** resident feature's own geometric extent (`:54-79`), with the style-independence limitation declared at `:34-43`. Raising it makes the named refusal appear **earlier** on zoom-out; nothing else about picking changes.

**4.2 The new declared value — PROPOSED-PENDING-SIGHT, the human sights it (entry 91 (c)).** `9`, with its basis stated at the constant: four times the **2.27 px** id-discrimination separation ADR-010 rule 6 records (`ADR-010:74`), carrying that figure's own two caveats verbatim (measured at 1:500 only, never verified at a second zoom; style-dependent, so a heavier symbol moves it). It is a **declared choice, not a measured or fitted constant** (ADR-010 rule 6, `:70`) — no claim is made that 9 px is where discrimination fails. **Recorded tension, for the human's sight:** entry 47's sharpened criterion (`DECISIONS-PENDING.md:1188-1194`) pulls the other way — an earlier refusal necessarily refuses at some cameras where the operator could still tell which feature is under the pointer. The trade is the human's.

**4.3 The cursor.** `Deck` is constructed with `controller: true` and **no `getCursor`** (`frontends/shell/src/canvas/WorkingCanvas.tsx:1723-1729`), so deck's default grab/grabbing hand shows while hovering. Declared: `getCursor` returns `"crosshair"` when no button is down and `"grabbing"` while dragging — exactly two values, no `"pointer"` (there is no click affordance to signal). The decision is a pure exported helper (`cursorForPointerState(isDragging)`), the file's established pattern.

**4.4 Tests.** (1) `pickResolution.test.ts`: the comparison at the new value — a feature extent just above and just below the line, and the existing cases re-derived from the constant, never from the literal `2`. (2) `cursorForPointerState` unit cases. (3) Seam: one `WorkingCanvas.test.ts`/E2E assertion that the canvas element's cursor is `crosshair` while hovering with no button down and `grabbing` during a drag. (4) **K6 re-run in full**, its zoom targets re-derived if the new threshold moves which camera crosses it; `K6_REFUSAL_TEXT` verbatim and unchanged. (5) K7 and FIND′ unchanged and PASS.

**4.5 Declared.** Unchanged: the averaging mechanic, the pick radius, the refusal's wording, every other constant. **Invalidators:** any test that hard-codes `2`; a cursor value that signals an affordance that does not exist. **Falsification:** the human sights the value and rejects it (a new value, re-sighted, no code change beyond the constant).

## §5. Block-on-sight

1. A typed refusal discarded with no session-log line anywhere in `mintAndStart` (principle 8).
2. A lease failure reported as a binder rejection, or any new `skp.filter_*` code (ADR-021 item 8; ADR-004).
3. Any change to the pool's never-block/never-queue contract or to lease/permit release ordering (ADR-014 reserved, `pool.rs:72-78`).
4. A requeue that is not bounded by "once per drain, still covered" — a retry spin, or a retry of a non-retryable code.
5. A stale readout displayed without a visible declared distinction (ADR-010 rule 5, `:68`).
6. §3's labelled state built before the human's ruling on entry 75 (3), or §4's value merged unsighted.
7. Any threshold or cursor value not declared at its constant with the quantity it bounds (ADR-010 rule 6).
8. Any performance number, anywhere.

## §6. Gates

**Architect** — ADR-028 Decision item 4 (`:35-36`); ADR-010 rules 2 (`:33`), 5 (`:68`), 6 (`:70`, `:74`); ADR-021 item 8; ADR-004 (no wire change, proven by the code set being unchanged); docs/01 principles 7 and 8; entry 75 (3)'s reopened item for §3; §5 checked one by one. **Reviewer** — the full diff; the K6 contract change (§3.3) and the threshold change (§4.2) each reviewer-gated in their own right; decisions pure and unit-tested. **Suites** — `npm run verify`; the regression suite once through the harness with **FIND′ reinstated** and **K6 re-run**; every mutation recorded. **Operator** — one walkthrough row per entry (87: the settled sentence under a filter; 88: the hover through a zoom gesture; 89: the refusal's onset and the cursor), committed with blank result logs and queued into the next sitting bundle (AI_DEVELOPMENT rule 11). Merge on the human's click.

## §7. Amendments

### Amendment 1 — 2026-09-13, made AFTER the piece's results were seen (two `npm run e2e:regression` runs against `polish/filter-and-hover`, the shell-side build at `8d9a72e`)

**(a) §2.2 discriminator: CONFIRMED, on both runs.** Run 1 (load-confounded by a concurrent 5 GB DuckDB simplification and a kernel build, per the custodian's own account): 378 `tile-stream-mint-refused` lines, 257 distinct tileKeys, 121 refused exactly twice, every one `skp.filter_rejected_by_binder` with `detail` naming the lease (`predicate.rs:120`'s own text), zero real binder refusals, none outside FIND′. Run 2 (quiet machine): 117 lines; per-tileKey repeat counts 26×1, 36×2, 2×3, 2×4, 1×5 — repeats above two are not a bound violation: FIND′ drives three separate zoom-out notches, each its own fresh candidacy under §2.3(iii)'s "per drain" (never per session) retry budget, so a tileKey legitimately earns a new one-shot retry each round. §2.1's hypothesis is CONFIRMED by the instrument; the engine half (ii) is the cause's actual fix, exactly as §2.1/§2.3(ii) said — the shell half was never claimed to resolve the capacity-1 lease contention alone, only to report it truthfully and mitigate it once.

**(b) FIND′ under the shell half alone: FAIL, by its own reinstated hard assertion, and NOT re-suspended.** Run 2 (quiet machine, uncofounded): `"FIND'/settled-partial-under-filter: ... .residency-status settled to the partial-view sentence ... 113 unminted tile query(ies) of 193"`. It cannot pass until (ii) lands (entry 91 (a), the human's ruling) — the assertion stands exactly as §2.4 test (4) reinstated it. The merge gate for the shell half alone is therefore: K6, K7, CURSOR′, the unit suite (`npm run verify`), and the §2.2 discriminator — all PASS/green. FIND′ is recorded FAILING for the held reason above; the custodian routes that fact to the human rather than amending anything to make it pass.

**(c) Run 1's K7-onward timeouts: machine load, not entry 87's logic.** K7, CURSOR′, FIND′'s own internal wait, B2′/B3′ and C2′/C3′ all timed out in run 1, coinciding with a 5 GB DuckDB simplification and a kernel build running beside the harness (the custodian's own account); zero `tile-stream-mint-refused` lines occurred before FIND′ in that run, so the timeouts are not attributed to this piece's requeue logic. Run 2 (quiet machine): K6, K7, CURSOR′, B2′/B3′, C2′/C3′ all PASS.

**(d) The `CURSOR'` E2E step.** Added to `e2e/regression.mjs` (not run by the worker that wrote it, run here) because `WorkingCanvas.test.ts`'s own unit seam cannot reach a real `Deck`/canvas element (no WebGL in jsdom); `cursorForPointerState`'s own two values are unit-tested directly in `pickResolution.test.ts`.

**(e) B2′/B3′ passing on run 2, stated as reported, not independently verified by this piece.** Run 2 got `engine.crs_undeclared` as expected rather than the code-mismatch anticipated at commit time; per the custodian's own account this is because `fix/no-crs-reaim` regenerated `no-crs-refused.parquet` in the shared fixture directory between the two runs — a cross-branch fixture dependency, recorded here as told, main's own drift being re-aimed on that separate branch.

**(f) Declared unchanged, confirmed.** `SUB_PIXEL_PICK_REFUSAL_THRESHOLD_PX` stays `2` (no threshold change). §3 (entry 88) untouched. No ceiling, enumeration, eviction, or wording change. Mutation, recorded: `isRetryableRefusal` forced to `return false` → three unit cases failed by name ("a retryable code (engine.connections_exhausted): one log line, the tile is back in the queue and issued by the next drain"; "the same tile refused twice: no more than one requeue per drain, and no spin"; "skp.filter_rejected_by_binder retries when its detail names the lease, not when it names a real binder refusal"); reverted, the suite green again (39/39). No number in this amendment is a performance figure — every count above is an event count or a declared bound, never a timing/perf claim (docs/01; ADR-018).

### Amendment 2 — 2026-09-14, written AFTER the gates (architect PASS with notes, reviewer FAIL with two blockers) and after the batched fix round they required

**(a) The merge, and the `latestMembership` substitution.** `origin/main` merged into `polish/filter-and-hover` (entry 66 (b): the enumerated cover replaced by the `coverMembershipFor` predicate, latched as `latestMembership`, with `drainQueueIfRoom` re-testing every queued tile against it at mint time). The reviewer's first blocker: this piece's own `currentCoveringKeys` field (the enumerated, windowed covering set) would have refused a retry for a tile that is still genuinely in view but sits outside the centred window past `MAX_COVERING_TILES` -- exactly the case entry 66 (b) exists to answer honestly. `currentCoveringKeys` is deleted; the bounded requeue's "still in view" test now reads `this.latestMembership` (`membership === null || membership.has(tileKey)`), the SAME geometric predicate entry 66 (b)'s own supersede keep-test and drain re-check use, never a second one. `requeuedTiles` (the per-candidacy one-shot retry budget) is now also cleared in `clearAll`/`stop`, matching `latestMembership`'s own sibling field for wholesale-invalidation hygiene.

**(b) The FIND′ assertion: the deletion in the earlier commit was an error, now corrected, and the counter is retry-aware.** The reviewer's second blocker: the worker's own comment claimed the suspended assertion was "REINSTATED, unchanged," but the counter beneath it was not retry-aware, so a build where every refused tile recovered on retry could still read as "unminted" by raw count -- the comment overstated what the code did. Fixed two ways: (i) the assertion itself is confirmed a HARD, unconditional `failures.push` (verified: no branch anywhere in `stepFind` reads `unminted` and chooses not to fail; the dead `records` array and its stale Amendment-1(b) comment are deleted); (ii) the counter is rebuilt from the SESSION LOG rather than the render trace (`tileMintOutcomesSince`, `e2e/regression.mjs`, renamed from `unmintedTileQueriesSince`) -- the render trace's `viewport_query`/`stream-issued` lines carry no tile key (unchanged, disclosed constraint), so the requeue's own retry re-firing `traceViewportQuery` inflated the old raw-count diagnostic. A new production line, `logMintRecovered` (`tileViewportStreamManager.ts`, fired ONLY for a tile that had spent its one retry and then reached a real stream -- never for an ordinary first-attempt success, so it adds no volume to the common case), is the one positive per-tile signal the session log carries; the new counter takes each distinct refused tile key's LAST event in the window (`"recovered"` = minted, `"refused"`/`"abandoned"` = did not) and asserts on the count of distinct tiles, never attempts. The step still FAILS on this build -- expected and honest, since the engine half (entry 91 (a), ADR-033) is not built and the shell-side requeue only mitigates the lease contention, it does not resolve it.

**(c) Amendment 1 (b)'s "113 unminted of 193" was a per-ATTEMPT count, inflated by this piece's own retries -- recomputed offline.** Reworked from run 2's own session log (`session-1789332432.log`, still on disk): 67 distinct tile keys were refused at least once (matching the per-tileKey figure Amendment 1 (a) already gives: 26×1 + 36×2 + 2×3 + 2×4 + 1×5 = 117 refusal lines over 67 distinct tiles). That run predates `logMintRecovered` (built in this fix round), so "did a refused tile go on to mint" was reconstructed offline from `candidate-tile-terminal <tileKey>` lines (a stream that later terminates necessarily minted) within a window after the first refusal, stable across every window tried from 10 s to 60 s (10 s/15 s/20 s/30 s/60 s all agree): 35 of the 67 distinct refused tiles reached a terminal, 32 did not. **Both numbers, and what each counts:** "113 unminted tile query(ies) of 193" (Amendment 1 (b), the OLD per-attempt render-trace count: every `viewport_query` attempt minus every `stream-issued`, so a tile refused-then-recovered was counted as inflating the deficit) vs. "32 unminted of 67 distinct tiles ever refused" (this amendment, the retry-aware per-tile reconstruction: a recovered tile is not counted at all). Neither is a timing or performance figure -- both are event/tile counts (ADR-018; docs/01).

**(d) The `MAX_QUEUED_TILES` guard.** The bounded requeue's push into `queue` is now gated on `this.queue.length < MAX_QUEUED_TILES` too, alongside the existing not-stopped/retryable/not-already-retried/still-in-view conditions -- a retryable refusal arriving when the queue already sits at its declared ceiling keeps today's drop (still logged), never a bypass of that ceiling. Unit-tested (`MAX_QUEUED_TILES` already at capacity: the retryable refusal is dropped, not requeued`).

**(e) `dataPlaneAttach` handling.** `dataPlaneAttach()` in `mintAndStart` sat outside any `try`/`catch` -- a rejection there (between a granted ticket and a live stream) left the tile `"issuing"` forever, an unhandled rejection, the minted ticket never cancelled. Now caught: named exactly like any other refusal (`logMintRefused` -- this binding command never throws an `SkpCallError`, so this always logs the declared `"unknown"` code), the already-minted ticket is cancelled (best-effort, itself logged if the cancel is rejected), and the tile is dropped and the queue re-drained. Unit-tested.

**(f) Declared operator-visible consequence, stated plainly.** A requeued tile is now outstanding work for slightly longer than the old immediate drop left it -- under over-budget-without-headroom, the residency status may therefore read a paused/stalled reading where the old immediate-drop behaviour would already have settled to a partial reading. Not a new status sentence and not a wording change (docs/01 owns wording unchanged); a declared consequence of the requeue existing at all, named here rather than discovered later.

**(g) Block-on-sight, recorded here (not in §5's committed text -- amendments only).** `LEASE_REFUSAL_DETAIL_SUBSTRING`'s temporariness is now declared at its own site (`REMOVE WHEN ADR-033's engine half lands (entry 91 (a))`): block on sight any later change that keeps this substring match alive once `EngineError::ConnectionsExhausted` is minted directly (entry 91 (a) lands) -- at that point the substring match and its constant are dead code, not merely obsolete, and their removal is owed, not optional.

**(h) Wording.** The requeue's own bound is per CANDIDACY (a tileKey's own admission cycle, cleared at each fresh candidacy and in `clearAll`/`stop`), tighter than the earlier "per drain" phrasing -- corrected at `requeuedTiles`'s own doc comment and at the call site. `logMintRefused` now states its own volume bound (tiles × zoom-out notches, the same bound every other per-tile trace/log line here already carries) and that it is fire-and-forget, never an ADR-004 data-hot-path concern (a binding-local diagnostic, ADR-010 rule 7).
