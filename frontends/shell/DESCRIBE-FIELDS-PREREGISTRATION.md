# Describe-summary field rendering -- five-line short form (`AUTONOMY.md` §21d)

Committed before any code, per the short-form discipline.

```
Authority: DECISIONS-PENDING.md RULED 2026-09-18, question round 16, item 1 (the human's word, "Render before Part N"); the sight first recorded at entry 111 (1); AUTONOMY.md §21d
Scope: frontends/shell/src/admission/describeSummaryText.ts, frontends/shell/src/admission/describeSummaryText.test.ts, frontends/shell/src/admission/DescribeSummary.tsx, PLAN.yaml; declared line budget <= 40 non-generated (insertions + deletions)
Change: two pure functions appended to describeSummaryText.ts -- crsProvenanceLine(crs) returning `${crs.provenance}, ${crs.axis_provenance}`, and sessionStatementLine(identity) returning identity.session_statement verbatim or null -- and DescribeSummary.tsx rendering a "CRS provenance" row after the existing License row, plus a "Session identity" row rendered only when sessionStatementLine's result is non-null
Tests+mutation: "crsProvenanceLine renders crs.provenance and crs.axis_provenance verbatim, comma-separated" -- mutation: the two fields swapped in the function body; "sessionStatementLine renders identity.session_statement verbatim, and null when the identity is not session-ordinal" -- mutation: the non-null return value replaced with a fixed paraphrase
Out-of-scope: no ADR status line or amendment; no security posture (ADR-020/ADR-009/ADR-021); no wire, control-plane or data-plane field, literal or message (protocol/skp/** untouched -- the three fields already exist on skp/0.3's DescribeResponse, frontends/shell/src/skp/types.ts:89,92,124); no stated guarantee or invariant changed. Rendering three already-wire fields is new user-visible behaviour, which crosses §21c's "no new user-visible behaviour" bound by construction (disclosed, not denied) -- so per §21b's mid-piece clause the architect gate is taken alongside the reviewer, this five-line form kept, mirroring cut/briefa-p3b-test-names's shape.
```

Amendment: class 6, budget deviation, Scope not edited. Budget: 98 of 40 lines across 4 files (`git diff --numstat origin/main...HEAD` over non-generated files, the form excluded: PLAN.yaml 19+1, DescribeSummary.tsx 11+1, describeSummaryText.test.ts 47+0, describeSummaryText.ts 19+0 -- 96 insertions + 2 deletions = 98); section 21c's user-visible bound crossed by construction, declared above. The full-shape IdentityInfo/CrsInfo fixture objects the second new test requires and the mirrored PLAN node's full field set were undercounted when the 40-line figure was declared. `AUTONOMY.md` §21c's own bound (<= 150 lines, <= 8 files) is not crossed; the single-gate route stays open per §21b's mid-piece clause.
Amendment 2: class 3, cite pins -- frontends/shell/src/skp/types.ts:89 @ cb516ba4e2f3 sha256:c6725741b15ca316448fb2c736ff7999b2fd67b2a1bf5be92b7f2b8276e68dd3, :92 @ cb516ba4e2f3 sha256:8bd454bb1a26e9e10d9a96b35dcb6167e4c950f0648dff2ebcf0a5ce7a22fded, :124 @ cb516ba4e2f3 sha256:94c9c62dfcac8a9a3d5e10b088b9fa3a43fa2892382c2ed5289b5bcb96f43cc7 (each line's hash from git show cb516ba:frontends/shell/src/skp/types.ts | sed -n 'Np' | sha256sum); the Authority line's quoted option label elides its trailing " (Recommended)", the ruling being cited by round and item; no claim changes.
Amendment 3: class 2, one file outside the declared Scope touched -- frontends/shell/src/skp/__tests__/fixtures.test.ts gains two assertions from the real describe fixture (the reviewer's attempt-1 B1); Scope not edited. Final figure after this round: 110 lines (107 insertions + 3 deletions) across 5 files, git diff --numstat origin/main...HEAD over non-generated files, this form excluded; class 6's figure of Amendment 1 is superseded by this line.
Amendment 4: class 2, a second file outside the declared Scope touched -- frontends/shell/MANUAL-WALKTHROUGH.md:945, one cite token corrected (fixtures.test.ts:185 -> :186) because the seam import the gates ordered shifted that file by one line (the reviewer's attempt-2 B1); DescribeSummary.tsx restored to append-below-only; Scope not edited. Final figure after this round: 111 lines (107 insertions + 4 deletions) across 6 files, git diff --numstat origin/main...HEAD over non-generated files, this form excluded; Amendment 3's figure is superseded by this line.
Amendment 5: class 3, the reference form of Amendment 4 corrected -- its bare line reference into frontends/shell/MANUAL-WALKTHROUGH.md (a file the same commit edited, round 14's rule) reads frontends/shell/MANUAL-WALKTHROUGH.md Part N, row N3; Amendment 4's classes are 2 + 3 (a Scope deviation and a cite correction); no figure or claim changes (the architect's attempt-3 R2 and the reviewer's attempt-3 B1, applied as an appended line, the form being append-only). The unit-test comment's coverage statement narrowed to byte-for-byte (the reviewer's attempt-3 S1) in this same commit.
