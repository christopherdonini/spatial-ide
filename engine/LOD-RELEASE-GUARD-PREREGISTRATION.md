# LOD release-only guard — five-line preregistration (`AUTONOMY.md` §21d)

*Committed before any code, per the header rule both full and short forms keep.*

```
Authority: state/directives/2026-09-22-part-n-n8-and-sequencing.md:159 (the human, "the missing release-only measurement guard" as one of three deferred clean-clone repairs); DECISIONS-PENDING.md entry 115, item (d); kernel/RESULTS.md's paragraph beginning "**Finding — a fourth test in the same binary lacks its siblings' release-only guard.**"; PLAN.yaml node drill-fix-release-only-guard; AUTONOMY.md §21d.
Scope: engine/tests/lod_tier_measurements.rs only; declared line budget <= 20 non-generated insertions+deletions across 1 file.
Change: each of the four #[ignore]d tests in this file whose ignore string names --release gains, as its first statement, the same assert!(!cfg!(debug_assertions), ...) guard already present at engine/tests/import_layout_digest.rs:428-433, worded for this file's reason (wall-time measurements; a debug build's numbers are not measurements); no other line in the file moves or is reflowed.
Tests+mutation: the proof is a debug run of each guarded test (cargo test -p spatial-engine --test lod_tier_measurements -- --ignored --exact <name>) failing within seconds on the new guard's message; the mutation is the guard removed from wall_time_arm_s_and_arm_p_over_polygons_100k and the same debug run observed getting past the guard (its first "fixture ..." println appears) and then stopped by a timeout, recorded, then the guard restored. The release path is not run tonight -- no measurement is made or claimed.
Out-of-scope: no measured number, no docs/08 row, no preregistered ceiling or constant changed, no change to any non-ignored test, no ADR/wire/security/guarantee text; siblings of the same gap in other files (kernel/tests/verify_bundle.rs, kernel/tests/scale_pass.rs, kernel/tests/query_window_attribution.rs, kernel/tests/publish.rs, kernel/tests/permission_boundary.rs, kernel/tests/import_layout_publish_determinism.rs, kernel/tests/first_batch_factorial.rs, kernel/tests/import_layout_factorial.rs, engine/tests/lod_tier_cancellation.rs, engine/tests/lod_tier_builder.rs) are named here, not fixed.
```

Budget: 10 of 20 non-generated insertions+deletions across 1 file (engine/tests/lod_tier_measurements.rs; git diff --numstat origin/main...HEAD, the form excluded).

## Correction (reviewer Documentation FAIL)

- **D1.** Defect: the Out-of-scope sibling list was wrong for six of its ten files. Corrected reference: the true unguarded siblings (release-only `#[ignore]`d tests with no `debug_assertions`/`refuse_debug` guard on their first reachable path) are exactly `kernel/tests/publish.rs`, `kernel/tests/permission_boundary.rs`, `engine/tests/lod_tier_builder.rs`, `engine/tests/lod_tier_cancellation.rs`; `kernel/tests/verify_bundle.rs` has no `#[ignore]`d test at all (`grep -c "#\[ignore" kernel/tests/verify_bundle.rs` → 0); the other five originally listed (`kernel/tests/scale_pass.rs`, `kernel/tests/query_window_attribution.rs`, `kernel/tests/import_layout_publish_determinism.rs`, `kernel/tests/first_batch_factorial.rs`, `kernel/tests/import_layout_factorial.rs`) already call `refuse_debug` (`kernel/tests/support/mod.rs:319`) as their guarded tests' first statement. Proof: `grep -rn "ignore = " engine/tests kernel/tests protocol renderer | grep -i release` for the full `--release`-named set, then `grep -c "refuse_debug\|debug_assertions" <file>` per file, confirming the reviewer's own four-file sweep.
- **D2.** Defect: the form's Change line implied each guarded test gains the guard as its own first statement. Corrected reading: for `wall_time_arm_s_over_parcels_5gb`, `wall_time_arm_p_over_parcels_5gb` and `the_5gb_ladder_outcomes_o1_to_o4` the guard is the first statement of `five_gb_build` (engine/tests/lod_tier_measurements.rs:422-427), each test's only call. Proof: `git show 409e152 -- engine/tests/lod_tier_measurements.rs`.
- **N1.** Defect: the guard message asserted in `five_gb_build` said "this file's tests are wall-time measurements", untrue of `the_5gb_ladder_outcomes_o1_to_o4` (an O1-O4 outcomes pass at scale, not a timing). Corrected in commit 7a12bc9 (this branch), which reworded the message to name both cases. Proof: the four debug runs recorded in this report.
- **N2.** Disclosure: DECISIONS-PENDING entry 115 (d) and this drill's own Authority line assumed the three `parcels-5gb` siblings already "carry" a release-only guard; on `main` (eab8e82) `five_gb_build` had zero `debug_assertions` lines before commit 409e152 added them (`git show eab8e82:engine/tests/lod_tier_measurements.rs | grep -c debug_assertions` → 0), so this piece is the first guard on all four tests in this file, not three-plus-one.

Budget: 11 of 20 non-generated insertions+deletions across 1 file (engine/tests/lod_tier_measurements.rs; git diff --numstat origin/main...HEAD, the form excluded).

## Closing commit (AI_DEVELOPMENT.md Amendment 1 §A)

- **1.** Defect: the first correction block (appended at `1187205`) carries no superseded index (round 12, item 1, clause (e)). Corrected reference: the superseded index below. Proof: the table below, one row per superseded item, each naming what supersedes it.
- **2.** Defect: N1's proof pointed at "the four debug runs recorded in this report", not in the tree. Corrected reference: the four are `wall_time_arm_s_and_arm_p_over_polygons_100k`, `wall_time_arm_s_over_parcels_5gb`, `wall_time_arm_p_over_parcels_5gb`, `the_5gb_ladder_outcomes_o1_to_o4` (`engine/tests/lod_tier_measurements.rs`), run at this closing commit's code with `cargo test -p spatial-engine --test lod_tier_measurements -- --ignored --exact <name> --nocapture` (no `--release`), each panicking at rc 101 on its guard's first statement (the first test's own; the other three at `five_gb_build`'s). Proof: re-observed in this round, reported to the custodian in this piece's report.
- **3.** Defect: D2's cite `engine/tests/lod_tier_measurements.rs:422-427` is a bare line cite into a file this branch edits, and is stale. Corrected reference: by name only — `five_gb_build`'s first statement, not a line span.
- **4.** Defect: both guard messages made a claim untrue of the tests reaching them (the 5 GB helper's message asserted a debug build's correctness result is untrustworthy, which the same code contradicts; the 100k test's message called "this file's tests" wall-time measurements, untrue of the file's non-ignored row-order test and its O1-O4 outcomes test). Corrected reference: `wall_time_arm_s_and_arm_p_over_polygons_100k`'s message now names only that test ("this test measures build wall time..."), and `five_gb_build`'s message now says a debug build's wall time is not a measurement, dropping the correctness claim — both in the family's honest form (`engine/tests/import_layout_digest.rs`'s `the_cross_file_digest_correctness_pass`: "every other pass in this family refuses a debug build on principle"). Proof: the four debug runs of item 2 above, each rc 101 on the new message; `the_row_order_check_fires_on_a_permutation` rc 0 in the same debug build.
- **5.** Defect: D1's proof grep (`grep -rn "ignore = " ... | grep -i release`) misses `kernel/tests/publish.rs` and `kernel/tests/permission_boundary.rs`, whose `#[ignore]` is bare and whose release-only reason is stated only in the doc comment above it. Corrected derivation: `grep -rn '#\[ignore' engine/tests kernel/tests protocol renderer`, then for each match check the doc comment lines above it (not the ignore string alone) for a release-only reason; this finds, across D1's four files, five unguarded functions — `kernel/tests/publish.rs`'s `a_dataset_whose_verified_row_count_exceeds_max_features_refuses_at_preflight_before_any_write` and `a_dataset_whose_verified_row_count_exceeds_max_features_refuses_before_any_hash_is_taken`, `kernel/tests/permission_boundary.rs`'s `an_adr_025_reader_ceiling_refusal_at_preflight_produces_no_audit_record`, `engine/tests/lod_tier_builder.rs`'s `the_5gb_ladder_under_disk_discipline`, `engine/tests/lod_tier_cancellation.rs`'s `cancel_observed_within_the_declared_ceiling_at_5gb`. Proof: each function's body re-read this round; none begins with a `debug_assertions`/`refuse_debug` guard.

**Superseded index** (round 12, item 1, clause (e)).

| id | status | reference |
| --- | --- | --- |
| Authority line's premise that the three `parcels-5gb` siblings carried a guard | withdrawn as to that premise | `engine/LOD-RELEASE-GUARD-PREREGISTRATION.md:6 @ 1187205 sha256:75b8f8d836f578e29b817f946f73e68494a390cff0145baeb599637671e27252`; withdrawn by N2, `engine/LOD-RELEASE-GUARD-PREREGISTRATION.md:20 @ 1187205 sha256:5040eac14fe6074fde1ca196b5cee5fe91248776fd6a9ca31055182a9c71965c` |
| Change line's "as its first statement" and "worded for this file's reason" | corrected as to which statement is first and which wording applies | `engine/LOD-RELEASE-GUARD-PREREGISTRATION.md:8 @ 1187205 sha256:659cfcea779aa10873c4587d8841b8b40b62bc0f75ad70a9febddff04aae34f6`; corrected by D2, `engine/LOD-RELEASE-GUARD-PREREGISTRATION.md:18 @ 1187205 sha256:45b3206991ff59558e8cbcd9c76900931f05e8b8e396f82daddfd1b0855a3d13` |
| Out-of-scope sibling list | corrected | `engine/LOD-RELEASE-GUARD-PREREGISTRATION.md:10 @ 1187205 sha256:062b644d587d2948f5c96a67bb2635eab6ecb53672a72b9681c1363c28348e1b`; corrected by D1, `engine/LOD-RELEASE-GUARD-PREREGISTRATION.md:17 @ 1187205 sha256:e184e6b85a71b3dac383205cd8eb0cb95cf9cc18128287c2faaae2ea8bda6dad` (itself refined by this commit's item 5 above) |
| N1's proof sentence ("the four debug runs recorded in this report") | corrected | `engine/LOD-RELEASE-GUARD-PREREGISTRATION.md:19 @ 1187205 sha256:b74a93c665152724e3527d063e7eb3622c2c182ea2a0b997675287bb05468e0a`; corrected by this commit's item 2 above |
| D2's line cite (`engine/tests/lod_tier_measurements.rs:422-427`) | corrected | `engine/LOD-RELEASE-GUARD-PREREGISTRATION.md:18 @ 1187205 sha256:45b3206991ff59558e8cbcd9c76900931f05e8b8e396f82daddfd1b0855a3d13`; corrected by this commit's item 3 above |

Budget: 11 of 20 non-generated insertions+deletions across 1 file (engine/tests/lod_tier_measurements.rs; git diff --numstat origin/main...HEAD, the form excluded).
