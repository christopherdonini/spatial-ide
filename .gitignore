# Rust
target/

# Node
node_modules/
dist/
dist-measure/
dist-metafile.json
.vite/

# Generated at build time (RELEASE-0.1 Amendment 3, item 2) -- one source, `notice.mjs`'s
# `notice()`, is what produces this file; never hand-edited, never committed.
frontends/shell/src/generated/

# Tauri
src-tauri/gen/

# Generated spike data (never commit datasets)
spikes/**/data/
*.arrow
*.parquet

# OS / editor
.DS_Store
Thumbs.db
*.log

# Claude Code local (per-machine permissions)
.claude/settings.local.json

# Stray scaffold remnants (root app/ was an accident; delete freely)
/app/
.cut-archive/

# Single-custodian session lease (AI_DEVELOPMENT.md mechanics) — carries a session id, never committed.
CUSTODIAN-LEASE
