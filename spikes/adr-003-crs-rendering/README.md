# Spike — ADR-003: Arbitrary-CRS Rendering

**Question:** can deck.gl inside a Tauri webview render and edit large datasets in a projected CRS (EPSG:2056) with centimetre picking accuracy — without converting to Web Mercator?

**Stakes:** the only open technical gate in the constitution (docs/adr/ADR-003). Pass → deck.gl is the projected working canvas. Fail → purpose-built WebGPU renderer for the projected canvas; Tauri shell unaffected either way. Both outcomes are wins — the failure mode is *not knowing*.

## Acceptance gate (from ADR-003)

> Load and interactively edit a large dataset in EPSG:2056 **without permanently converting it to EPSG:3857**, maintaining centimetre-level picking accuracy, within docs/08 budgets, on all three system webviews.

This spike proves Windows/WebView2 first; macOS (WKWebView) and Linux (WebKitGTK) run in CI before the ADR flips to accepted.

## Technical approach

- deck.gl `OrthographicView` over raw projected metres — no geographic projection in the view at all.
- **Offset-relative rendering** is where precision lives or dies: EPSG:2056 eastings ≈ 2.6×10⁶ m; float32 carries ~7 significant digits, so absolute coords lose centimetres. Re-center on the view origin (f64 math kernel-side, or deck.gl `coordinateOrigin`) before f32 GPU upload.
- Data path: Rust generates/loads → Arrow IPC → webview via custom protocol / binary channel (**no JSON, even in the spike**) → binary attributes into deck.gl layers.
- Picking: GPU pick → feature id → resolve exact f64 coordinate on the Rust side; compare against ground truth.

## Test data (synthetic first, generated — never committed)

EPSG:2056 extent: E 2,485,000–2,834,000 · N 1,075,000–1,296,000.

- **P1** — 10M random points, uniform over extent
- **P2** — 100k "parcel" polygons / ~10M vertices (perturbed grid)

Real data later: swisstopo open data (cadastral surveying, swissTLM3D) for the final validation run.

## Milestones

- **M0** — Tauri v2 app boots; log renderer availability (WebGL2/WebGPU), GPU adapter info from inside WebView2.
- **M1** — P1 rendered via Arrow → binary attributes; pan/zoom ≥ 60 fps p50, measured p95.
- **M2** — Precision: no vertex jitter at 1:500 zoom anywhere in the extent; screen-space error < 0.5 px.
- **M3** — Picking: click → feature id + exact source coordinate; error < 1 cm.
- **M4** — P2 polygons + vertex-drag editing; precision and frame rate hold during edits.
- **M5** — Data-plane audit: count every copy end-to-end (Rust → IPC → JS → GPU), MB/s throughput, cancellation < 100 ms mid-load (ADR-004 honesty check).

## Results

| Milestone | Metric | Target | Measured | Hardware / method |
|---|---|---|---|---|
| M0 | WebGL2 / WebGPU available | report | WebGL2: **available** (ANGLE/D3D11, GLSL ES 3.00) · WebGPU: **available** (18 features, `core-features-and-limits`) | Windows 10 Pro 22H2 (build 19045), Intel UHD Graphics 630 (gen-9, ANGLE D3D11 backend), WebView2/Edge runtime 150.0.4078.105, via `tauri dev` |
| M1 | Frame time p50 / p95 (pan, 10M pts) | ≤16 ms / report | **MISSED.** Run 1: p50 157.4 ms / p95 160.9 ms (121 samples). Run 2: p50 158.5 ms / p95 163.9 ms (118 samples). ~10× over the ≤16 ms (60 fps) budget on both runs; p50/p95 tight within a run and consistent across runs, so this reads as a real sustained cost, not noise. | Windows 10 Pro 22H2 (build 19045), Intel UHD Graphics 630 (gen-9, ANGLE/D3D11, WebGL2), WebView2/Edge 150.0.4078.105, via `tauri dev`, 2 independent runs. Method: self-driven synthetic pan+zoom sweep (`src/m1-render.ts`) over P1 (10M pts, uniform random over the EPSG:2056 extent) rendered as one `ScatterplotLayer` with binary `getPosition` attributes in an `OrthographicView`; 1000 ms warm-up, then frame time = wall-clock delta between consecutive deck.gl `onAfterRender` calls, collected until 150 post-warmup samples or 20 s elapsed (both runs hit the 20 s cap with 118-121 samples). p50/p95 computed by sorting samples and indexing. |
| M1 | Time to first pixels | < 100 ms | **MISSED.** Run 1: 2178 ms (fetch 1906 ms, parse 107 ms). Run 2: 2243 ms (fetch 1970 ms, parse 113 ms). ~22× over the <100 ms budget; fetch of the single unchunked P1 response dominates. | Same hardware/method as above. Measured as `performance.now()` delta from just before the `p1://` fetch starts to the first deck.gl `onAfterRender` callback (query start → first rendered frame), per docs/08. `fetchMs`/`parseMs` are sub-measurements within that span, taken around the Arrow IPC fetch and `tableFromIPC` parse respectively (`src/p1-loader.ts`). Both runs confirmed `pointCount: 10000000` in the report and log line `[M1] P1 generated: 10000000 points, 162500488 bytes Arrow IPC` — evidence the path carried valid binary Arrow (a malformed/JSON payload would fail `tableFromIPC` or yield a wrong count), not the JSON path ADR-004 forbids. Caveat: "query start" is the P1 fetch, which starts only after the app window exists; P1 generation currently runs synchronously *before* the window is created (`src-tauri/src/lib.rs`), so real cold-launch-to-pixels latency is this number plus that startup delay, not reflected here. |
| M2 | Screen-space error @1:500 | < 0.5 px | **PASS** — the verdict is on the offset-relative path measured at its designed worst case. **offset-dynamic-max-drift** (origin pinned exactly at the 131 072 m re-centre threshold, i.e. the worst state the policy permits before it rebuilds): worst case **0.0446 px** over 5 probes × (1 static + 100 pan frames), ~11× inside budget. **This is the number the milestone rests on** — it is what the policy actually guarantees. Its static component, **0.0323 px, sits above the 0.02538 px calibration floor**, so unlike the zero-drift case it carries a real, *resolvable* f32 contribution rather than pure estimator noise; confirmed outside the harness, where f32 rounding of the local coordinates at that drift (92 686.837 / 92 678.779 m, ULP 0.0078 m) predicts 0.0187 px against a measured 100-phase mean of 0.0186 px. The figure that generalises to arbitrary coordinates is the analytic half-ULP at threshold, **0.0418 px**; the measured 0.0323 px is one sample of it. **offset-dynamic at zero drift** (origin set *to* the view target): 0.0358 px, static 0.0253795 px against a floor of 0.0253795 px — floor-limited, no *detectable* precision contribution, and nothing claimed below the floor. **Both dynamic modes' five-location invariance is algebraic, not empirical:** the origin is defined relative to the view target, so `coord − origin` is the same value at every probe and the f32 buffer literally holds identical numbers five times. That confirms the implementation has no location-dependent bug; it is **not** independent evidence of location independence. **offset-fixed** (M1's extent-centre origin): worst 0.0813 px, static 0.022–0.072 px. **naive-absolute control** (origin 0,0 — absolute EPSG:2056 narrowed straight to f32): worst **0.9494 px**, static 0.899–0.935 px at the corners and 0.656 px at the centre — over budget at all five. The control's failure is a property of coordinate *magnitude*, not of probe choice: the f32 residual here is ~uniform within ±half an ULP, and enumerating **every** centimetre fraction in a 1 m window at the far corner (10 000 coordinates, computed outside the harness) puts **56.96 % of them over the 0.5 px budget**, worst 1.0142 px against the harness's own `predictedNaiveBound` of 1.0564 px. These five probes simply sample the upper half, at ~0.92 px. Measured 100-phase means match independent `f32(coord) − coord` arithmetic within 0.001 px everywhere (control 0.9196 predicted / 0.9194–0.9201 measured at corners, 0.6414 / 0.6422 at centre; offset-fixed 0.0459 / 0.0455–0.0463). Run-validity gate: **`valid: true`, `invalidReasons: []`, `captureRetries: 0`**. Re-run independently after the reviewer's fixes: my run and the coordinator's are **bit-identical** on every field but the timestamp. | Windows 10 Pro 22H2 build 19045, Intel UHD Graphics 630 (gen-9), WebGL2 via `ANGLE (Intel, Intel(R) UHD Graphics 630 (0x00003E9B) Direct3D11 vs_5_0 ps_5_0, D3D11)`, WebView2/Edge 150.0.4078.105, `RUN_M2=1 npm run tauri dev`, harness `src/m2-precision.ts` + `src/offset-frame.ts`, markers `src-tauri/src/markers.rs`. (Run note: TCP 1420 is inside a Windows excluded-port range (1350–1449) on this box, so the default dev port fails with `EACCES` before the app starts; both people who ran this worked around it out-of-repo — here via a `tauri dev --config` override moving Vite to port 3000 — rather than changing committed config. A dev-server port cannot reach the renderer.) Scale 1:500 at 96 CSS px/inch = **0.1322917 m/px** (7.559055 px/m, deck zoom 2.918206), `useDevicePixels: false` so 1 CSS px == 1 device px; framebuffer 800×600, waited for a settled size, pinned, and re-checked for mid-run resize. Ground truth: 125 markers (5 locations × a 5×5 grid at 0.1 m ≈ 0.756 px spacing), generated in Rust f64, delivered over the `p1://markers` Arrow IPC path (no JSON, ADR-004); the client recomputes the same f64 values and asserts bitwise equality — `roundTripBitExact: true`. Probe coordinates carry centimetre fractions (e.g. 2 833 999.63) rather than the round extent bounds, which are integers < 2²⁴ and therefore f32-exact. **Prediction is independent of deck.gl**: computed from first principles in f64 as `W/2 + pxPerM·(worldE − targetE)` (y-up to match `readPixels`), deliberately *not* `viewport.project()`, since predicting deck.gl's output with deck.gl would only prove self-consistency. **Actual** position: `gl.readPixels` of a 41×41 window around the prediction, intensity-weighted centroid with the window minimum subtracted as background and pixel centres at i+0.5. **Measurement floor**: a calibration probe runs the identical measurement at f32-exact coordinates (~(−4.9, 3.1)), where precision cannot be in play, and returns **0.02538 px**; it bounds systematic convention error (half-pixel, y-flip, stray DPR) plus estimator noise, and no result is claimed below it. Four origin policies run through the *identical* harness, differing only in the origin of `f32(coord − origin)`: naive-absolute (0,0); offset-fixed (extent centre 2 659 500 / 1 185 500); offset-dynamic (`OffsetFrame`, re-centring past a budget-derived drift threshold — a 0.1 px budget implies ~222 km, capped at the 131 072 m sanity ceiling; 5 origin placements = 1 initial + 4 probe transitions); and offset-dynamic-max-drift, which pins the origin at `target − 131 072/√2` per axis to measure the threshold state that the self-centring mode never visits. **Run-validity gate** (added after review): the report carries `valid` / `invalidReasons`, failing the run on a non-bit-exact round trip, a non-finite or >0.1 px calibration, any failed capture, any location with fewer than 100 jitter frames, or a mid-run framebuffer resize — previously a failed capture produced `NaN` and a `maxErrorPx` seeded from 0, so a broken run could score *better* than a working one. `captureRetries` is now counted rather than silent; a retried empty window during a jitter sweep would have meant a >20 px displacement — the most interesting event available — quietly replaced by a settled frame. **Scope limits:** five probe locations are not "anywhere in the extent", and as noted above the dynamic modes' agreement across them is algebraic; what actually bounds those policies is the analytic half-ULP at the drift threshold, with the probes confirming the implementation matches it. f32 shader behaviour is driver-dependent, so this is a Windows / ANGLE-D3D11 / Intel UHD 630 result only, and ADR-003's gate still requires macOS/WKWebView and Linux/WebKitGTK before the ADR can flip. |
| M2 | Vertex jitter (wobble) during sub-pixel pan @1:500 | < 0.5 px | **PASS** — target declared before the run (`BUDGET_WOBBLE_PX` in `src/m2-precision.ts`). **offset-dynamic-max-drift** (the policy's worst permitted state, and the headline): worst case **0.0352 px** across all five probes, ~14× inside budget. offset-dynamic at zero drift 0.0360 px; offset-fixed 0.0360 px; **naive-absolute control 0.0366 px** — the control passes the wobble budget comfortably. All four policies agree within 0.0004 px, so **wobble is common-mode and does not discriminate between them**: it sits at the same scale as the 0.0254 px estimator floor and reads as the estimator's sub-pixel-phase dependence, not a renderer artefact. That the control shows a large *static* error (0.95 px) but *small* wobble is counter-intuitive, and is the substantive finding — precision was destroyed **when the absolute coordinate was narrowed to f32 at upload time**, and nothing downstream recovers it, which is exactly ADR-003's argument that the re-centring must happen in f64 *before* the narrowing. (The mechanism behind the *small* wobble specifically: deck.gl's `OrthographicView` auto-offsets by `Math.fround(target)` in the shader but recomputes the compensating projection centre in f64 each frame, so the origin term cancels and contributes no jitter. **That explanation is read from deck.gl 9.3.7 source** — `@deck.gl/core` shaderlib/project — **not measured here**; it is consistent with the observed small control wobble, but it is third-party source reading and a deck.gl version bump invalidates the explanation, though not the measurement.) **Forced re-centre crossing** (threshold cut to 3 m to force swaps the 131 km production threshold never reaches): 3 origin swaps in 120 frames; max error **0.02538 px on swap frames, 0.02538 px off-swap, and 0.02538 px on the first post-swap frame** — all equal — with **0 frames** where the marker was missing from the 41×41 window. The first-frame figure is the one that matters and it is new: the harness previously skipped one frame before every capture, so this test read the *second* frame after the swap and stepped straight past any single-frame transient. It now captures at `skipFrames = 0` with no retry, so the claim "the f32 buffer rebuild and the view-target rebase land in the same frame with no snap" is backed by the code rather than merely asserted. | Same hardware, harness, scale, estimator and validity gate as the row above. **Wobble definition:** at each probe the view is panned in ~0.1 px diagonal steps (0.1 px × 0.1322917 m/px ÷ √2 per axis) for 100 frames, capturing every frame; wobble = max over frames of \|error_i − mean_error\| as a 2-D vector distance, so a constant offset contributes nothing and only frame-to-frame variation counts. Only `viewState` changes between frames — the `data` object is reused by reference so the attribute buffer is never re-uploaded — meaning anything measured is projection-side, not re-upload noise. Capture volume: 4 policies × 5 locations × 100 frames = 2000 pan captures (100/100 frames located a mark everywhere, `captureRetries: 0`), plus 120 crossing frames and the per-probe static and grid captures. Secondary observation (not evidence): the full 25-point 0.1 m grid rendered once per probe held an intensity RMS spread of ~2.0–2.3 px in every policy, so the decimetre structure did not collapse — but with the control's 0.25 m easting ULP being ~1.9 px against a 0.76 px point spacing, spread is a weak discriminator and is reported as an observation only. **Scope limit — origin-swap cost is untested:** the swap was validated for *correctness* at 5 probe points and 125 markers, never for *cost* at P1 scale. `offsetPositions` (`src/offset-frame.ts`) is a synchronous main-thread loop, so the same swap over P1's 10M points is ~20M float writes on the canvas thread — a near-certain dropped frame, given M1.5 measured 10M-point frames at ~130 ms. That is a docs/01 principle 7 concern ("never block the canvas") and it is **unmeasured here**; it belongs to M4/M5. Same remaining limits as the row above: five probes, one driver, Windows only. |
| M3 | Picking error | < 1 cm | **PASS** — worst error **exactly 0 m** (bitwise zero) over all **70** isolated-feature clicks: **70/70 correct id, 70/70 bit-exact f64 round trip**, zero clicks that picked nothing. Read this as the pre-recorded metric definition says to read it — **a correctness assertion with a small tolerance, not a continuous accuracy measurement**. The metric is bimodal by construction: correct id + exact round trip is identically 0, while a wrong id in these datasets costs ≥ 0.1 m (class c pairs) to ≥ 206 km (classes a/b probes), so no outcome can land anywhere *near* 1 cm and the margin "0 m vs 1 cm" carries no information about headroom. **Scope of the verdict, stated up front:** it rests on the isolated-feature classes (a) and (b). The dense-pair class (c) *does* mis-resolve ids below 2.27 px separation, and those mis-picks would be 0.10–0.20 m errors — they are excluded from this row because the pre-recorded metric definition classes (c) as "a finding to be reported, not a pass/fail", written before the harness existed, not because they were inconvenient. They are in the next row, unfiltered. **Class (a) on its own proves very little and is deliberately not the headline.** Resolving a pick index against a dataset regenerated in the same order is the identity function `dataset[i] == dataset[i]` — it would pass in any CRS, at any zoom, with no renderer attached at all. What gives the result content is the **id indirection**: the Arrow payload carries an explicit non-nullable `id` column (`arrow_en::serialize_en_id`), the client reads `ids[ordinal]` out of the buffer it actually uploaded and asks the host by **id**, and class (a) runs a second pass against a **deliberately reversed buffer** (`ordinalDivergedFromId: true`) in which sending the ordinal instead would have returned a feature 206.6–221.0 km away. Honest limit on that second pass: reversing a 5-element buffer leaves the middle element a fixed point, so **4 of the 5 shuffled probes actually discriminate** and the fifth is still the identity case — recorded as data (`discriminatingProbes: 4` of `totalProbes: 5`) rather than left for a reader to re-derive. Class **(b) also runs entirely against the reversed buffer**, so the indirection carries 60 further clicks rather than only class (a)'s second pass. The hazard it guards is concrete rather than hypothetical: any cull, chunk, sort or LOD makes the GPU ordinal buffer-local, and a system that resolved the *ordinal* would then return a **wrong-but-plausible coordinate with nothing raised** — the failure would be invisible precisely because the returned value is still shaped like a coordinate. Breakdown — **(a) dead-centre, isolated features:** 10/10 correct id, 10/10 bit-exact, worst 0 m, over both the natural and reversed passes. **(b) 1–3 px offsets × 4 directions × 5 locations:** 60/60 correct id, 60/60 bit-exact, worst 0 m — but scoped honestly, the five probes are ≥ 206 km apart and the pick radius is 4 px, so **there is no competing feature to lose to**; (b) shows that a near-miss click still resolves and round-trips exactly, *not* nearest-among-competitors selection, which is what the next row measures. **Picking latency was NOT measured** — named explicitly in the method column rather than left silently absent. **f32 audit — the returned coordinate never touches f32**, re-verified here independently of the harness's own assertion: `grep -E 'f32\|Float32\|fround'` over `src-tauri/src/` matches **only comments, never code**, and deck.gl carries the index as an integer RGB triple (`encodePickingColor`), so no float sits in the id path either. The only `Float32Array` in play is `offsetPositions`' GPU upload, which is never read back; render path and pick path share the f64 source vectors and meet nowhere else. | Windows 10 Pro 22H2 (build 19045), Intel UHD Graphics 630 (gen-9), WebGL2 via `ANGLE (Intel, Intel(R) UHD Graphics 630 (0x00003E9B) Direct3D11 vs_5_0 ps_5_0, D3D11)`, WebView2/Edge 150.0.4078.105, `RUN_M3=1 npx tauri dev`, harness `src/m3-picking.ts` + `src/offset-frame.ts` + `src-tauri/src/markers.rs` (`centres` / `pairs` / `resolve_by_id`) + the `resolve_pick` command. Scale 1:500 at 96 CSS px/inch = **0.1322917 m/px** (7.559055 px/m), `useDevicePixels: false` so 1 CSS px == 1 device px, framebuffer 800×600, `offset-dynamic` rendering (`OffsetFrame`, budget-derived threshold capped at 131 072 m; the five probes are ≥ 206 km apart, so the origin re-centres at every location and the local coordinates are the same small numbers everywhere — the same algebraic invariance M2 flagged, so five locations are again *not* five independent samples). **Per click:** predict the feature's framebuffer position in f64 **independently of deck.gl** (`W/2 + pxPerM·(worldE − targetE)`, same basis as M2, deliberately not `viewport.project()`), floor to an integer pixel, `deck.pickObjectAsync({x, y, radius})`, read `ids[info.index]` from the uploaded buffer, `invoke("resolve_pick", {id})` → Rust indexes the untouched f64 source vectors and returns `{crs: "EPSG:2056", e, n}`; error = `hypot(resolved − intended)` in f64, and round-trip exactness is `===` against the Arrow-decoded f64 (no NaN and no −0 in play, so `===` here is bitwise equality, not "close"). Datasets are 5 features (classes a/b) and 10 features (class c) delivered over the same `p1://markers` Arrow IPC path as M2 — binary, no JSON (ADR-004); the f64 coordinate itself comes back over Tauri's JSON `invoke`, which is control-plane (one small message per click, not a data hot path) and is verified bitwise rather than trusted. Run-validity gate: **`valid: true`, `invalidReasons: []`** — the gate fails a run on: the buffer ordinal never diverging from the feature id (i.e. the reversed pass silently not taking effect, which would collapse the result back into `dataset[i] == dataset[i]` while still reporting 70/70 and 0 m — the one failure that would be perfectly transcribable and completely meaningless); fewer than two discriminating probes; any class-(a) *or* class-(b) click that picked nothing or failed to round-trip bit-exactly; a mid-run framebuffer resize (every predicted click position depends on those dimensions); any render-settle timeout or `resolve_pick` rejection; zero unprojection-control samples; or `info.coordinate` never looking local-frame, which would mean the control's premise did not hold and its numbers measure something else. Counters for the last few are recorded fields (`failures: {settleTimeouts: 0, resolveRejections: 0, framebufferChanged: false}`), not console scrollback. **Three independent runs — the coordinator's plus two of mine — are bit-identical on every field but the timestamp** (`src-tauri/m3-report.json` diffed field-by-field; 1150 clicks and 1150 `info.coordinate` samples per run), and no run logged a settle timeout or a rejected `resolve_pick`. **Picking latency: not measured, and named rather than omitted.** docs/08 lists picking latency among the measured quantities and ADR-003's gate says *interactively* edit, but M3 picks against 5- and 10-feature datasets, where any latency number would describe the harness rather than the system. A pick runs a full picking render pass over the layer, and M1.5 measured 10M-point frames at ~131–136 ms, so picking at P1 scale plausibly stalls the canvas (docs/01 principle 7) — deferred to M4/M5 in the same terms M2 used for origin-swap cost. **Scope limits:** synthetic 5- and 10-feature datasets, not P1/P2 scale; one zoom (1:500); one origin policy; rasterization and f32 behaviour are driver-dependent, so this is a Windows / ANGLE-D3D11 / Intel UHD 630 result only, and ADR-003's gate still requires macOS/WKWebView and Linux/WebKitGTK before the ADR can flip. (Run note: TCP 1420 is inside a Windows excluded port range on this box — `netsh interface ipv4 show excludedportrange protocol=tcp` reports 1350–1449 — so the default dev port fails with `EACCES` before the app starts; worked around out-of-repo via a `tauri dev --config` override moving Vite to port 5180, never by editing committed config. A dev-server port cannot reach the renderer.) |
| M3 | Class (c) id-discrimination separation @1:500 | report (a finding, not a threshold) | **Reliable from 2.27 px** — 40/40 intended-feature hits at that separation and at every larger one, on **all three axes** (easting / northing / diagonal), which at 1:500 is **0.30 m**. **Quote the pixel figure first:** pixels are the scale-free property of the pick machinery, whereas the metre figure is that number times m/px and is therefore a statement about 1:500 and nothing else. Below the knee, all three axes give an identical curve: **1.51 px (0.20 m) → 30/40** with both members returned at least once; **1.13 px (0.15 m) and 0.76 px (0.10 m) → 20/40 with `bothMembersEverReturned: false`**, i.e. one member of the pair won *every* click and the pair was never discriminated at all. **Three floors bound this number and none of them is coordinate precision** (f32 ULP on the local frame at these magnitudes is ~5×10⁻⁷ m, five orders of magnitude below the smallest separation tested): (1) the **effective picking footprint** — features are styled at 1 px radius, and the picking buffer keeps whichever fragment drew last where the discs overlap, so this figure moves with *style*, not with data; (2) **integer-pixel click quantization**, which perturbs which pixel is actually sampled by up to ~0.7 px; (3) the pick reads the exact pixel (`radius: 0`). **Correction to the obvious reading of the 20/40 + `bothMembersEverReturned: false` signature: it is *not* "the two clicks were literally identical input".** Recomputing the harness's own deterministic click positions outside the harness shows that at 1.13 px the two clicks land on **distinct integer pixels in all 12 axis × phase combinations**, and at 0.76 px they coincide in only **4 of 12** — yet both rows score 20/40 with one member winning throughout. So identical-input degeneracy explains at most a minority of those trials, and the binding floor is floor (1), the rendered/picked footprint with last-writer-wins. Corroborating (a fit, not a measurement): a one-parameter model — disc of effective radius R px, pixel sampled at its centre, later-drawn wins — reproduces **all 27 measured cells** (9 separations × 3 axes: exact hit counts *and* the `bothMembersEverReturned` flags) for **R ∈ [1.49, 1.57] px** and for no R below 1.49 or above 1.57, i.e. the styled 1 px radius plus ~0.5 px of rasterization/antialiasing spread. That reframes the finding: at 1 px radius the answer is ~2.3 px, and a heavier style would push it up with no change to the data or the CRS. **Scale-invariance of the pixel figure was NOT verified at a second zoom — that remains open**, and until it is, "2.27 px" is a measurement at one zoom rather than a demonstrated invariant. | Same hardware, webview, harness, scale, offset policy, validity gate and three bit-identical runs as the row above. Sweep: 3 axes × 9 separations (0.10 / 0.15 / 0.20 / 0.30 / 0.50 / 0.80 / 1.20 / 2.00 / 3.00 m) × 5 probe locations × **4 sub-pixel view phases** (0, 0.25, 0.5, 0.75 px, applied to the view target) × 2 pair members = **1080 clicks**. Pairs are generated Rust-side (`markers::pairs`) as an integer millimetre count divided by 1000.0 on *both* sides using the identical expression, so the two ends derive the same f64 and the client's bit-equality check cannot fail for reasons unrelated to picking; the diagonal axis exists because the f32 ULP is asymmetric across the extent (~0.25 m at easting 2.8e6 vs ~0.125 m at northing 1.3e6), so an easting-only sweep would leave that 2× asymmetry untested. Feature radius for this class is **1 px** (deliberately small: with a large disc the answer would measure the radius the harness chose rather than anything about the pipeline) and pick radius is **0** (exact pixel). Phases exist to stop a single lucky alignment reading as discrimination; `bothMembersEverReturned` exists to distinguish "sometimes works" from "always returned the same member", which scores 50 % by construction. "Reliable from X" is defined as the smallest separation such that X *and every larger separation on every axis* is 40/40 — a lone pass under an unreliable band does not qualify. The distinct-pixel counts and the R fit above were computed **outside the harness**, from the harness's own click-position formula, and are analysis of the recorded result rather than additional measurement. Same scope limits as the row above; additionally, the five locations are not five independent samples here either (the origin re-centres at each, so the local geometry repeats), which is why the hit counts come out as exact multiples of 5. |
| M4 | Frame p50 / p95 during vertex drag (P2) | ≤16 ms (visible subset, graded); full-P2-visible reported, not graded | **MISSED** — marginally, and by design of the measurement, not a rendering defect. Both GPUs land the graded visible-subset metric right at the display's vsync floor rather than inside the 16 ms budget: **GTX 1650** p50 16.7 ms / p95 17.4 ms, bit-identical across both runs (150 frames); **UHD 630** p50 16.7 ms both runs / p95 17.2–17.4 ms across the two runs. p50 is 0.7 ms (~4.4%) over budget in all 4 runs on both vendors — a 60 Hz compositor cannot present faster than its ~16.667 ms tick regardless of how cheap the frame is, and this workload (15 polygons / ~1,500 vertices visible) is cheap enough that GPU choice makes no measurable difference; the discrete GTX 1650 does not buy the 0.7 ms back because there is nothing left to buy back — the ceiling here is the display, not the renderer. Graded plainly against the stated ≤16 ms target: **MISSED** on both profiles, not rounded up to a pass for being close. **fullP2Visible (reported, not graded)** shows the divergence the visible-subset number hides: **GTX 1650** p50 16.8 ms (bit-identical both runs) / p95 17.7–18.0 ms, 20 frames — still at the vsync floor even with the full 100k-polygon / 10M-vertex static buffer live underneath the drag; **UHD 630** p50 137.0–139.0 ms / p95 141.2–145.1 ms — **~8× the GTX 1650 figure and ~8× over the 16 ms budget**. That split — GPUs tied on the small graded workload, diverging sharply once the full P2 static buffer is in play — is the same GPU-bound-scales-with-point-count shape M1/M1.5 established on the synthetic scaling curve, now reproduced with a second vendor directly on the M4 scenario rather than a synthetic sweep (see the M1.5 diagnostic-notes addendum below for the second-vendor scaling-curve confirmation in full). The harness previously froze on both GPUs before any of these four M4 rows could be measured at all, root-caused to an unhandled BigInt-serialization `TypeError` in `runM4()`'s report assembly (fixed and independently verified on both GPUs) — see "Diagnostic notes (M4 freeze forensics)" for the full account; this row's numbers are the first this milestone has ever produced. | Windows 10 Pro 22H2 (build 19045), WebView2/Edge 150.0.4078.105 — identical on both profiles. Two GPUs, otherwise identical setup (scale 1:20,000, 800×600 framebuffer, P2 dataset of 100,000 polygons / 10,000,000 vertices confirmed via `[M4] P2 generated` in every run's log): **NVIDIA GeForce GTX 1650** (`ANGLE (NVIDIA, NVIDIA GeForce GTX 1650 (0x00001F91) Direct3D11 vs_5_0 ps_5_0, D3D11)`) vs. **Intel UHD Graphics 630** (`ANGLE (Intel, Intel(R) UHD Graphics 630 (0x00003E9B) Direct3D11 vs_5_0 ps_5_0, D3D11)`), each forced via Windows per-app GPU preference (`HKCU:\Software\Microsoft\DirectX\UserGpuPreferences`, `GpuPreference=2;` / `=1;` respectively, set for both `app.exe` and the versioned `msedgewebview2.exe` path) and confirmed live via each run's own `[M0 GPU REPORT]` before any measurement began. `RUN_M4=1 npx tauri dev --config <override>` (TCP 1420 excluded on this box, same port-5180 workaround as M2/M3; the override carries only the port change — no debug or anti-throttling flags, unlike the correctness-check run quoted in the freeze-forensics section, so these are clean measurement conditions). **2 independent runs per GPU profile, 4 runs total, all `valid: true` / `invalidReasons: []`.** p50/p95 computed the same way as every other frame-time figure in this spike (sorted `onAfterRender` deltas); frame counts as designed by the M4 scenario definition above (150 visible-subset, 20 full-P2-visible). |
| M4 | Pick-to-grab latency (P2) | report | Three configurations × 2 GPUs × 2 runs, 12 samples/configuration, 0 timeouts anywhere (`PICK_TIMEOUT_MS` 15 s never approached on either GPU). **Overlay** (~100 vertices) is GPU-independent and essentially free: p50 1.7 ms, bit-identical across all 4 runs on both GPUs. **Visible subset** (~1,500–2,000 vertices) is close but not identical: **GTX 1650** p50 1.1–1.2 ms; **UHD 630** p50 1.4–1.6 ms — a small, consistent gap, nowhere near the order-of-magnitude divergence seen elsewhere in this milestone. **Full set** (all 10,000,000 vertices, unfiltered — the deliberate worst case) is where the two GPUs separate sharply: **GTX 1650** p50 11.2–14.2 ms across the two runs; **UHD 630** p50 135.6–139.5 ms — **~10× slower**, matching the frame-rate row's fullP2Visible divergence and M1.5's per-point-cost scaling curve. No fixed budget applies to this row (target is "report"; docs/08 lists picking latency among measured quantities without a per-milestone number here), so nothing is graded — reported as the row was always going to be. In passing, confirms what the "24-bit picking ceiling" note above the results table already established: 10,000,000 features is 59.6% of the 16,777,215-feature-per-layer ceiling, unaffected by which GPU is picking against it. This row previously carried no data at all, blocked by the same hardware-independent BigInt-serialization freeze described in the row above and root-caused in "Diagnostic notes (M4 freeze forensics)" — these are its first measurements. | Same two GPU profiles, shared setup, and 2-runs-each protocol as the row above; same 4 runs, same validity gate (`valid: true` throughout). Method as designed: `deck.pickObjectAsync({x, y, radius: 6})` at the dragged vertex's predicted screen pixel, p50 of 12 calls per configuration (fullSet / overlay / visibleSubset), `src/m4-editing.ts::measurePickLatency`. |
| M4 | Origin-swap / full-scale commit rebuild cost (P2) | ≤16 ms, mitigation tested if missed | **MISSED unmitigated, on both GPUs — chunked mitigation tested per this row's own "mitigation tested if missed" clause, and it resolves the part it targets but not the whole picture.** Idle-frame baseline with the full P2 static buffer loaded and no rebuild in flight (10 frames): **GTX 1650** 26.9–30.2 ms; **UHD 630** 158.1–159.7 ms — already ~5–6× higher before any rebuild starts, i.e. merely *presenting* a live 10M-vertex layer costs more per frame on the integrated GPU, consistent with M1.5's scaling curve. **Unmitigated forced re-centre rebuild** (`offsetPositionsSync` over the full 10M-vertex buffer, `CROSSING_DELTA_M` = 500 m): the CPU-side wall-clock rebuild time (`rebuildWallMs`) lands in the same range on both GPUs — **GTX 1650** 44.6–56.7 ms; **UHD 630** 42.4–45.0 ms — expected, since this step is pure array-write CPU work with no GPU call in it; both **MISS** the 16 ms budget by roughly 3×. The setProps-to-rendered-frame proxy (`setPropsToNextRafMs`, 2-rAF) is where the GPU cost actually shows, though with much more run-to-run spread on the GTX 1650 itself: **GTX 1650** 42–121 ms (both runs still over budget, but a ~3× spread between its own two runs); **UHD 630** 298.7–311.2 ms — a clear **MISS**, roughly 2.5–7× the GTX 1650 figure depending which of its two runs you compare against. **Chunked mitigation** (`offsetPositionsChunked`, 200,000 vertices/chunk, ~50 chunks): per-chunk cost **PASSES** budget comfortably and near-identically on both GPUs — **GTX 1650** chunk p50 1.5–2.1 ms / max 3.6–4.8 ms; **UHD 630** chunk p50 1.5–1.6 ms / max 4.2–5.7 ms — confirming the mitigation itself is not GPU-bound (same CPU loop, smaller slices, spread across `requestAnimationFrame`). Total wall-clock to complete the full chunked rebuild: **GTX 1650** 829.8–831.7 ms; **UHD 630** 1117.4–1134.0 ms, ~1.3–1.4× slower overall despite near-identical per-chunk cost — the GPU-touching work folded into each chunk's rAF callback still pays the integrated GPU's higher per-frame tax, it just no longer blocks a single frame. The **final single GPU upload+render frame** after the chunked rebuild completes — measured separately, since chunking the CPU loop was never assumed to also fix the reupload cost — **MISSES** on both, and is close between GPUs unlike every other GPU-sensitive figure in this row: **GTX 1650** 66.2–67.5 ms; **UHD 630** 65.5–66.1 ms. **`fullScaleCommit.patchAndReuploadMs`** (the M4-scale single-vertex commit's reupload cost, reusing this same measurement per the M4 scenario definition's own reasoning above) **MISSES** on both: **GTX 1650** 80.1–82.2 ms; **UHD 630** 325.9–333.5 ms, ~4× slower. Net reading: the chunked mitigation solves what it was built for — the CPU rebuild loop no longer blocks a frame on either GPU — but it does not, and was never claimed to, fix the GPU-side full-buffer reupload, which stays over budget on both vendors and is the real ceiling on how fast a full-P2-scale origin swap or commit can complete. This row previously had no data at all, for the same hardware-independent freeze reason as the rows above (BigInt serialization, fixed and verified — see "Diagnostic notes (M4 freeze forensics)"). | Same two GPU profiles, shared setup, and 2-runs-each protocol as the frame-rate row above. Method as designed: idle-frame baseline (10 frames), forced re-centre crossing measured two ways (`rebuildWallMs` = synchronous CPU loop wall time; `setPropsToNextRafMs` = 2-rAF proxy for setProps-to-rendered-frame latency), chunked mitigation triggered per the row's own budget-miss criterion, full-scale commit patch+reupload cost measured as its own figure, `src/m4-editing.ts` lines ~774-889. |
| M4 | Precision & write-path correctness during edit (P2) | < 0.5 px screen-space; bit-exact commit round trip | **Split verdict: PASS on precision, not reliably PASS on bit-exactness.** `precision.worstErrorPx` is bit-identical across every run observed (both GPUs, both the 4 official runs and every follow-up run below), at **0.05409135498382251 px** — ~9.2× inside the 0.5 px budget. Expected, not coincidence: deterministic drag geometry/phase sampling, same prediction-vs-`readPixels` f64/CPU-side path M2 used, no GPU-vendor dependency per M2's own finding. **The bit-exact commit round trip is a genuine, reproducible finding, not a clean pass.** The 4 official runs this table is otherwise built from all happened to show `commitRoundTrip.bitExact: true`. Follow-up verification after a later, unrelated code change (re-running the identical protocol 9 times total across this investigation, both GPUs) found `bitExact: false` in **3 of those 9 runs (~33%)** — always a mismatch of exactly 1 in the f64 mantissa's least significant bit on one coordinate (never both), e.g. one observed case: JS reported sending easting bit pattern `41444a815dce737b`; the value read back after commit was `41444a815dce737a`. **Localized, not left as a guess:** a one-off instrumented run compared the *raw IEEE-754 bits* JS sent (via `DataView`/`getBigUint64`) against what `commit_vertex_edit` received (`f64::to_bits()`), logged before any Rust-side use — the mismatch was **already present on arrival at the Tauri command handler**, ruling out Rust-side storage (`P2Dataset::commit_vertex`/`resolve_vertex` are a pure array store/load, no arithmetic — read the source, it's four lines) and ruling out the return trip. That places the loss specifically in the JS→Rust `invoke()` argument-passing boundary. **What this does and doesn't mean:** practically nothing for the precision verdict, though not by the margin an earlier draft of this row claimed — corrected here rather than left wrong. 1 ULP on the quoted example's ~2.66×10⁶-magnitude coordinate is **4.657×10⁻¹⁰ m** (computed directly from the bit pattern above, not estimated), against **this row's own 0.5 px budget at M4's actual scale (1:20,000, `M_PER_PX` ≈ 5.29 m/px) — 2.6458 m, not M2's 1:500-scale 0.066 m**, which an earlier version of this paragraph wrongly carried over. That's ~9.75, call it **~10 orders of magnitude** below budget (not thirteen) — still enormous headroom, so the precision verdict stands, just not for the number first written here. But M3 built its own "bit-exact round trip" claim on `===` meaning literal bitwise equality (its picking path never showed this), and M4's commit path — a genuinely different operation, a client-computed float sent *into* Rust rather than a stored value merely looked up — does not reliably clear that same bar. **Named as an open item, not chased further here**: the exact mechanism inside Tauri's IPC layer that drops the low bit was not identified (out of scope for a CRS-rendering spike to debug a dependency's serialization internals). Two things this finding is and isn't: the "already wrong on arrival at the command handler" localization was confirmed structurally **once** (the one instrumented run quoted above) and reasonably assumed to generalize, not re-proven on each of the 3 mismatches — those 3 are read directly off the ordinary `sentE/N` vs `resolvedE/N` fields already present in every report, no special instrumentation needed for that part. And this is **not** a confirmation of an existing ADR-004 warning — read directly, ADR-004 puts JSON-avoidance on the *data plane* only and explicitly states "Control plane — commands, handles, schemas, progress, cancellation, errors. Tauri IPC is sufficient here," with no float-fidelity caveat; `docs/10_SKP_Protocol.md` repeats the same split. This finding sits in tension with that "sufficient" claim, on exactly the kind of value it covers (a client-computed f64 command argument) — worth feeding back into that ADR's assumptions, not something it already anticipated. Commit round-trip `===` checks elsewhere in this spike (M3's pick/resolve path) were never re-tested against this same bit-pattern instrumentation and should not be assumed immune. All runs carry `valid: true` except the ones this finding is about, which correctly self-report `valid: false, invalidReasons: ["commit round trip was not bit-exact"]` — the validity gate caught exactly what it was built to catch. This row previously had no data at all, for the same hardware-independent BigInt-serialization freeze as the rows above (fixed and verified; full account in "Diagnostic notes (M4 freeze forensics)"). | Same two GPU profiles, shared setup, and 2-runs-each protocol as the frame-rate row above for the 4 official runs; the bit-exactness finding draws on those plus 5 additional ad hoc verification runs (same harness, same protocol, GTX 1650 unless noted, collected while re-checking an unrelated fix) — 9 runs total, individually reproducible by anyone running `RUN_M4=1 npx tauri dev` repeatedly and watching `commitRoundTrip.bitExact`. Method as designed: predicted-vs-`readPixels`-centroid check on the isolated dragged vertex at 5 phases (0/0.25/0.5/0.75/1.0 × 2π) of the 2 m-radius drag circle, budget 0.5 px (M2's own budget, reused); commit round trip checked via `===` bit-exactness against the Arrow-decoded f64, M3-style, `src/m4-editing.ts::runPrecisionCheck` + the `commit_vertex_edit`/`resolve_p2_vertex` Tauri commands in `src-tauri/src/lib.rs`. Bit-pattern localization used temporary instrumentation (JS `DataView`-based bit extraction, Rust `f64::to_bits()`), not left in the committed code — the finding is preserved here since the instrumentation itself was throwaway. |
| M5 | Copies on hot path (count) | measured, minimized | | |
| M5 | Cancellation latency | < 100 ms | | |

**Two corrections applied to earlier rows while recording M2** (both loud on purpose — neither is a measurement change, and no earlier number moved):

1. *Hardware label.* Every row and the M1.5 paragraph previously read "Windows 11". `Get-CimInstance Win32_OperatingSystem` on the machine that produced all of M0–M2 reports `10.0.19045` — **Windows 10 Pro 22H2**. Same machine, same GPU, same webview build; only the OS label was wrong, and it is now corrected throughout rather than left split between rows.
2. *M1's code no longer exists verbatim.* M2 refactored both ends of the loader by extraction only: the client's Arrow fetch+decode moved out of `fetchAndParse` into `fetchArrowEN` (`src/p1-loader.ts`), and the Rust IPC serializer moved out of `P1Dataset::serialize` into `arrow_en::serialize_en` (`src-tauri/src/arrow_en.rs`), both now shared with the M2 marker path. Verified by reading the diff rather than by re-running M1: client-side operation order is preserved, and the Rust side keeps the identical schema (`e`/`n`, `Float64`, non-nullable) and identical `StreamWriter` stream framing, so P1's emitted payload is still the 162 500 488 bytes M1 recorded. M1's committed numbers therefore stand as recorded — but they were produced by code that has since been restructured, so treat them as valid-at-the-time rather than freshly reproduced, and re-run before quoting them as current.

## M3 metric definition (written before measuring)

Recorded here ahead of the harness so the target cannot be quietly reinterpreted to fit whatever comes out.

**Picking error = |returned f64 coordinate − true f64 coordinate of the *intended* feature|**, where *intended* is the feature nearest the click point.

What the < 1 cm target therefore tests:

1. **Id-resolution correctness** — the GPU pick returns the index of the feature the user actually aimed at.
2. **f64 round-trip exactness** — that index resolves, Rust-side, back to the stored source coordinate with no precision lost in transit.

What it explicitly does **not** test: sub-pixel unprojection. At 1:500 one pixel is 0.132 m, so 1 cm is **0.076 px** — recovering a centimetre by unprojecting a click position is arithmetically impossible per-pixel, and any harness that appeared to achieve it would be measuring something other than what it claimed. This is why ADR-003 specifies "GPU pick → feature id → resolve exact f64 coordinate on the Rust side" rather than unprojecting the cursor: the coordinate is *looked up*, not *reconstructed*.

The consequence is that the metric is close to bimodal by construction. Correct id + exact round-trip gives error identically 0. A wrong id gives the distance to whichever feature was returned instead — in the 0.1 m marker grid that is ≥ 10 cm, an order of magnitude past the budget. So < 1 cm is in practice a correctness assertion with a small tolerance for round-trip loss, not a continuous accuracy measurement, and it should be read that way.

**Three click classes**, all under `offset-dynamic` rendering at the five M2 probe locations:

- **(a) Dead-centre on isolated features** — requires the correct id and a bit-exact f64 round-trip.
- **(b) Offset by 1–3 px** — requires deterministic nearest-feature selection; the intended feature is still the nearest one.
- **(c) Dense pairs** at decreasing separation, starting from the 0.1 m marker-grid spacing (0.76 px at this scale) — measures **the separation at which id discrimination becomes reliable**. That separation is a *finding to be reported, not a pass/fail*: two features 0.76 px apart cannot be distinguished by a pixel-resolution pick buffer, and the useful output is the number at which they can.

Additionally recorded: whether the returned coordinate touches f32 anywhere on its path (it must not — the render path narrows to f32 for the GPU, but the pick path is index → f64 lookup and never reads a rendered position).

## M4 scenario definition (written before measuring)

Recorded here ahead of the harness, same discipline as M3's metric definition: the target cannot be quietly reinterpreted once numbers exist.

**The ≤16 ms budget applies to a declared, geometry-derived viewport, not to full P2.** M2/M3's own precedent is to derive geometry from a declared scale + framebuffer rather than pick a feature count first — and their 1:500 scale is a category error here: at 0.1322917 m/px, a single ~872.5 m × 884 m parcel cell would span ~6,600×6,680 px, far larger than any framebuffer. M2/M3 exist to test sub-pixel *point* precision; M4 is a parcel-scale editing/throughput scenario and needs its own scale. Declared: **1:20,000** (M_PER_PX = 5.2916667, PX_PER_M = 0.1889763), on the spike's actual native window — 800×600 (`tauri.conf.json`), confirmed empirically by M2/M3 and re-confirmed (not assumed) by M4's own settle-and-pin step, identical in method to M2's "waited for a settled size, pinned, and re-checked for mid-run resize."

**P2 layout** (`src-tauri/src/p2.rs`): a 400×250 grid of cells over the same EPSG:2056 extent as P1 — **exactly** 100,000 polygons (matches docs/08's polygon budget class), each an inset rectangle (10% margin, so parcels never touch) walked at 100 vertices per side-proportional step (25/side) with independent ±3 m jitter per vertex (safe against both the ~28 m average inter-vertex spacing and the inset margin — no self-intersection, no neighbour-cell crossing) — **exactly** 10,000,000 vertices. Cells are ~872.5 m × 884 m.

**Visible subset**, derived from the declared geometry rather than picked first: at 1:20,000 on an 800×600 window, the on-screen ground extent is ~4,233 m × ~3,175 m, centred on the polygon nearest the extent centroid ("the edit target," resolved from grid indices at runtime, not hardcoded). Against ~872.5×884 m cells that predicts on the order of **5 columns × 3-4 rows ≈ 15-20 polygons (~1,500-2,000 vertices)** — a genuinely small, zoomed-in fraction of P2 (≈0.02% of its polygons). The exact count comes from a polygon-centroid bbox filter (`p2.rs::arrow_ipc_bbox`, an O(100k) polygon-level scan, not O(10M) vertex-level — 100× cheaper than P1's own bbox scan for the same reason M1.5 flagged P1's as a confound) and is **logged, not assumed** — matching M1.5's "999,841 / 10,000,000" convention rather than rounding to a clean number that pixel alignment might not actually produce. This subset is what the ≤16 ms budget is graded against.

**Full-P2-visible drag is a reported finding, not the pass/fail** — same framing M1 used for the full-10M-point pan/zoom miss: informative about the ceiling, not something M4 is expected to pass.

**Layer design — two layers, not four.** P2 is rendered as its vertex sets only (`ScatterplotLayer`, the same binary-attribute idiom P1/M2/M3 already use), not as filled or outlined shapes: M4's ask is about vertex-drag editing mechanics (the static/overlay split, picking, origin-swap/commit cost), not polygon rendering fidelity, and a `PathLayer` outline would double every buffer this milestone measures (a closing-vertex duplicate per polygon, plus `startIndices` bookkeeping) without changing any of the three quantities M4 is asked to measure. "Polygon" lives in the data model (100 vertices grouped by id arithmetic — `polygon_id = id / 100`, `vertex_index = id % 100`; no separate id array stored server-side, since storage is polygon-major and the mapping is arithmetic) and in the generation method (perimeter walk), not in the render style.

- **Static layer**: every vertex not currently being edited. Built once per scenario (visible-subset or full-P2) and **never rebuilt during a drag** — its `data`/attribute-value reference is reused by identity for the whole interaction, so per M2's own documented finding ("the data object is reused by reference so the attribute buffer is never re-uploaded"), it costs nothing per frame regardless of size. This is deliberately literal: the static buffer is not even patched to exclude the polygon being edited, because doing so would itself cost a rebuild proportional to the static set's size — trivial at visible-subset scale (~2,000 vertices) but a real ~10M-vertex-scale cost at full-P2 scale, i.e. exactly the class of cost the origin-swap test below exists to measure, and paying it twice (once to exclude at grab-time, once again to include at commit-time) would be worse than paying it once, at commit.
- **Overlay layer**: the one actively-edited polygon's 100 vertices, rebuilt every frame from a **fresh** `Float32Array` (not mutated in place — deck.gl's diffing is reference-based, so an in-place mutation without a reference change risks a silently-stale GPU buffer, the same mechanism M2 relied on for the opposite purpose). Always the **last** element of the `layers` array passed to `setProps`. Consequence: the static layer's stale pre-drag position for the edited polygon remains visible underneath as a harmless double-draw for the drag's duration, and per M3 diagnostic note 3 ("the picking buffer keeps whichever fragment drew last"), the overlay's live vertex — not the static ghost — is what a pick returns wherever the two coincide or overlap. This is asserted at runtime (`overlayWinsCollision`), not just assumed.
- **Commit (pointer release)**: `commit_vertex_edit(id, e, n, crs)` sends the new f64 coordinate to Rust, tagged `"EPSG:2056"` and rejected host-side if the tag doesn't match (the write-path mirror of `resolve_pick`'s outbound tagging — a mislabeled frame here would corrupt ground truth, not just misreport a click). A follow-up `resolve_p2_vertex(id)` read-back asserts the stored value round-trips bit-exact, M3-style. Client-side, the static buffer's one polygon is patched: **not** the same operation as the origin swap below, and deliberately distinguished rather than conflated — an origin change moves *every* vertex's local offset, so it needs an O(N) CPU rebuild; a commit changes exactly one vertex's world coordinate, so it only ever needs an O(1) CPU patch (write two floats into the existing buffer). What the two share is the part chunking can't help: deck.gl has no partial-attribute-update API, so *any* change signal reuploads the whole static buffer to the GPU — an O(N) cost regardless of how many values actually changed. At **visible-subset** scale (~2,000 vertices) that reupload is cheap and measured directly. At **full-P2** scale it is the same order of magnitude as the origin-swap test's own GPU-reupload measurement, so that number is reused rather than re-derived from scratch — but the CPU-side chunked-rebuild mitigation is specific to the origin-swap's O(N) rebuild and is not needed (or applicable) here.

**Drag path**: authored in absolute f64 EPSG:2056 around the dragged vertex's true coordinate (a small synthetic circle, radius 2 m, self-driven the same way M1/M1.5/M3 self-drive pan/zoom/clicks — no browser-automation path reaches the native WebView2 window), then run through `OffsetFrame`/`offsetPositions` exactly like every other coordinate in this spike. Stated explicitly because M3 diagnostic note 1 found a local-frame value can cross a boundary "wearing a coordinate's clothes" — the drag path is never authored directly in local/screen space.

**Precision is re-checked, not assumed carried over from M2.** M4 is the first milestone with a *write* path; nothing in M2/M3 validates that the overlay's per-frame fresh-buffer-reupload strategy holds the same sub-pixel guarantee. A small M2-style check (predict the dragged vertex's framebuffer position in f64 independently of deck.gl, capture via `readPixels`, intensity-centroid) samples a few frames during the drag against the same <0.5 px budget M2 used — duplicated rather than imported from `m2-precision.ts`, per this spike's established convention of keeping each milestone's committed numbers independent of modules a later milestone might change.

**Pick-to-grab latency**, measured three ways at the same predicted screen pixel (repeated `pickObjectAsync`, p50 of ~20 calls): against the **full 100k-polygon/10M-vertex** vertex layer (unfiltered — explicit worst case), against the **overlay** (~100 vertices — explicit fast path for continuing to edit the same polygon), and against the **visible subset** (~1,500-2,000 vertices — the scenario's own realistic number, included for context since docs/08 lists picking latency among the measured quantities). The visible-subset fetch is also where the id-indirection hazard M3 diagnostic note 1 and `markers.rs`'s `shuffle` test exist to guard against is checked for real rather than assumed: bbox-filtering by construction makes buffer ordinal diverge from the true global vertex id for (almost) every returned vertex, and that divergence is asserted (`ordinalDivergedFromId`), not just relied upon.

**Deferred origin-swap cost, finally measured.** M2's diagnostic notes flagged this as unmeasured: "the same swap over P1's 10M points is ~20M float writes on the canvas thread — a near-certain dropped frame... belongs to M4/M5." With the full P2 static buffer loaded and idle, a forced re-centre crossing (small threshold, same mechanism as M2's own forced-crossing test) rebuilds the full ~10M-vertex offset buffer synchronously; expected to blow the ≤16 ms budget given M1.5 already measured full-10M-point *render alone* at ~130 ms and this rebuild is a separate ~20M-write cost on top. If it does, the one mitigation tested is a **chunked CPU-side rebuild** spread across `requestAnimationFrame` calls (~200k vertices/chunk, ~50 chunks) rather than an off-thread Web Worker — chosen to avoid Worker/Vite-bundling setup this spike has no other need for, named explicitly as a choice of *a* mitigation, not *the only possible* one. Measured: each chunk's wall time (expected to individually clear budget), total wall-clock time to complete the rebuild, and — separately, since deck.gl has no partial-buffer-upload API — the cost of the single final GPU upload+render frame once the chunked rebuild completes (chunking the CPU loop is not assumed to also solve the upload-frame cost; that is checked, not inferred).

**24-bit picking ceiling** (M3 diagnostic note 2: `encodePickingColor` silently truncates past index 16,777,214 — 16,777,215 features/layer). Only the vertex-handle `ScatterplotLayer`s are `pickable: true`; nothing else consumes picking-index budget. The full-P2 vertex layer holds exactly 10,000,000 features — **59.6% of the ceiling**, the same order of headroom P1 already runs at (60%). The visible-subset (~2,000) and overlay (100) layers are trivially under it. Forward-looking constraint, not a finding: a P2 much larger than M4's declared scale would need either multiple picking passes (index ranges reassembled across layers) or coarser, polygon-level (not vertex-level) picking — one layer suffices only up to ~16.7M vertices.

**Scope limits named up front, not discovered later:**

- The server holds P2 behind a single `Mutex` (`Arc<Mutex<P2Dataset>>`); a long-held lock from the O(100k) bbox scan overlapping a concurrent `commit_vertex_edit` is architecturally possible but not exercised by this harness — single interactive session, no concurrent request in flight during a drag.
- The chunked rebuild is not cancellable mid-flight: if the origin threshold is crossed again while a chunked rebuild is in progress, this spike does not abort/restart it (docs/01 principle 7 asks for cancellable operations generally; this spike's chunking solves the block-the-canvas problem for a single rebuild, not composability of overlapping rebuilds).
- Full-P2-visible drag, and the full-scale commit cost derived from the same operation as origin-swap, are reported findings — not graded against ≤16 ms, per the framing above.
- Several parameters here are smaller than they would otherwise be — full-P2-visible drag's target sample count (20, not the 150 used for the graded visible-subset case) and pick-to-grab-latency's sample count (12, not 20) — and the origin-swap test runs earlier in the harness than its place in the report's own narrative order would suggest (right after the full P2 fetch, before the pick-latency and second drag-benchmark phases). None of this is arbitrary: it is the accumulated result of a real investigation into a reproducible smoke-testing failure on this machine (Windows 10 22H2, Intel UHD 630 / ANGLE-D3D11), whose root cause was **not** conclusively identified, named here rather than silently avoided:
  - Repeated runs consistently stalled somewhere in the harness's tail end, at a roughly fixed wall-clock offset from launch (~17-20 s) — not at a fixed iteration count of any one operation. A sustained `requestAnimationFrame` loop rebuilding a pickable 10M-feature layer every frame stalled at this mark in one run; a pure-CPU chunked array rebuild with **no WebGL calls at all** stalled at the same mark in another; a single-shot GPU buffer reupload stalled there in a third. That rules out any one specific operation as the cause.
  - Once stalled, not just `requestAnimationFrame` but a 30 s `setTimeout` safety guard also stopped firing, meaning the JS event loop itself was blocked, not merely throttled — consistent with the renderer process being stuck inside a synchronous call (plausibly a GPU/driver-level stall, e.g. Windows TDR recovery on the integrated GPU) rather than any script-level bug or scheduler starvation.
  - Explicitly disabling Windows process power throttling (`SetProcessInformation` / `PROCESS_POWER_THROTTLING_EXECUTION_SPEED`) on both the host process and every `msedgewebview2` subprocess did not prevent the stall, ruling out OS background-window power management as the cause.
  - Reducing an individual operation's iteration count did not reliably prevent the stall (halving the chunked rebuild's iteration count from ~50 to ~15 chunks made no difference), but reordering the harness so the *total* elapsed time before the historically-riskiest operations is shorter did — this is the strongest evidence for a wall-clock/cumulative-session trigger over a per-operation one, though the mechanism remains unconfirmed and the fix is empirical, not root-caused.
  - `DRAG_BENCHMARK_TIMEOUT_MS` (30 s) bounds both drag benchmarks, the chunked rebuild, and the full-scale commit reupload, so a recurrence degrades to a `timedOut`/null result instead of blocking the harness *when the JS event loop is still running* — but per the point above, the observed failure mode can freeze that loop entirely, in which case this guard cannot fire either. That gap is real and unresolved, not papered over. If the tester agent's independent run hits this same stall, that is itself corroborating data for this finding, not a harness bug to fix silently.

## Diagnostic notes (M1.5)

Informal follow-up to M1, not a milestone: M1 already recorded (unfiltered) that both the frame-time and time-to-first-pixels budgets were missed. M1.5 exists only to characterize *why* — GPU-bound, framework-bound, or workload-shaping — to feed M2–M5 planning. No pass/fail verdicts below, and the M0–M5 table above is unchanged. All four experiments reuse M1's exact fixed-seed 10M-point EPSG:2056 P1 dataset, the same `p1` custom protocol delivering Arrow IPC (never JSON, ADR-004), and the same self-driving pan/zoom benchmark harness (still no browser-automation path into the native WebView2 window). Run twice for a consistency check; both runs agree closely.

Hardware/method common to all four blocks below (same as M1): Windows 10 Pro 22H2 (build 19045), Intel UHD Graphics 630 (gen-9, ANGLE/D3D11, WebGL2), WebView2/Edge 150.0.4078.105, via `RUN_M1_5=1 npm run tauri dev`, `src/m1_5-diagnostics.ts`. 2 independent runs, `frameTimeP50`/`p95` computed the same way as M1 (sorted `onAfterRender` deltas). Sample target/cap per block: scaling curve and toggles use 60 samples / 10 s cap; visible-count uses 120 samples / 20 s cap (closer to M1's own 150/20 s, since it's the block most directly comparable to M1's headline number); streaming doesn't use frame-time percentiles at all (see block 4).

**1. Scaling curve** — same pan/zoom sweep at n = 500k/1M/2M/5M/10M points (`?n=` slices a prefix of the same dataset, no new RNG draw).

| n | Run 1 p50 / p95 (ms) | Run 2 p50 / p95 (ms) |
|---|---|---|
| 500,000 | 16.8 / 17.5 | 16.7 / 17.2 |
| 1,000,000 | 16.7 / 17.2 | 16.7 / 17.1 |
| 2,000,000 | 33.1 / 33.7 | 33.2 / 34.1 |
| 5,000,000 | 66.7 / 85.9 | 66.7 / 83.3 |
| 10,000,000 | 131.9 / 147.0 | 136.2 / 193.7 |

500k and 1M both floor at ~16.7 ms (the 60 fps/vsync interval) — frame time only starts climbing once per-frame GPU work exceeds that budget. From 1M→10M, p50 grows roughly in proportion to point count (2M ≈ 2× the 1M-and-up baseline, 5M ≈ 4×, 10M ≈ 8×; 5M→10M alone is almost exactly 2.0× time for 2.0× points in both runs). p95 at 10M is noticeably noisier across runs (147 vs. 194 ms) than p50, consistent with occasional GC/compositor stalls layered on top of a real, roughly-linear-in-n cost floor.

**2. Cheap toggles at 10M** — one reference layer config (pickable / antialiasing / depth-test all on, `radiusMinPixels: 0`) plus 6 single-setting variants, each measured once, delta vs. the reference's p50.

| Variant | Run 1 p50 (Δ) | Run 2 p50 (Δ) |
|---|---|---|
| reference | 136.7 ms (+0) | 143.4 ms (+0) |
| pickable:false | 132.5 ms (−4.2) | 135.1 ms (−8.3) |
| antialiasing:false | 137.3 ms (+0.6) | 143.3 ms (−0.1) |
| useDevicePixels:false | 138.2 ms (+1.5) | 143.8 ms (+0.4) |
| radiusMinPixels:1 | 138.7 ms (+2.0) | 144.6 ms (+1.2) |
| depthTest:false | 139.3 ms (+2.6) | 144.9 ms (+1.5) |

All deltas are single-digit-ms against a ~137–143 ms baseline (a few percent each); the largest and most consistent is `pickable:false` (−4 to −8 ms), the rest sit within noise of each other. None of these individually, nor all six summed, gets anywhere near closing the ~120 ms gap between the reference and the 16 ms/60 fps budget.

**3. Visible-count scenario** — `?bbox=` does an unindexed linear scan (`src-tauri/src/p1.rs` `arrow_ipc_bbox`) over the same 10M-point dataset, sized via a uniform-density assumption for ~1M points, then benchmarks pan/zoom over just that subset.

| | Run 1 | Run 2 |
|---|---|---|
| Points selected | 999,841 / 10,000,000 | 999,841 / 10,000,000 |
| Fetch (bbox scan + transfer) | 476.2 ms | 492.7 ms |
| Parse | 21.2 ms | 11.5 ms |
| Frame time p50 / p95 | 16.7 / 17.2 ms | 16.7 / 17.1 ms |

p50 lands back at the 60 fps/vsync floor — essentially identical to the n=1M point on the scaling curve (16.7 ms both places, all four runs). Caveat: the bbox filter's own O(n) unindexed scan cost (~0.5 s fetch here) is a confound on this experiment's numbers — it shows "does a smaller *rendered* set help," not "is server-side viewport filtering itself cheap." A real spatial index would be needed to answer the latter, and that's undesigned (M5's job).

**4. Streaming first-pixels** — Tauri 2.11.5's `register_uri_scheme_protocol` only resolves one complete response per request; no lower-level streamed-body API exists in this version (confirmed while building this diagnostic — worth carrying into M5's transport decision directly). Simulated instead via 40 sequential fetch+parse+render round trips over disjoint ~256k-row slices, each its own self-contained Arrow IPC message (own schema message per chunk, unlike real IPC streaming's schema-once framing), accumulating into a growing binary attribute buffer.

| | Run 1 | Run 2 |
|---|---|---|
| Time to first batch rendered | 143.7 ms | 138.6 ms |
| Time to stable view (all 40 in) | 4724.6 ms | 4696.7 ms |

First-batch-rendered is far closer to the <100 ms budget than M1's monolithic transfer (~2200–2900 ms) — roughly 15–20× better — though still ~40–45 ms over it. Time-to-stable-view is *slower* than M1's single-request transfer of the full 10M points, as expected: 40 round trips add per-request overhead (including a re-sent schema message each time) that a real backpressured streaming connection wouldn't pay. Caveat: this is a request-level simulation of chunking, not real streaming — it answers "does splitting the payload help first-pixels," not "what will the real ADR-004 transport cost," which is still undesigned and is M5's job. The `?n=`/`?bbox=`/`?chunk=` query params used across all four experiments here are spike-local diagnostic scaffolding, not a proposed SKP wire protocol shape.

Reading the four together: the scaling curve is flat at the vsync floor through 1M points and then grows roughly linearly with point count from 1M to 10M, not flat — that rules out a fixed per-frame framework tax as the dominant cost. The toggle deltas are all small (single-digit ms on a ~140 ms baseline) and don't sum to anything close to the gap, which rules out any one of these particular deck.gl/WebGL settings as the culprit. The visible-count experiment, despite its own scan-cost confound, recovers the 60 fps budget almost exactly by cutting the rendered set to roughly the same size as the n=1M scaling-curve point — i.e., cost tracks *points rendered per frame*, and reducing that count is the one lever tried here that actually closes the gap. Verdict: this reads as **GPU-bound work that scales with point count** (per-point/per-instance processing cost, not a fixed framework overhead and not explained by any single cheap toggle), with **workload-shaping** (rendering fewer points per frame, via viewport culling or level-of-detail) as the practical mitigation confirmed to work here — the two aren't competing explanations so much as cause and cure: M2–M5 should treat "reduce points rendered per frame" as the primary lever, and treat server-side spatial indexing and a real streaming transport (both still undesigned) as the open questions that determine whether that lever can be pulled cheaply.

**Addendum — opportunistic second-vendor check (GTX 1650), scaling curve only.** After a discrete NVIDIA GeForce GTX 1650 was installed for the M4 freeze investigation (see "Diagnostic notes (M4 freeze forensics)" below), block 1 above — the scaling curve — was re-run on it alone; the other three blocks (toggles, visible-count, streaming) were not, and remain single-vendor (UHD 630) results. The point of re-running just this block: check whether the GPU-bound verdict above generalizes to a second vendor, rather than being an artefact of one Intel iGPU. Same harness (`src/m1_5-diagnostics.ts`), same fixed-seed P1 dataset, same `?n=` prefix-slicing, same 60-samples/10 s-cap-per-point protocol, 2 independent runs. Hardware: Windows 10 Pro 22H2 (build 19045), NVIDIA GeForce GTX 1650 (`ANGLE (NVIDIA, NVIDIA GeForce GTX 1650 (0x00001F91) Direct3D11 vs_5_0 ps_5_0, D3D11)`), WebView2/Edge 150.0.4078.105, GPU forced via the same Windows per-app GPU-preference mechanism used for M4 (`GpuPreference=2;`, confirmed via each run's own `[M0 GPU REPORT]`), `RUN_M1_5=1 npx tauri dev --config <port-only override>` (port-only, no debug/anti-throttling flags).

| n | Run 1 p50 / p95 (ms) | Run 2 p50 / p95 (ms) |
|---|---|---|
| 500,000 | 16.7 / 17.1 | 16.7 / 17.4 |
| 1,000,000 | 16.7 / 17.3 | 16.7 / 17.2 |
| 2,000,000 | 16.7 / 17.4 | 16.7 / 17.2 |
| 5,000,000 | 16.7 / 17.2 | 16.7 / 17.6 |
| 10,000,000 | 16.7 / 17.6 | 16.6 / 17.6 |

Flat at the vsync floor (~16.6–16.7 ms p50) across the *entire* 500k–10M range — contrast directly against the UHD 630 table above, which climbs from ~16.7 ms at 500k/1M to 131.9–136.2 ms at 10M for the identical dataset and code path. This is strong second-vendor confirmation of the "GPU-bound work that scales with point count" verdict, not just a restatement of it: the same per-point workload that saturates the UHD 630's throughput at 10M points sits comfortably under budget on a GPU with more headroom, which is exactly what a GPU-bound explanation predicts and a fixed-framework-cost or workload-shaping-only explanation would not — a framework tax would show up on both GPUs equally, and it doesn't. (The fixed-framework-cost explanation was already weak per the four-block reading above; this is corroboration from a different axis — hardware — not a repeat of the same axis.) Bonus data point, not a full write-up of its own: the same run's raw M1-style single-request 10M-point benchmark — the one block 2's toggle-reference setup runs once, unconditionally, before any toggle variant — also came back at 16.7 ms p50, a ~9–10× improvement over M1's own committed UHD 630 number (157.4–158.5 ms) for the literal same, unmodified rendering path; consistent with, but not a substitute for, the scaling-curve table above. Scope: scaling curve only — the toggle, visible-count and streaming blocks were not re-run on this GPU and remain single-vendor (UHD 630) results, so nothing above should be read as a full second-vendor pass of M1.5.

## Diagnostic notes (M3) — picking-buffer surprises under offset-relative rendering

Recording what the picking path does that the rendering path did not prepare you for. This is an explicit M3 deliverable, not a milestone: no pass/fail verdicts below, and the results table is unchanged. Same hardware, harness and three bit-identical runs as the M3 rows.

**1. `PickingInfo.coordinate` is a local-frame value wearing a coordinate's clothes — and it is untagged.** This is the concrete form of the local-frame leak the M2 review flagged, and it is the single reason the measured path is `pick id → host-side f64 lookup` rather than unprojecting the cursor. deck.gl unprojects through the viewport, and under offset-relative rendering the viewport is itself in the local frame, so `info.coordinate` comes back as metres from a renderer-internal origin — a bare `[x, y]` with no frame attached, structurally indistinguishable from an EPSG:2056 pair. **Two distinct error terms live here, and conflating them would make a *type* error look like a rounding error:**

- **(i) Frame error — 3 116 272 m.** Taking the raw value at face value, as any caller reading a `[x, y]` would: a sampled raw value is `[4.894791666666664, −3.042708333333324]` where the real easting is ~2.6×10⁶. `rawCoordinateIsLocalFrame: true` on every run. The error is not a precision artefact; it *is the origin*. Computed independently, the frame origin at the NE probe has magnitude 3 116 271.7 m against the harness's worst-case 3 116 272.08 m — the 0.4 m difference is just the click's own offset within the local frame. 3.1×10⁸ times the 1 cm budget, and a fixed offset rather than noise, so no amount of averaging or zooming touches it.
- **(ii) Residual after correctly restoring the origin — 0.1789 m = 1.352 px**, dead-centre clicks only (the 1–3 px offset clicks are reported separately at 0.4770 m = 3.606 px, since unprojection there correctly recovers the *cursor*, not the feature; folding them in would let "the user clicked 3 px off" masquerade as a pixel-resolution limit). Still **~18× past the 1 cm budget**. But the mechanism is worth naming precisely, because it is *also* not precision: recomputing `hypot(frac(x), frac(y))` over the harness's own dead-centre click positions — the harness floors each predicted position to an integer pixel before picking — gives a worst case of **0.1789177761753911 m**, against the measured **0.1789177761906838 m**. Those agree to **1.5×10⁻¹¹ m**. So the residual is *entirely* integer-pixel click quantization; deck.gl's unprojection arithmetic contributes nothing measurable at f64. The conclusion the metric definition drew is strengthened rather than weakened: at 1:500, 1 cm is 0.0756 px, so even a hypothetical perfect sub-pixel cursor would leave ~0.5 px = 0.066 m, still **6.6× the budget**. Unprojection cannot reach centimetres here at any implementation quality.

By contrast the host-side reply is frame-tagged on purpose — `resolve_pick` returns `{crs: "EPSG:2056", e, n}` and `wrongCrsTagSeen: false` on every run. That asymmetry is the finding: the path that crosses the Rust boundary names its CRS, and the path that comes out of the renderer does not (docs/01: CRS is a type).

**2. deck.gl's pick index is 24-bit, with a silent ceiling at index 16 777 214 (2²⁴ − 2) — 16 777 215 features per layer.** Read from deck.gl 9.3.7 source (`@deck.gl/core` `dist/lib/layer.js`), **not measured here**, in the same spirit as M2's shader auto-offset note — a version bump invalidates the explanation: `encodePickingColor` writes `(i + 1)` into an RGB triple as `(i+1) & 255`, `((i+1) >> 8) & 255`, `(((i+1) >> 8) >> 8) & 255`, and `decodePickingColor` reads `i1 + i2·256 + i3·65536 − 1`. Past that index the third byte truncates the bits above 24 and a pick returns a **wrong-but-plausible index with nothing raised** — the same silent-wrong-answer shape as the ordinal-vs-id hazard, one level lower. P1's 10M points sit at ~60 % of that ceiling, so the spike's own headline dataset fits with less headroom than is comfortable, and M4/M5 should treat the ceiling as a real constraint on any "one layer, all the features" plan rather than an abstraction.

**3. The picking footprint is bigger than the styled footprint.** Class (c)'s discrimination curve is only reproducible (all 27 cells, exact counts and flags) with an effective radius of **1.49–1.57 px** against a styled `getRadius: 1` px — roughly half a pixel of rasterization/antialiasing spread that the picking pass keeps and the visible disc fades out. Fitted outside the harness, so corroboration rather than measurement. The practical consequence is that **picking resolution is a style-dependent property**: two features are distinguishable or not depending on how big they are drawn, and where footprints overlap the picking buffer keeps whichever drew last — a draw-order artefact, deterministic but arbitrary. Any future "pick the nearest feature" behaviour has to be built on top of that, not assumed from it.

**4. Nothing here is a coordinate-precision effect, and that is itself the result.** Every M3 surprise above is a frame error, an input-resolution limit, an encoding ceiling or a style artefact. At the local-frame magnitudes offset-relative rendering produces (metres, not megametres), the f32 ULP is ~5×10⁻⁷ m — five orders of magnitude below both the smallest separation tested (0.10 m) and the 0.18 m unprojection residual. The precision question M2 answered stays answered; what M3 exposes is that the *plumbing* around picking has its own failure modes, and they fail silently by default.

## Diagnostic notes (M4 freeze forensics) — root cause found: unhandled BigInt serialization `TypeError`

Follow-up to the M4 scope-limits paragraph above, which recorded the freeze as observed on one GPU (Intel UHD 630) with one hypothesis (GPU/driver-level stall, plausibly TDR) that was never confirmed. This section documents a second, deeper investigation, run after a discrete GPU (NVIDIA GeForce GTX 1650) was installed and the app forced onto it via Windows per-app graphics settings, specifically to test that hypothesis. The investigation below initially converged on a different, also-wrong hypothesis before the actual cause surfaced — both are recorded here, in the order they were found, rather than silently editing history down to just the right answer.

**Verdict: not GPU-vendor-specific, not occlusion/throttling, not a Rust-side lock, and — contrary to an earlier draft of this section — not WebGL-context churn either. The freeze was an unhandled `TypeError: Do not know how to serialize a BigInt`, thrown synchronously inside `JSON.stringify(report, null, 2)` at the end of `runM4()`, which silently terminated the harness's async function with zero visible symptom.** `PickLatencyResult.lastReturnedId` (`src/m4-editing.ts`) was typed and populated as a raw `bigint` — read directly out of a `BigUint64Array` (the P2 vertex-id buffer) via `arrayIdResolver`/`resolveVisibleId` — and three of these values (`pickToGrab.fullSet`, `.overlay`, `.visibleSubset`) are embedded directly in the final `M4Report` object. `JSON.stringify` has no native support for `bigint` and throws unconditionally the instant it reaches one; there is no partial output, no `NaN`, just an immediate, synchronous exception with nothing downstream ever running. This is a one-line application bug, unrelated to any GPU, driver, WebGL context, or lock — it would have frozen identically on the Intel UHD 630, the GTX 1650, or any other hardware, every single time, at exactly the same point in the harness. **Fixed and verified end-to-end on the GTX 1650** (see "Fix and verification" below); the four M4 result rows above remain `NOT OBTAINED` pending an official measurement run under normal (non-debug) conditions, not because the cause is still unknown.

**GPU switch confirmed working, and the freeze followed anyway.** `[M0 GPU REPORT]` reported `ANGLE (NVIDIA, NVIDIA GeForce GTX 1650 (0x00001F91) Direct3D11 vs_5_0 ps_5_0, D3D11)` (two prior attempts, before the Windows per-app graphics setting was corrected to include the exact versioned `msedgewebview2.exe` path, still reported the Intel adapter and were aborted without running anything further, per instruction). With NVIDIA confirmed active, an `RUN_M4=1` run still stalled — same qualitative signature as every UHD 630 run before it. That single fact already excludes "Intel driver bug" as the sole explanation; everything below narrows further.

**Methodology pitfall found and fixed first: heartbeat files must not live in `src-tauri`.** The first instrumented run silently became a rebuild-restart loop — the dual heartbeat (below) was originally written to the CWD, which is the same tree `tauri dev`'s file watcher monitors for rebuild triggers, so every 1 Hz write retriggered a full rebuild-and-relaunch. Fixed by writing to `std::env::temp_dir()` instead. Worth recording as a general pitfall for any future diagnostic file-writing in a `tauri dev` session, not just this one.

**Instrumentation added** (all diagnostic, none of it part of the M4 measurement path itself):
- **Dual heartbeat**, 1 Hz each: `rust-heartbeat.txt` from a plain OS thread in `run()` that acquires no application locks (proves the *process* is scheduled, independent of the webview); `js-heartbeat.txt` from a JS `setInterval`, fire-and-forget `invoke()` (proves the JS event loop ticks *and* the IPC round trip completes). Both temp-dir-resident per the pitfall above.
- **BEGIN/END checkpoints**, immediately flushed (`[CHECKPOINT] <ts_ms> <label>`, `std::io::stdout().flush()` after every line — necessary because a hung operation must not leave its marker sitting in an unflushed buffer): every `Mutex::lock()` in `lib.rs` (`commit_vertex_edit`, `resolve_p2_vertex`, the `/p2` protocol handler), and every one of `runM4()`'s 12 `setStatus()`-delineated phases, via `setStatus` itself emitting `END <previous phase>` then `BEGIN <new phase>`. The scheme's whole value: **the last BEGIN with no matching END names the culprit** without needing to guess where to look.
- **Remote debugging** (`--remote-debugging-port=9222` via `additionalBrowserArgs` — the correct Tauri 2 window-config key; note it *replaces* wry's own default `--disable-features=msWebOOUI,msPdfOOUI,msSmartScreenProtection`, so any config carrying it must repeat those or accept losing them) plus a small Node.js CDP client (native `fetch`/`WebSocket`, no extra dependencies) that attaches to the page target and runs `Runtime.evaluate`.

**Reproducer/control pair, run to the discipline the addendum specified** (same phase as spontaneous freezes, same heartbeat signature, foreground-restore as the discriminator, not just "did it stall when minimized"):

- **Test 1a (minimize immediately after launch).** Stalled at the *same* phase every UHD 630 run eventually hit: the last checkpoint is `BEGIN M4: full-scale commit patch + reupload`, no `END`. Both heartbeats kept ticking at 1 Hz throughout (`rust-heartbeat.txt` and `js-heartbeat.txt` both advancing steadily) — the process and the JS event loop's own timer callbacks were alive. **CDP `Runtime.evaluate("1+1")` responded in 3 ms** while the phase was stalled: the JS *engine* was fully evaluable, not wedged. A follow-up CDP check found `webgl2ContextLost: false` and `document.visibilityState: "visible"` (correctly reflecting the restore, done next) — ruling out a lost WebGL context or a stuck Page Visibility state as the mechanism. **The window was then restored to the foreground and kept there for 60+ seconds — no recovery.** Per the addendum's own logic, that is the sharp discriminator: reversible throttling would have resumed on restore; this didn't, meaning whatever state it entered was entered *permanently*, not just suspended for as long as the window was occluded.
- **Test 1b (foreground the entire time, display sleep disabled via `powercfg /change monitor-timeout-ac 0`, `AppActivate` right after launch, window never minimized).** **Stalled at the identical phase** — `BEGIN M4: full-scale commit patch + reupload`, no `END`. This is the decisive result: with zero window-occlusion events of any kind, the freeze reproduced anyway. **Occlusion/throttling is excluded as the cause**, not merely unconfirmed — a genuine reproducer that requires no minimizing at all falsifies it directly. (Test 1c, the anti-throttling/anti-occlusion browser-arg run, was not executed after this result: it tests a mechanism already excluded by 1b, and running it would have spent time confirming a negative the evidence already established rather than narrowing further. Named as a deliberately skipped test, not a silently dropped one.)
- **A live specimen from test 1a was used for two more checks before being killed:** a fresh `requestAnimationFrame` scheduled via CDP **fired in 0.5 ms** — generic rAF scheduling was completely healthy at the browser/page level, at the exact moment the harness's own render-dependent phase had been stuck for over a minute. And an Event Viewer sweep (`Get-WinEvent`, `System`/`Display` provider id 4101 for TDR, `Application` id 1002 for hangs, plus a free-text sweep for `app.exe`/`msedgewebview2` across both logs, ±10 minutes around the freeze timestamp) returned **zero matches in every category** — no TDR, no WER-logged hang, nothing. Combined with the Rust checkpoints never showing an unmatched `LOCK_BEGIN` in any run, this rules out a GPU driver reset, a Windows-logged application hang, and a Rust-side mutex as the mechanism, leaving the stall entirely inside this one JS-side operation.

**Confirming experiment: reverting the specific code suspected first did *not* fix it.** The stalling phase (`full-scale commit patch + reupload`) had, in the version committed for M4, been changed from constructing a dedicated `Deck` instance to reusing `pickDeck` (already used for three prior pick-to-grab renders) — a change made late in that session as one of several throughput mitigations for the then-unexplained freeze, and the obvious first suspect once this phase was implicated. It was reverted back to a fresh `Deck` instance and the foreground-only control (test 1b) was re-run: **identical stall, same phase, same signature.** `pickDeck` reuse is therefore excluded as *the* cause — correctly, as it turns out, though not for the reason believed at the time (below). *(Retracted: this section originally also read the ordinal position of the stall — consistently the 8th/9th sequential `new Deck(...)` call in `src/m4-editing.ts`'s execution order — as the strongest remaining correlate, and built a "WebGL-context churn" verdict on top of it. That ordinal position is real but coincidental: it is simply where in `runM4()`'s linear phase sequence the final report-assembly-and-`JSON.stringify` step sits, which is deterministic and has nothing to do with how many `Deck` instances preceded it. Left visible here, struck through in substance rather than deleted, because the reasoning was evidence-based and wrong in a way worth being honest about.)*

**Interpretation matrix** (as specified), with this investigation's actual reading against it — **all of it correctly excludes what it excludes; the row it was closest to ("both alive + rendering stopped → deck.gl/ANGLE presentation") is the one that turned out to be the wrong closest match, because nothing about deck.gl or ANGLE was actually involved:**

| Signature | Meaning | Observed here? |
|---|---|---|
| Rust alive + JS stopped + DevTools responsive | Scheduling/occlusion | Partially — JS *engine* and generic rAF stayed responsive, but excluded as the full explanation by test 1b's occlusion-free reproduction |
| Rust alive + JS stopped + DevTools unresponsive | Renderer blocked / synchronous call | Not observed — DevTools stayed responsive in every check |
| Rust stopped | Backend/IPC/lock | Not observed — `rust-heartbeat.txt` never stopped in any run, and no Rust checkpoint ever showed an unmatched `LOCK_BEGIN` |
| Both alive + rendering stopped | deck.gl/ANGLE presentation | Ruled out by the actual finding below — nothing was "stopped": `runM4()`'s promise chain had already terminated via an unhandled synchronous exception, which looks identical to "rendering stopped" from every signal this matrix checks (heartbeats, CDP eval, rAF), because none of them can detect an uncaught exception on an unrelated promise chain |
| Everything dead + GPU dump blocked | WebView2/ANGLE/driver stack | Not reached — never needed, since CDP and rAF both stayed live throughout |

**The gap in the matrix, and what actually closed it.** Every check above answers "is something still scheduled and alive?" — and the honest answer was yes, at every layer, every time. None of them answer "did an exception fire?", which is a different question with a different instrument: a global `unhandledrejection`/`error` listener, not a liveness probe. That gap is exactly what the reviewer caught in review, independent of any of the above: the stalling phase's own `CAPTURE_TIMEOUT_MS` `setTimeout` (5 s, GPU-independent, no `await` between it and the harness's final phase) never fired either — and "the render loop is stuck" does not explain a plain JS timer failing to fire. That inconsistency is what prompted adding `startErrorReporting()` (`src/main.ts`) — `window.addEventListener("unhandledrejection", ...)` and `("error", ...)`, both reporting through the same `js_checkpoint` sink as the BEGIN/END markers. **The very next run surfaced the answer immediately:**

```
[CHECKPOINT] 1785749767895 JS:UNHANDLED_REJECTION: TypeError: Do not know how to serialize a BigInt STACK: TypeError: Do not know how to serialize a BigInt
at JSON.stringify (<anonymous>)
at runM4 (http://localhost:5180/src/m4-editing.ts:794:43)
```

Cross-referencing against the source: `PickLatencyResult.lastReturnedId` (`src/m4-editing.ts`) was declared `bigint | null` and populated in `measurePickLatency` by reading elements straight out of a `BigUint64Array` (the P2 vertex-id buffer) via `arrayIdResolver`/`resolveVisibleId`. Three instances of it — `pickToGrab.fullSet`, `.overlay`, `.visibleSubset` — are embedded directly in the `M4Report` object, which two call sites at the end of `runM4()` pass straight to `JSON.stringify(report, null, 2)` (one to render `#m4-stats`, one to build the `log_m4_report` payload). `JSON.stringify` has no `bigint` serializer and throws synchronously and unconditionally the instant it reaches one — not a slow path, not a partial write, an immediate `TypeError` with nothing after it ever running.

**Why this explains every prior observation, including the ones that looked like evidence for a GPU/rendering cause:**
- **Both heartbeats stayed alive** because they run on execution paths structurally independent of `runM4()`'s promise chain — a `setInterval` fire-and-forget `invoke()` and a Rust background thread with no application locks. An uncaught exception inside one specific async function has zero effect on either.
- **CDP `Runtime.evaluate` and a fresh `requestAnimationFrame` both worked instantly** because the JS engine and event loop were never blocked in any sense — there was nothing frozen to unblock. `runM4()`'s own promise chain had simply ended, silently, like any unhandled rejection with no listener.
- **The phase's own `setTimeout` safety guards never fired** because none were pending — the exception happens in the synchronous report-assembly/stringify step, *after* every timed operation in the harness had already completed successfully. There was no timer left running to time out.
- **The foreground-restore tests showed "no recovery"** because there was nothing to recover: the async function had already permanently terminated, well before either occlusion test began. Occlusion was never in the causal chain — a true statement then and now, just for the wrong reason (there was no live thing left for occlusion to affect, rather than "occlusion doesn't affect it").
- **The 8th/9th-`Deck`-construction ordinal correlation** was `runM4()` reaching the same deterministic point in its own linear phase sequence every time, not an accumulating resource cost.

**Fix and verification.** `PickLatencyResult.lastReturnedId` is now typed `string | null`; `measurePickLatency` converts at its own return boundary (`lastReturnedId === null ? null : lastReturnedId.toString()`), so the raw `bigint` never leaves the one function that legitimately needs it (resolving a picked index against the id buffer) and everything downstream — report construction, DOM display, IPC logging — only ever sees a plain string. `npx tsc --noEmit` clean. A fresh `RUN_M4=1` run (GTX 1650, forensics browser args from this investigation still active — `--remote-debugging-port=9222` plus the anti-throttling/anti-occlusion flags — so **this run's numbers are a correctness check, not a clean measurement, and are not being transcribed into the results table above for that reason**) completed `runM4()` end-to-end for the first time in this investigation: `valid: true`, `invalidReasons: []`, a full `M4Report` printed and logged, `END M4: editing measurement complete` reached, no further unhandled rejections or errors. Sample of what a complete run now looks like: visible-subset drag p50/p95 16.7 ms / 19.5 ms (150 frames) against the 16 ms budget, pick-to-grab p50 12.1/2.1/1.4 ms (full-set/overlay/visible-subset), chunked origin-swap rebuild 827 ms total across 50 chunks, full-scale commit patch+reupload 82.7 ms, precision worst error 0.054 px (budget 0.5 px), commit round trip bit-exact. These are evidence the harness now runs to completion, not the milestone's official numbers.

**Consequence for the UHD 630 rows above and for the rest of the original request.** The bug is a plain JS `TypeError`, entirely independent of GPU vendor, driver, or WebGL context — it would have hit the Intel UHD 630 identically, at exactly the same point, every time. The UHD 630 rows' own recorded inference ("plausibly Windows TDR recovery on this Intel UHD 630/ANGLE-D3D11 combination... inference, not measurement") was reasonable given the evidence available at the time and is left as originally written per instruction (no M4 row is overwritten here), but is now understood to be the wrong mechanism — not a hardware-specific failure at all. Practically, this also retires the "split the M4 harness into per-block launches" workaround from the original request's part (3): that plan assumed a hardware-triggered stall that per-block isolation might dodge, and the actual cause has nothing to do with block boundaries or hardware. With the bug fixed, the straightforward next step is to re-run the standard (non-debug-args) M4 protocol directly on the UHD 630, same as the GTX 1650, rather than the exotic split-launch harness.

## Outcome

_To be written when milestones conclude: **Accept deck.gl** / **Fallback WebGPU renderer**, with rationale. Feeds the ADR-003 status change. Spike code is throwaway; this section is the deliverable._
