# Wave 1, A4 (batch 1, pair 2: A4+A5): the session's report

**Source:** session_01NZDimj5kbXchTDKA9MQrPT, the final assistant turn, copied at 2026-09-25 about 23:24Z with the message's own Copy action in claude.ai/code, and saved with LF line endings. Everything between the two rules below is the session's text, verbatim. The WAVE1 REPORT proper begins at `# WAVE1 REPORT`; the lines above it are the same turn's progress narration. The custodian fields follow the second rule. They are filled locally, per `state/cloud/wave1-prompts.md` §3.

---

Reading the TypeScript refusal formatters to compare their code tables against the kernel's.

Checking the Tauri publish seam and other `to_string()` flattening sites.

I have four candidate findings mapped; now checking the toolchain so I can build reproducers.

Reproducer file written; building the kernel test target (DuckDB bundled compile, will take a while).

Build running in background. Meanwhile setting up the branch and proving the DCO hook.

Hook rejects unsigned commits (exit 1, HEAD unchanged). Checking the known-limits and pending-decisions files for duplicates while the build runs.

Still compiling DuckDB; I'll wait for the build notification.

Waiting for the kernel test build to finish.

All four reproducers fail at baseline as expected. Confirming exact line numbers, then committing.

# WAVE1 REPORT
## Worker fields
Item: A4          Lens/purpose: Refusal exhaustiveness: does every failure reach the caller as a typed, named code with no catch-all, and keep that code on every path?
Baseline SHA: bb98f71f43a2891d317b10a124387df9d5ee0ebf (confirmed at start with `git -C /tmp/wave1-baseline rev-parse HEAD`)
Branch: cloud/wave1-A4   Commits: a7835b1b65ea723c9ad423bfdf908667a1bdd650 (one test file, `kernel/tests/wave1_a4_refusal_codes.rs`, signed off; no PR opened)
Environment: Linux 6.18.44 x86_64 · rustc 1.94.1 · cargo 1.94.1 · node v22.22.2 · npm 10.9.7. Network: crates.io only, for the cargo build. No npm install was run. The final git push used the session's git remote.
Commands run:
- `git worktree add /tmp/wave1-baseline bb98f71…` → exit 0; `rev-parse HEAD` → bb98f71…
- `cargo test -p spatial-kernel --test wave1_a4_refusal_codes --no-run` → exit 124 (my 590 s timeout), then re-run in the background → exit 0
- `cargo test -p spatial-kernel --test wave1_a4_refusal_codes` → exit 101: 4 run, 4 failed (each failure is the reproduction)
- `git checkout -b cloud/wave1-A4`, `git config core.hooksPath .githooks` → exit 0
- DCO proof: unsigned `git commit --allow-empty -m "test: unsigned probe"` → exit 1, "commit has no Signed-off-by trailer", HEAD unchanged
- `git -c user.name=chris -c user.email=chrys92d@gmail.com commit -s` → exit 0
- `git push -u origin cloud/wave1-A4` → exit 0
- Reading and grepping (grep, sed, cat) → all exit 0

Findings: 4

Checked and clean: `error_of` (`kernel/src/skp.rs:1233-1356`) matches every one of the 29 `EngineError` variants, with no wildcard arm. `filter_error_of` and `PublishError::code()` have no wildcard arms either. The TypeScript client types `code` as a string, and `formatRefusal` keeps every code as it arrives, so the kernel emits no code that the client drops.

### Finding A4-1
Claim: A `PublishError` raised during publish execute reaches the shell without its `publish.*` code, so `formatPublishRefusal` labels it `publish-refused`.
Suggested severity: S2
Code path:
- `kernel/src/permission/boundary.rs:155` (`BoundaryError::Publish(e) => write!(f, "{e}")`, with no `refusal_detail`)
- → `frontends/shell/src-tauri/src/publish.rs:734` (`Err(e) => ExecuteOutcome::Refused { message: e.to_string() }`)
- → `frontends/shell/src/publish/PublishPanel.tsx:272` → `formatPublishRefusal.ts:48` (`code: "publish-refused"`)
Reproducer: `kernel/tests/wave1_a4_refusal_codes.rs::a4_1_…`, run with `cargo test -p spatial-kernel --test wave1_a4_refusal_codes`. Observed: `publish.cancelled -> shell sees 'publish-refused' from "cancelled: no bundle exists…"`, and the same for `publish.destination_exists` and `publish.ceiling_exceeded`.
Guarantee violated: `protocol/skp/SKP-V0.md:676-684`, which says a refusal "scoped to `PublishError`" reaches the shell as `"<code>: <display>"`. It names only the two preflight sites; the execute site carries `PublishError` too.
Evidence: The test builds `BoundaryError::from(e).to_string()`, the exact expression at `publish.rs:734`. None of the three variants tested kept its code once parsed.
False-positive check: I searched KNOWN-LIMITATIONS.md and DECISIONS-PENDING.md for ExecuteOutcome, execute_with_progress, BoundaryError, refusal_detail and "typed code". DECISIONS-PENDING entry 96 recorded a publish code being lost at the preflight site, and that has since been fixed (`publish.rs:513`). The execute site is not recorded anywhere.
Confidence: proven. The kernel expression is reproduced; the Tauri line is established by code path only, because `src-tauri` is not a workspace member and was not built.

### Finding A4-2
Claim: Every `EngineError` that reaches the publish surface is collapsed into the single code `publish.engine`, and the engine's own code appears nowhere in the string.
Suggested severity: S3
Code path:
- `kernel/src/publish/mod.rs:453` (`ds.resolve_projection(..)?` inside `preflight_pinless_parts`, which gives `AttributeUnpublishable`) and `:632`/`:811` (`SourceChangedUnderPublish`)
- → `kernel/src/publish/error.rs:357-364` (`From<EngineError>`) → `:206` (`Self::Engine(_) => "publish.engine"`) and `:354` (`Self::Engine(e) => write!(f, "{e}")`)
- → `frontends/shell/src-tauri/src/publish.rs:409`/`:513` (`refusal_detail`)
Reproducer: `…::a4_2_…`. Observed: `engine.source_changed_under_publish -> "publish.engine: refused: the source is not what was pinned…"`, and likewise for `attribute_unpublishable` and `connections_exhausted`.
Guarantee violated: the audit's no-catch-all rule (ADR-021 decision 8, applied through `error_of`). The engine's typed identity is dropped when it crosses onto the publish surface.
Evidence: `refusal_detail()` for all three variants tested contains no `engine.*` code.
False-positive check: I found no entry for `publish.engine` or `Engine(EngineError)` in KNOWN-LIMITATIONS.md or DECISIONS-PENDING.md. `typed_terminal_codes.rs` tests only `GeographicCrsNotPublishable`, `RowFilterNotRecordable` and `SourceNotPinned`.
Confidence: proven.

### Finding A4-3
Claim: An engine refusal during the publish pin phase reaches the shell with no code at all, neither `engine.*` nor `publish.*`.
Suggested severity: S2
Code path:
- `engine/src/pin.rs:83` (`Source`) and `engine/src/dataset.rs:608`
- → `frontends/shell/src-tauri/src/publish.rs:1073` (`Err(e) => EnsurePinnedOutcome::Failed(e.to_string())`)
- → `:417` (`PrepareOutcome::Refused { message }`) → `formatPublishRefusal.ts:48`
Reproducer: `…::a4_3_…`. The test opens a real fixture, removes the file, then calls `pin_content_observed`. Observed: ``the pin-phase refusal `engine.source` reaches the shell with no code (parsed `publish-refused`): "source: open for hashing: No such file or directory (os error 2)"``.
Guarantee violated: the audit's rule that every failure is a typed, named refusal. SKP-V0.md:680-683 lists the untyped exceptions on this seam (permission error, IPC rejection, unknown-attempt sentence), and an engine error is not one of them.
Evidence: This is a real engine error, stringified exactly as line 1073 does it.
False-positive check: I searched ensure_pinned and pin in DECISIONS-PENDING.md. Entries 7 and 1429 concern the pin's cancel and progress behaviour, not its code. Nothing in KNOWN-LIMITATIONS.md covers it.
Confidence: proven. The Tauri line is established by code path.

### Finding A4-4
Claim: On the raw-params data-plane admission path, an engine refusal at stream-create time reaches the `TERM_PRODUCER_FAILED` terminal without the `engine.<code>: ` prefix that mid-stream refusals on the same path carry.
Suggested severity: S3 (the shell installs `ticket_only`; this path belongs to the kernel's own binary)
Code path:
- `kernel/src/main.rs:158` (`EngineSourceFactory::with_connection_reports`, which is raw mode)
- → `kernel/src/lib.rs:366` (`open_engine_stream(..).map_err(|e| e.to_string())`)
- → `protocol/data-plane/src/server.rs:388-399` (`terminal_and_drain(.., TERM_PRODUCER_FAILED, &detail, ..)`)
- For contrast, the mid-stream path uses `terminal_detail_of` (`lib.rs:553-556`).
Reproducer: `…::a4_4_…`. Observed: `"refused: the viewport is expressed in EPSG:4326 and the dataset is in EPSG:2056…"`, with no `engine.viewport_crs_mismatch: ` prefix.
Guarantee violated: `SKP-V0.md:676-678` and the `terminal_detail_of` doc (`kernel/src/skp.rs:1212-1225`), which say a data-plane terminal's detail is `"<code>: <display>"`.
Evidence: The test calls `EngineSourceFactory::new(catalog).create(..)` directly with a mismatched viewport CRS.
False-positive check: I searched raw-params, create_from_raw and slice-host in KNOWN-LIMITATIONS.md and DECISIONS-PENDING.md and found nothing. `typed_terminal_codes.rs` covers only the `ticket_only` path.
Confidence: proven.

### Unproven observations (not findings; at most five, one line each)
- Ticket-redemption refusals (`kernel/src/skp.rs:210-220`, and `lib.rs:326`/`:396`/`:399`) are plain strings with no code, so the shell labels them `stream-failed`; SKP-V0.md may treat these as outside the taxonomy, and I did not check that.
- `PublishError::StagingNotRemoved { after }` has its own code, so the inner failure's code appears only in the prose (`error.rs:205`/`:344`); I did not reproduce this.
- The engine's `Source`, `GeoMetadata`, `Wkb`, `Query` and `Arrow` refusals carry their detail only in `message` with empty `fields` (`skp.rs:1236`/`:1286`/`:1290`/`:1295`/`:1296`); the code survives, so this is probably out of scope.
- Grant, permission and audit refusals at `publish.rs:518`/`:531`/`:543`/`:556`/`:690` cross without a code; SKP-V0.md:681 declares "a permission error" untyped, so this looks intended.
- An execute-time cancel arrives as `Refused` prose rather than a distinct outcome, so the shell cannot tell it apart by code; this follows from A4-1 and may not be separate.

### Stops (if any): none

---

## Custodian fields (filled locally, never by the worker)

Session ID: session_01NZDimj5kbXchTDKA9MQrPT   Model: Opus 5.5, Medium (as launched)   Launched/ended: 2026-09-25T22:48:55Z / the report posted by about 23:16Z (its commit a7835b1 at 23:14:46Z).
Spend: not individually attributable; batch delta $12 for the pair (A4+A5). Cloud session credits read "$237 of $250 left" at about 22:48Z and "$225 of $250 left" at 23:25:30Z after a refresh. A5's session still showed one running task at that reading, so the delta may not be final. The page shows whole dollars only, and no per-session figure is shown anywhere.

Triage against main at 001115f (wave-1 rule 1, `state/directives/2026-09-25-cloud-hooks.md` §4): `git diff bb98f71 001115f` over `kernel`, `engine`, `protocol` and `frontends` touches only `engine/B1-PROJECTION-PREREGISTRATION.md` and `protocol/skp/SKP-V0.md`. SKP-V0's change is an insertion after its line 722, so the cited lines 676–684 read the same. Every item below is **still-present**.

Finding A4-1: RECORD. Final severity: S2 (§4: a refusal under the wrong code). Reason: the code path convinces. `kernel/src/permission/boundary.rs:155` @ 001115f displays a `Publish` error without its code, and `frontends/shell/src-tauri/src/publish.rs:734` @ 001115f passes that display on as `Refused { message }`. The shell therefore labels an execute-time publish refusal `publish-refused`, where SKP-V0.md:676-684 routes a `PublishError` refusal as code plus display. The refusal is still shown and still refuses; only its code is lost.
Reproduced locally (Windows): not attempted. The kernel expression is reproduced on cloud/wave1-A4 (a7835b1), and the Tauri line was read at main.

Finding A4-2: RECORD. Final severity: S2. Reason: `publish.engine` (`kernel/src/publish/error.rs`, `From<EngineError>`) is a catch-all code on the publish surface. It drops the engine's own typed code, against the no-catch-all rule the audit applied. Reproduced on cloud/wave1-A4.
Reproduced locally (Windows): not attempted.

Finding A4-3: RECORD. Final severity: S2 (§4: a refusal under the wrong code, here none at all). Reason: `publish.rs:1073` @ 001115f stringifies a pin-phase `EngineError` into `EnsurePinnedOutcome::Failed`, and it reaches the shell with no code. Reproduced on cloud/wave1-A4 with a real engine error.
Reproduced locally (Windows): not attempted.

Finding A4-4: RECORD. Final severity: S2 (§4: a defect on a path the shell does not take today). Reason: on the kernel binary's raw-params admission path, a create-time engine refusal reaches `TERM_PRODUCER_FAILED` without the `engine.<code>: ` prefix that the mid-stream path carries. The shell installs `ticket_only`, so the shell never meets it. It is reachable through the headless kernel binary. Reproduced on cloud/wave1-A4.
Reproduced locally (Windows): not attempted.

Unproven observations:
1. DISCARD, S3: unverified. The worker did not check whether SKP-V0 places ticket-redemption refusals inside the taxonomy.
2. DISCARD, S3: speculation, not reproduced. `StagingNotRemoved` carries its own code.
3. DISCARD, S3: the code survives, and the worker judged it out of scope.
4. DISCARD, S3: declared. SKP-V0.md:681 names the permission error untyped.
5. DISCARD, S3: a consequence of A4-1, recorded there.

The S2 records (this file's, `A1.md`'s, `A2.md`'s and `A3.md`'s) are written as KNOWN-LIMITATIONS rows or PLAN notes after the wave, together (§4). A4-1 through A4-4 are one class, a refusal's code lost on the publish or raw path, and are best recorded as one PLAN note naming all four.
The reproducers on cloud/wave1-A4 (a7835b1) assert the contract and fail today. Whether they become regression tests is decided in the piece that fixes them; nothing merges from that branch.
