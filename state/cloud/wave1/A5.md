# Wave 1, A5 (batch 1, pair 2: A4+A5): the session's report

**Source:** session_018DZybsCUV4Zb8z81uUUEAQ, the final assistant turn, copied at 2026-09-25 about 23:25Z with the message's own Copy action in claude.ai/code, and saved with LF line endings. Everything between the two rules below is the session's text, verbatim. The WAVE1 REPORT proper begins at `# WAVE1 REPORT`; the lines above it are the same turn's progress narration. The custodian fields follow the second rule. They are filled locally, per `state/cloud/wave1-prompts.md` §3.

---

F1 (the data-plane registry that only ever grows) is a strong candidate. Checking it myself.

Status: the four sub-audits are back. I'm now writing reproducers for the strongest candidate, the data-plane stream registry that grows with every stream served.

Confirmed: 200 finished streams leave 200 `StreamState`s and 200 terminal records; the sub-audit's claim about `active()` was incorrect since it returns 0. I'm dropping that claim and correcting the test's doc comment.

Status: registry reproducer done. Now checking whether the shell closes a dataset handle when an open resolves after the user has cancelled or superseded it.

Status: engine/DuckDB build still compiling. Meanwhile I'm confirming the publish-staging path on shell exit and CLI termination.

Now the DCO hook proof and commit.

The hook rejected the unsigned commit (exit 1, HEAD unchanged). Formatting, re-running, then committing signed.

# WAVE1 REPORT
## Worker fields
Item: A5          Lens/purpose: lifecycle and resource-leak audit (leases, tickets, streams, file handles, temp files, background tasks, shell listeners and deck.gl teardown)
Baseline SHA: bb98f71f43a2891d317b10a124387df9d5ee0ebf (confirmed by `git -C /tmp/wave1-baseline rev-parse HEAD` at start)
Branch: cloud/wave1-A5   Commits: 20b5619d46fd33c3df8f91eb28a72a426e5e3c98 (one test file, +162 lines, no product source touched)
Environment: Linux 6.18 container · rustc 1.94.1 · cargo 1.94.1 · node v22.22.2 · npm 10.9.7. Network: crates.io only, for the data-plane test build, plus the git fetch and push to origin. No npm install.
Commands run:
- `git fetch origin bb98f71…` → 0
- `git worktree add /tmp/wave1-baseline bb98f71…` → 0
- `git -C /tmp/wave1-baseline rev-parse HEAD` → 0 (bb98f71…)
- `cargo test --no-run -p spatial-engine` (background) → killed by me before it finished (exit 144). It was not needed: no reproducer touches the engine.
- `CARGO_TARGET_DIR=… cargo test -p spatial-data-plane --test wave1_a5_registry_retention -- --ignored --nocapture` → 101 (the reproducer fails as intended)
- the same command without `--ignored` → 0 (1 ignored)
- `cargo clippy -p spatial-data-plane --tests` → 0
- `rustfmt --check --edition 2021 <test>` → 0 after formatting
- `git config core.hooksPath .githooks` → 0
- unsigned `git commit` → 1, rejected by the hook ("commit has no Signed-off-by trailer"); HEAD stayed at bb98f71
- `git -c user.name=chris -c user.email=chrys92d@gmail.com commit -s` → 0
- `git push -u origin cloud/wave1-A5` → 0

Findings: 2

### Finding A5-1
Claim: The data plane's `StreamRegistry` keeps one `Arc<StreamState>` and one `(String, Terminal)` for every stream it has ever served and never removes either. In the shell the registry lives for the whole process, so it grows with every tile stream.
Suggested severity: S2. It qualifies for S1 because it grows with ordinary use and has no ceiling, but each entry is small: two IDs, a Mutex and eight atomics, plus the terminal string.
Code path:
- A tile or viewport stream is admitted and recorded at `protocol/data-plane/src/server.rs:417` (`st.registry.record(state.clone())`), which pushes at `:128-130`.
- When the stream ends, `:457` (`record_terminal`) pushes at `:131-136`.
- Nothing in the crate removes an entry. The only readers are `snapshot`, `terminals` and `active` (`:137-148`).
- The shell keeps the registry for the process lifetime: `frontends/shell/src-tauri/src/lib.rs:367` (`serve(…)`) and `:477` (`Box::leak(Box::new(running))`).
Reproducer: `protocol/data-plane/tests/wave1_a5_registry_retention.rs` on cloud/wave1-A5. Run: `cargo test -p spatial-data-plane --test wave1_a5_registry_retention -- --ignored --nocapture`. Observed:
```
after 200 completed streams: snapshot().len()=200 terminals().len()=200 active()=0
panicked at …wave1_a5_registry_retention.rs:152:5:
the registry retains 200 StreamStates after 200 finished streams (grows with every stream served; no ceiling)
```
Guarantee violated: none stated for this registry. Its doc says "Every stream this process has served" (`server.rs:118`), so the retention is deliberate, but it was not scoped to a bounded lifetime. The kernel's sibling `StreamRegistry` records the same shape as a defect it had to fix: `kernel/src/skp.rs:48-56` says "Left unbounded, `StreamRegistry`'s map would grow for the whole life of the process". That fix is the time bound at `:57`.
Evidence: 200 sequential completed streams left exactly 200 entries in each vector after every terminal had been recorded.
False-positive check:
- KNOWN-LIMITATIONS.md has no item on the data-plane registry or on memory growth.
- DECISIONS-PENDING.md has no match for leak, orphan, "never removed" or the data-plane registry.
- One of the sub-audits also claimed `active()` counts completed streams as active. The test measured `active()=0`, so that claim is false and is not part of this finding.
Confidence: proven (Linux)

### Finding A5-2
Claim: Closing the shell window while a publish is writing leaves that attempt's `.<name>.staging-<random>` directory beside the destination. Nothing cancels or waits for the running publish at exit, and nothing ever sweeps these directories. Each interrupted publish leaves a new one, because every attempt gets a new name.
Suggested severity: S2 (no ceiling, but it grows only with interrupted publishes).
Code path:
- The publish runs in `frontends/shell/src-tauri/src/commands.rs:407-410`: `tokio::task::spawn_blocking(… publish::execute_with_progress …)`.
- The staging directory is created at `kernel/src/publish/mod.rs:1331-1332`, and the name is unique per attempt (`random_suffix`, `:1543-1554`).
- Cleanup happens only in-process: the error arm at `mod.rs:551-566` and `Staging::drop` at `:1509-1531`.
- `frontends/shell/src-tauri/src/lib.rs:501` is a bare `.run(tauri::generate_context!())`. There is no `RunEvent::ExitRequested`, `CloseRequested` or `on_window_event` hook (grep of `lib.rs` and `main.rs`), so `RunningPublishes` is never cancelled or awaited at exit.
- The code's own comment (`mod.rs:1314-1315`) accepts only "a crashed publish leaves the directory behind"; an ordinary window close is not a crash.
Reproducer: code path only. It needs a GUI shell on Windows; I did not run the shell here.
Guarantee violated: docs/01's non-negotiable that every operation is cancellable (CLAUDE.md digest). The operator's ordinary exit reaches no cancel and runs no cleanup.
Evidence: read at the baseline, as listed above. I observed no leftover directory myself.
False-positive check:
- KNOWN-LIMITATIONS.md has no item on staging leftovers or on exiting mid-publish.
- DECISIONS-PENDING.md has no match for staging or orphan.
- ADR-024's only staging mention (line 222) is about filter-active refusal.
- The existing cleanup tests (`kernel/tests/publish.rs:390`, `publish_cancellation.rs`, `permission_boundary.rs:520`, `cancel_rescore.rs`) cover only in-process cancellation and errors.
- The loss of the Tauri blocking thread at process exit is my inference from the code path. It is not verified on Windows.
Confidence: code-path-only

### Unproven observations (not findings; at most five, one line each)
- LoD (`engine/src/lod.rs:1018-1098`): a cancel after tier 1 has finished leaves a complete `tier-1.parquet` that `tiers.json` does not list, and the per-hash tier directories are never garbage-collected; bounded by the fixed names, and `build_tiers` has no product caller today.
- LoD `lod.rs:1256-1259`: a `?` after `write_tier` succeeds (for example a cancel during the hash) skips the only removal of `out_path`; the file is overwritten on the next build.
- Data plane `adapter_ws.rs:287` vs `:305-308`: after a cancel terminal, `drive` keeps `rx` for up to `PEER_DRAIN_TIMEOUT` (30 s), so the parked pump and producer can hold a stream-class connection lease after the admission slot is free; the shell closes on terminal, so only a slow peer triggers it.
- Engine `stream.rs:1657-1666`: a cancel that lands between the last `is_cancelled` check and `stream_arrow` is not latched (`cancel.rs:87-92`), so an `ORDER BY` publish sort can run to completion holding its lease after cancel; one stream per race.
- Shell `tileViewportStreamManager.ts:382`: `issueEpoch` is only ever set, never deleted, so it grows with the distinct tile keys visited in one dataset session; it is freed when the dataset closes.

### Stops (if any): none.

I checked these and ruled them out:
- The two Tauri `listen` sites unlisten correctly. `publish/client.ts:177-201` handles the late-unlisten race and has a test; `originSelfCheck.ts` is a page-lifetime singleton.
- `deck.finalize()` runs on every WorkingCanvas unmount (`WorkingCanvas.tsx:2009-2015`).
- Pool leases are released on every non-panic path.
- Descriptor and footer re-reads close their file handles by RAII.
- Pending tickets expire only lazily (declared design, `skp.rs:150-158`), capped at 8 per dataset.
- The `close_dataset`/`viewport_query` generation race is declared in ADR-035 Decision 3.
- The ticket-drop-under-lock hang is DECISIONS-PENDING entry 132.

---

## Custodian fields (filled locally, never by the worker)

Session ID: session_018DZybsCUV4Zb8z81uUUEAQ   Model: Opus 5.5, Medium (as launched)   Launched/ended: 2026-09-25T22:49:26Z / the report posted by about 22:59Z (its commit 20b5619 at 22:57:29Z). The session still showed "1 running task" at 23:25Z, after the report.
Spend: not individually attributable; batch delta $12 for the pair (A4+A5). The readings are in `A4.md`'s custodian fields.

Triage against main at 001115f (wave-1 rule 1, `state/directives/2026-09-25-cloud-hooks.md` §4): every cited code file is unchanged since bb98f71 (see `A4.md`), so every item below is **still-present**. None of them is in the watcher's code, so none folds in under rule 2. A5-2 cites `frontends/shell/src-tauri/src/lib.rs`, which the watcher's phase 1 wires; A5-2 is S2, so rule 2 does not apply to it.

Finding A5-1: CUT CANDIDATE. Final severity: S1 candidate. Reason: §4's S1 line names an unbounded resource leak under ordinary use. The data plane's `StreamRegistry` (`protocol/data-plane/src/server.rs`) keeps one `StreamState` and one terminal record for every stream served and removes neither, and the shell keeps it for the process lifetime. Each tile stream adds an entry, with no ceiling. The worker's bound stands beside it: each entry is small, so growth per stream is modest. The kernel's sibling registry already bounds the same shape by time (`kernel/src/skp.rs`, `StreamRegistry`). It goes to the human in the after-wave batch.
Reproduced locally (Windows): yes. `cargo test -p spatial-data-plane --test wave1_a5_registry_retention -- --ignored --nocapture` ran in worktree `C:/dev/wt/triage-a3-obs3` at 31d7d34 (`protocol/data-plane` is unchanged to 001115f), with the reproducer copied from 20b5619 as an untracked scratch file. It exited 101, and the failure message was the same as the cloud run's: 200 `StreamState`s retained after 200 finished streams.

Finding A5-2: RECORD. Final severity: S2. Reason: the code path convinces. `frontends/shell/src-tauri/src/lib.rs:501` @ 001115f is a bare `.run(...)` with no exit or close hook, so a publish running when the window closes is neither cancelled nor awaited, and its per-attempt staging directory is left behind. It grows only with interrupted publishes, not with ordinary use.
Reproduced locally (Windows): not attempted (it needs a GUI run).

Unproven observations:
1. DISCARD, S3: `build_tiers` has no product caller at the baseline or at main. Noted for `lod-tier-selection`, as A3's observation 4 was.
2. DISCARD, S3: same reason as 1.
3. DISCARD, S3: bounded by `PEER_DRAIN_TIMEOUT`, and the shell, the only product peer, closes on terminal.
4. RECORD, S2: a cancel lost in a window. The code path convinces: `engine/src/stream.rs` checks `is_cancelled` just before `stream_arrow`, and `engine/src/cancel.rs` records that an interrupt on an idle connection is not latched. A cancel landing between the two is lost for that query, which is bounded to one query. Not reproduced.
5. RECORD, S2 (§4: a per-session leak with a ceiling). `issueEpoch` in `tileViewportStreamManager.ts` grows with the distinct tile keys visited and is freed when the dataset closes. The file is in the watcher's scope, but this is S2, so it is recorded, not folded in.

The S2 records are written after the wave, together with the others (§4).
The reproducer on cloud/wave1-A5 (20b5619) is `#[ignore]`d, asserts the contract, and fails when run. Whether it becomes a regression test is decided in the cut that fixes A5-1, if one is ruled; nothing merges from that branch.
