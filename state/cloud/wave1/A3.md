# Wave 1, A3 (calibration): the session's report

**Source:** session_01STxb5ot2J5RaYpnzffmSq6, the final assistant turn, copied at 2026-09-25 about 07:00Z with the message's own Copy action in claude.ai/code, and saved with LF line endings. Everything between the two rules below is the session's text, verbatim. The WAVE1 REPORT proper begins at `# WAVE1 REPORT`; the lines above it are the same turn's progress narration. The custodian fields follow the second rule. They are filled locally, per `state/cloud/wave1-prompts.md` §3.

---

Checking a self-documented reachable panic in `kernel/src/publish/ceilings.rs`.

Checking duckdb-rs's Arrow bridge for panics reachable from a crafted Parquet schema.

Engine decode paths look defended so far. Now checking SKP request fields that feed arithmetic in the kernel.

Checking the bundle viewer's manifest parser (JSON recursion, BigInt conversions).

I'll check the build to test DuckDB's C++ parser recursion on a deeply nested filter predicate running on a 2 MiB-stack thread, since the viewer already routes throws to a typed failure screen.

Build still compiling DuckDB. Drafting a reproducer probe for deep-but-short predicates meanwhile.

Build done. Running the predicate-depth probes one at a time.

Predicate probes all yielded a typed `FilterError` up to the 4096-byte limit on a 2 MiB thread — nothing found there. Next I'll check duckdb-rs's result→Arrow conversion for unusual Parquet column types, triggered by `probe_schema` at open.

The redaction scanner runs over bundle bytes that carry file-derived content; checking its index arithmetic.

I reviewed the engine's code and found no reachable panic. Now I'll verify this empirically by crafting malformed GeoParquet inputs—bad WKB counts, deep `geo` JSON, and mistyped geometry columns—and running them through a full open-and-stream cycle.

All 14 crafted-file cases and every predicate shape ended in a typed `EngineError`/`FilterError`; no panic. Checking CI triggers before deciding what to push, then the DCO hook.

No clippy/fmt gate. Setting up the branch and proving the DCO hook rejects an unsigned commit.

Hook rejected the unsigned attempt (exit 1, HEAD unchanged). Committing signed.

# WAVE1 REPORT
## Worker fields
Item: A3          Lens/purpose: Panics, aborts and unbounded recursion reachable from untrusted input (GeoParquet, PROJJSON, SKP requests, bundle files, filter predicates)
Baseline SHA: bb98f71f43a2891d317b10a124387df9d5ee0ebf (confirmed by `git -C /tmp/wave1-baseline rev-parse HEAD` at start)
Branch: cloud/wave1-A3   Commits: db9527fef11acd58a60676aab32795d8764ec8e9 (signed off; both files are test-only probes that show no defect, no reproducers)
Environment: Linux 6.18.44 (cloud container), rustc 1.94.1, cargo 1.94.1, node v22.22.2, npm 10.9.7. Network used: crates.io only, through the cargo build. No npm install was run. The `git push` went to the origin repo.
Commands run:
- `git worktree add /tmp/wave1-baseline bb98f71…` → 0; `git -C /tmp/wave1-baseline rev-parse HEAD` → 0
- `cargo test -p spatial-engine --no-run` → 0
- `cargo test -p spatial-engine --test wave1_a3_predicate_parser_depth --no-run` → 0
- `cargo test -q -p spatial-engine --test wave1_a3_predicate_parser_depth -- --exact probe_left_assoc_plus_under_byte_ceiling --nocapture` → 0
- same with `probe_unary_minus_under_byte_ceiling` → 0; same with `probe_not_chain_under_byte_ceiling` → 0
- `cargo test -q -p spatial-engine --test wave1_a3_crafted_geoparquet -- --nocapture` → 0
- `git checkout -b cloud/wave1-A3` → 0; `git config core.hooksPath .githooks` → 0
- unsigned `git -c user.name=chris -c user.email=chrys92d@gmail.com commit -m …` → 1. The hook printed "commit has no Signed-off-by trailer", HEAD stayed at bb98f71, and the attempt was discarded.
- `git -c user.name=chris -c user.email=chrys92d@gmail.com commit -s …` → 0; `git push -u origin cloud/wave1-A3` → 0

Findings: 0

What was covered, and why none of it is a finding:
- **WKB decode:** `engine/src/wkb.rs` reads everything through bounds-checked `get()`. The stream producer's `arrow.next()` sits inside `catch_unwind` (`stream.rs:1727`). Offsets go through `checked_offsets` (`geoarrow.rs`).
- **Crafted GeoParquet, run through `Dataset::open` and a full stream drain (all queries and a viewport query), 14 cases:**
  - WKB with `n_rings` = u32::MAX, `n_pts` = u32::MAX, empty, NULL, truncated after a valid row, and NaN rings
  - a VARCHAR geometry column and VARCHAR covering columns
  - `geo` and `crs` JSON nested 100,000 levels deep
  - non-object axis entries, a 200-character multi-byte unit name, and a garbage `bbox` member
  
  Every case ended in a typed `EngineError` or opened correctly. There were no panics.
- **Filter predicates under the 4096-byte ceiling, run on a 2 MiB-stack thread (the same stack size as `spawn_blocking`):**
  - `1+1+…` at depth 100–2000 and a `NOT` chain at depth 100–1000 were all refused with a typed `Unparsable`. Two bounds fire before the crate's own `MAX_PREDICATE_DEPTH`: serde_json's 128-level recursion limit, and DuckDB's own `max_expression_depth` of 1000.
  - A `-(` chain up to depth 1300 was admitted, because DuckDB folds it into a constant.
  - None of these overflowed the stack.
- **SKP decoding and field validation:** the strict `HexF64`/`DecU64` codecs, the data-plane `parse_control`/`parse_start` (all `get()`-based), and `create_from_ticket` are panic-free on 64-bit.
- **Bundle viewer:** `main.ts:88-98, 577-578` sends window errors, unhandled rejections and `load()` failures to a typed `BundleFailure` screen, so there are no uncaught exceptions.
- **Files checked for duplicates:** `KNOWN-LIMITATIONS.md` contains no panic-related lines. `DECISIONS-PENDING.md` was not needed, since no finding was filed.

### Unproven observations (not findings; at most five, one line each)
1. `kernel/src/permission/audit/reader.rs:196`: `&at[11..16]` panics the `publish-bundle --audit-show` CLI if a hand-edited audit log has a multi-byte character straddling byte 16. The audit log is not one of the listed untrusted inputs, and this was not reproduced.
2. `engine/src/index.rs:709`: `hi + 1` overflows (and panics in debug builds only) if the candidate list holds `u64::MAX` twice. That needs both `IndexUse::Experimental` and an identity opened `DeclaredNotVerified`. Release builds wrap instead. Not reproduced.
3. `renderer/bundle-viewer/src/partition.ts:151-155`: a hash-consistent bundle whose geometry column is not a nested list throws a TypeError. It ends up as the generic `unhandled-error` state rather than `partition-decode-failed`. Out-of-range offsets can cause a long loop (a hang), not a crash. Not reproduced.
4. `engine/src/lod.rs:1502-1519`: `wkb::reader::read_wkb` and `geo::simplify_vw_preserve` were not audited for panics. The inline single-worker arm has no `catch_unwind`; the multi-worker arm turns a worker panic into a typed error (`lod.rs:1456`). The tier builder is not reachable from any SKP command at the baseline (`kernel/src/skp.rs:1343`).
5. `engine/src/predicate.rs:428-435`: in practice, parse-time depth is bounded by serde_json's 128-level limit and DuckDB's `max_expression_depth` (1000), not by `MAX_PREDICATE_DEPTH`. The only thing I saw was typed refusals; I did not test those two bounds under a different DuckDB setting or a different serde_json configuration.

### Stops (if any): none.
Note: the session's own checkout at `/home/user/spatial-ide` was on `claude/relaxed-pascal-1rdeps` when the session started, not `main`, and I did not touch it. All work stayed in `/tmp/wave1-baseline`.

---

## Custodian fields (filled locally, never by the worker)

Session ID: session_01STxb5ot2J5RaYpnzffmSq6   Model: Opus 5.5, Medium (as launched)   Launched/ended: 2026-09-25T06:41:30Z / the report posted about 06:54Z (its commit db9527f at 06:52:41Z). The session still read "Running" at 07:00Z, idle after the report.
Spend: not individually attributable; batch delta $7. The batch is A3 alone. Cloud session credits read "$250 of $250 left" at about 06:40Z and "$243 of $250 left" at 07:01:50Z after a refresh. The page shows whole dollars only, and no per-session figure is shown anywhere.

Findings: 0, so there are no finding dispositions. The five unproven observations, triaged against main at 7ed3465+ (every cited file is unchanged since bb98f71, so each is still present):
1. `kernel/src/permission/audit/reader.rs` `plain_date`: RECORD, final severity S2. A non-char-boundary slice panics `publish-bundle --audit-show` on a hand-edited audit log. That is a trusted, local input (§4, S2: "a panic reachable only from trusted or internal input").
2. `engine/src/index.rs` `compress_to_ranges`: DISCARD, S3. The `hi + 1` panics in debug builds only, and only on `u64::MAX` duplicates under `IndexUse::Experimental`. In release it wraps into a redundant range, which is harmless.
3. `renderer/bundle-viewer/src/partition.ts` (the partition decode's offset walk): two parts.
   - (a) A geometry column that is not a nested list reaches the generic `unhandled-error` state instead of `partition-decode-failed`: RECORD, S2 (a refusal under the wrong code).
   - (b) Offsets beyond the coordinate array drive the ring and vertex loop bounds unchecked (typed-array reads past the end return undefined and do not throw), so a crafted, hash-consistent bundle can hold the viewer's main thread in a very long loop: an S1 candidate (§4: a hang reachable from a bundle), pending the custodian's own reproduction on Windows (§4 requires a reproducer or that reproduction).
4. `engine/src/lod.rs` (the tier builder's WKB and simplify calls): DISCARD, S3. No defect is shown, and the builder is unreachable from any SKP command at the baseline or at main. Noted for `lod-tier-selection`, whose piece makes it reachable.
5. `engine/src/predicate.rs`, the depth bounds: DISCARD, S3. This is informational, and every probe ended in a typed refusal.

Reproduced locally (Windows): observation 3(b) — attempted, result pending (see `state/cloud/wave1.md`); the others — not attempted.
The reproducers on cloud/wave1-A3 (db9527f) are negative-evidence probes (two test files under `engine/tests/`). Whether either becomes a regression test is the custodian's call inside a later cut; nothing merges from that branch.

## Custodian triage update, 2026-09-25 about 22:30Z: observation 3 reproduced on Windows

The custodian's reproduction was run by a tester agent in worktree `C:/dev/wt/triage-a3-obs3` at 31d7d34, with untracked scratch files only. It called the real `decodePartition` (`renderer/bundle-viewer/src/partition.ts`), which `main.ts`'s `load()` calls after the content-hash check. The input was Arrow IPC bytes built with the viewer's own `apache-arrow`, satisfying every envelope check and carrying a consistent sha256. Each case ran in a child process under a 60 s kill.
- **(a) Reproduced.** A flat Float64 geometry column raises a raw `TypeError: Cannot read properties of undefined (reading 'children')`, not a `BundleFailure`. `main.ts`'s `load().catch` routes that to `unhandled-error`. Final severity S2 (RECORD), as triaged.
- **(b) Mechanism reproduced.** A ring end offset N, over a coordinate buffer that holds 2 points, decoded without an exception:
  - 55.3 ms at N = 1e6, 471.9 ms at N = 1e7, and 4621.8 ms at N = 1e8;
  - linear, about 46 ns per iteration, with the file 992 bytes at every N and the bounds garbage.
  - Polygon offsets took 14.2, 72.5 and 694.7 ms.

  A run past 60 s was not observed directly: the test's Arrow builder capped the offset it could construct. Extrapolating the measured rate to the Int32 maximum gives about 99 s for the ring loop alone. That is an estimate from the measured scaling, not a timed run. Final severity: S1 candidate, standing. The stall has no bound but the bundle's own offsets, and a bundle is untrusted input (§4, S1: a hang reachable from a bundle). It goes to the human in the after-wave batch with this evidence. Nothing becomes a cut in this wave.
