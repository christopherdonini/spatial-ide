Question round 7 — 2026-09-16 (custodian → human). One item: Brief A P3a stopped under Rule 7 a second time (DECISIONS-PENDING entry 98).

1. P3a attempt 2 FAILED both gates — narrowly, and all of one class. PASSED (verified by both gates): every ordered removal absent; the three seams proven from the real shape (fixtures byte-identical to the Rust pins); your round-5 string exactly in the guidance; the post-check's bytes reported from the read already done with the ≤ 8 MiB bound named and no duration; slice.rs re-aimed to the budgeted interval; every declared accessor names an existing test; 619 cargo / 1000 vitest green, zero warnings; nothing owner-side crept in. FAILED: (1) the ENGINE's own SourceChanged Display text still says "Everything read for this session is discarded" — the sentence your ruling took off the guidance is the primary text the operator reads on every route (banner; camera pre-check, which renders no guidance; filter panel, one paragraph above the corrected guidance), false because P3a clears nothing, and now pinned by exact equality on both sides; (2) a test narration ("the degradation is shown, never silent") survived the sweep Amendment 4 (i) records, plus a stale doc link and a stale mutation record naming the function deleted this round; (3) `without_modification_time_for_test` — a pub constructor, test-only caller, that ACTS and names no test, four lines from the exemption the tree used to delete a sibling; its duplicated literal leaves the shipped no-mtime branch untested; (4) "reported per cancellation, never silent" is not true of the shipped build (trace off by default; the accessor test-only) while Amendment 4 (ix) says both obligations discharged — a one-line in-licence fix exists (carry bytes + bound on the always-on session line); (5) ten ruling cites off by one item number. The pattern, a third time: an amendment states as done something the tree does not do.
Options:
  1. A fresh worker on exactly the listed items, then gates attempt 3 (Recommended): the fix list is fully specified by both reports; the over-claim recurred three rounds from the same, very long, context.
  2. The same worker, same list, gates attempt 3.
  3. Hold — you read both reports first (I file them to state/ on request).
Not offered: landing with the false Display sentence recorded — your round-5 ruling forbids a false status string on main.
