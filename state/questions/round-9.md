Question round 9 — 2026-09-17 (custodian → human). One item: P3b's end-to-end driver T10 (DECISIONS-PENDING entry 100). Also on your desk: PR #84 (the LOD builder, all gates + tester PASS) waits for the click.

1. P3b is built per its preregistration (both routes' owner-side invalidation, the three-valued dead-ticket refusal with its real caller, the §12e amendment, the four items carried from P3a; 27 recorded mutations; cargo + vitest green). Its §9 requires the end-to-end driver T10 green before either gate, and T10 has failed three times in its own PRECONDITION — never reaching the owner-side assertions. All three runs launched the real app through the harness on a quiet machine, opened the copy, filled the canvas (10,000 features / 188,665 vertices every time) and read those totals live through the new counts-only hook; the copy's hash never changed. Run 1: a 60 s step bound (driver bug, fixed). Run 2: a centre-anchored hover grid found no pixel yielding an id. Run 3 (a bounded attempt I authorized on the in-tree A9' bisection): converged on an 80% non-background patch, but at the fit-to-bounds camera in the harness's 1280×200 frame no INTERIOR pixel exists anywhere — exactly the half of A9' my scope excluded: its zoom-notch loop (real wheel zoom-ins until a pixel interior-verifies). Never evaluated: the session-ended block, residency zero, the hover refusal — the consequences P3b exists for — and the recorded mutation. Amendments 5–7 record every run verbatim.
Options:
  1. A fourth bounded attempt with the FULL A9' mechanism — the zoom-notch loop around the bisection, as regression.mjs runs it — then the three-run sequence (green / mutation red by name / green), gates after (Recommended: the suite's own proven mechanism; the failure names the missing half exactly; nothing about the product is in doubt).
  2. Drop the hover precondition: assert the hover refusal against the interior-verified pixel from the read-back — a weaker "formerly occupied", grounded in capturePixels rather than in a hover that showed an id.
  3. Leave T10 unrun and let the gates weigh the unit/integration evidence with this record — a deviation from §9 of the preregistration you accepted; P3b would then claim nothing about what an operator sees end to end.
  4. Hold.
