Question round 15 — 2026-09-18 (custodian → human). Four items (DECISIONS-PENDING entry 107): the consult mechanism's missing step and six clauses; the closes for P3b and the LOD; the checker under Rule 7; acknowledgements and one hygiene ruling. Status: P3b attempt 6 architect PASS / reviewer FAIL on one consult row; LOD attempt 6 architect FAIL on two consult sentences (reviewer pending at filing); the checker attempt 4 FAIL/FAIL, fresh count spent, STOPPED; the RESIDENCY note re-drafted on a fresh branch, its second read running. Every clause below is byte-copied by script from the gate report that proposed it; this file is their committed source.

1. The mechanism. Three defects of one class in one day: a consult's frozen prose that no step resolved against its own steps, its evidence, or the site it describes. Proposed clauses:
  (a) The resolution step (the LOD architect, gate-log 67): A consult whose deliverable is a frozen, byte-copied block carries a final numbered step: before committing, the worker resolves each summary sentence of the block against the consult's own steps and against every piece of evidence the consult orders collected, and **STOPS on a mismatch** instead of committing.
  (b) Per-row resolution before delivery (the P3b reviewer, gate-log 65): A re-scope that freezes prose and forbids adjustment needs a per-row resolution step, or the row's claim must be reduced to what its own pin proves.
  (c) The tool-commit clause (the LOD architect, 67): a record statement about a tool's behaviour names the tool's commit, the same way a reference names its own; a tool claim read as current is a pin read as current.
  (d) Reference form in source comments (the LOD architect, 67): a `path:line @ <rev> sha256:<hex>` reference is written contiguous on one line, even past 100 columns (rustfmt does not reflow comments).
  (e) The durable-reference clause (the checker architect, 66): a hash reference written in an append-only record carries an explicit `@ <rev>` **at a commit on main**, never a branch commit and never the HEAD default.
  (f) The restore bound (the P3b architect, 64): a record's committed byte is restored only to revert an in-place edit the append-only rule forbade, on a branch that has not merged, with the intended correction re-carried by an appended row; append-only is then proved against the named base commit, not the immediate parent.
  (g) Gap 2 (the P3b architect, 64): confirm class 1 for a withdrawal row in a record-correction round, on the reading both amendments state — a gate round's findings are the round's results, and a row that records what they invalidate is a post-result record.
Options:
  1. Adopt all seven as byte-copied, into the template's §10, both checklists, the worker brief and AI_DEVELOPMENT (Recommended).
  2. Adopt (a)–(e) now; hold the restore bound (f) and gap 2 (g).
  3. Hold.

---

2. P3b and the LOD closes. P3b needs one clause (engine Amendment 12's row on Amendment 11's line restated to what that line does) and two rows, one per document, saying round 14 item 2 settled gaps 1 and 3; the LOD needs one clause (the per-span counts: nine for seven, four for six, two for two, net neutral) and one row naming the checker's commit and what it reported.
Options:
  1. One appended erratum row per document, written by the architect with each sentence resolved against its site under clauses (a)–(b), executed by a fresh worker, then ONE scoped read of the appended rows only, then the PRs (Recommended).
  2. Amend the unmerged closing commits in place on the :1371 precedent — a history rewrite of unmerged branches, which only you can license.
  3. Hold both.

---

3. The checker under Rule 7. The build is sound on its own tree and red on main's real corpus; the record has the day's defects; the count is spent.
Options:
  1. Hold until P3b and #84 merge, then one round under a fresh count: a closing amendment by re-scope (keyed by amendment and item, per-row resolved) plus the bounded build fixes — the marker's reproduction accepted on its own line, a route for hash findings in immutable text, tracked fixture lines with positive assertions, the site.mjs revert, the `//` continuation, the header gaps — and the baseline re-derived against the final main; one fresh worker; both gates (Recommended).
  2. The same round now, in parallel with the closes.
  3. Drop the widened scan to land sooner (both architects advise against).

---

4. Acknowledgements and one hygiene ruling. (a) engine/ADMISSION-PREREGISTRATION.md:220 has left the baseline by the ratchet's mechanism after the tool's own bug was fixed: 18 entries, not 19; the round-14 Applied text describes a state the tree lacks and a ledger note corrects it when the checker lands. (b) The RESIDENCY note's first branch (record/residency-125-note) is abandoned for the fresh draft; delete it on your word. (c) The LOD's four hash-reference findings sit in immutable Amendment 12 text — owed-correction for :545 through Amendment 13's (g) row, :541 genuinely unfixable — dispositions for the checker's baseline. (d) The six byte-copied markers in your round-12 and round-14 adoption text fail the current checker only because the tool reads the next line; your bytes stand and the tool moves. (e) The untracked e2e reports cited as quoted-output sources inside immutable P3b amendment text predate round 14's untracked-Authority rule.
Options:
  1. Acknowledge (a)–(d) and rule (e): the disclosure in the record suffices and the rule is dated non-retroactive (Recommended).
  2. Acknowledge (a)–(d); for (e) open a node to land the two quoted strings in a tracked evidence file.
  3. Hold.
