Question round 23 — 2026-09-25 (custodian → human). Four items, in ask order. Item 2 is a RED LINE: typed answer only. Status at filing: the A3 calibration is recorded in state/cloud/wave1/A3.md and state/cloud/wave1.md (0 findings; 5 unproven observations; 3(b) an S1 candidate under reproduction; balance 250 to 243 dollars). PR #122 (the ADR-035 Decision 4 note) passed both gates and waits on your click. Consults: state/consults/2026-09-25-adr-035-decision-4-note.md (S1–S4) and state/consults/2026-09-25-test-claims-followups-form.md (O1–O9).

---

1. Wave 1, the next launches. A3 cost 7 dollars (the balance moved 250 to 243; the page shows whole dollars and no per-session figure). Under section 5 of the prompts, only the shared balance moves, so batch 1 runs as pairs.
  (1) Batch 1 as two pairs, A1+A2 and then A4+A5, recording each pair's balance before and after; then C+D; then B alone. Launched from claude.ai/code with each prompt hash-checked before sending (Recommended).
  (2) All four A audits at once (loses per-pair attribution).
  (3) Hold.

---

2. RED LINE — S1 (from the ADR-035 Decision 4 note's consult; it must be settled before the watcher's skp/0.5 section 8 entry). Round 21 item 1 confirmed that an opaque, per-open session reference does not engage skp/0.3's no-generation-value rule, and SKP-V0 section 8's rider-(c) note limits that clarification to a reference minted per open that routes the event to its open. Round 22 item 1 now adds a reference no client holds, minted for a generation not created by open_dataset. Whether that reference also stays outside the rule is not ruled.
  Custodian's recommendation: yes, under the same riders. It is the same opaque, non-authorising value kind; it routes nothing (no client holds it); it carries no ticket attribution and is never persisted or published (round 21 riders (a) and (b)). Recorded as a second dated note to SKP-V0 section 8, in the watcher piece.
  Or hold (the watcher's skp/0.5 entry waits).

---

3. S2–S4 (the same consult; settled at the watcher preregistration's gate).
  S2: the unheld reference is minted from the same source as open_dataset's (the OS CSPRNG) and is never reused.
  S3: the logged line for a dropped unknown-session event carries no reference: the watcher preregistration reads rider (b)'s never persisted or published as covering log lines (its section 8 item 19).
  S4: SKP-V0 section 3's third minting rule is worded in the watcher piece to cover a reference no client holds.
  (1) All three as recommended (Recommended).
  (2) Hold; asked item by item next round.

---

4. The test-claims follow-ups form (round 22 item 3's piece; full form, at most 520 lines). Nine OPEN markers; the architect's recommendations:
  O1 the two lines already on main carrying the marker outside a backtick span (TEST-CLAIMS-WITHDRAWN-PREREGISTRATION.md, Amendment 2's Discharged clause; VERIFY-MUTATION-MULTILINE-ATTRS-PREREGISTRATION.md, Amendment 7's heading) are corrected in place by wrapping the token in backticks — two bytes each, no word changed, the pre-edit bytes pinned — read as authorised by your round-22 condition.
  O2 a refused line's ruling and carrier are both checked (one finding, the grammar's reason first).
  O3 pin-condition failures on accepted rows stay unnamed (naming them would widen your ruling).
  O4 the gate-log node id stays; the join goes to governance-record-round-count.
  O5 stale counts in old mutation comments get the commit they were observed at, no re-run.
  O6 condition (b) accepts superseded or Superseded as a whole word, refuses all-caps or a preceding not/never.
  O7 an overrun of the full form's budget is recorded as class 2.
  O8 the node's gate names the form; its budget minutes are re-estimated.
  O9 temp-directory cleanup covers this test file only.
  (1) All nine as recommended (Recommended).
  (2) Hold; asked item by item next round.
