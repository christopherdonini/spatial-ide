Question round 4 — 2026-09-16 (custodian → human). One item: Brief A P3 stopped under Rule 7 (DECISIONS-PENDING entry 96).

1. P3 (session-identity tier, cut/briefa-p3): both gates FAILED twice. Attempt 2 verified as fixed everything from attempt 1 except the same shape relocated: the worker writes each module-boundary seam to an interface the other side does not have, with a test that encodes the imagined interface. Sound and verified: engine + kernel + SKP 0.3 (typed terminal prefix pinned both ends; post-check rule (ii) effected with the flag before any terminal; one "changed or unreadable" rule; GenerationRegistry tested; ticket map bounded; your crs.source = "format-rule" ruling applied exactly; 617 cargo / 995 vitest green). Still failing: (i) owner-side invalidation — "residency cleared, picks refused" is in P3's own row (NEXT-CUT.md:103), the managers fire an onSessionEnded callback nothing in the product subscribes to, so after engine.source_changed the old geometry stays on the canvas and pickable; a test narrates the owner doing it; not recorded as a gap. (ii) The publish refusal code is discarded by the shell consumer (formatPublishRefusal.ts hardcodes "publish-refused"; the directory has an empty diff) and every pin-free publish refusal now renders "publish.<code>: refused: …" to the operator. (iii) The kernel-authoritative dead-ticket refusal is never wired (three pub items with zero callers, two with false "has a real caller" docs) and, where it would run, it fabricates a source change for unknown handles — the collapse the cut itself recorded as a defect. (iv) A Drop-backstop comment overstates what the code does.
Options:
  1. Split P3 (Recommended): land the engine + kernel + SKP 0.3 half now under its own gate, with the owner-side consequences, the publish-consumer parsing and the redemption wiring recorded in a §12e amendment for your sight as a P3b piece; P3b gets its own preregistration and gates. The sound half stops accruing rebase debt; the unbuilt half gets a definition the gates can hold it to.
  2. One more bounded round (attempt 3) on exactly the listed items, with a hard gate-checkable condition: every cross-module path exercised end-to-end from the real producer shape, and no callback, code path or pub item lands without a product caller (a grep both gates run). Risk: a fourth imagined interface if the condition is not enforced.
  3. Hold: you read both attempt-2 reports before ruling (I can file them to state/ on request); the branch stays pushed at its head; nothing merges.
