# The human's message of 2026-09-23 (late) — verbatim (entry 119's rulings; the shell direction "Map studio")

Recorded by the custodian as received. The message came into the chat as one pasted block with no other text. Nothing here is paraphrased.

Entry 119, my rulings (typed, red line): (1) ADR-015 Amendment 1: accepted as it stands. (2) ADR-032: accepted as it stands. (5) ADR-013 Amendment 1: the short-form piece first. Render the equirectangular statement in the existing DescribeSummary and at open, with no new status surface; then N1 is re-sighted. Accepted once item 5 is true of the build. (3) ADR-016 Amendment 1: accepted with a dated acceptance note naming item 2's degradation display as owed. The owed display is delivered by the watcher piece's detection-state field on describe, with degradation as one of its values (checks-only / watching / coverage-lost / degraded); no separate wire field for degradation alone. (4) ADR-028 Amendment 4: the short N8 rerun on the merged build, in the same sitting as N1's re-sight. Then one PR appends all five for my click.

Shell redesign, for your awareness, not dispatch: I've chosen a direction, "Map studio": menu bar and search, Layers | Map | Inspector (Layer/Feature tabs, pinning), an Activity panel (Jobs / Problems / History / Console) collapsed by default and movable bottom/right, one attention strip above the map, and a status bar with labelled items. It is a prototype held outside the repository. It is a design reference, not Authority, and no structural shell work is authorised. Until the migration plan is ruled:

(a) Add a proposed PLAN node shell-redesign-map-studio in the shell lane, citing that decision by this round.
(b) Hold new UI surfaces for it: B1's shell half (hover attributes, categorical style UI), B2's screens, and the watcher's status display wait for the new structure. B1's engine/kernel half proceeds as sequenced.
(c) Small fixes inside existing components (DescribeSummary, RefusalBlock, the panels) are fine; those components move wholesale.
(d) Add no new permanent elements to the canvas status stack or over the map. If a piece needs to show something new, put it in an existing component and list it in the ledger as a migration-inventory item.
(e) PR #97 and the drill repair proceed as ruled.

Report ≤10 lines.
