# The human's messages of 2026-09-24 (about 00:15Z, during the handoff) — verbatim (the watcher sight, and its place in the night program)

Recorded by the custodian as received. The first message came into the chat as one pasted block; the second was typed right after it. Nothing here is paraphrased.

## The watcher sight

Watcher sight, 2026-09-24. The brief is accepted as the record of the settled direction, with these additions binding on its preregistration:

describe carries two independent facts, not one list: coverage: watching | checks-only{reason} and checks: full | degraded{components}. This corrects the value list in my entry-119 ruling, which conflated them. Degradation (ADR-016 A1 item 2's owed display) is delivered by checks.
Coverage loss is its own typed refusal, never SourceChanged. P3b's ended-generation state carries a reason (observed change vs lost coverage) through the same owner-side invalidation path. Wording at P6 sight.
No new status surface (redesign rule (d)): the checks-only label goes in the existing DescribeSummary; case (f)'s status is P3b's existing session-ended block with the new reason.
The idle push path is named and proven: the kernel-to-shell event that delivers an idle signal, with case (f) as its acceptance test.
The directory is never locked: share flags permit rename and delete; an adapter test proves renaming or deleting the watched directory both works and ends the generation.
The final path is resolved at arming (junctions and symlinks), names compared case-insensitively, and temp-write-then-rename-over counts as a change.
Wire order: the describe fields take the SKP literal after crs-unit-fact-and-bounds.

## Its place tonight

Tonight: add it to the night program after item 4: preregistration, architect consult and implementation on a branch under both gates, no merge. If the preregistration hits a choice beyond these points, stop the piece and queue it.
