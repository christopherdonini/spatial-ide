# The human's message of 2026-09-23 (late, after PR #102's merge) — verbatim (entry 120's ruling)

Recorded by the custodian as received. The message came into the chat as one pasted block with no other text. Nothing here is paraphrased.

Entry 120, my ruling:

(1) ADR-013 Amendment 1 is accepted in the appending PR, with a dated acceptance note naming item 6 (unit-aware bounds) as owed. Unlike ADR-016's item 2, this affects every degrees dataset admitted today, so it is scheduled, not parked:

(a) A KNOWN-LIMITATIONS line now: datasets in degrees use tile and render-precision bounds declared for metres; the effect at close zoom is bounded as stated in the owed piece's record.
(b) A proposed node, placed by me in the kernel-protocol lane, first after the acceptance PR and ahead of B1's engine half. describe gains a typed CRS unit fact, a wire change under full gating that takes the next SKP literal, with B1 following. MIN_ANCHOR_SPAN gets its declared per-unit values, including the preregistered 1e-6 degree. RECENTER_MAX_DRIFT_M gets a degrees value declared by the architect, derived from the float32 precision requirement of ADR-010 rule 1, not guessed. Both are pinned by tests.
(c) The piece's P0 opens with one arithmetic bound, no measurement campaign: the worst-case float32 residual under today's bounds for the CRS84 corpus file at the deepest zoom. The KNOWN-LIMITATIONS wording is revised to state that bound.

(2) Recorded as a known limitation, no piece now. Add a migration requirement for the Map studio redesign (node shell-redesign-map-studio): every fact display binds to the dataset on the canvas, never to an in-flight open attempt.

Then the appending PR carries all five acceptances for my click.
