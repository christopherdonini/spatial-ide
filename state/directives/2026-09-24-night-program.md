# The human's message of 2026-09-24 (received after 00:10Z, before the handoff) — verbatim (the night program and the night rules)

Recorded by the custodian as received. The message came into the chat as one pasted block with no other text. Nothing here is paraphrased.

Before handoff: apply my entry-120 ruling, then record the night program below in the ledger and PLAN, regenerate the queue so these are ready in this order, flush, push, and relinquish the lease. Night rules: work proceeds on branches; merges wait for my morning clicks (docs-only auto-merge under §9 still applies); ordering constraints between pieces become merge order, not work order. Nothing past a piece's ruled scope is decided at night: any new decision goes to the queue with a recommendation and the next item starts. Rule 7 stops the piece, not the night. No AskUserQuestion overnight. Telegram only if every remaining item is blocked. Everything waiting on me comes as one question round in the morning. No measurement cells, so no quiet-machine requirement. If a usage limit is reached, flush and stop cleanly.

Night program, in order:

The appending PR for the five acceptances, with the entry-119/120 notes: prepared, CI green, left for my click.
PR #97's full gate on its branch, as ruled.
The three drill fixes, each its own light-lane PR. Stack the viewer-build-order fix on #97's branch if it touches the same scripts.
crs-unit-fact-and-bounds, per the entry-120 ruling: P0's arithmetic bound first, then preregistration, implementation and both gates on a branch, taking the next SKP literal. If P0 shows a visible defect, record it and continue. If any choice goes beyond the ruled scope, stop this piece and move on.
Placed and approved now: engine-tests-configured-connections, and the three gate-tool defects (verify-mutation multi-line attrs, verify-mutation header token, verify-gate-file-exists), plus test-claims-landedness-bound. All are light-lane fixes to tools that can silently skip tests, not new policy.
KNOWN-LIMITATIONS rows owed: N9's five and entry 120's two lines, one docs PR.
B1's engine/kernel half, preregistration and architect consult only. No code; the draft comes to my morning sight.
If time remains: an architect assessment for MultiPolygon support, consult only.
