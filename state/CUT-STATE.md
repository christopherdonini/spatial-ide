# CUT-STATE — post-v0.1.0 (opened 2026-09-13 at the release cut's close)

## SESSION-CONTINUITY
flushed_at: 2026-09-16T11:14:42Z
tip: f3c625fe80c97c26a9077ba4914f0415a4797231
branches: #81 engine/extension-autoload-off OPEN (both gates PASS; CI running) -- the click accepts the ADR-021 amendment. cut/briefa-p3: fix round DONE (rebased on 355ed9a, 3 commits, 56 files +4035/-81, 617 cargo / 995 vitest green), reviewer + architect ATTEMPT 2 RUNNING (the last under Rule 7). governance/test-claims-planned: worker building the verify:test-claims planned-claims rule (stop-the-line fix); no PR yet. main = f3c625fe80c9 (this flush is ledger-only).
position: 2026-09-16 -- STOP-THE-LINE, self-inflicted: main's governance CI RED since 2a939d3 (10:20Z) on two pre-gate self-checks tripped by my own direct docs commits: verify:cites (a bare `Cargo.toml:41-45` in engine/LOD-PREREGISTRATION.md resolved against engine/Cargo.toml; the scanner strips ../ too) -- FIXED in prose, green again at HEAD; verify:test-claims (a preregistration committed before code names tests that do not exist yet -- the template's own rule vs the A3 check) -- fix in flight: claims in a not-done node's gate file become planned/advisory, binding once the node is done (reads PLAN.yaml; worker on governance/test-claims-planned under node self-check-false-readings, now in-progress). P3 attempt-2 gates running on the fix round. #81 open. Nothing merges until main is green.
half-made judgments: Lesson (twice today): a commit chain must GATE on the verifying command's exit code, never print it and commit anyway (I pushed a still-failing cite 'fix' at fcb8d15f-class; caught on the next run). The planned-claims rule is my design choice (PLAN.yaml gate + status), not the human's -- reviewer-gated as tooling under §21b; if the reviewer thinks A3's intent needs the human, it goes to round 4. P3: if attempt 2 fails either gate -> STOP, entry with both reports; if PASS -> PR flagging §12e Amendment 2, the four P6-sight strings, the boundary-4 pre-check cost (F3, the human's question after the tester's numbers), the worker's stash incident (restored, nothing lost). LOD tier builder waits for the machine.
intended sequencing (next session): (1) Tooling worker reports -> single reviewer gate -> PR (the human's click; main green on verify:test-claims after merge; until then main is red on that step only). (2) P3 attempt-2 gates report -> PR or STOP. (3) The human clicks #81 -> node done (pr 81). (4) LOD tier builder dispatch when the P3 gates and the tooling gate are done. (5) Round 4 batch: F3 relaxation (after tester numbers); anything the gates return to the human. (6) ADR-029 G1, drill-clean-clone. (7) Entry-95 re-feel + P6 acceptances at the next sitting.
unreported findings: verify-cites: a bare basename cite resolves against the citing file's directory first (engine/Cargo.toml for a doc in engine/), and `../` is stripped -- the root Cargo.toml cannot be cited as path:line from a subdirectory; use prose. verify-test-claims only recognises narrative-prefixed names (a_, the_, ...), so it flagged 3 of 13 planned tests -- inconsistent by construction, but the planned rule fixes the class. The P3 worker briefly ran `git stash push -u` in its worktree (restored via apply <sha>, dropped; verified intact). engine/tests/ 10 unconfigured DuckDB connections (node proposed). METRIC_RE misreads 'ADR-0NN security'. Telegram mirror silent on success.
in-flight gate states: briefa-p3-p6: reviewer a11396fb66c14d41f + architect aab178b871bf9efdd RUNNING (attempt 2, three-dot range on the P3 worktree). self-check-false-readings: worker a53a399938b6c8c79 RUNNING (no gate yet). engine-extension-autoload-off: both gates PASS, #81 open. gate-log: 29 records.

Previous ledger: `state/cut-archive/CUT-STATE-2026-09-13-release-0.1.0.md` (the release cut, 2026-09-08 → 2026-09-13; v0.1.0 = b391e43 on release/0.1.0, RC2 = 13471a9; merged back to main at e9900a3).

## Open at the close (the human sequences; nothing scheduled by the custodian)
- Entry 87 — the filtered per-tile mints refused with `skp.filter_rejected_by_binder` and discarded silently; the fix (shell mitigation at least; kernel if the sub-cause holds) with the suspended FIND′ assertion of the 84/85 preregistration as its test; the KNOWN-LIMITATIONS line then retires.
- Entry 66 (b) — PR #45 (both batches gated; the fits latch inverted; drop-at-drain); the ADR-028 closing note on the human's word (Amendment 4's text with Amendment 5 (e)'s clause); the 13b line retires at its landing; its Part K row into the next sitting bundle.
- Entries 88/89 — the hover blink per notch (reopen 75 (3)?); the pick-resolution threshold and the cursor shape.
- Entry 86 — the bundle viewer's zoom drift; KNOWN-LIMITATIONS if reproducible.
- Brief A — P3 held for the human's word (entry 83's question); PR #46 holds entries 80/81; entries 73 (sight list), 81's follow-through, the P4 records ledgered.
- Entries 69 (MSVCP140), 75 (the re-pick switch), 77 (post-tag signing evaluation: SignPath OSS then Certum), 78 (the human runs the repo edit), 79 (B1's close), 82 (closed by the third attempt).
- M13's two bracketed values (the human's); the GitHub release URL for Amendment 19.

- Entry 90 — static CRT as the v0.1.1 build piece (the spike passed); the licence-posture question first.
- Backend side-tracks (2026-09-13, parallel to Brief A, no walkthroughs): the LOD feasibility spike (`spike/lod-feasibility`, headless, reported-only → ADR-031's preregistration); the ADR-029 operation-lifecycle decision draft for the human's sight; B1's engine/kernel half on a branch once ADR-023's Decision text is ruled (sent), SKP bump coordinated with Brief A (0.3 → 0.4).

## Ledger
- 2026-09-13 — **RELEASE VISIBLE**: https://github.com/christopherdonini/spatial-ide/releases/tag/v0.1.0 (pre-release, not draft; asset `Spatial.IDE_0.1.0_x64-setup.exe` 10,559,893 B). Verified logged out (curl, no token): page HTTP 200; the unauthenticated API lists the asset; the public download hashes to c3f48320… = RC2. The first `gh release create` had left a draft after a failed asset upload (why the API showed nothing). The human's lesson added to RELEASE-DAY-CHECKLIST §5 verbatim; Amendment 19 complete. The release cut is closed in full.
- 2026-09-13 — **THE POST-TAG ARC OPENED — the human's directive, verbatim:** "v0.1.0 done; opening the post-tag arc. Three tracks, parallel where independent: Land the held work: #45 (66(b), with the ADR-028 closing note and 13b's retirement) and #46 (entries 80/81) merge on my click; entry 87's fix + 88/89 (hover blink, refusal threshold/cursor) as one small "filter-and-hover polish" piece through gates, 86 only if it reproduces. These ride alongside, not ahead. Brief A to close — the big one starts here. Send me the P3 digest now (preregistration §2, corpus manifest, each ADR draft's Decision paragraph, phone-readable). On my ruling, P3–P6 run: the session-identity tier, the corpus table, Part N, the ADR acceptances queued. Then Brief B's B1 opens immediately on A's close — the recipe milestone's first stage. Post-release housekeeping, docs-only, no ruling needed: the asset filename with the dot in QUICKSTART and the release body; the entry-78 description/topics I'll apply myself; the SignPath evaluation (entry 77) as a note. Draft 1's amendment is in force; delegated closures reported, not queued. Report ≤20 lines." Applied: the P3 digest assembled and sent; #45 merged onto main by a worker (conflicts with 47/84/85 resolved; the ADR-028 note appended on this word; 13b removed; the suspended FIND′ assertion re-evaluated per the merge-back note) then reviewer-checked and un-drafted; #46 mergeability checked and un-drafted; the polish piece (87 + 88 + 89) preregistered via an architect consult, then a worker and gates; 86 reproduction attempted (bundle viewer only, no app); QUICKSTART and the release body corrected for the dot filename; the SignPath note researched. One app at a time: the #45 harness run first, the polish piece's after.
- 2026-09-13 — Post-tag arc dispatched. **Housekeeping done (closed under delegation, §B docs):** QUICKSTART/README note the release page's dotted filename (main); the release notes edited on the release page (`gh release edit`, docs-only per the directive); PR #46 taken out of draft (mergeability recomputing). **In flight:** the P3 digest (assembling from main + PR #46's branch); the #45 landing worker (merge origin/main into cut/geometric-protection, conflicts with 47/84/85 resolved keeping both; the ADR-028 note appended on the human's word; 13b retired; the suspended FIND′ record re-evaluated under the widened set; verify + one harness run — harness slot 1); the polish piece's architect consult (87 + 88 + 89 preregistration; the 87 fix's home decided under docs/01 7/8, ADR-004/014/010-6); the 86 reproduction (bundle viewer in a browser, no app); the SignPath note (entry 77). Brief A P3–P6 wait for the human's ruling on the digest; B1 opens on A's close.
- 2026-09-13 — **Backend side-tracks dispatched** (the human's directive verbatim: "Backend side-tracks, no walkthroughs, parallel to Brief A: (1) LOD feasibility spike — headless, reported-only … (2) ADR-029 + operation lifecycle: architect consult, then the decision draft for my sight … (3) B1 engine/kernel half on a branch once I rule ADR-023's Decision text — send it; SKP bump coordinated with Brief A. Static CRT: if the spike passed, queue it as a build piece for v0.1.1."): the LOD spike (tester agent; `spike/lod-feasibility` in a new worktree; `geo` NOT a product dependency — spike-crate only; DuckDB spatial via the corpus venv; disk ≥ 20 GB kept; outputs deleted after measuring); the ADR-029 consult (architect; the draft filed under post-tag/ for sight; no data-plane change; the non-authorising id never derived from the handle); ADR-023's Decision text SENT (the B1 doc with the 2026-09-11 rulings appended); **entry 90 queued** (static CRT for v0.1.1; the licence question). B1's half waits for the ADR-023 ruling; its bump is 0.4 after A's 0.3.
- 2026-09-13 — **Entry 86 REPRODUCED** (headless; playwright-core + serve-bundle on the human's bundle): the viewer's fixed 1280×900 backing store vs its CSS-stretched box — `e.offsetX/Y` (CSS px) fed to `unproject` measuring from the attribute-set half-extents; no resize/DPR handling; pan and hover picking share the mismatch; not cumulative. KNOWN-LIMITATIONS line 16 added on main (retires when the viewer piece lands); entry 86 carries the finding. **Viewer fix piece → architect consult for its preregistration** (one pixel space for pointer and view; the reproduction driver `scratchpad/zoomdrift.mjs` becomes its E2E at two window sizes; no app; ADR-017 viewer ceilings untouched).
- 2026-09-13 — Three agents stalled on the stream watchdog (infrastructure; no work lost): the #45 landing worker after its merge commit 64e684f (worktree clean; resumed from step 2), the LOD spike (worktree + `spikes/lod-feasibility/{rust,scripts}` created; one route-A 5 GB output at tolerance 1.0 left under `target/spikes/lod/` — resumed with the measure-then-delete instruction; 27 GB free), the SignPath note (nothing started; resumed). **Dispatched:** the polish worker (the unconditional parts — §2.2 discriminator, §2.3 (i)/(iii) log + bounded requeue, FIND′ reinstated, §4.3 cursor, tests re-derived from the constant; harness held behind #45's run); the viewer worker (`viewer/zoom-anchor`, browser only). **Filed:** the polish preregistration + PROPOSED ADR-033 (d011be7 on polish/filter-and-hover); the viewer preregistration (8c6be2e on viewer/zoom-anchor); the ADR-029 decision draft (`post-tag/architect-consult-adr-029-operation-lifecycle.md`) — SENT; **entries 91 and 92 queued.** P3 digest SENT (886 lines, 38 blocks verbatim). ADR-023 Decision SENT.
- 2026-09-13 — **The Claude Code process exited with five agents running (infrastructure; no committed work lost).** State verified on resumption: the #45 worktree holds the merge 64e684f + fe216df (the ADR-028 appended note, present at the ADR's line 520) + 6944622 (13b retired), unpushed (origin at c959f17); `polish/filter-and-hover` and `viewer/zoom-anchor` clean at their preregistration commits (nothing written); the LOD spike had edited the WORKSPACE `Cargo.toml` (forbidden) — reverted by the custodian (`git checkout -- Cargo.toml`); its route-A 5 GB tolerance-1.0 output (2,872,575,989 B, measured in its JSON) deleted; the 12 `chrome.exe` processes are Google Chrome under Program Files (not a worker's Playwright) — untouched; no cargo/node/python/app process found. Disk: 13.8 GB free (the main checkout's shell cargo cache is 29.8 GB; the two worktree caches 16.3 GB each — the geometric-protection cache was NOT deleted after all). **Re-dispatched:** the #45 landing (steps: ADR-028 note compared mechanically to Amendment 4 + 5 (e); 6944622's diff; verify; cargo build + ONE regression run with K6/K7/FIND′ verbatim; fast-forward push — it holds the harness), the polish worker (unconditional parts; harness held behind #45), the viewer worker (browser only), the LOD spike (tester; standalone spike crate for route B with spike-only `geo`; disk discipline ≥ 6 GB; measure-then-delete), the SignPath note (general-purpose; read-only; nothing applied for).
- 2026-09-13 — **#45 landing run (worker, reported):** ADR-028's appended note = Amendment 4's text + 5 (e)'s clause word-for-word, plus one attribution paragraph naming the human's directive (additive; recorded). 6944622 removes only the 13b block (5 lines; no renumbering). `npm run verify` exit 0 (954 vitest + 76 + 29). `cargo build` exit 0. Regression once through the harness (`npm run e2e:regression`): **K6 PASS, K7 PASS, FIND′ PASS with the suspended entry-84 assertion recording "PREMISE BROKEN — 83 of 158 covering tiles reached no terminal" (entry 87, as expected)**; 9 PASS, 1 INFO, **1 FAIL: B2'/B3' expected `engine.crs_undeclared`, got `engine.format_default_contradicted`.** App PID 27912 (the worktree's debug exe) closed by PID. Pushed fast-forward c959f17..6944622. **B2'/B3' classified as main's own drift, not 66 (b)'s:** `git diff main cut/geometric-protection -- engine kernel protocol renderer` is empty; the fixture `no-crs-refused.parquet` is written with `CrsMode::AbsentKey` (`kernel/tests/manual_walkthrough_fixtures.rs:129-139`, its doc comment still stating the pre-Brief-A refusal), and since Brief A's P1/P2 (#44, on main) an absent key is the format default (`engine/src/fixture.rs:98-101`) contradicted by the metre-domain bbox → `FormatDefaultContradicted` (`engine/src/dataset.rs:1006`; `engine/tests/slice.rs:197` asserts it; ADMISSION-PREREGISTRATION §4 rows F-2/F-3). The E2E step, the walkthrough's B2/B3 rows and the generator's comment were never re-aimed to the ruled contract (#46 does not touch them). Same class as entries 80/81 → a delegated re-aim on its own small branch, reported (§B: test re-aims of ruled contracts); no product behaviour changes.
- 2026-09-13 — **SignPath note (entry 77) closed under delegation** (§B docs): `RELEASE-DRAFTS-0.1.0/decision-notes/signpath-evaluation.md`, 92 lines, 9 sources (three saved SignPath pages, signpath.org/about, /terms, /projects, docs.signpath.io, two Certum pages), read-only fetches on the custodian's authority, nothing applied for; the pointer appended to entry 77 (docs commit on main). Temp file deleted.
- 2026-09-13 — **#46 MERGED by the human** (7e47432, 19:47Z — entries 80/81 on main). **#45 READY FOR REVIEW** (un-drafted; the hold sentence replaced; the post-tag landing section appended to its body; `mergeable=MERGEABLE`; the branch does not carry #46's engine/kernel commits — 66 (b) touches nothing under `engine/`, `kernel/`, `protocol/`, `renderer/`, verified empty against current main). The click is the human's. Entry 77's note pointer committed on main (36f7f09, after a rebase of the one docs commit onto the #46 merge). **Re-aim worker dispatched (delegated, §B):** `fix/no-crs-reaim` off origin/main in the geometric-protection worktree — `no-crs-refused.parquet` becomes `CrsMode::ExplicitNull` (F-3 keeps B2/B3's assertion-prompt path), a new `absent-crs-contradicted.parquet` (F-2) with its own E2E step asserting the code and message only (the assertion-form question left to Brief A's shell parts), the walkthrough's B2/B3 rows and the generator's comment corrected; kernel builds via `CARGO_TARGET_DIR` on the main checkout's cache (disk). **Harness queue:** the polish worker first, then the re-aim worker; both hold until messaged.
- 2026-09-13 — **Polish piece, unconditional parts built (worker, reported; harness now released to it):** `polish/filter-and-hover` dab0457 (fix) + 61d4faf (test), pushed. §2.3 (i) the refusal log line (`tile-stream-mint-refused`, code from `SkpCallError`, `unknown` for non-SKP throws) and both epoch-abandon branches logged; §2.3 (iii) the bounded requeue (retryable: `engine.connections_exhausted` always; `skp.filter_rejected_by_binder` only when `detail` carries the lease substring minted at `predicate.rs:120`; one retry per tile per drain; only while in the current cover); §2.4 (2) six unit cases (the `../skp/client` mock now spreads the original so `SkpCallError` survives `instanceof`); §2.4 (4) FIND′'s suspension removed — the hard assertion is back; §4.3 `getCursor` → `cursorForPointerState` (crosshair / grabbing), asserted by a new `CURSOR'` E2E step (the unit seam cannot reach a real Deck canvas); §4.4 (1) `pickResolution.test.ts` re-derived from the constant (value still 2). `npm run verify` green (66 files / 942 tests). Mutation: retryable set forced empty → 3 tests fail by name, restored. Held: §2.3 (ii), §3, §4.2, both preregistration files. Gap sent back: §4.4 (2)'s `cursorForPointerState` unit cases (pre-committed) — to be added before the run. Expected on its run: B2'/B3' FAIL (main's drift; the re-aim branch).
- 2026-09-13 — **THE AUTONOMOUS WORK SYSTEM directive received** (the human; verbatim in `AUTONOMY.md` Appendix A — fifteen items + the seeded queue order + the first AskUserQuestion). Hook contracts verified against Claude Code 2.1.270's own documentation (Appendix B; the reference and guide pages saved locally at fetch time): Stop hooks receive `stop_hook_active`/`background_tasks`/`session_crons` and block with `{"decision":"block","reason":…}` under an 8-consecutive cap; PreCompact receives `trigger`/`custom_instructions`, CAN block compaction (proactive auto → skipped; context-limit recovery → the request fails) and cannot inject text; SessionStart `compact` re-injects stdout. `AUTONOMY.md` written and committed on `governance/autonomy` (56603ef, pushed). **Dispatched:** the plan seed (`governance/plan-seed`), the tooling (`governance/tooling`: yamlSubset parser, plan/queue/verify/site/health/docsOnly, the three hooks + tests, `.claude/settings.json`, `governance-ci.yml`), the docs (`governance/docs`: AI_DEVELOPMENT Amendment 2, PRECEDENTS.md, checklist §4 item 11, `scripts/evidence/archive.mjs`). **`state/CUT-STATE.md` becomes tracked from this commit** (AUTONOMY §7: the flush must be pushable); the SESSION-CONTINUITY block opened at its top.
- 2026-09-13 — **#45 MERGED by the human** (2687603 on main): entry 66 (b) landed — geometric protection at every zoom without enumeration, both batches; the ADR-028 closing note appended (on the human's word; the 2026-09-09 exception closed on both paths); KNOWN-LIMITATIONS 13b retired. Queued per rule 11: the Part K operator row. The seed worker told (`land-45` done, evidence PR 45).
- 2026-09-13 — **Polish piece, run 1 (worker, reported): CONFOUNDED BY LOAD** — it coincided with the spike's DuckDB 5 GB simplification and a kernel build. Discriminator (§2.2): 378 `tile-stream-mint-refused` lines, ALL `skp.filter_rejected_by_binder` with `detail` naming the lease as minted at `predicate.rs:120` ("…this dataset's maintenance connection capacity (1) is fully leased…"), zero real binder refusals; 257 distinct tiles, 121 refused exactly twice (one requeue, bound held), 136 once; all during FIND′, none during K6/K7/CURSOR'. **The hypothesis of §2.1 is confirmed by the instrument; the engine half (ii) stays on the human's word (entry 91 (a)).** K6 PASS; K7 timed out at 240 s and everything after it timed out (CURSOR', B2'/B3' as a timeout rather than the expected code mismatch, C2'/C3'); FIND′ hard assertion FAIL ("the filtered scan never left the in-flight family … livenessGone:true, cancelGone:false"). App PID 33932 closed. `cursorForPointerState` unit cases added (8d9a72e). **Re-run ordered on the quiet machine** (the spike finished; no build running). Mechanics lesson sent to the docs worker: a harness run needs a quiet machine, not only a single app.
- 2026-09-13 — **LOD feasibility spike CONCLUDED (tester, reported):** 90ccd15 on `spike/lod-feasibility`, pushed; nothing outside `spikes/lod-feasibility/`; workspace `Cargo.toml`/`Cargo.lock` unchanged. Route A (DuckDB spatial via the corpus venv): polygons-100k at 0.1/1.0/5.0 × preserve/simple and parcels-5gb at 0.1 preserve, 1.0 preserve/simple, 5.0 preserve/simple — every run identity-preserving (id round-trip), `invalid_after` 0; cancel via `interrupt()` returns in about a second, partial file left and deleted. Route B (spike-only Rust crate: `geo` 0.33.1, `wkb` 0.9.2, `geo-traits` 0.3.0, `parquet` 59.3.0, `arrow` 59.3.0 — NOT added to the product): builds; 100k vertex counts match route A exactly; RDP at 5.0 yielded 5 invalid outputs where DuckDB's yielded 0 (unexplained divergence, recorded); 5 GB streamed with zero disk write; cooperative cancel per row group with about ten seconds' detection latency. Confounds recorded honestly: route B single-threaded vs DuckDB multi-threaded; VW vs RDP families. **Engine fact:** DuckDB's `spatial` extension is not loaded by the engine (`engine/Cargo.toml:32`). All figures are spike measurements, not docs/08 claims. Every 5 GB output deleted after measurement; 10.25 GB free at its end. Its commit's `Co-Authored-By` names Claude Sonnet 5 (the tester agent's own model) — the human's identity rule holds (Signed-off-by is the configured user).
- 2026-09-13 — **Viewer piece (entry 86) built (worker, reported):** `viewer/zoom-anchor` 9e69f59 (E2E first, pre-fix readings: large window 110 px off, small 182 px off, forced 1280×900 exact) → 35763d1, e0558f7 (tolerance and DPR revisions AFTER results — a §3 deviation; Amendment 1 ordered to record it) → 95309d7 (the fix: `toStore` with the measured ratio; store sized to the client box × DPR on load and ResizeObserver; `zoomAt`/`panBy`/`resizeStore` pure; viewer-local `MAX_BACKING_STORE_DIM`/`PIXELS`; `ceilings.json` byte-identical) → 9c1064c (five unit tests; E2E wired as `test:e2e`, not in `verify` — CI has no playwright-core and no bundle; the `run-acceptance.mjs` precedent). Mutation: the wheel-site conversion reverted → all three E2E cases fail by name. `npm run verify` exit 0. Line-16 replacement drafted for the human's sight (appended to entry 86). Reviewer + architect gates to be routed after the amendment; the Part G row queued (sitting).
- 2026-09-13 — **Re-aim piece (worker, reported):** `fix/no-crs-reaim` 46dabb8 + 5549d52 (unpushed): `no-crs-refused.parquet` → `CrsMode::ExplicitNull` (F-3); `absent-crs-contradicted.parquet` (F-2, bbox in metres) with the E2E step `ABSENTCRS'` asserting the code and the message the engine mints (obtained from the real file by a throwaway probe, deleted); the walkthrough's B2/B3 facts corrected, no operator row added. Sibling found by the worker (§14's sibling search, before the rule landed): `bothneeded-refused.parquet` (AbsentKey, metres) and `admission-remediation.mjs`'s `stepBothNeeded` — folded into the piece (ExplicitNull, same treatment). A stray hand-typed Signed-off-by with the wrong email on the unpushed 46dabb8 — amended before push (unpushed, not a history rewrite). Harness queue: polish re-run (running) → re-aim (regression + admission-remediation).
- 2026-09-13 — **PR #51 opened** (`spike/lod-feasibility` → main; the human's click; spikes/ only). **Entry 93 queued** (the spike's two dependency proposals and three route questions), **entry 86's line-16 replacement filed for sight** — f6ea224 on main. In flight: the viewer's Amendment 1 + clamp test; the polish re-run on the quiet machine; the re-aim's `bothneeded` sibling; the three governance branches.
- 2026-09-13 — **SECOND DIRECTIVE received (verbatim, as it arrived — with a duplicated numbering; quoted in `AUTONOMY.md` Appendix A):** items 16 Telegram alerts via a Notification hook (and the Stop hook when blocking on the human), one message per blocking event, bot token in an env var only; 17 the ledger tracked under `state/` (CUT-STATE.md, NEXT-CUT.md, the archive), committed at every flush and report — the checkpoint is the commit; 18 the halt switch `state/CUSTODIAN-HALT` honoured by the Stop hook; 19 an issue template for the public repo (CRS situation, key columns, writer) — bug reports feed the corpus; 20 the human's own: branch protection on main + a v* tag ruleset (required CI + DCO, no force-push, no deletion), secret scanning with push protection, Dependabot alerts, a patch-bump precedent for the custodian; off-repo, theirs: an external drive and a monthly disk image. **Done now:** `state/CUT-STATE.md` (a40ccfe), `state/NEXT-CUT.md` and `state/cut-archive/` now TRACKED — the archive had been gitignored all along (never in the repository; the `.gitignore` rule lifted); `.cut-archive/README.md` left as the pointer because accepted ADRs cite the old paths and are immutable; references in AI_DEVELOPMENT.md and DECISIONS-PENDING.md updated. The tooling and docs workers extended with items 16, 18, 19 and 20's precedent; AUTONOMY.md §16–§20 on the integration branch.
- 2026-09-13 — **Polish piece, run 2 on the quiet machine (worker, reported):** K6 PASS, K7 PASS (15/15 notches), CURSOR' PASS (crosshair/grabbing observed), B2'/B3' PASS (the re-aimed fixture in the shared directory), C2'/C3' PASS; **FIND′ FAIL by its reinstated hard assertion: "113 unminted tile query(ies) of 193"** — 117 refusal lines, all the lease detail, none outside FIND′; repeats above two per tile are per-notch candidacies, not a spin. The shell half alone cannot discharge FIND′: the cause is the Maintenance lease collision and its fix is the engine half (ii), the human's (entry 91 (a)). Amendment 1 recorded on `polish/filter-and-hover` (aaa913f, post-result, FIND′ NOT re-suspended). Gates on the shell half dispatched (reviewer + architect); the PR then goes to the human with FIND′'s state stated plainly. Run 1's timeouts explained by load (quiet-machine rule sent to the docs worker).
- 2026-09-13 — **PLAN.yaml seeded** (`governance/plan-seed` 62be84f; 56 nodes: 15 done with verified evidence, 5 in progress, 17 blocked on the human, 5 proposed, 14 unscheduled; nulls listed, nothing guessed; phase `prototype` applied to process nodes as a declared choice). **Viewer gates dispatched** (reviewer + architect on c72b9a9). **The harness handed to the re-aim worker** (regression + admission-remediation). AUTONOMY.md §16–§20 + Appendix A2 committed on `governance/autonomy` (712e641, with origin/main merged in). Disk: 10.2 GB free.
- 2026-09-13 — **Viewer gates on c72b9a9: reviewer FAIL (B1 the per-axis clamp does not preserve aspect though three sites declare it does — at the ceiling a square extent paints non-square; B2 the early return at `main.ts:181` leaves the ratio stale so a resize becomes a zoom); architect PASS-with-notes, the same clamp declaration its one must-fix (ADR-010 rule 6: a declared behaviour must be true); §2's core rule, the fix, the mutation evidence and the ADR-017 contract all clear; the ceiling's viewer-local placement ruled constitutionally right (ADR-025 governs reader ceilings the preflight predicts against; a pixel ceiling in `ceilings.json` would force a kernel change).** Also: test 4 tautological; `MAX_BACKING_STORE_DIM` without a reason clause; the clamp test binds a replica; Amendment 1's post-fix row mislabelled; the line-16 draft must replace the trailing HTML comment too; resident-bytes accounting vs the store's bytes to be declared. Fix round dispatched to the worker (one shared clamp factor; ratio measured before the early return; a pure `clampStoreSize`; Amendment 2). First failed gate — Amendment 1 §A not yet in play.
- 2026-09-13 — **Governance docs landed (worker, reported):** `governance/docs` 57354f2 + d962e38 — AI_DEVELOPMENT.md Amendment 2 §A–§N (flush obligation, docs-only auto-merge, 2× budget, v0.1.1 CI artifacts, evidence archive, public-repo rule, gate defaults, generations/health/drill, what does not change, the quiet-machine harness rule, halt switch, Telegram, the `state/` ledger, the human's repository-settings checklist) + dated pointer bullets in the mechanics sections; PRECEDENTS.md with 33 entries (the three least sure to generalise flagged inside: P-022 Rule-7 accounting, P-020 signing scope, P-021 re-aim wording); RELEASE-DAY-CHECKLIST §4's v0.1.1 step (artifact name bracketed); `scripts/evidence/archive.mjs` (STORE-only zip in Node's standard library, manifest with SHA-256s, upload gated; dry run on the real `e2e/out`: 409 files, 81,989,422 B; round-tripped through `Expand-Archive`); `.github/ISSUE_TEMPLATE/bug-report.yml` + `config.yml`. **Finding (hygiene, delegated):** `residency-harness.mjs`'s `REGEN_FILTER_ZONED` names a generator that does not exist (`generate_the_filter_zoned_fixture`; the real one is `generate_the_filter_fixture`) — folded into the re-aim branch. `kernel/FIXTURES.md` complete for its scope. **Merged into `governance/autonomy`:** plan-seed (b860284) and docs (74f9b34), clean; pushed. Remaining: the tooling branch, then the tooling run on the real plan, the reviewer gate on the whole PR, the live dry runs, the first question set.
- 2026-09-13 — **Polish shell half — architect gate PASS with notes** (mergeable as a unit; FIND′'s discharge the human's): pre-merge items — (1) the `dataPlaneAttach` refusal path in `beginIssue` is unguarded and unlogged (pre-existing; §5 item 1 "anywhere in mintAndStart" not literally met — fix the same way or narrow the claim and queue the residual); (2) the bound is per candidacy, tighter than the declared "per drain" — wording; (3) the requeue pushes without a `MAX_QUEUED_TILES` check — add the guard or declare the overshoot; (4) the lease-substring match is a boundary loan, acceptable only as declared AND temporary — a block-on-sight to remove it when ADR-033's engine half lands (amendment + a REMOVE-WHEN line); (5) an undeclared operator-visible consequence: a requeued tile is outstanding work, so the status may read the paused/stalled suffix where the old drop settled partial — declare it in §2.5 and the PR text; (9) the PR text must quote FIND′'s actual failing assertion (the settled-partial sentence), name the held cause, say this build reports and retries but does not fix entry 87, and note the per-attempt counts are inflated by the retry. Reviewer gate pending; one batched fix round after it.
- 2026-09-13 — **Re-aim piece — harness runs (worker, reported):** regression all PASS (B2'/B3' `engine.crs_undeclared` with the assertion form; ABSENTCRS' `engine.format_default_contradicted`, the form's presence recorded as INFO, not asserted; FIND′ PASS on that branch with its 84 assertion suspended as on main); admission-remediation `BOTHNEEDED'` PASS; **`AXISTRAP'` FAIL — a stale hardcoded message against Brief A P1's `error.rs` wording, the same drift class — folded into the branch (one more run).** The `REGEN_FILTER_ZONED` name fixed (e723430). Pushed: 4ab6e99, 68ac675, cddb54e, e723430. Apps closed by PID each time.
- 2026-09-13 — **Viewer fix round landed (worker, reported):** `viewer/zoom-anchor` 774da00 (B1: `clampStoreSize` with one shared factor, both axes, aspect kept — the reviewer's counterexample now 3462×2423; B2: the ratio measured before the early return; one `lastRatio`) → fef5984 (reason clause on `MAX_BACKING_STORE_DIM`; the resident-bytes vs store-bytes declaration; "exact" reworded as the arithmetic property; the ceiling test binds the shipped function; test 4 rebuilt on the recording context; `ZOOM_ANCHOR_BUNDLE_DIR` override) → 2bac289 (Amendment 2, post-gates, with a fresh three-configuration table). `npm run verify` 67/67; E2E 3/3; mutation fails by name. Re-checks dispatched to the same reviewer and architect.
- 2026-09-13 — **Re-aim branch complete (worker, reported):** `AXISTRAP'` re-aimed to `error.rs:238`'s Display text verbatim, citing 92d87f1 (Brief A P1, ADR-032, ADMISSION-PREREGISTRATION §3 row 3); admission-remediation all PASS (eleven steps), exit 0; app closed by PID. `fix/no-crs-reaim` at 54d8dc0, pushed (five commits: 4ab6e99, 68ac675, cddb54e, e723430, 54d8dc0). Reviewer gate dispatched; the PR opens on PASS (delegated closure, reported).
- 2026-09-13 — **Viewer architect re-check at 2bac289: PASS with notes — the must-fix discharged** (one shared factor; the declaration true; the ceiling test binds the shipped function; the resident-bytes distinction declared). Three new non-blocking notes sent as one closing round: the pixel-ceiling regime still lets a uniform-aspect resize act as a zoom (correct `scale` before the early return + a unit case); the reachability sentence in `MAX_BACKING_STORE_DIM`'s reason clause is wrong by arithmetic (delete it); Amendment 1's tolerance basis superseded by Amendment 2's run (Amendment 3 records the corrected basis; a stray backtick fixed). Reviewer re-check on 2bac289 still running; a delta re-check follows the closing round.
- 2026-09-13 — **Polish shell half — reviewer gate FAIL (first failed gate; §A not in play):** (1) the branch is behind main in five of its eight files and conflicts in `tileViewportStreamManager.ts` + test; post-merge its windowed `currentCoveringKeys` would refuse retries outside the 256×256 window — must yield to 66 (b)'s `latestMembership`; (2) §2.4 (4) violated — the FIND′ entry-84 hard assertion was DELETED, not reinstated, the in-file comment saying the opposite; and a verbatim restore would now be wrong because the requeue re-fires the trace so "unminted = queries − streams" counts retried tiles — the "113 of 193" handed to the human is per-attempt and inflated. Also: `MAX_QUEUED_TILES` overshoot; `dataPlaneAttach` outside the try/catch (a tile stuck `issuing` forever); the substring coupling fragile; dead `records`. Everything else PASS (counts verified mechanically from the session logs; mutation re-run; verify exit 0). **One batched fix round dispatched** (merge main + `latestMembership`; a retry-aware per-tile FIND′ assertion, hard; the room check; `dataPlaneAttach` handled; the REMOVE-WHEN line + block-on-sight; wording; Amendment 2 with both numbers; one regression run).
- 2026-09-13 — **Viewer reviewer re-check at 2bac289: FAIL — B1 verified fixed (three counterexamples), test 4 and the ceiling test PASS, gates PASS; but the B2 remedy regressed the ordinary resize path:** `main.ts:186` divides the old store width by the new client box, so every resize at constant DPR scales the view (measured in Chromium: 1.6822× growth where the invariant demands 1.0; the single-token fix `ratioBefore = lastRatio` reads 1.0000). The architect's note 3 (the pixel-ceiling regime) has the same fix. **Rule-7 reading recorded:** the closing round already in flight addresses this same code path and was dispatched before this verdict arrived — the correction was sent into that round (the same attempt, not a new one); a third reviewer FAIL on this path stops the piece and queues it to the human. The forced-CSS mutation reading 259.19 was the regression's fingerprint; Amendment 3 must say so.
- 2026-09-13 — **Re-aim reviewer gate FAIL on two stale texts** (the `BOTHNEEDED'` doc comment still "no crs key"; the walkthrough's I3 row quoting the pre-P1 axis-order message as verbatim) — everything else PASS (the F-2 message reproduced mechanically from `error.rs` + `dataset.rs` + the fixture's grid bounds, exact; `AXISTRAP'` char-for-char; cites; one sign-off each; the kernel test green on the main cache). Fix round dispatched with two cite corrections and the kernel test comment (same class). **Queued entry 94:** KNOWN-LIMITATIONS line 40 carries the same stale message — release text the human sighted, so a sight item, not folded in.
- 2026-09-13 — **Viewer closing round landed (worker, reported):** `viewer/zoom-anchor` 6e24bf4 (the resize regression fixed with the persisted `lastRatio` as the denominator; the pixel-ceiling regime now corrects `scale` instead of early-returning; the reachability sentence deleted; the backtick fixed) → 2243e37 (two unit cases: same dims + moved ratio; ordinary resize at constant ratio) → 70c4a94 (Amendment 3 — corrects Amendment 2's "benign bookkeeping" reading: the 259.19 / 2.36 pair was the regression's fingerprint; the fresh run reads 0.22 / 11.67 / 14.56 again, tolerance 20 unchanged; forced-CSS mutation back to 150.36). `npm run verify` 69/69; E2E 3/3. Reviewer delta re-check dispatched — the third reviewer pass; a FAIL stops the piece and queues it.
- 2026-09-13 — **Re-aim fix round landed (worker, reported):** 2c45798 on `fix/no-crs-reaim` — the `BOTHNEEDED'` comment, the walkthrough's I3 quote (now `error.rs:238-243`'s text), the row-3 cite narrowed to the today column, the `dataset.rs` cite, the kernel test comment, the `.pre-reaim` copies deleted; `node --check` clean; the kernel remediation test compiles. Reviewer delta re-check dispatched; the PR opens on PASS.
- 2026-09-13 — **Tooling landed (worker, reported):** `governance/tooling` deb56ed + a9df161 + bfb3ae7 — the YAML-subset parser, plan/queue/verify/site/health/docsOnly, the Stop / PreCompact / SessionStart / Notification hooks with the Telegram sender (env only) and the halt switch, `.claude/settings.json`, `governance-ci.yml`; 85 tests. Cross-branch discrepancy found by it: three seeded ids with periods — renamed on the integration branch (`v0-1-0-shipped`, `entry-90-static-crt-v0-1-1`, `evidence-archive-v0-1-0`). **Assembled on `governance/autonomy` (b21ee04):** tooling merged; verify accepts a felt verdict recorded in a release record (the v0.1.0 sittings live in RELEASE-0.1 Amendments 15/18); CI runs the tests by glob (the directory form failed on this Node); `CUSTODIAN-QUEUE.md/.json` and `site/` generated — **the queue reads "nothing ready": every open node waits on the human (160 min listed) or on a running piece**; `verify:plan --offline` PASS. The Bash tool's heredoc mangled a `\b` into a control character on the first patch — repaired by a script file (the standing memory rule). Reviewer gate on the whole branch dispatched.
- 2026-09-13 — **Viewer piece PASSED its third reviewer pass at 70c4a94** (Chromium probes: ordinary resize 1.0000; pixel-ceiling regime 0.5003 vs 0.5000) and the architect's must-fix; nit applied at d4cca87. **PR #53 opened** (the human's click). **Re-aim reviewer delta PASS at 2c45798; PR #52 opened** (delegated closure, reported; the human's click since it touches test code and fixtures).
- 2026-09-13 — **Governance branch — reviewer gate FAIL at b21ee04, five blocking, all tooling/docs:** (1) CI checkout without `fetch-depth: 0` (no tags, no origin/main — the branch's own run red); (2) `docsOnly.mjs` treats `docs/adr/*.md` and `docs/01_Principles.md` as docs-only-eligible (§9 says non-ADR); (3) the health strip prints `[object Object]` and lacks the two age rows; (4) the Notification input fields quoted in the hooks README are not in AUTONOMY Appendix B (the second directive's hook); (5) the Notification sender is not deduped. Plus suggestions (double reason print; `@{u}` reachability by merge-base; verify's `main` fallback; three-dot diff; in-progress/gate validation; health→site coupling; `</script` escaping; the missing `asset-filename-docs-fix` done node; the per-stop fetch cached) and nits. PASS on everything else: red lines untouched, PLAN.yaml honest (six evidences spot-checked live), Amendment 2 verbatim, 22/23 precedent quotes verbatim (the 23rd an escaping artifact), sign-offs, no perf claim. **Fix rounds dispatched:** tooling (`governance/tooling-fixes`), docs (`governance/docs-fixes`); Appendix B's Notification quotes are the custodian's (from the saved reference page).
- 2026-09-13 — Governance: Appendix B extended with the Notification input fields, the no-block sentence and the matcher-pattern rules quoted from the saved reference (b70976b); docs-fixes (3a68958: the Context ellipsis; the precedent escapes; the stale "not yet in the tree" sentences, found by sibling search in Amendment 2 too) merged (955da0f). Remaining before the re-check: the tooling fix round.
- 2026-09-13 — **Polish fix round landed (worker, reported):** `polish/filter-and-hover` 5391136 (the merge of main with the fixes inside it — conflicts resolved; `currentCoveringKeys` deleted, the requeue's cover test reads 66 (b)'s `latestMembership`; FIND′ a HARD assertion again with a retry-aware per-distinct-tile counter; `MAX_QUEUED_TILES` gates the push; `dataPlaneAttach` handled; the REMOVE-WHEN line; wording; `requeuedTiles` cleared; the `unknown` case; the amplification sentence) → 2a1fc3c (Amendment 2: "113 of 193" restated as per-attempt; per-tile figures **32 unminted of 67 distinct** (run 2, offline) and **34 of 63** (run 3); the operator-visible consequence; the block-on-sight). Runs: `npm run verify` 966/966; mutation 3 named cases fail and restore; harness once — K6, K7, CURSOR', B2'/B3', C2'/C3' PASS; **FIND′ FAIL as expected on the shell half alone** ("34 unminted tile(s) of 63 distinct tile(s) ever refused"). Reviewer + architect re-checks dispatched. The figure for entry 91 (a) is the per-tile one.
- 2026-09-13 — **Polish architect re-check at 2a1fc3c: PASS with notes** — all three pre-merge items discharged; the `latestMembership` substitution keeps ADR-028's semantics untouched; the FIND′ metric honest (a retried-then-minted tile counts as minted). Two nits: the run-3 figure (34 of 63, the shipped counter's first reading) is not yet in the record — Amendment 3; a stale cite at `tileViewportStreamManager.ts:274-275`. Held for the reviewer's verdict. **Governance: tooling fixes landed** (`governance/tooling-fixes` 82d1733 + e9266bb + 5be1b28: `fetch-depth: 0`; `commitOnMain` fallback; ADR paths excluded from docs-only and merge-base diff; six readable health rows; deduped Notification sender; single allow print; `@{u}` reachability; in-progress/gate validation; READMEs; `</script` escaped; the `asset-filename-docs-fix` node; the HALT probe cached; 95 tests) — merged (6b6acd3); verify --offline PASS, both drift checks current. Reviewer re-check next.
- 2026-09-13 — **Polish shell half — reviewer re-check at 2a1fc3c: PASS** (every earlier blocker fixed and verified: FIND′ hard and honest — a refused-then-retried-then-minted tile counts as minted; the recount reproduced from the session log, 67 distinct / 35 recovered / 32 unminted at every window; merge-tree clean; the room check; the attach handling; the REMOVE-WHEN line; mutation and verify re-run; eight sign-offs). One new non-blocking note: the harness now reads the app's session-log directory — a second app-layout dependency, disclosed, fails safe. Architect PASS with notes. Two closing nits sent (Amendment 3 with run 3's shipped-counter reading; a stale cite). **The PR opens on their landing; the human decides whether the shell half merges before the engine half (entry 91 (a)) — FIND′ stays recorded FAILING on it.**
- 2026-09-13 — **The human merged #51 (the LOD spike, 91cfb44), #53 (the viewer zoom-anchor fix, 4c25832) and #52 (the no-CRS re-aim, bf02011).** Consequences: entry 86's KNOWN-LIMITATIONS line-16 rewrite is now the human's sight item (the bracketed text and its source comment in entry 86); the Part G row queued (sitting); entry 93's route questions and entry 94's line-40 correction stand; the plan nodes `viewer-86-zoom-anchor`, `reaim-no-crs-fixture`, `lod-feasibility-spike` become done (evidence PRs 53, 52, 51) — recorded on the governance branch after its re-check, with the queue and site regenerated.
- 2026-09-13 — **Governance — reviewer re-check at 6b6acd3: PASS; CI green on the tip (run 34784922443).** Plan data updated for the human's three merges (a4daa39; verify PASS; queue: nothing ready, 15 items waiting on the human). **PR #54 opened** (the human's click: it carries code paths — hooks, scripts, CI, settings). **Polish — Amendment 3 (ea7a494: run 3's shipped-counter reading 34 of 63 as the figure for entry 91 (a); the stale cite fixed). PR #55 opened** (the shell half; FIND′ recorded FAILING for the held engine cause; the human decides whether it merges before the engine half). **Every unblocked node is now exhausted: the first question set follows** (lane priorities; the waiting-on-you list; the confirmations; the disk reclaim; the evidence attach; Pages; the autocompact window).
- 2026-09-14 — **Disk ruling applied:** the main checkout's shell debug cache deleted (the release cache with RC2's build kept). **Dispatched:** `release-artifacts-from-ci` (a worker: the tag-triggered `tauri-build` with the installer as a workflow artifact + its SHA-256 in the step summary; the checklist's v0.1.1 step filled; a dispatch dry run). **Held for a quiet machine:** the drill (the engine-half worker holds the harness and builds). The two remaining sights (entry 94's line 40; M13's values) go to the human as question set 2 now.
- 2026-09-14 — **Question set 2 (sights) answered and recorded verbatim:** entry 94 applied (KNOWN-LIMITATIONS line 40 now quotes `error.rs:238-243`'s text); M13's session-log error line given verbatim and recorded in the Part M log (the exit code not given — stays bracketed, re-asked). Disk: 30.7 GB free after the cache reclaim.
- 2026-09-14 — **CI artifacts (worker, reported):** `release/ci-artifacts` — the `tauri-build` job on `v*` tag pushes with the installer uploaded as `spatial-ide-<tag>-x64-setup` and its SHA-256 in the step summary; the checklist's v0.1.1 step filled (eight steps); dispatch dry run 34786140667 green (`Spatial IDE_0.1.0_x64-setup.exe`, 10,425,624 B, SHA-256 EECE72CB…6142948 — a build from the branch, not a release artifact). **Flagged risk taken seriously:** GitHub combines a push event's `tags` filter with its `paths` filter, so a tag at an existing commit might never trigger the job — restructure ordered: a dedicated `release-artifacts.yml` (`v*` tags, no paths filter; `workflow_dispatch`), the build steps shared via a `workflow_call` job; `product-ci-shell.yml`'s pull_request behaviour restored exactly. Its commits' `Co-Authored-By` names the worker's own model (Sonnet 5) — the human's identity rule holds.
- 2026-09-14 — **Pages directive (the human, verbatim):** "Pages source is set to GitHub Actions. Add .github/workflows/pages.yml: on push to main (and manual dispatch), run verify:plan and the site generator, upload site/ with actions/upload-pages-artifact, deploy with actions/deploy-pages; permissions pages: write and id-token: write only; the four official actions pinned to full commit SHAs like the existing workflows. Gate it on verify:plan passing so an inconsistent plan never publishes. Docs/CI-only PR for my click; then confirm the page renders logged-out at https://christopherdonini.github.io/spatial-ide/." **Done:** `governance/pages` dd39eab — two jobs (verify-and-build with `contents: read` for the checkout and verify's read-only calls, mirroring governance-ci; deploy with `pages: write` + `id-token: write` ONLY, `needs` the gate); pins resolved live from each action's latest release (checkout v7.0.1 3d3c42e…, setup-node v7.0.0 8207627…, upload-pages-artifact v5.0.0 fc324d3…, deploy-pages v5.0.1 368f825…). **Premise noted for the human:** the existing workflows pin by TAG (`@v4`), not by SHA — upgrading them is a separate hygiene piece (proposed). No dispatch dry run from the branch (it would deploy before the click). Reviewer gate dispatched; the PR opens on PASS; the logged-out check follows the merge.
- 2026-09-14 — **Pages workflow gated PASS (reviewer: every SHA verified against its tag and the latest release; the gate order and `needs`; `contents: read` the minimum; the github-pages environment's branch policy allows only main). The token moved to the verify step (7c97304). PR #56 opened** (the human's click). After the merge: the first run deploys; the logged-out check of https://christopherdonini.github.io/spatial-ide/ is recorded here. Noted: `site/.nojekyll` is not in the artifact (hidden files excluded by default) and not needed (build type `workflow`); the published health strip is the committed `health.json`, dated by its own timestamp.
- 2026-09-14 — **CI artifacts restructured (worker, reported):** `release/ci-artifacts` 9121d5a + 14e0fe5 — a reusable `tauri-build.yml` (`workflow_call`, inputs `ref`/`artifact_name`, `contents: read`), a dedicated `release-artifacts.yml` (`v*` tags, NO paths filter; `workflow_dispatch` naming-only; a resolve job computes `spatial-ide-<tag>-x64-setup` then calls the reusable build); `product-ci-shell.yml` byte-identical to before except its `tauri-build` job now calls the reusable workflow. Substitute dry run through the identical reusable chain: run 34786925280 green (`Spatial IDE_0.1.0_x64-setup.exe`, 10,424,593 B, SHA-256 129B0E59…353AAA — a branch build, not a release artifact). `release-artifacts.yml` itself cannot be dispatched until it exists on main (GitHub registers dispatchable workflows from the default branch) — its tag trigger is verified post-merge by a dispatch or at the v0.1.1 tag. Checklist §4 and AUTONOMY §11 name both files. **Reviewer gate and PR deferred to the restarted session (the human's instruction: stop once the agents finish).**
- 2026-09-14 — **Engine half landed (worker, reported):** `polish/engine-lease-class` 447118d (feat: `LeaseClass::Admission`, `MAX_ADMISSION_CONNECTIONS = 4` with its reason clause — the shell's three tile streams plus the baseline query; ADR-010 rule 6; admission-side, not ADR-014; `MAX_PHYSICAL_CONNECTIONS` re-derived) → 03480f6 (the two tests: N concurrent admits succeed; residual exhaustion surfaces as `ConnectionsExhausted`, never `RejectedByBinder`) → ec8216a (the shell's lease-substring match removed per the block-on-sight; the retryable set is `engine.connections_exhausted` alone) → 8efe757 (ADR-033's text: all three classes and capacities as declared ceilings, does not pre-empt ADR-014, Status untouched; Amendment 4). **FIND′ PASS: "0 unminted tile(s) of 0 distinct tile(s) ever refused; .residency-status settled to 'Showing all 99 features in view'"** — zero contention, every covering tile minted (the ruling's "63 of 63" as observed: nothing refused at all); K6, K7, CURSOR' PASS. Engine 127 unit + integration, kernel 211, `npm run verify` green; mutation (class reverted to Maintenance) fails both new tests by name. **Design point for the gates:** `admit` now returns `PredicateAdmitError`; a `From` impl keeps the kernel byte-identical but folds a residual `ConnectionsExhausted` back into `RejectedByBinder` at the kernel's one call site — composition-unreachable at capacity 4, recorded as owed in ADR-033 and the code; the ruling's "never a false binder refusal" is fully met only with that kernel arm — the gates decide whether it lands in this piece. Sibling search: `duckdb_version()` (`dataset.rs:447`, once per publish) is per-request-shaped on the Maintenance lease — reported, not moved. **Gates and PR deferred to the restarted session (the human's instruction).**
- 2026-09-14 — **PAGES LIVE:** the human merged #56 (a6bd7d6); the first `pages.yml` run on main succeeded (2026-09-13T22:44:48Z); **verified logged out (curl, no token): https://christopherdonini.github.io/spatial-ide/ → HTTP 200, 33,717 B, title "Spatial IDE — custodian's plan", the "Waiting on you" panel present.** Every push to main redeploys behind the verify:plan gate. **SESSION STOPPED HERE at the human's request (restart for the Telegram environment); no agent running; the next session resumes by the SESSION-CONTINUITY block above.**
- 2026-09-14 — **Flush per the PreCompact hook** (it blocked the human's /compact: the tip field was a description, not a hash). The hook now requires the literal HEAD hash, with the flush commit's parent accepted only for a ledger-only commit (the one commit that cannot cite its own hash) — committed directly on main at the human's instruction (a057b24); this ledger-only commit cites it. The human compacts and restarts next.
- 2026-09-14 — Second hook defect found by the manual check: untracked paths (the drafts directory, kept untracked by design) counted as a dirty tree — fixed (d52992d, porcelain `??` ignored, test); this ledger-only flush commit cites it.
- 2026-09-14 — **Incident, mine:** d52992d shipped a hook that did not parse (the Bash tool's heredoc mangled a regex) and my command chain masked the failing test behind a pipeline's exit code; governance CI on main red for that commit. Repaired (cb1d670; the runner's own exit code checked). Lesson re-recorded: multi-line code patches go through a script FILE, never a heredoc; test exit codes are read from the runner, never from a pipe.
- 2026-09-14 — **Session resumed after the compact/restart.** `CUSTODIAN_TELEGRAM_BOT_TOKEN` and `CUSTODIAN_TELEGRAM_CHAT_ID` are visible in the session's environment (presence checked; values never printed). **The human's directive (verbatim):** "Add DRAFT-4 (C:\Users\Christopher\Development\Claude\Spatial IDE\DRAFT-4-landing-page-design.md) to the plan as a governance node site-design-v1, low priority, after the drill; fix its "bugs first" section now as a delegated docs/tooling piece since two of the three are the health line lying (CI "unknown", PRs "offline" — read GitHub's API at Pages build time, not gh on the dev machine). Then you can keep going with resuming the session". **Applied:** DRAFT-4 copied into the tree byte-for-byte (LF; 4,779 B; sha256 6b9efb20…) as `scripts/plan/DRAFT-4-landing-page-design.md` (and, untracked, beside the other drafts under `RELEASE-DRAFTS-0.1.0/post-tag/`); `site-design-v1` appended (governance, `order: 6`, `depends_on: [drill-clean-clone, site-bugs-first]`, blocked — derived); `site-bugs-first` appended in-progress (governance, `order: 5`, `evidence: {branch: governance/site-bugs-first}`); the plan-data commit on main as the previous ones (the confirmation on plan-data commits stays queued). Queue and site regenerated; verify:plan PASS (exit 0). **Live-page check before the brief (Chrome, the logged-out URL):** the health strip reads "CI on main unknown" and "Open PRs offline: gh pr list skipped" — the committed `health.json` was generated with `--offline` (the lie the human named). The "Shipped since your last visit" panel's JS did run in Chrome and rendered "(nothing since your last visit)" although twenty-two done nodes carry `dates.done: 2026-09-13` — a second defect in the same panel: the comparison is instant-vs-midnight, so a same-day return visit hides everything landed that day; the human's "loading…" is the no-JS/JS-failed state, for which the generator emits no fallback. Both go into the piece. **Gates on the engine half dispatched** (reviewer + architect in parallel; the kernel-arm question put to both).
- 2026-09-14 — **Worker dispatched on `site-bugs-first`** (delegated, §B "CI, watcher, hygiene and tooling mechanics"; tag `node:site-bugs-first@g1`; the custodian's brief, not in the tree — saved in the session scratchpad; its mechanisms, decided by the custodian within DRAFT-4's "Bugs first" text): (1) a new `scripts/plan/buildHealth.mjs` reads GitHub's REST API with Node's global `fetch` (standard library; no dependency), `GITHUB_TOKEN` when present with one anonymous retry on 403 (the repository is public), for three facts — the latest `product-ci-rust.yml` run on main (conclusion/status/sha/url), open PRs (count; oldest by age with number and url), the latest release (tag/date/url) — every failure recorded per fact as an error string, never a silent "unknown"; it writes `site/data/build-health.json`, gitignored (generated only in the Pages build); `site.mjs --check` renders with that file absent, so the drift check never depends on it; `pages.yml` runs it between verify:plan and the generator; `governance-ci.yml` gets a smoke step writing to a temp directory so the scoped token's reach shows in every PR's log. (2) `health.mjs` drops `ci`, `open_prs` and `--offline`: machine facts only (drift, disk, stray processes, waiting ages) with `source: "the custodian's machine"`; the strip renders two labelled lines, each with its own timestamp; with the build file absent the build line says so honestly. (3) The flush procedure refreshes the machine facts as a separate `chore(site): health refresh` commit before the ledger-only flush commit (the PreCompact hook's ledger-only exception untouched). (4) The shipped panel: a static list at generation time (done nodes within seven days of the plan's newest `dates.done`, so the output stays deterministic); JS in try/catch only replaces it, comparing calendar days inclusive of the last-visit day; no "loading…" anywhere. (5) Need-type as a separate chip element with a leading space and an aria-label; zero or absent minutes render "unestimated" and stay out of totals. (6) Ids leave the visible text (title attribute + anchor only; cross-lane "after:" notes link the dependency's title). (7) Done dates labelled by evidence kind ("evidence dated" for release/tag evidence, "landed" otherwise). Tests for every bullet (`site.test.mjs` new; `buildHealth.test.mjs` with an injected fetch; `health.test.mjs` updated); README, AUTONOMY §5/§7/§15 updated; no redesign (that is `site-design-v1`). Reviewer-gated; the PR is the human's click (code paths change — not docs-only).
- 2026-09-14 — **Engine half — both gates FAIL on the one finding the block predicted.** Architect (verbatim verdict line): "VERDICT: FAIL — (1) a residual admission-lease exhaustion still reaches the wire as `skp.filter_rejected_by_binder` via `engine\src\predicate.rs:394-410` → `kernel\src\skp.rs:521`/`:388`/`:749-753`, violating the ruling's "never a false binder refusal", preregistration §5 block-on-sight 2, `protocol\skp\SKP-V0.md:217` item 8, and ADR-010 rule 6's "comfortably under it today is not a strategy"; aggravated by ec8216a removing the only retry path for that case. (2) `engine\src\predicate.rs:383-386`'s "today: none inside this workspace" is factually false and is the load-bearing justification for (1) — correct it with the fix. (3) ADR-033 line 19's "no wire change" claim and its stale cites (`tileGridConstants.ts:38`, `pool.rs:251-255`) need updating before it goes to the human for acceptance." and "KERNEL ARM: in-piece — the typed fact only becomes `engine.connections_exhausted` on the wire if `kernel\src\skp.rs` matches `PredicateAdmitError` and routes the residual through the existing `error_of` arm at `:677-680` (no new `skp.*` code, ADR-021 item 8 untouched), which is precisely the role §2.3's header assigns the kernel." Reviewer (verbatim verdict line): "VERDICT: FAIL — 1. `kernel/src/skp.rs:388`+`:497` (via `engine/src/predicate.rs:394-410`) still reports a residual admission-lease exhaustion to clients as `skp.filter_rejected_by_binder`, the false binder refusal the 2026-09-13 ruling forbids and the branch's own preregistration §5 block-on-sight item 2 names; the shell's post-`ec8216a` retryable set (`engine.connections_exhausted` alone) makes that refusal a silent tile drop." and "KERNEL ARM: in-piece — the engine half's typed cause cannot reach any client without the one match arm at `kernel/src/skp.rs:388`, and the shell change in this same branch removed the only compensating path." Everything else affirmative: the engine change (`LeaseClass::Admission`, the ceilings const-asserted, no wait/queue introduced — docs/01 principle 7 kept), the two tests as named (`cargo test -p spatial-engine --test predicate_admission` → 6 passed, cargo exit 0; kernel `cargo check --all-targets` exit 0), the shell's substring match gone (`npm test` 966 passed, exit 0), ADR-033 Status still Proposed with the three classes declared as ADR-010 rule 6 ceilings and the ADR-014 sentence explicit. Non-blocking nits both gates raised: the test mock's stale `maintenance/1` fields; `pool.rs:58`'s "names no constant belonging to a binding" vs `:106-107`; the recorded mutation never exercised test (b)'s discrimination; `MAX_PHYSICAL_CONNECTIONS` 5→9 raises per-dataset resident connections with no docs/08 claim made (no violation; any later first-pixels figure re-measures). **Bounded closing attempt 1 of 2 (Amendment 1 §A) dispatched to a worker:** the kernel match arm at `skp.rs:388` with `build_viewport_query` returning `PredicateAdmitError`, the `From` fold deleted, one kernel test with its mutation, a second mutation for engine test (b), the mock fields, ADR-033's Decision/gap/cites, `pool.rs:58` + the composition fact in `kernel/README.md`, preregistration Amendment 5 (post-result, says so first); FIND′ not re-run (the arm changes the wire only on the path FIND′ recorded as never taken). Re-gate both when it reports; Rule 7 applies after a second failure.
- 2026-09-14 — **CI artifacts — reviewer gate PASS** (verbatim verdict line: "VERDICT: PASS"). Affirmed: the three workflows parse; `workflow_call` inputs wired at both call sites; the resolve job names `spatial-ide-v0.1.1-x64-setup` on a tag push and guards an empty dispatch input; `product-ci-shell.yml` differs from origin/main in one hunk (the `tauri-build` job's steps → the reusable call); `contents: read` everywhere, no secrets, no new third-party action, tag pins kept; `on.push.tags: ["v*"]` with no `paths` key; dispatch builds and uploads only, never publishes; ADR-020 untouched (no tauri.conf.json/CSP/signing change); AUTONOMY §11 and the checklist name the same artifact and the from-v0.1.1 rule; actionlint absent (PyYAML used; nothing installed). Non-blocking, swept under delegation (§B "CI, watcher, hygiene and tooling mechanics"; worker dispatched): `product-ci-shell.yml`'s path filters now watch `tauri-build.yml`; the dispatch's `inputs.ref` passed to the pwsh script via `env:` instead of expression substitution; the reusable workflow's dead `ref` input removed (both callers build `github.ref`; the dispatch's ref is naming-only by design); the "behaviourally identical" comment corrected (the reusable job adds the SHA-256 summary and `retention-days: 90`); the checklist states the precondition (the local `uses:` resolves at the tagged commit, so only tags cut at or after the merge work — the from-v0.1.1 rule) and that the installer's filename follows `tauri.conf.json`'s `version`. Then the PR for the human's click; the tag trigger itself is verified post-merge.
- 2026-09-14 — **CI artifacts sweep done (worker, reported):** 6e1089f — path filters watch `tauri-build.yml`; the dispatch input read from `$env:INPUT_REF`; the dead `ref` input removed (both callers pass `artifact_name` only); the "behaviourally identical" comment corrected; the checklist precondition and the filename clause added; the three workflows parse (exit 0). Off-scope noted, not done: a comment in `product-ci-shell.yml` says "below" where the triggers are above; both first-green record slots stay empty by design. **PR #57 opened** (`https://github.com/christopherdonini/spatial-ide/pull/57`) for the human's click; post-merge: one dispatch confirms registration, the tag trigger at v0.1.1.
- 2026-09-14 — **site-bugs-first built (worker, reported):** `governance/site-bugs-first` 730a0c7 (four commits). DRAFT-4 "Bugs first" 1–5: `scripts/plan/buildHealth.mjs` (GitHub REST via Node's global `fetch`; CI run on main, open PRs, latest release; 404 on releases → null; other failures → `{error}` rendered verbatim; 403-with-token retried once anonymously, `auth` recorded per fact; output `site/data/build-health.json`, gitignored); `health.mjs` machine facts only (`source: "the custodian's machine"`; no `gh`, no `--offline`); `site.mjs` renders two labelled, separately dated groups and never mixes a row, `checkSiteDrift` always renders with build facts absent (tested); the shipped list static, keyed to the plan's newest `dates.done`, no "loading…", the JS in try/catch with a calendar-day compare inclusive of the last-visit day; `.chip-need` with aria-label, 0/absent minutes → "unestimated" and out of the total; title-only labels with ids in anchors and `title=`, cross-lane "after:" links the dependency's title; release/tag → "evidence dated", otherwise "landed". Tests: `node --test` glob form → tests 122 · pass 122 · fail 0, runner exit 0 (new `site.test.mjs` 15, `buildHealth.test.mjs` 8; `health.test.mjs` updated); `health.mjs`, `site.mjs`, `queue.mjs --check`, `site.mjs --check`, `verify.mjs --offline` each exit 0. Files: the three new/changed scripts and tests, README, AUTONOMY §5/§7/§15 only, `.gitignore`, `pages.yml`, `governance-ci.yml`, `site/index.html`, `site/data/health.json`. Off-scope noted, not done: the raw CI badge image stays in the header (the redesign's status pill, `site-design-v1`). **Reviewer gate dispatched.** Plan-data corrections applied on main in parallel (the digest's findings): `entry-79-b1-consult-items` no longer a ruling (79 RULED 2026-09-11; it follows the B1 half), `adr-031-lod-preregistration` carries entry 93, `release-artifacts-from-ci` evidence → PR #57; the site branch rebases and regenerates before its PR.
- 2026-09-14 — **site-bugs-first — reviewer gate 1 FAIL** (verbatim verdict line): "VERDICT: FAIL — 1. node id `entry-69-runtime-dependency` published as visible text (`scripts/plan/site.mjs:327` → `site/index.html:107`), with a bug-4 test that cannot reach the health strip (`scripts/plan/site.test.mjs:115,305`)." Everything else affirmed (the three API calls with per-fact errors; the token header-only and absent from payload and logs; the two dated groups; `checkSiteDrift` pinned to build facts absent; API strings escaped; the static shipped list; the chip and "unestimated"; workflows and pins unchanged; README/AUTONOMY accurate; the hook untouched; 122 tests, exit 0; the simulated Pages order showed the build-time group). Suggestions: a corrupt `build-health.json` must not print the "outside that build" sentence; a token-never-in-output test; `deepEqual`; `https://`-only hrefs from API data; README's "decoupled" wording. **Bounded closing attempt 1 (same worker):** all five applied (the waiting row renders the node's title with the id in `title=`; `readBuildHealth` reports "unreadable: <message>"; the token test; `externalLink` https-only; README) and the branch rebased onto c43bf27 with the generated files regenerated — `governance/site-bugs-first` a443fed (six commits over main); tests 127 · pass 127, runner exit 0; the three checks exit 0. Worker's off-scope finding, queued as hygiene: `scripts/plan/verify.test.mjs:125` matches the substring "pr" against a `mkdtemp` path (flaky once; pre-existing on main). **Reviewer gate 2 dispatched.**
- 2026-09-14 — **Engine half — closing attempt 1 done (worker, reported):** `polish/engine-lease-class` 1031a8f (fix(kernel): `build_viewport_query` returns `PredicateAdmitError`; the call site matches through `predicate_admit_error_of` at `kernel/src/skp.rs:511-518`, used at `:393` — `Filter(fe)` → `filter_error_of`, `ConnectionsExhausted{class,capacity}` → the existing `error_of` arm; the `From` fold deleted with a comment saying why it may not return; the false "today: none inside this workspace" comment deleted; the shell mock's fields `admission`/`4`) → f16fb3a (docs(adr): ADR-033's Decision no longer claims "no wire change", its gap paragraph closed, cites current; `pool.rs:58` corrected; the composition fact in `kernel/README.md` beside the `Stream` one, the pool arithmetic there brought to the current total; preregistration Amendment 5, post-result, says so first) → 6bd45ab (the custodian substituted the architect's verbatim verdict sentence for the worker's labelled paraphrase in Amendment 5). Suites: `cargo test -p spatial-engine` 260 passed, exit 0; `cargo test -p spatial-kernel` 212 passed, exit 0; clippy exit 0 (no new warning); `npm test` 966 passed, exit 0; no harness run. Mutations, both by name: the new kernel test `a_residual_admission_lease_exhaustion_maps_to_engine_connections_exhausted_never_filter_rejected_by_binder` failed under a re-routed arm ("a lease-capacity fact must never be reported as a binder rejection"); engine test (b) failed under an explicit `Filter(RejectedByBinder)` fold in the `map_err` arm (the literal "drop the arm" mutation did not compile — E0283 — recorded as done, Amendment 5 (e)). Owed and unchanged: `dataset.rs:447` (`duckdb_version`) still leases `Maintenance` (Amendment 4). **Re-gate dispatched: reviewer + architect (gate 2 — Rule 7 applies after this one).**
- 2026-09-14 — **site-bugs-first — reviewer gate 2 PASS** on a443fed (verbatim verdict line: "VERDICT: PASS"); the blocking finding closed (the waiting row renders the title, id in `title=` only; the sibling search repeated independently found no visible-text id left); the five suggestions confirmed applied; scope 14 files within the piece; `scripts/hooks/` and `docs/` untouched; exit codes 0 throughout (127 tests). Two non-blocking notes left for the redesign node (a shape check for a non-object build-time file; the token test's comment overstating its hostile body). The flaky `verify.test.mjs:125` assertion confirmed path-sensitive and pre-existing — a hygiene item. **PR #58 opened** (`https://github.com/christopherdonini/spatial-ide/pull/58`) for the human's click. Plan-data rule while it is open: no regeneration of `site/index.html` on main until it merges (the branch carries the new generator's output; a regeneration on main would conflict), so the two branch→PR evidence flips (this node and the engine half) wait for that merge.
- 2026-09-14 — **Engine half — architect gate 2 FAIL on two stale numbers, every first-gate finding closed** (verbatim verdict line): "VERDICT: FAIL — 1. `engine/README.md:263-264` declares `MAX_PHYSICAL_CONNECTIONS` **5** and "two bounded classes" against `pool.rs:132`/`:136-137`'s three classes and 9 (docs/01 principle 8; ADR-010 rule 6). 2. `kernel/README.md:126-127` declares "up to 5 resident in-memory DuckDB instances per open dataset" against its own `:70-75` (9) and ADR-033 `:25` ("5 → 9") (docs/01 principle 8)." and "ADR-033: not ready — its own text is internally consistent and Status is unchanged, but its Acceptance clause requires an affirming architect gate, and this one fails on the two declared-ceiling docs its Decision/Consequences point at. No ADR edit is owed; correcting `engine/README.md:263-264` and `kernel/README.md:126-127` clears it." Affirmed: the wire now carries `engine.connections_exhausted` with its typed fields (SKP-V0 item 8; ADR-021 item 8 untouched); the kernel holds no pool logic; test + mutation on both sides; ADR-033's Decision and gap paragraph consistent, cites resolve, Status Proposed; the README composition fact placed by the file's own pattern; Amendment 5 post-result and FIND′ not re-run within §6 Gates. **Closing attempt 2 — a docs sweep by the custodian (§B "formatting, citation, line-number nits"):** 2f55761 — the engine README's Capacity row (4 + 1 + 4 = 9 per dataset, ADR-033) and Classes row (three classes), the kernel README's remainder passage (up to 9 resident instances); grep finds no other "5 per dataset" / "two bounded" / "up to 5 resident". **Architect gate 3 dispatched (scoped to the two READMEs and a sweep for any other stale ceiling figure); reviewer gate 2 still running.** Rule 7: a third failure stops the piece and queues it for the human.
- 2026-09-14 — **site-bugs-first — a finding of the custodian's after gate 2:** PR #58's branch CI (run 34790896329) shows the smoke step working with the scoped token (`auth: "token"`; CI on main success; open PRs 1 — #57 at the time) but `"latest_release": null`, because `GET …/releases/latest` excludes pre-releases and **v0.1.0 is published as a pre-release** (2026-09-13T18:56:58Z; `gh release view` confirms `isPrerelease=true`) — the page would claim no release exists. Sent to the same worker: read the releases list (newest first, drafts skipped), record `prerelease`, render "(pre-release, <date>)"; tests and README. PR #58 carries a comment saying the correction is incoming and that the custodian will note when it has landed and its re-check has passed. CI note for the hygiene node: `actions/checkout@v4` and `actions/setup-node@v4` in `governance-ci.yml` print the "Node 20 is being deprecated" runner warning — the SHA-pin/upgrade sweep covers it.
- 2026-09-14 — **site-bugs-first — the pre-release correction landed and re-checked; PR #58 ready for the click.** fb61f77 (worker): the releases list endpoint (newest first, drafts skipped; empty/drafts-only/404 → null), `prerelease` recorded and rendered "(pre-release, <date>)", CLI summary line, five list-case tests + both strip forms, README clause; tests 132 · pass 132, exit 0; `site.mjs --check` and `verify.mjs --offline` exit 0. The branch's CI smoke log now prints `latest release: v0.1.0 (pre-release)`, `ci: success`, `open PRs: 2`, all with `auth: "token"` — the default scoped token reads all three facts; no permission change needed. Scoped re-check by the gate-2 reviewer (verbatim verdict line): "VERDICT: PASS" (nit noted: an absent `draft` field is treated as a draft — conservative). PR #58's body updated (evidence, the pre-release note, the two green runs 34791231806 / 34791234018) and a comment says the correction landed and passed. **Closed under delegation** (§B "CI, watcher, hygiene and tooling mechanics"): the whole piece; the click is the human's.
- 2026-09-14 — **Engine half — architect gate 3 FAIL on one spelled-out sibling** (verbatim verdict line): "VERDICT: FAIL — (1) `C:\dev\spatial-ide\.claude\worktrees\cut-hover-repick\engine\README.md:316` still declares the pre-ADR-033 remainder "up to **five** resident in-memory instances per dataset" while `pool.rs:136-137`, `engine\README.md:263` and `kernel\README.md:127` all say 9 (ADR-010 rule 6; docs/01 principle 8)." and "ADR-033: not ready — acceptance (`:29`) requires the architect gate to affirm the landed piece, and the engine README's own remainder passage still contradicts the ADR's `MAX_PHYSICAL_CONNECTIONS` 9. One-word edit at `engine\README.md:316` clears both." B1 and B2 closed; the sweep found no other stale ceiling figure (ADR-033's "5 → 9" and "declared two lease classes" at filing are historical, correct as written); the design affirmed in full since gate 2. My numeric grep missed the spelled-out word — the sibling search must include number words. Gate count on this piece: two failed closing attempts (the kernel arm; the two README numbers) → **AI_DEVELOPMENT Amendment 1 §A applies**: a root-cause note and ONE bounded closing commit, self-verified mechanically, once the reviewer gate 2 has also affirmed the design (still running).
- 2026-09-14 — **Engine half — reviewer gate 2 PASS** (verbatim verdict line: "VERDICT: PASS"; on 6bd45ab): the fold gone and the routing exact (`build_viewport_query` → `PredicateAdmitError`; `predicate_admit_error_of` at `skp.rs:511-518`, taken at `:393`; no new `skp.*` string; every `RejectedByBinder` occurrence justified); the kernel test exercises the real helper; `cargo test -p spatial-kernel` and `-p spatial-engine` all result lines ok, exit 0; `npm test` 966 passed, exit 0; the preregistration numstat `16 0` (append-only); both mutations recorded by name with observed messages, (e)'s explicit fold accepted as the same defect stated unambiguously; ADR-033's Status line unchanged, its Decision retracts "no wire change" and the gap paragraph reads CLOSED with both test names; the `pool.rs` header corrected; the README arithmetic 4+1+4 = 9; the mock fields; sibling search — no new `Maintenance` request-path admission beyond `dataset.rs:447` (owed, Amendment 4). Nits noted: a long unwrapped README line; ADR-033's Decision paragraph would read better with a sub-bullet on acceptance. **§A closing commit** b2c9aac (the custodian): `engine/README.md:316` "five" → "nine (`MAX_PHYSICAL_CONNECTIONS`, ADR-033)"; self-verified mechanically — a grep of both READMEs for 5/five/two near the pool words finds nothing; prose only; the commit message carries the root-cause note in short, the ledger in full: *Gate 1 failed on one semantic finding — the engine's `From<PredicateAdmitError> for FilterError` fold let a residual admission exhaustion reach the wire as `skp.filter_rejected_by_binder`; the kernel match arm was owed and Amendment 4 had misread §2.3 (ii) as "no wire change". Closing attempt 1 (worker) fixed it with the arm, the deleted fold, a kernel test and two mutations; both gates then affirmed the design. Gate 2 (architect) failed on two mechanical items — declared-ceiling numbers left at the pre-ADR-033 figures (`engine/README.md:263-264`: 5, "two classes"; `kernel/README.md:126-127`: 5); closing attempt 2 (the custodian's sweep, 2f55761) fixed those; gate 3 (architect) failed on one mechanical sibling the numeric grep missed — the spelled-out "five" in `engine/README.md:316` — and affirmed everything else with no other stale figure found. Class of the residue: mechanical, record-only, one word in prose against a constant; no behaviour, guarantee, authority, scope or tested property touched. Lesson: a sibling search for a number must include its word form.* **PR #59 opened** (`https://github.com/christopherdonini/spatial-ide/pull/59`) for the human's click; ADR-033 stays Proposed until the human's typed acceptance (the ruling: "accepted on my word after its gate") — raised in the next question set; the Status line changes only on that record.
- 2026-09-14 — **The human merged all three PRs** — #58 (024270d, 04:30:41Z), #59 (c3d258f, 04:30:54Z), #57 (a6646ca, 04:45:37Z); my push of the set-B/C record was rejected until rebased (d9a15f9). Main now carries the engine half (ADR-033 Proposed), the site fixes and the CI-artifacts workflows. Post-merge follow-ups: one `release-artifacts.yml` dispatch to confirm registration (after the harness run, machine-quiet rule); the first Pages run with build-time facts checked logged out.
- 2026-09-14 — **Question sets A–D asked and answered in one window** (four consecutive AskUserQuestion calls; every answer verbatim in `DECISIONS-PENDING.md`: 7563b98, d9a15f9, 5d41dad). Standing changes: **plan-data changes go via PR from now (A1)** — the custodian merges only when `docsOnly.mjs` and CI are green (§9), otherwise the click; the ledger and the verbatim ruling records keep their direct commits (the flush mechanism; the ruling's scope is plan data). ADR-033 accepted on the condition FIND′ green at #59's head (A4) — the human merged #59 before the run, so the Status line now changes on main in a commit quoting their words once FIND′ is recorded. The patch-bump precedent's scope confirmed (C4) and recorded in `PRECEDENTS.md` P-033.
- 2026-09-14 — **FIND′ on #59's head started** (the acceptance evidence for ADR-033): the shell's debug build pre-warmed into the main checkout's target dir in 6m46s (exit 0; the pre-build so the harness's 300 s attach window is met), then `npm run e2e:regression` in the `cut-hover-repick` worktree at b2c9aac with the same target dir, logged to the session scratchpad (`find-prime-b2c9aac.log`). Machine quiet: no cargo, no other app; the three concurrent delegations are docs-only and told to run no build or test. The app the harness leaves running is closed by PID afterwards (path verified).
- 2026-09-14 — **Delegated (docs-only, running):** (1) ADR-023's Decision text ported per D2 (worker; branch `docs/adr-023-decision`; PR for the click — ADR files are never docs-only-eligible); (2) the static-CRT licence note per C1 (general-purpose agent; read-only fetches with URL, date, hash; entry-51 discipline; written under `RELEASE-DRAFTS-0.1.0/decision-notes/`); (3) tonight's sitting prep pack per D1 (worker; `RELEASE-DRAFTS-0.1.0/sitting-2026-09-14-prep-pack.md`). **The plan PR** (A1) in preparation on `governance/plan-2026-09-14`: the three merged pieces done; the rulings applied (91, 75, 73 done; 92/ADR-029 ready; 69 folded into 90; 90 ready on the note; M13 re-tested at v0.1.1; the sitting dated; B1 no longer waiting on a ruling); five nodes appended (`adr-023-decision-text` in progress, `hover-confirming-marker` ready, `lod-route-b-divergence` ready, `geometry-types-beyond-polygons` placed after Brief A P3–P6 in the engine lane, `signpath-application-draft` after v0.1.1).
- 2026-09-14 — **FIND′ on #59's head: PASS** (`npm run e2e:regression` in the `cut-hover-repick` worktree at b2c9aac, 10:31:12Z → 10:36:29Z, harness exit 0, every step PASS: A1′ A3′ A4′ A7′ A8′ A9′ K6 K7 CURSOR′ FIND′ ABSENTCRS′). The instrument's own line, verbatim: **"[FIND'] PASS (14929ms): applied "id > 99900" via the real panel DOM, scan completed on its own; "Zoom to layer" fitted at zoom -1.9466138465216192 (1.67% non-bg); 3 zoom-out notch(es) left 0.10% non-bg with 0 covering-truncated line(s) and 0 unminted tile(s) of 0 distinct tile(s) ever refused; .residency-status settled to "Showing all 99 features in view"; the second "Zoom to layer" logged the same fit (zoom -1.9466138465216192) and left 1.67% non-bg"**. Stated plainly against the human's condition ("FIND′ recorded green — 63 of 63 tiles minted — on #59's head"): green by the instrument's own reinstated assertion; with the admission class in place no tile was ever refused, so the instrument reports zero unminted of zero refused rather than a minted-of-refused ratio — the "63 of 63" phrase describes the earlier failing run's denominator (34 unminted of 63 refused at 2a1fc3c), which no longer exists. The log is preserved at `frontends/shell/e2e/out/find-prime-b2c9aac-2026-09-14.log` (untracked evidence dir; archived by campaign). The app the harness left running was closed by PID 35888 (path verified `C:\dev\spatial-ide\target\debug\spatial-ide-shell.exe`) together with the harness's own dev-server children (the `tauri dev`/vite/esbuild processes whose command lines named the worktree); nothing else touched. **Next: ADR-033's Status line and the acceptance record, quoting the human's words, in a PR for their click** (their words: "the Status line changes in #59 quoting these words and I merge" — #59 merged first, so the change rides its own PR).
- 2026-09-14 — **v0.1.0's evidence archive prepared (A3; dry run, no upload):** the files of `frontends/shell/e2e/out/` dated at or before the tag (409 files; the one later file, today's FIND′ log, excluded by date so the archive carries only v0.1.0's own evidence) staged and zipped: `target/evidence/evidence-v0-1-0-2026-09-14.zip`, 81,993,512 bytes, SHA-256 `56660e9570c82a0de56eaf43a59d9818670dca86d8a774ded77dd5462de6186a`, manifest beside it. **The human's command:** `gh release upload v0.1.0 "C:\dev\spatial-ide\target\evidence\evidence-v0-1-0-2026-09-14.zip"` — then the citation line goes into `kernel/RESULTS.md` (§12): `Evidence: https://github.com/christopherdonini/spatial-ide/releases/download/v0.1.0/evidence-v0-1-0-2026-09-14.zip (SHA-256 56660e95…e6186a)`.
- 2026-09-14 — **PR #60 (the plan update) merged by the custodian under §9** (docsOnly.mjs: eligible; Governance CI green on the push and the PR run) — the first plan-data PR under ruling A1; 2bcb7fb. **PR #61 opened** (ADR-023's Decision text as ruled; 422b301) for the human's click. **`release-artifacts.yml` dispatched once from main** (run 34833992340) as the post-merge registration check.
- 2026-09-14 — **Sitting prep pack written (worker, reported):** `RELEASE-DRAFTS-0.1.0/sitting-2026-09-14-prep-pack.md` (128 lines; untracked drafts dir): 6 items / 9 runnable row-steps — K7 (66 (b)), L7+L8 (47), M14+M15 (85; 84/87), Part G / 86, the polish rows, the set-B rows (the 5/9/15 px row; the pan-feel row); B1's marker row deferred until the piece lands. **Three findings, acted on:** (1) `pickResolution.ts:45` still reads `SUB_PIXEL_PICK_REFUSAL_THRESHOLD_PX = 2` — entry 89's held half (the 9 px value, ruled B2) never landed; sent to the confirming-marker worker under the polish preregistration's own §4 (its tests §4.4) as a second commit set on the same branch. (2) No walkthrough rows exist for Part G / 86 (PR #53 added none) nor for 87/88/89 (the polish preregistration's Operator clause at `:63` promises one each; PR #55/#59 added none) — rule 11 breached by omission; a docs worker writes the rows with blank result logs (`docs/walkthrough-rows-86-89`, a docs-only PR the custodian merges under §9). (3) `renderer/bundle-viewer/dist` was dated 2026-09-09, older than PR #53 — rebuilt by the custodian so the entry-86 row runs against the anchored-zoom code. **Disk:** 7.3 GB free after the shell's debug build went into the main checkout's target dir (the regenerable cache the human's ruling gave to the drill). Sequencing decided: tonight's sitting launches the dev app with `CARGO_TARGET_DIR=C:/dev/spatial-ide/target` (the warm build; a default-path build would need the same space again and cannot fit); **the drill runs after the sitting**, once `target/debug` is deleted again to re-fund it (regenerable, per the ruling). The prep pack's launch section is updated with that variable. Off-scope from the pack, queued as hygiene: a line in `e2e/README.md` on the stale-overlay hazard.
- 2026-09-14 — **Static-CRT licence note done (delegated, reported) — and it STOPS entry 90 by the human's own condition:** `RELEASE-DRAFTS-0.1.0/decision-notes/static-crt-licence-note.md` (70 lines; 15 sources fetched, one unlocated — no standalone VS 2022 Build Tools EULA on Microsoft's license-terms index; 25 quotes re-verified by grep). (i) NOTICE set partly confirms (the CRT joins the set under ADR-030's own reopen condition; the "static `.lib` files on the Distributable List" premise is refuted — the list has no `.lib` entry); **(ii) the System Library reading is NOT confirmed** (the FSF's text on point blesses dynamic linking and says libraries qualify "only as long as they're not distributed with the program"; AGPL §1 carries no such sentence — a narrow, real contest); (iii) the Excluded License clause bites only on source distribution — confirms conditionally on (ii). The SDK EULA permits linker-produced `.lib` results (covers `libucrt.lib`); no VS-side grant found for `libcmt.lib`/`libvcruntime.lib`; the flow-down requirement unresolved against AGPL §10. Appended to entry 90; the node returns to a ruling in the next plan PR; the build setting does not land. Closed under delegation (§B wording/record; the stop itself is the human's condition executed, not a custodian decision).
- 2026-09-14 — **ADR-033 acceptance PR opened — #62 (worker, reported; the human's click — an ADR Status change):** `docs/adr-033-accepted` 473c64d: the file renamed off its `PROPOSED-` prefix; the Status line now "Accepted 2026-09-14 — on the human's word after the completed gate …, the FIND′ condition met at #59's head (b2c9aac …). Ruling: DECISIONS-PENDING.md, RULED 2026-09-14 question set A, A4 …" with the history kept; the Acceptance section records the ruling verbatim, the FIND′ line verbatim with timestamps, exit and log path, the wording note (zero of zero refused vs the "63 of 63" phrase), the gate history and the merge; `docs/README.md`'s ADR table row added; preregistration Amendment 6 appended (numstat 10 0). The worker wrote the README row by hand to the generator's contract because npm was out of its scope — **verified by the custodian with the check of record:** `node scripts/adrIndex.mjs --check` in the branch's `frontends/shell` → "adrIndex: PASS -- 31 ADRs, index in docs/README.md matches every Status line", exit 0. The five stale "PROPOSED ADR-033" mentions the worker flagged (`engine/src/pool.rs:117`, `predicate.rs:337`, `engine/tests/predicate_admission.rs:376/:402`, `kernel/README.md:86`) swept by the custodian on the same branch (comments and README prose only; no code change) — a second commit on #62. Left: `docs/README.md`'s hand-written conventions paragraph does not name ADR-033 (it names none of the recent ADRs individually — not a defect).
- 2026-09-14 — **Walkthrough rows for entries 86–89 written (worker, reported) — PR #63,** `docs/walkthrough-rows-86-89` 52434f2, one file (`frontends/shell/MANUAL-WALKTHROUGH.md`, +38): G11 (Part G, after G9 — entry 86, the zoom anchored at the pointer at a window that is not 1280×900, pass/fail per `ZOOM-ANCHOR-PREREGISTRATION.md` §3, the binding rule quoted from `:14`); M16 / M17 / M18 (Part M, after M15 — entry 87's settled sentence under a filter per §2.4; entry 88 marked runnable once `shell/hover-confirming-marker` lands, the expected state B1 verbatim; entry 89's cursor half runnable now, its threshold half runnable once the 9 px constant lands, the 5/9/15 px row B2 verbatim); blank result-log stubs in the Part G and Part M run sections; a placement note records that the polish preregistration's Operator clause names no Part (nothing to correct; the append-only files untouched). Verification: sixteen verbatim quotes re-matched mechanically (16/16). `docsOnly.mjs`: eligible; the product-ci-shell checks run on it (the file sits under `frontends/shell/`) — **merged by the custodian under §9 once green** (recorded below when it lands). Off-scope noted: L7/L8's result logs still hold the previous build's text with their queued re-run brackets (the sitting re-runs them); the zoom-anchor preregistration's `:47`/`:55` still call KNOWN-LIMITATIONS line 16 "owed" although the rewrite landed (de939d1) — a stale-cite nit for a sweep.
- 2026-09-14 — **PR #63 merged by the custodian under §9** (docsOnly eligible; every check green). The prep pack for tonight cites the new rows G11 and M16-M18.
- 2026-09-14 — **hover-confirming-marker built (worker, reported):** `shell/hover-confirming-marker` 941c072 — preregistration first (`frontends/shell/HOVER-CONFIRMING-MARKER-PREREGISTRATION.md`, 82 lines + Results), then the code: `PickConfirming{standing}` as a state the render cannot bypass (`pick.ts`, `pickResolution.ts`, `WorkingCanvas.tsx`, a new `HoverReadoutView.tsx`, `App.tsx`, `styles.css`); mid-gesture the standing id stays with a muted marker beside it (never concatenated into the id), further camera changes leave it standing, crossing below the threshold replaces it with the named refusal; at settle only a re-pick RESULT removes it (same id → bare id; different feature → that id; nothing → clear; below threshold → the refusal); the pre-committed disarm case: any settle that runs no re-pick (resize/DPR per B3 (2), off canvas, invalidated capture, nothing armed) and the two cancel sites clear the readout — "the marker never outlives a settle". `npm test` 67 files / 977 passed, exit 0; `npm run typecheck` exit 0; nine new tests, each with one mutation observed failing by name and reverted (recorded in the preregistration's Results). **The 9 px threshold (entry 89, B2) landed on the same branch under POLISH §4:** `SUB_PIXEL_PICK_REFUSAL_THRESHOLD_PX = 9`, the comment's rationale verbatim from B2 with the 2.27 px figure and both ADR-010:74 caveats beside it as a declared choice revisable only by a walkthrough felt verdict; §4.4's tests (the declared value; the sitting row's three sizes: ~5 px refused, ~9 and ~15 px answered; exactly at the threshold not below it) with mutations. `npm ci` was run in the worktree's `frontends/shell` and `renderer/bundle-viewer` (absent `node_modules`; nothing copied). Owed and sent back: the K6 (ii) harness contract (POLISH §3.3) — `regression.mjs`'s `hoverReadoutId` would read the marker text as a confirmed id; hooks `.hover-readout-confirming` / `…-marker` left for it. Then: reviewer gate + architect consult (ADR-010 rule 5), PR for the click before tonight's sitting if the clock allows.
- 2026-09-14 — **Disk survey and cleanup (hygiene, delegated class; regenerable caches only):** the survey found the space — `cut-geometric-protection/frontends/shell/src-tauri/target` 17 GB and `cut-hover-repick/…/target` 16 GB (both branches merged and pushed; the FIND′ build used the main checkout's target dir, so neither cache was in use), `release-0.1.0/…/target` 4.5 GB, and the main checkout's `target/debug` 28 GB (tonight's warm build for the sitting — kept until after it, then deleted to fund the drill, per the ruling that the debug cache is regenerable). Action: the three worktree caches deleted; every worktree whose branch is merged into origin/main AND pushed AND has no modified tracked file removed (`git worktree remove --force` clears only ignored build outputs); kept: `adr-023`, `adr-033`, `confirming-marker` (unmerged, in flight) and `cut-hover-repick` (its cache gone; the worktree itself small). Result recorded below when the background removal reports (free space before: 7.3 GB).
- 2026-09-14 — **Cleanup result:** three worktree caches deleted; sixteen merged, pushed worktrees removed (only ignored build outputs inside); `release-0.1.0` failed to delete ("Permission denied" — a locked file; its git registration is gone, the directory remains for a later retry); remaining worktrees: adr-023, adr-033, confirming-marker, cut-hover-repick. **Free disk 48 GB** (from 7.3 GB). The main checkout's `target/debug` (28 GB, the warm build) stays until after the sitting.
- 2026-09-14 — **hover-confirming-marker — architect consult PASS with two merge-order conditions** (verbatim verdict: "VERDICT: PASS"). Affirmed: the labelled state is a type the render cannot bypass (`PickConfirming` carries `standing`, no `id` field; `HoverReadoutView` the only renderer; the two `@ts-expect-error` make a bypass a tsc failure — ADR-010 rule 5 held as a type, not CSS); every settle path and both cancel sites walked — the marker outlives no settle; inside the ruling (no new gesture, no timing constant, one new state plus the three ruled outcomes; the disarm-clear the only reading of both halves of B1, pre-committed in M4/§6 before code); docs/01 "no numbers, no claim" and ADR-018 clean; the 9 px comment matches B2 clause for clause with both ADR-010:74 caveats and the 2.27 px figure verified verbatim; the preregistration is dated before the code with the ruling's two named tests plus the result and disarm cases, nine mutations one per new test. **Condition A (merge order):** the branch must not merge ahead of the K6 (ii) harness-contract change — `regression.mjs`'s `hoverReadoutId` would classify `id <n> · confirming…` as a confirmed id (docs/01 principle 8; POLISH §3.3) — the change is in flight (a worker; the structural hooks `.hover-readout-confirming` / `…-marker` already exist). **Condition B:** POLISH §4.4 (4) pre-commits "K6 re-run in full, its zoom targets re-derived" for the 2 → 9 px change; this branch ran no E2E, so entry 89 is not dischargeable until that run exists — an owed preregistered test, not a new gate. **Notes (record-only, §B — an append-only amendment owed):** (3) two removal paths the preregistration's invariant 4 does not enumerate — a real `onHover` cancelling the pending settle then emitting a fresh pick (honest, but "removed only by a re-pick result" reads wider than the code), and unmount cancelling with no M4 clear (visually unreachable, the readout slot lives inside `{admitted && …}`); (7) two placeholder-word residues outside the declared constant — the test literal `"confirming"` (goes vacuous, not false, on a re-word) and `hoverReadoutTraceLabel`'s `confirming id <n>` (declare it tracks the placeholder or derive it). **Plan:** after the K6 (ii) worker reports — the append-only amendment (notes 3, 7), the reviewer gate over the whole branch, then the full K6 re-run on the branch head (entry 89 §4.4 (4); reuses the warm `CARGO_TARGET_DIR` — the branch changes no Rust; a quiet machine), then the PR with the marker wording declared a placeholder pending tonight's felt verdict. The STATE is ruled; only the wording waits for the sitting.
- 2026-09-14 — **#61 (ADR-023 Decision, f3c9762) and #62 (ADR-033 accepted, e0b32a6) MERGED by the human.** ADR-023 Decision is on main (Proposed until B1 close, the two s2 amendments and four conditions recorded); **ADR-033 is Accepted on main** (the file at `docs/adr/ADR-033-...`, no PROPOSED prefix, the Status line quoting A4 and the FIND' line verbatim; the ADR index check passed). Plan follow-ups for the next plan PR (A1, via PR): `adr-023-decision-text` -> done {pr: 61}; `b1-engine-kernel-half`'s dep on it now satisfied; `polish-engine-lease-class` stays done (ADR-033 acceptance recorded on its own PR #62). **K6 (ii) harness contract landed (worker, 5b6f5dc):** `readHoverReadoutState(page)` in `regression.mjs` classifies clear/refusal/confirming(+marker)/confirmed by CLASS not wording; `hoverReadoutId` returns an id only for `confirmed`; K6 (ii) asserts the standing id appears mid-gesture only under a marker labelling that same id and is gone after the settle, the labelled state sighted at least once; A9'/K6 (iii)-(v)/K7 made explicit about accepted states (a marker standing after a settle fails by name). `node --check` exit 0; citationIntegrity 29 and residencyTrace 76 passed; no unit test covers the harness helpers; no harness run. The one sibling it flagged — `residency-harness.mjs`'s `/^id \d+/` read — sent back (the §14 sibling rule). Next on the marker branch: the sibling fix -> my append-only preregistration amendment (architect notes 3, 7) -> reviewer gate -> the full K6 re-run (entry 89 §4.4(4), quiet machine) -> PR, wording a placeholder pending tonight's verdict.
- 2026-09-14 — **hover-confirming-marker — reviewer gate PASS** (verbatim verdict: "VERDICT: PASS"; on bde2eb3). Affirmed: `PickConfirming { standing }` with no `id` field, `HoverReadoutView` the sole renderer (App delegates; the trace prefixes `confirming id`, never bare), the confirming branch emits `idLine` + a separate marker span in one return (never concatenated), the two `@ts-expect-error` load-bearing (typecheck exit 0); removal/disarm walked (re-pick or fresh hover replaces; `clearLabelledStateWithoutRepick` on no-re-pick; both cancel sites `clearStandingLabelledState`; mid-gesture keeps the labelled state above threshold, swaps to refusal below) — Amendment 1 (a)/(b) matches the code; `SUB_PIXEL_PICK_REFUSAL_THRESHOLD_PX = 9` with the declared-choice comment (2.27 px + both ADR-010:74 caveats, walkthrough-revisable, no perf claim), strict `<` so exactly-9 is not below; `npm test` 67 files / 977 passed exit 0, `npm run typecheck` exit 0; two mutations spot-checked failing by name; the harness readers classify by class (`readHoverReadoutState`/`readConfirmedHoverId`; `hoverReadoutId` returns an id only for `confirmed`); sibling search — no reader takes the marker text for a confirmed id (K7 and residency-harness both reject the marker classes before their `/^id/` test); no `.rs` in the diff, no new gesture/timing constant, both preregistrations append-only. Reviewer's note (correct): the piece's own preregistration is `HOVER-CONFIRMING-MARKER-PREREGISTRATION.md`; entry 89's §4.2/§4.4 live in the already-merged `POLISH-87-88-89-PREREGISTRATION.md` — no red line implicated. **Both gates now affirm.** Running: the full regression suite on the branch head (bde2eb3) with `CARGO_TARGET_DIR` at the warm build — it discharges entry 89's owed POLISH §4.4 (4) "K6 re-run in full" and exercises the new K6 (ii) contract in one pass; machine confirmed quiet first.
- 2026-09-14 — **hover-confirming-marker: K6 re-run GREEN, marker PR #64 opened.** `npm run e2e:regression` on the branch head bde2eb3, 11:21:03Z→11:26:33Z, exit 0, every step PASS, zero FAIL — discharges POLISH §4.4(4) (entry 89 2→9 px) and is the first live K6 (ii) exercise. Instrument, verbatim: the labelled "confirming" state sighted mid-gesture, "0 mid-gesture read(s) landed on a settle's own different id, 0 on the same id, and it never outlived a settle"; release edge cleared to `{"state":"clear",...}`. Log at `frontends/shell/e2e/out/k6-rerun-confirming-marker-2026-09-14.log` (branch, untracked). App + dev children closed by PID (spatial-ide-shell 36200, path verified; the branch's tauri dev/vite/esbuild). **PR #64** carries both gate PASSes and the run; the merge is the human's click; **the marker WORDING is a placeholder pending tonight's felt verdict** — the STATE is ruled and exercised. After the sitting: apply the wording verdict as a follow-up commit; the plan node `hover-confirming-marker` moves to done on the merge (via the next plan PR, A1). Entries 88, 89, 91 (b)/(c) and 75 (3) are now all discharged in code.
- 2026-09-14 — **Stop hook: adr-029 was ready; corrected without over-holding.** The Decision-text port (machine-light, RULED C2) dispatched to a worker (branch `docs/adr-029-decision`, opens its own PR); only ADR-029's G1 feasibility gate is the cargo-heavy part, split into its own node `adr-029-g1-feasibility` (blocked, after the sitting). **Plan PR #65 merged under §9** (docsOnly eligible; Governance CI green): `adr-023-decision-text` done {pr:61}; `hover-confirming-marker` in-progress {pr:64}; `adr-029-operation-lifecycle` in-progress {branch}; `entry-90-static-crt-v0-1-1` blocked/needs-ruling (returned to the human by their own stop condition — the licence note found the AGPL s1 System Library reading contested); `drill-clean-clone`, `lod-route-b-divergence` and the new `adr-029-g1-feasibility` all depend on `sitting-4-rows` now (machine/disk-sequenced after the sitting; one heavy run at a time). verify:plan PASS, **0 ready nodes**, 6 waiting-on-human, 10 blocked-on-deps. The lesson: "ready but machine-heavy" is expressed as a dependency on the sitting, not left falsely ready; the light half of a node is done now, the heavy half sequenced.
- 2026-09-14 — **ADR-029 Decision-text port done (worker) — PR #66 for the human's click.** `docs/adr-029-decision` 09cfc72: the drafted Decision ported (monotone source-side count; a control-plane poll as a sixth SKP command, no data-plane change; typed refusal states; non-authorising op id; counted-words-only shell; its own wire literal; the non-decisions), C2 folded explicitly (Q1 typed states `{ scanning | finished | unknown }`; Q2 the wire literal numbered by merge order, not fixed now; Q3 the liveness string sighted), the standing conditions and G1 carried (G1 cross-referenced to its own node `adr-029-g1-feasibility`); **Status stays Proposed**. Two coherence nits the port introduced, swept by the custodian (ca3aa1a, §B): the Status line's duplicated "binds nothing" dropped; the original `## Decision` OPEN blockquote marked "superseded 2026-09-14" pointing at the drafted section (kept as the record, per the immutable-ADR append discipline — this ADR is Proposed so its body is still editable). The plan node `adr-029-operation-lifecycle` is in-progress {branch: docs/adr-029-decision} (the branch head is now ca3aa1a; the evidence pointer is the branch, not the commit, so no plan edit needed). PR #66 is the human's click (an ADR body change); on its merge the node moves to done via the next plan PR.
- 2026-09-14 — **Rulings done meanwhile (the human, present): Brief A P3 released; entry 90 ruled.** Recorded verbatim (28aceeb). **Plan PR #67 merged under §9:** `briefa-p3-p6` P3 released to a branch under its gates, build machine-sequenced after the sitting (depends on sitting-4-rows), P6 acceptances the human's; `entry-90-static-crt-v0-1-1` and `entry-69-runtime-dependency` DONE (69a stands for v0.1.1 — dynamic declared; static does not land; 69c not taken; points 2-5 parked behind the contested System Library reading; the licence note the recorded state); `entry-77-code-signing` reconciled to D3 (no ruling owed; folded into `signpath-application-draft`); a new PROPOSED milestone `release-v0-1-1` (the human schedules it; 69a leaves it no forcing content) with `m13-bracketed-values` and `signpath-application-draft` re-pointed onto it. verify:plan PASS; **0 ready nodes**; waiting-on-human is now EXACTLY the three laptop items for tonight: sitting-4-rows, evidence-archive-v0-1-0 (upload), entry-78-repo-description (edit). Open PRs for the human: #64 (marker), #66 (ADR-029 text). No agent running. Windows busy-handle leftovers (git-unregistered, harmless): .claude/worktrees/release-0.1.0 and plan-3 dirs — retry delete when handles clear.
- 2026-09-14 — **Laptop items done (the human, present).** #64 (marker) and #66 (ADR-029 text) MERGED (82f061c, 333aeef). The human ran `gh repo edit` — description A (321 chars) + fourteen verified topics set (entry 78 done). The v0.1.0 evidence archive attached to the release by the human (`gh release upload v0.1.0`); it sits beside the installer (81,993,512 B). **Plan PR #68 merged under §9:** `hover-confirming-marker` done {pr:64}, `adr-029-operation-lifecycle` done {pr:66}, `entry-78-repo-description` done, `evidence-archive-v0-1-0` done (citation = RELEASE-0.1.md Amendment 20; RELEASE-DAY-CHECKLIST §2 boxes checked); new node `marker-wording-final` (blocked on the sitting) for the marker wording follow-up. verify:plan PASS; **0 ready nodes; waiting-on-human is now EXACTLY one item: the sitting.** After the sitting: apply the marker wording verdict; then the post-sitting heavy work (Brief A P3 build, the drill, the LOD divergence, ADR-029 G1). Windows busy-handle worktree leftovers (git-unregistered, harmless): release-0.1.0, plan-3, plan-4.
- 2026-09-14/15 — **The speed-work batch (governance lane, the human's third directive, Appendix A3): five pieces, reviewer-gated.** (1) **Telegram round mirror** — `questions-mirror.mjs` + `sendDocument`; reviewer PASS; **PR #69 MERGED**. (2) **Health strip** — both drives, median opened→done (day resolution, honest-labelled), gate first-pass rate from `state/gate-log.json`; reviewer PASS; **PR #70 MERGED**. (3) **CI caching + path filters** — already fully present in all four product workflows (worker verified; no-op, no branch). (4) **Pre-gate self-checks** — `verify:cites` (path:line tree-wide, two tiers, 0 gated after fixing one ADR-023 cite `protocol/SKP-V0.md`→`protocol/skp/SKP-V0.md`), `verify:test-claims`, `verify:mutation` (heuristic, ungated), wired into governance-ci; reviewer PASS; **PR #72** (the click). (5) **AUTONOMY policy** — proportionate gating (§21; declared threshold ≤150 non-generated lines / ≤8 files, the custodian's choice, the human's to adjust), the Telegram/pre-gate/D: sections, `docs/PREREGISTRATION-TEMPLATE.md`, the one-disk resolution (entries 38 + 49 F-12(d); "62" a confirmed mis-cite; the D:-copy hash cell records the human's dual-copy instruction). Reviewer gate 1 FAIL (two overclaims), fixed (cross-branch disclosure), gate 2 PASS; **PR #71** (the click — ratify the threshold). Incident, mine: a `;` let a git chain run after a python patch failed partway, committing a partial fix (ec06916) to the autonomy branch; finished in f5b235f. **`state/gate-log.json` seeded** with today's 15 gate records (first-pass ~62%) so the health metric has data; appended after each gate henceforth.
- 2026-09-15 — **D: migration verified CLEAN (the human: "the migration's rmdir was irreversible, so this is the check that closes it").** `target/` is a junction to `D:\spatial-ide	arget`. Every recorded hash matches, nothing missing: (1) **compat-corpus 21/21** files present, every SHA-256 in `target/fixtures/compat-corpus/MANIFEST.json` (files + retired + mutations) matches its bytes, 0 mismatch/0 missing; (2) **the 5 GB fixture `parcels-5gb.parquet`** verified on **both** physical copies against the recorded `5ae955c5fb7ee4d3f10436df271e19361d84f0845fbaa69dc60516f1b60c1788` — the D: canonical copy (`target/slice-evidence/scale-pass/`, via the junction) MATCH, and the C: backup (`C:\Users\Public\spatial-ide-fixtures\parcels-5gb.parquet`, 5,004,376,705 B) MATCH. The dual-copy DR story now holds across two physical disks (the FIXTURES.md cell on #71 to be filled with both results). No file was corrupted or lost by the migration. Nothing else ran until this closed, per the human.

- 2026-09-15 - **STOP-THE-LINE (main red on the product suite) diagnosed and fixed forward; health strip false-green fixed; flush hardened.** main went red on product-ci-shell.yml at 0510c77 - root cause the ADR-029 index row in docs/README.md drifted from the ADR's own (legitimately-edited, Proposed) Status line; the `verify:adr-index` guard ran ONLY inside product-ci-shell.yml's `npm run verify`, whose path filters do not watch docs/adr/** or docs/README.md, so the desync landed latently and 0510c77 (a frontends/shell docs file, a WATCHED path) re-triggered the suite and exposed it - the path-filter defect the human named. **#73** regenerates the index (1 line; full `npm run verify` green; reviewer PASS). **#74** (stacked on #73) adds a dedicated `.github/workflows/adr-index-ci.yml` running the check on docs/adr/** + docs/README.md + the generator, closing the class (reviewer PASS; visible-not-blocking until an admin adds it to branch protection). **Health strip false-green CONFIRMED** (it read only product-ci-rust.yml = green and reported ci:success while shell was red) and **FIXED (#76)**: buildHealth reads all three product-ci workflows worst-of, names the red one, one badge per workflow; live-verified `ci: failure [product-ci-shell.yml=failure]`; 110 plan tests; reviewer PASS. **Flush hardened (#75):** a canonical fail-loud writer `scripts/hooks/flush.mjs` + a field-name test pinned to the real block (would have caught the `(next)` vs `(next session)` typo that silently ate a flush), and the PreCompact freshness rule tightened to 10 minutes + `flushed_at` at/after the last ledger change ('hash equality alone is not freshness'); 51 hook tests; reviewer PASS. **Viewer dist rebuilt for Row 4;** the 100k bundle data on C: is intact (the sitting's 'not found' was the mid-robocopy window). **FIXTURES dual-copy cell filled on #71** (c766f8e): both 5 GB copies (D: canonical via the target/ junction, C: backup) verified MATCH 2026-09-15. Merge order: #73 first, then re-trigger product-ci-shell.yml on main (workflow_dispatch) to produce a fresh green run (a docs-only merge won't auto-rerun it); nothing else merges until main is green; #71 re-runs on the fixed main.

- 2026-09-15 - **The stop-the-line reds RESOLVED and the sitting CLOSED.** #73 (the ADR-index green-restorer) merged at 04:19 (4b7bdd6) - the ADR-029 index drift is fixed on main; the sitting-record commit 69e14f6 (touching frontends/shell/MANUAL-WALKTHROUGH.md) re-triggered product-ci-shell, which ran against the fixed index and went **SUCCESS** (04:45). So the shell red the human flagged is gone. The product-ci-rust red was a **flaky shared-fixture race** in engine/tests/predicate_admission.rs (six parallel #[test]s wrote and opened one fixed zoned.parquet - one truncated it while another read it, 'too small to be a Parquet file'); it surfaced on the #71 BRANCH PR run, not on main (main's last rust run is green). Diagnosed and fixed in **PR #77** (write the fixture once via OnceLock; reviewer PASS) - it was the only engine test with a fixed shared path. **The 2026-09-14/15 sitting is CLOSED:** Rows 1-6 all PASS (Row 4 zoom anchor 'fine'; Row 6 'now perfect' - the 'id 6430 . confirming...' wording accepted, the 9 px threshold STANDS by the felt verdict that alone can move it, the pan re-pick good). Its one defect is **entry 95** (pan anchoring: the grabbed point drifts slightly sideways; the felt re-verdict is tonight's sitting). Ledger + gate-log housekeeping: gate-log at 94aaead (+ the race-fix gate); the sitting recorded at 69e14f6. Human away till tonight; entry 95's fix to be built on the quiet machine before then, its re-verdict held for the sitting.

- 2026-09-15 - **Entry 95 (the pan 'slightly sideways') investigated; a distinct recenter-runaway fixed; the human's actual 'slight' surfaced honestly and left to their steer.** Architect consult -> preregistration (`frontends/shell/PAN-ANCHOR-PREREGISTRATION.md`, cause-before-fix, tolerance frozen off a known-correct reference) -> worker reproduce+measure. **Normal panning measured geometrically PINNED (0 excess drift; the DPR hypothesis refuted at the host DPR 1.0 where deck-logical == client == buffer, ratio 1.000).** The reproduce found instead **candidate 3**: the shell re-framed deck's uncontrolled camera MID-DRAG (`maybeRecenter`->`setProps`+`render`) while deck held the old pan-start, so threshold-crossing zoomed-out pans ran the grab point away ~1100-18160 buffer px (and could close the WebView2 page). **FIXED in PR #78** (defer the recenter to the settled gesture-end tick; origin still advances, rule-3 precision preserved; stays uncontrolled) + corrected viewportBbox.ts's device->CSS comment; standalone `e2e/pan-anchor.mjs`, mutation fails by name, verify green; **reviewer PASS + architect PASS** (ADR-010-blockable). I verified the commit discipline myself (pre-fix readings in 02b1cd8 touch no WorkingCanvas.tsx; the gate lands only in 84add57). **The human's 'slight' is NOT #78** -- its likeliest cause is **candidate 2** (deck anchors the pan at the first pointer-move, not the press; standard mjolnir behaviour, not headlessly reproducible), left UNBUILT and put to the human (options: pursue the pointerdown-anchor / land #78 and re-feel tonight / accept). Also opened this session: **PR #77** (the engine predicate_admission shared-fixture race, reviewer PASS -- the flaky product-ci-rust failure). Recorded in DECISIONS-PENDING entry 95 + gate-log (22 records). The felt re-verdict on Row 6(c) is the human's, tonight.

- 2026-09-15 - **Question round 1 ruled and applied; the sitting node closed; five post-sitting nodes ready; #71 unblocked.** The human offered rulings; the genuinely open ones were two (entries 92/93/94 were already ruled in sets C and 2, so not re-asked). Written to state/questions/round-1.md and mirrored to Telegram BEFORE the AskUserQuestion prompts, options in the same order. Ruled verbatim: entry 95 '2. Re-feel first (Recommended)' -- candidate 2 (deck anchoring the pan at the first pointer-move) held, nothing built, the human re-feels at tonight's sitting with #78 in; sitting-4-rows '1. Yes, close it' -> done (evidence 69e14f6, verdict cited to the RULED block). Closing it cascaded dependents: briefa-p3-p6, drill-clean-clone, lod-route-b-divergence, adr-029-g1-feasibility, marker-wording-final all aligned blocked->ready to match the derivation; queue/site regenerated; verify:plan PASS; waiting-on-human now empty. #71 (OPEN -- my prior flush wrongly said merged) was failing only because it was 8 commits behind main; origin/main merged into it (e532615) so its checks re-run on a green base. Two lessons recorded in the block: never pipe a gating command (a `| tail` masked verify's exit and let a broken plan commit locally, caught unpushed and amended); a felt-verdict close needs verdict:{by:human, cite} and cascades -- align + regen before verify.

- 2026-09-15 - **Brief A P3 scoped and dispatched; the ready set worked.** A read-only scoping pass found P3's definition is NOT in the preregistration (no phase table) but in the brief -- state/NEXT-CUT.md:103, tracked (the sighted digest lives in the untracked RELEASE-DRAFTS-0.1.0/); it enumerated the deliverables (engine identity/dataset/stream/error, kernel skp ticket->generation, protocol describe fields + SKP 0.3 with fixtures both sides, the shell live-ticket-set mirror, the three held P2 parts riding P3 per the P2 gates), the block-on-sight set (§12a A1-A7 + the ADR-016 draft's nine), what is NOT P3 (P4 corpus, P5 gate tests/E2E, P6 Part N/acceptances), and eight ambiguities. Routed: the §13 F escalation gate (file_row_number on the vendored duckdb) is the worker's STEP 1 and can stop the phase to the human; the post-check call site and the physical-vs-scan check placement are the architect's at the gate; the format-default describe/publish spelling is the human's sight at the PR. Worker dispatched (isolated worktree, opus, 180-min ceiling, CARGO_TARGET_DIR on the warm D: cache). Earlier this turn: #71 unblocked by merging origin/main in (all checks now green, MERGEABLE/CLEAN); #79 opened (marker wording final, comment-only); LOD divergence and ADR-029 G1 held for sequential dispatch after P3 -- one cargo build at a time.

- 2026-09-15 - **LOD divergence (C3 steps 3 + 1) dispatched in parallel with P3; both nodes marked in-progress (91daccc).** Read the C3 ruling verbatim (DECISIONS-PENDING.md:42) and the spike README before briefing: the divergence is route B's `geo::Simplify` (plain RDP) at tolerance 5.0 on polygons-100k, 5 invalid of 100,000, against route A's `ST_Simplify` 0 and route B's `SimplifyVwPreserve` 0. The worker's brief demands per-feature evidence (input validity too, `explain_invalidity` verbatim, the same 5 ids through the venv's offline-loaded spatial extension), the mechanism stated as a hypothesis (GEOS's ensure-valid post-fix vs geo's no-fix RDP) and verified only by read-only fetches recorded URL/date/sha256 -- unfetched = labelled inferred. Two ADR-021 facts gathered by me before dispatch, mechanically: the vendored `libduckdb-sys 1.10505.0` `[features]` table has NO `spatial` (`extensions-full = [json, parquet]`), and `duckdb_extensions()` in the pinned venv reports spatial `install_mode = 'REPOSITORY'` (fetched). Whether a custom static build counts as 'can be statically bundled' is the human's reading; I will recommend 'A out on principle' per the ruling's own consequence clause. #79 is green and CLEAN for the click. No sitting till tonight; the machine carries two builds (the spike crate has its own target dir, so no workspace lock contention).

- 2026-09-15 - **Round 2 ruled and applied; P3 built and in its gates; the autoload hardening preregistered before code.** LOD worker reported (branch `spike/lod-divergence-c3`): I re-fetched 7 of its 14 recorded sources -- all SHA-256 MATCH -- and checked every quoted line range (11 remote, 9 local cargo-registry) byte-for-byte before opening **PR #80**. The explanation: both RDP cores collapse the hole to the same 3-vertex chord; GEOS drops the degenerate ring (`transformLinearRing` -> nullptr, `GeometryTransformer.cpp:194-202`), `geo` refuses the cull at `POLYGON_INITIAL_MIN = 4` and emits a self-crossing ring; `SimplifyVwPreserve(5.0)` valid on all five. Round 2 (mirrored `state/questions/round-2.md`, RULED a6fd06a): **route A OUT on principle**; **autoload hardened by configuration, full two-agent gate, ADR-021 amendment appended.** Applied: `engine/EXTENSION-AUTOLOAD-PREREGISTRATION.md` (full form, d70e6f7; sibling search: pool.rs:377 + layout.rs:260 are the two product open sites; two venv probes recorded -- DuckDB's defaults repository-fetched httpfs on one https:// read, both-off fails closed with zero files) and node `engine-extension-autoload-off` READY; the ADR-031 route-B draft commissioned from the architect. P3 worker reported: step 1 PASSED (file_row_number on the vendored duckdb), 597 cargo tests / 984 vitest green, 46 files (+2209/-60); reviewer + architect gates dispatched (corrected to the three-dot range after a two-dot count misled me). #79 green and CLEAN for the click.

- 2026-09-16 - **Round 3 ruled and applied; P3 attempt 1 FAILED both gates (one consolidated fix round sent); the autoload piece built with P1 confirmed on the engine's own build; #79 + #80 merged by the human.** The ADR-031 route-B preregistration (architect-drafted, every cite verified by me, `engine/LOD-PREREGISTRATION.md` aa5dcdb) ACCEPTED with two class-5 amendments in the human's words (sidecar tiers in the app-local cache, never beside the source without the user's word; geographic-CRS tiers a dated follow-on) and the dependency ruling (geo/wkb/geo-traits + parquet promotion, subject to §8's gate steps; a wkb-writer test on the first tier). crs.source gains `format-rule` (SKP 0.3 value-domain widening) -- into P3's fix round. P3 gates: the reviewer's six blocking findings and the architect's F1/F2/F4 agree -- the tier's three module-boundary seams (producer→terminal, post-check→kernel, publish→shell) were written to interfaces that do not exist, and their tests encoded the imagined interface; the architect ruled the per-query pre-check cost shape CONFORMS (boundary 4 + R-D2) and routes any relaxation to the human. gate-log 24 records. Autoload: pre-fix T2 observed the latent runtime fetch on the engine's build (httpfs downloaded into the temp extension dir); post-fix fails closed at every lease class; ADR-021 amendment appended, additions only. Plan: three nodes done (pr 79, pr 80, aa5dcdb), the tier builder node added READY, autoload in-progress. A fixed misquote in the spike README (`[not]` inverted Cargo.toml's arrow-pin reason) landed on #80 before its merge.

- 2026-09-16 - **Autoload piece gated: code PASS on every axis, one sentence of the ADR amendment blocked and tightened; the LOD dependency gate step done read-only.** Reviewer PASS on 60d91d0 -- it re-applied all three mutations itself (T1/T2 fail by name when the two SETs go; T3 when the retained setting goes), reproduced the pre-fix fetch (httpfs 28,527,638 bytes into the temp extension dir, nothing under ~/.duckdb), ruled the file size in an evidence comment is not a performance claim, and found 10 unconfigured raw connections in engine/tests/ (a follow-on, not this piece). Architect FAIL attempt 1 scoped to the property sentence -- 'no engine connection can fetch or load an extension at runtime' over-claims (the settings govern implicit first-reference acquisition only; explicit INSTALL/LOAD and a later SET are outside them; cfg(test) connections are unconfigured); the human's click would have made it immutable. Applied by me (the worker was bound to placeholders): the architect's drafted sentence, Decision 3 quoted verbatim from ADR-021:64, stable site cites, T1+T2 for the per-class claim, §10 Amendments 1 (class 5) and 2 (class 2); 769507b, ADR diff still additions-only (31/0). Re-read pending. LOD gate step (5b88b31): no second Arrow major -- geo/wkb/geo-traits reach no arrow crate, the spike's Arrow 59 was its own direct pin; 49-crate added closure, all permissive (num_enum's 'BSD-3-Clause OR MIT OR Apache-2.0' the only non-allowlisted string, every branch permissive); evidence under spikes/lod-feasibility/dep-check/. P3 fix round still running.

- 2026-09-16 - **#81 opened: the engine's DuckDB extension autoload/autoinstall off by configuration, both gates PASS, the ADR-021 amendment appended for the human's click.** Two gate rounds on TEXT, none on code: the architect blocked the pre-committed property sentence as an over-claim (implicit acquisition only; cfg(test) connections unconfigured) and I adopted its drafted wording; the reviewer then caught that my edit had shifted the very cites I had moved onto `CONFIGURE_SQL`/`configure_connection` (verify-cites cannot see a cite that points at prose) -- corrected, verified against the file, recorded as prereg §10 Amendment 3 (class 3) because I had corrected Amendment 1 in place, which the architect rightly named a small append-only deviation. gate-log 29 records. LOD dependency gate step recorded (5b88b31): I1 does not fire, closure permissive. Two governance/engine nodes proposed from the gates' findings. P3's fix round still running; attempt 2 is the last.

- 2026-09-16 - **Self-inflicted stop-the-line: main's governance CI red since 2a939d3 on verify:cites and verify:test-claims, both tripped by my direct docs commits (the LOD preregistration's amendments).** Found via the P3 worker's report, confirmed by `gh run list` (four failing runs). verify:cites: the draft's `Cargo.toml:41-45` (meaning the repo root) resolves against `engine/Cargo.toml` from a doc in engine/, and my first 'fix' (`../Cargo.toml`) failed the same way because the scanner strips `../` -- and I pushed it without gating the commit on the check's exit code; the second fix (prose) is gated and green, with the same defect in the dep-check README fixed alongside; LOD prereg §10 Amendment 4 (class 3) records it. verify:test-claims: a preregistration committed BEFORE code (the template's rule) names tests that do not exist yet; the A3 check reads them as claims -- a design conflict the check must resolve, not the preregistration: fix in flight on governance/test-claims-planned (claims in a not-done node's gate file are planned/advisory, binding once the node is done; PLAN.yaml is the source of planned-ness), node self-check-false-readings promoted to in-progress under the stop-the-line rule. P3's fix round landed (3 commits, all six blocking findings + the crs.source ruling + corrections 7-14 claimed); attempt-2 gates dispatched. #81 open on its own merits.
