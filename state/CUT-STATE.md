# CUT-STATE — post-v0.1.0 (opened 2026-09-13 at the release cut's close)

## SESSION-CONTINUITY
flushed_at: 2026-09-13T21:14:15Z
tip: feaf966 (main, before this flush commit); governance/autonomy 74f9b34 (plan-seed + docs merged; tooling pending); polish/filter-and-hover aaa913f (+ the fix round in flight); viewer/zoom-anchor 2bac289 (+ the closing round in flight); fix/no-crs-reaim 54d8dc0 (reviewer gate in flight); spike/lod-feasibility 90ccd15 (PR #51 open)
position: the autonomous work system is nearly assembled — AUTONOMY.md §0–§20 + Appendices on governance/autonomy; PLAN.yaml seeded (56 nodes); Amendment 2 + PRECEDENTS.md + issue template + evidence archive merged; the tooling branch (parser, plan/queue/verify/site/health/docsOnly, the three hooks + Telegram + halt, settings.json, governance-ci.yml) still being built by its worker. Five code gates/rounds in flight (see tip line).
half-made judgments: PLAN.yaml-change PRs custodian-mergeable only with verify:plan green and no lane-priority / felt-verdict change — to be confirmed in the first question set; the PreCompact block reason may not reach the model on automatic compaction (undocumented) — the obligation and a proactive /autocompact window are the backstop; the patch-bump precedent's scope is the human's to confirm; the polish shell half lands with FIND′ recorded FAILING for the held engine cause (entry 91 (a)) — the human decides whether to merge it before the engine half; the "113 of 193" figure is per-attempt and inflated — the per-tile figure comes with the fix round.
intended sequencing: tooling lands → merge into governance/autonomy → run queue/verify/site on the real plan → reviewer gate on the whole governance PR → open the PR (the human's click: it carries code paths) → live Stop-hook dry run at my next natural stop → forced /compact dry run → first AskUserQuestion (lane priorities; the waiting-on-you list; the confirmations above; the disk reclaim question; v0.1.0 evidence attach) → report ≤ 20 lines. In parallel: polish fix round → re-checks → PR; viewer closing round → reviewer delta re-check → PR; re-aim reviewer → PR.
unreported findings: none beyond the ledger. Disk about 10 GB free; the main checkout's shell debug cache is 21.2 GB (the reclaim question).
in-flight gate states: polish — architect PASS with notes, reviewer FAIL (fix round running); viewer — architect PASS with notes (must-fix discharged), reviewer FAIL on c72b9a9 then re-check on 2bac289 running, closing round running; re-aim — reviewer running; #51 — the human's click; governance — reviewer not yet run.

Previous ledger: `state/cut-archive/CUT-STATE-2026-09-13-release-0.1.0.md` (the release cut, 2026-09-08 → 2026-09-13; v0.1.0 = b391e43 on release/0.1.0, RC2 = 13471a9; merged back to main at e9900a3).

## Open at the close (the human sequences; nothing scheduled by the custodian)
- Entry 87 — the filtered per-tile mints refused with `skp.filter_rejected_by_binder` and discarded silently; the fix (shell mitigation at least; kernel if the sub-cause holds) with the suspended FIND′ assertion of the 84/85 preregistration as its test; the KNOWN-LIMITATIONS line then retires.
- Entry 66 (b) — PR #45 (both batches gated; the fits latch inverted; drop-at-drain); the ADR-028 closing note on the human's word (Amendment 4's text with Amendment 5 (e)'s clause); the 13b line retires at its landing; its Part K row into the next sitting bundle.
- Entries 88/89 — the hover blink per notch (reopen 75 (3)?); the pick-resolution threshold and the cursor shape.
- Entry 86 — the bundle viewer's zoom drift; KNOWN-LIMITATIONS if reproducible.
- Brief A — P3 held for the human's word (entry 83's question); PR #46 holds entries 80/81; entries 73 (sight list), 81's follow-through, the P4 records ledgered.
- Entries 69 (MSVCP140), 75 (the re-pick switch), 77 (post-tag signing evaluation: SignPath OSS then Certum), 78 (the human runs the repo edit), 79 (B1's close), 82 (closed by the third attempt).
- M13's two bracketed values (the human's); the GitHub release URL for Amendment 19.

- Entry 90 — static CRT as the v0.1.1 build piece (the spike passed); the licence-posture question first.
- Backend side-tracks (2026-09-13, parallel to Brief A, no walkthroughs): the LOD feasibility spike (`spike/lod-feasibility`, headless, reported-only → ADR-031's preregistration); the ADR-029 operation-lifecycle decision draft for the human's sight; B1's engine/kernel half on a branch once ADR-023's Decision text is ruled (sent), SKP bump coordinated with Brief A (0.3 → 0.4).

## Ledger
- 2026-09-13 — **RELEASE VISIBLE**: https://github.com/christopherdonini/spatial-ide/releases/tag/v0.1.0 (pre-release, not draft; asset `Spatial.IDE_0.1.0_x64-setup.exe` 10,559,893 B). Verified logged out (curl, no token): page HTTP 200; the unauthenticated API lists the asset; the public download hashes to c3f48320… = RC2. The first `gh release create` had left a draft after a failed asset upload (why the API showed nothing). The human's lesson added to RELEASE-DAY-CHECKLIST §5 verbatim; Amendment 19 complete. The release cut is closed in full.
- 2026-09-13 — **THE POST-TAG ARC OPENED — the human's directive, verbatim:** "v0.1.0 done; opening the post-tag arc. Three tracks, parallel where independent: Land the held work: #45 (66(b), with the ADR-028 closing note and 13b's retirement) and #46 (entries 80/81) merge on my click; entry 87's fix + 88/89 (hover blink, refusal threshold/cursor) as one small "filter-and-hover polish" piece through gates, 86 only if it reproduces. These ride alongside, not ahead. Brief A to close — the big one starts here. Send me the P3 digest now (preregistration §2, corpus manifest, each ADR draft's Decision paragraph, phone-readable). On my ruling, P3–P6 run: the session-identity tier, the corpus table, Part N, the ADR acceptances queued. Then Brief B's B1 opens immediately on A's close — the recipe milestone's first stage. Post-release housekeeping, docs-only, no ruling needed: the asset filename with the dot in QUICKSTART and the release body; the entry-78 description/topics I'll apply myself; the SignPath evaluation (entry 77) as a note. Draft 1's amendment is in force; delegated closures reported, not queued. Report ≤20 lines." Applied: the P3 digest assembled and sent; #45 merged onto main by a worker (conflicts with 47/84/85 resolved; the ADR-028 note appended on this word; 13b removed; the suspended FIND′ assertion re-evaluated per the merge-back note) then reviewer-checked and un-drafted; #46 mergeability checked and un-drafted; the polish piece (87 + 88 + 89) preregistered via an architect consult, then a worker and gates; 86 reproduction attempted (bundle viewer only, no app); QUICKSTART and the release body corrected for the dot filename; the SignPath note researched. One app at a time: the #45 harness run first, the polish piece's after.
- 2026-09-13 — Post-tag arc dispatched. **Housekeeping done (closed under delegation, §B docs):** QUICKSTART/README note the release page's dotted filename (main); the release notes edited on the release page (`gh release edit`, docs-only per the directive); PR #46 taken out of draft (mergeability recomputing). **In flight:** the P3 digest (assembling from main + PR #46's branch); the #45 landing worker (merge origin/main into cut/geometric-protection, conflicts with 47/84/85 resolved keeping both; the ADR-028 note appended on the human's word; 13b retired; the suspended FIND′ record re-evaluated under the widened set; verify + one harness run — harness slot 1); the polish piece's architect consult (87 + 88 + 89 preregistration; the 87 fix's home decided under docs/01 7/8, ADR-004/014/010-6); the 86 reproduction (bundle viewer in a browser, no app); the SignPath note (entry 77). Brief A P3–P6 wait for the human's ruling on the digest; B1 opens on A's close.
- 2026-09-13 — **Backend side-tracks dispatched** (the human's directive verbatim: "Backend side-tracks, no walkthroughs, parallel to Brief A: (1) LOD feasibility spike — headless, reported-only … (2) ADR-029 + operation lifecycle: architect consult, then the decision draft for my sight … (3) B1 engine/kernel half on a branch once I rule ADR-023's Decision text — send it; SKP bump coordinated with Brief A. Static CRT: if the spike passed, queue it as a build piece for v0.1.1."): the LOD spike (tester agent; `spike/lod-feasibility` in a new worktree; `geo` NOT a product dependency — spike-crate only; DuckDB spatial via the corpus venv; disk ≥ 20 GB kept; outputs deleted after measuring); the ADR-029 consult (architect; the draft filed under post-tag/ for sight; no data-plane change; the non-authorising id never derived from the handle); ADR-023's Decision text SENT (the B1 doc with the 2026-09-11 rulings appended); **entry 90 queued** (static CRT for v0.1.1; the licence question). B1's half waits for the ADR-023 ruling; its bump is 0.4 after A's 0.3.
- 2026-09-13 — **Entry 86 REPRODUCED** (headless; playwright-core + serve-bundle on the human's bundle): the viewer's fixed 1280×900 backing store vs its CSS-stretched box — `e.offsetX/Y` (CSS px) fed to `unproject` measuring from the attribute-set half-extents; no resize/DPR handling; pan and hover picking share the mismatch; not cumulative. KNOWN-LIMITATIONS line 16 added on main (retires when the viewer piece lands); entry 86 carries the finding. **Viewer fix piece → architect consult for its preregistration** (one pixel space for pointer and view; the reproduction driver `scratchpad/zoomdrift.mjs` becomes its E2E at two window sizes; no app; ADR-017 viewer ceilings untouched).
- 2026-09-13 — Three agents stalled on the stream watchdog (infrastructure; no work lost): the #45 landing worker after its merge commit 64e684f (worktree clean; resumed from step 2), the LOD spike (worktree + `spikes/lod-feasibility/{rust,scripts}` created; one route-A 5 GB output at tolerance 1.0 left under `target/spikes/lod/` — resumed with the measure-then-delete instruction; 27 GB free), the SignPath note (nothing started; resumed). **Dispatched:** the polish worker (the unconditional parts — §2.2 discriminator, §2.3 (i)/(iii) log + bounded requeue, FIND′ reinstated, §4.3 cursor, tests re-derived from the constant; harness held behind #45's run); the viewer worker (`viewer/zoom-anchor`, browser only). **Filed:** the polish preregistration + PROPOSED ADR-033 (d011be7 on polish/filter-and-hover); the viewer preregistration (8c6be2e on viewer/zoom-anchor); the ADR-029 decision draft (`post-tag/architect-consult-adr-029-operation-lifecycle.md`) — SENT; **entries 91 and 92 queued.** P3 digest SENT (886 lines, 38 blocks verbatim). ADR-023 Decision SENT.
- 2026-09-13 — **The Claude Code process exited with five agents running (infrastructure; no committed work lost).** State verified on resumption: the #45 worktree holds the merge 64e684f + fe216df (the ADR-028 appended note, present at the ADR's line 520) + 6944622 (13b retired), unpushed (origin at c959f17); `polish/filter-and-hover` and `viewer/zoom-anchor` clean at their preregistration commits (nothing written); the LOD spike had edited the WORKSPACE `Cargo.toml` (forbidden) — reverted by the custodian (`git checkout -- Cargo.toml`); its route-A 5 GB tolerance-1.0 output (2,872,575,989 B, measured in its JSON) deleted; the 12 `chrome.exe` processes are Google Chrome under Program Files (not a worker's Playwright) — untouched; no cargo/node/python/app process found. Disk: 13.8 GB free (the main checkout's shell cargo cache is 29.8 GB; the two worktree caches 16.3 GB each — the geometric-protection cache was NOT deleted after all). **Re-dispatched:** the #45 landing (steps: ADR-028 note compared mechanically to Amendment 4 + 5 (e); 6944622's diff; verify; cargo build + ONE regression run with K6/K7/FIND′ verbatim; fast-forward push — it holds the harness), the polish worker (unconditional parts; harness held behind #45), the viewer worker (browser only), the LOD spike (tester; standalone spike crate for route B with spike-only `geo`; disk discipline ≥ 6 GB; measure-then-delete), the SignPath note (general-purpose; read-only; nothing applied for).
- 2026-09-13 — **#45 landing run (worker, reported):** ADR-028's appended note = Amendment 4's text + 5 (e)'s clause word-for-word, plus one attribution paragraph naming the human's directive (additive; recorded). 6944622 removes only the 13b block (5 lines; no renumbering). `npm run verify` exit 0 (954 vitest + 76 + 29). `cargo build` exit 0. Regression once through the harness (`npm run e2e:regression`): **K6 PASS, K7 PASS, FIND′ PASS with the suspended entry-84 assertion recording "PREMISE BROKEN — 83 of 158 covering tiles reached no terminal" (entry 87, as expected)**; 9 PASS, 1 INFO, **1 FAIL: B2'/B3' expected `engine.crs_undeclared`, got `engine.format_default_contradicted`.** App PID 27912 (the worktree's debug exe) closed by PID. Pushed fast-forward c959f17..6944622. **B2'/B3' classified as main's own drift, not 66 (b)'s:** `git diff main cut/geometric-protection -- engine kernel protocol renderer` is empty; the fixture `no-crs-refused.parquet` is written with `CrsMode::AbsentKey` (`kernel/tests/manual_walkthrough_fixtures.rs:129-139`, its doc comment still stating the pre-Brief-A refusal), and since Brief A's P1/P2 (#44, on main) an absent key is the format default (`engine/src/fixture.rs:98-101`) contradicted by the metre-domain bbox → `FormatDefaultContradicted` (`engine/src/dataset.rs:1006`; `engine/tests/slice.rs:197` asserts it; ADMISSION-PREREGISTRATION §4 rows F-2/F-3). The E2E step, the walkthrough's B2/B3 rows and the generator's comment were never re-aimed to the ruled contract (#46 does not touch them). Same class as entries 80/81 → a delegated re-aim on its own small branch, reported (§B: test re-aims of ruled contracts); no product behaviour changes.
- 2026-09-13 — **SignPath note (entry 77) closed under delegation** (§B docs): `RELEASE-DRAFTS-0.1.0/decision-notes/signpath-evaluation.md`, 92 lines, 9 sources (three saved SignPath pages, signpath.org/about, /terms, /projects, docs.signpath.io, two Certum pages), read-only fetches on the custodian's authority, nothing applied for; the pointer appended to entry 77 (docs commit on main). Temp file deleted.
- 2026-09-13 — **#46 MERGED by the human** (7e47432, 19:47Z — entries 80/81 on main). **#45 READY FOR REVIEW** (un-drafted; the hold sentence replaced; the post-tag landing section appended to its body; `mergeable=MERGEABLE`; the branch does not carry #46's engine/kernel commits — 66 (b) touches nothing under `engine/`, `kernel/`, `protocol/`, `renderer/`, verified empty against current main). The click is the human's. Entry 77's note pointer committed on main (36f7f09, after a rebase of the one docs commit onto the #46 merge). **Re-aim worker dispatched (delegated, §B):** `fix/no-crs-reaim` off origin/main in the geometric-protection worktree — `no-crs-refused.parquet` becomes `CrsMode::ExplicitNull` (F-3 keeps B2/B3's assertion-prompt path), a new `absent-crs-contradicted.parquet` (F-2) with its own E2E step asserting the code and message only (the assertion-form question left to Brief A's shell parts), the walkthrough's B2/B3 rows and the generator's comment corrected; kernel builds via `CARGO_TARGET_DIR` on the main checkout's cache (disk). **Harness queue:** the polish worker first, then the re-aim worker; both hold until messaged.
- 2026-09-13 — **Polish piece, unconditional parts built (worker, reported; harness now released to it):** `polish/filter-and-hover` dab0457 (fix) + 61d4faf (test), pushed. §2.3 (i) the refusal log line (`tile-stream-mint-refused`, code from `SkpCallError`, `unknown` for non-SKP throws) and both epoch-abandon branches logged; §2.3 (iii) the bounded requeue (retryable: `engine.connections_exhausted` always; `skp.filter_rejected_by_binder` only when `detail` carries the lease substring minted at `predicate.rs:120`; one retry per tile per drain; only while in the current cover); §2.4 (2) six unit cases (the `../skp/client` mock now spreads the original so `SkpCallError` survives `instanceof`); §2.4 (4) FIND′'s suspension removed — the hard assertion is back; §4.3 `getCursor` → `cursorForPointerState` (crosshair / grabbing), asserted by a new `CURSOR'` E2E step (the unit seam cannot reach a real Deck canvas); §4.4 (1) `pickResolution.test.ts` re-derived from the constant (value still 2). `npm run verify` green (66 files / 942 tests). Mutation: retryable set forced empty → 3 tests fail by name, restored. Held: §2.3 (ii), §3, §4.2, both preregistration files. Gap sent back: §4.4 (2)'s `cursorForPointerState` unit cases (pre-committed) — to be added before the run. Expected on its run: B2'/B3' FAIL (main's drift; the re-aim branch).
- 2026-09-13 — **THE AUTONOMOUS WORK SYSTEM directive received** (the human; verbatim in `AUTONOMY.md` Appendix A — fifteen items + the seeded queue order + the first AskUserQuestion). Hook contracts verified against Claude Code 2.1.270's own documentation (Appendix B; the reference and guide pages saved locally at fetch time): Stop hooks receive `stop_hook_active`/`background_tasks`/`session_crons` and block with `{"decision":"block","reason":…}` under an 8-consecutive cap; PreCompact receives `trigger`/`custom_instructions`, CAN block compaction (proactive auto → skipped; context-limit recovery → the request fails) and cannot inject text; SessionStart `compact` re-injects stdout. `AUTONOMY.md` written and committed on `governance/autonomy` (56603ef, pushed). **Dispatched:** the plan seed (`governance/plan-seed`), the tooling (`governance/tooling`: yamlSubset parser, plan/queue/verify/site/health/docsOnly, the three hooks + tests, `.claude/settings.json`, `governance-ci.yml`), the docs (`governance/docs`: AI_DEVELOPMENT Amendment 2, PRECEDENTS.md, checklist §4 item 11, `scripts/evidence/archive.mjs`). **`state/CUT-STATE.md` becomes tracked from this commit** (AUTONOMY §7: the flush must be pushable); the SESSION-CONTINUITY block opened at its top.
- 2026-09-13 — **#45 MERGED by the human** (2687603 on main): entry 66 (b) landed — geometric protection at every zoom without enumeration, both batches; the ADR-028 closing note appended (on the human's word; the 2026-09-09 exception closed on both paths); KNOWN-LIMITATIONS 13b retired. Queued per rule 11: the Part K operator row. The seed worker told (`land-45` done, evidence PR 45).
- 2026-09-13 — **Polish piece, run 1 (worker, reported): CONFOUNDED BY LOAD** — it coincided with the spike's DuckDB 5 GB simplification and a kernel build. Discriminator (§2.2): 378 `tile-stream-mint-refused` lines, ALL `skp.filter_rejected_by_binder` with `detail` naming the lease as minted at `predicate.rs:120` ("…this dataset's maintenance connection capacity (1) is fully leased…"), zero real binder refusals; 257 distinct tiles, 121 refused exactly twice (one requeue, bound held), 136 once; all during FIND′, none during K6/K7/CURSOR'. **The hypothesis of §2.1 is confirmed by the instrument; the engine half (ii) stays on the human's word (entry 91 (a)).** K6 PASS; K7 timed out at 240 s and everything after it timed out (CURSOR', B2'/B3' as a timeout rather than the expected code mismatch, C2'/C3'); FIND′ hard assertion FAIL ("the filtered scan never left the in-flight family … livenessGone:true, cancelGone:false"). App PID 33932 closed. `cursorForPointerState` unit cases added (8d9a72e). **Re-run ordered on the quiet machine** (the spike finished; no build running). Mechanics lesson sent to the docs worker: a harness run needs a quiet machine, not only a single app.
- 2026-09-13 — **LOD feasibility spike CONCLUDED (tester, reported):** 90ccd15 on `spike/lod-feasibility`, pushed; nothing outside `spikes/lod-feasibility/`; workspace `Cargo.toml`/`Cargo.lock` unchanged. Route A (DuckDB spatial via the corpus venv): polygons-100k at 0.1/1.0/5.0 × preserve/simple and parcels-5gb at 0.1 preserve, 1.0 preserve/simple, 5.0 preserve/simple — every run identity-preserving (id round-trip), `invalid_after` 0; cancel via `interrupt()` returns in about a second, partial file left and deleted. Route B (spike-only Rust crate: `geo` 0.33.1, `wkb` 0.9.2, `geo-traits` 0.3.0, `parquet` 59.3.0, `arrow` 59.3.0 — NOT added to the product): builds; 100k vertex counts match route A exactly; RDP at 5.0 yielded 5 invalid outputs where DuckDB's yielded 0 (unexplained divergence, recorded); 5 GB streamed with zero disk write; cooperative cancel per row group with about ten seconds' detection latency. Confounds recorded honestly: route B single-threaded vs DuckDB multi-threaded; VW vs RDP families. **Engine fact:** DuckDB's `spatial` extension is not loaded by the engine (`engine/Cargo.toml:32`). All figures are spike measurements, not docs/08 claims. Every 5 GB output deleted after measurement; 10.25 GB free at its end. Its commit's `Co-Authored-By` names Claude Sonnet 5 (the tester agent's own model) — the human's identity rule holds (Signed-off-by is the configured user).
- 2026-09-13 — **Viewer piece (entry 86) built (worker, reported):** `viewer/zoom-anchor` 9e69f59 (E2E first, pre-fix readings: large window 110 px off, small 182 px off, forced 1280×900 exact) → 35763d1, e0558f7 (tolerance and DPR revisions AFTER results — a §3 deviation; Amendment 1 ordered to record it) → 95309d7 (the fix: `toStore` with the measured ratio; store sized to the client box × DPR on load and ResizeObserver; `zoomAt`/`panBy`/`resizeStore` pure; viewer-local `MAX_BACKING_STORE_DIM`/`PIXELS`; `ceilings.json` byte-identical) → 9c1064c (five unit tests; E2E wired as `test:e2e`, not in `verify` — CI has no playwright-core and no bundle; the `run-acceptance.mjs` precedent). Mutation: the wheel-site conversion reverted → all three E2E cases fail by name. `npm run verify` exit 0. Line-16 replacement drafted for the human's sight (appended to entry 86). Reviewer + architect gates to be routed after the amendment; the Part G row queued (sitting).
- 2026-09-13 — **Re-aim piece (worker, reported):** `fix/no-crs-reaim` 46dabb8 + 5549d52 (unpushed): `no-crs-refused.parquet` → `CrsMode::ExplicitNull` (F-3); `absent-crs-contradicted.parquet` (F-2, bbox in metres) with the E2E step `ABSENTCRS'` asserting the code and the message the engine mints (obtained from the real file by a throwaway probe, deleted); the walkthrough's B2/B3 facts corrected, no operator row added. Sibling found by the worker (§14's sibling search, before the rule landed): `bothneeded-refused.parquet` (AbsentKey, metres) and `admission-remediation.mjs`'s `stepBothNeeded` — folded into the piece (ExplicitNull, same treatment). A stray hand-typed Signed-off-by with the wrong email on the unpushed 46dabb8 — amended before push (unpushed, not a history rewrite). Harness queue: polish re-run (running) → re-aim (regression + admission-remediation).
- 2026-09-13 — **PR #51 opened** (`spike/lod-feasibility` → main; the human's click; spikes/ only). **Entry 93 queued** (the spike's two dependency proposals and three route questions), **entry 86's line-16 replacement filed for sight** — f6ea224 on main. In flight: the viewer's Amendment 1 + clamp test; the polish re-run on the quiet machine; the re-aim's `bothneeded` sibling; the three governance branches.
- 2026-09-13 — **SECOND DIRECTIVE received (verbatim, as it arrived — with a duplicated numbering; quoted in `AUTONOMY.md` Appendix A):** items 16 Telegram alerts via a Notification hook (and the Stop hook when blocking on the human), one message per blocking event, bot token in an env var only; 17 the ledger tracked under `state/` (CUT-STATE.md, NEXT-CUT.md, the archive), committed at every flush and report — the checkpoint is the commit; 18 the halt switch `state/CUSTODIAN-HALT` honoured by the Stop hook; 19 an issue template for the public repo (CRS situation, key columns, writer) — bug reports feed the corpus; 20 the human's own: branch protection on main + a v* tag ruleset (required CI + DCO, no force-push, no deletion), secret scanning with push protection, Dependabot alerts, a patch-bump precedent for the custodian; off-repo, theirs: an external drive and a monthly disk image. **Done now:** `state/CUT-STATE.md` (a40ccfe), `state/NEXT-CUT.md` and `state/cut-archive/` now TRACKED — the archive had been gitignored all along (never in the repository; the `.gitignore` rule lifted); `.cut-archive/README.md` left as the pointer because accepted ADRs cite the old paths and are immutable; references in AI_DEVELOPMENT.md and DECISIONS-PENDING.md updated. The tooling and docs workers extended with items 16, 18, 19 and 20's precedent; AUTONOMY.md §16–§20 on the integration branch.
- 2026-09-13 — **Polish piece, run 2 on the quiet machine (worker, reported):** K6 PASS, K7 PASS (15/15 notches), CURSOR' PASS (crosshair/grabbing observed), B2'/B3' PASS (the re-aimed fixture in the shared directory), C2'/C3' PASS; **FIND′ FAIL by its reinstated hard assertion: "113 unminted tile query(ies) of 193"** — 117 refusal lines, all the lease detail, none outside FIND′; repeats above two per tile are per-notch candidacies, not a spin. The shell half alone cannot discharge FIND′: the cause is the Maintenance lease collision and its fix is the engine half (ii), the human's (entry 91 (a)). Amendment 1 recorded on `polish/filter-and-hover` (aaa913f, post-result, FIND′ NOT re-suspended). Gates on the shell half dispatched (reviewer + architect); the PR then goes to the human with FIND′'s state stated plainly. Run 1's timeouts explained by load (quiet-machine rule sent to the docs worker).
- 2026-09-13 — **PLAN.yaml seeded** (`governance/plan-seed` 62be84f; 56 nodes: 15 done with verified evidence, 5 in progress, 17 blocked on the human, 5 proposed, 14 unscheduled; nulls listed, nothing guessed; phase `prototype` applied to process nodes as a declared choice). **Viewer gates dispatched** (reviewer + architect on c72b9a9). **The harness handed to the re-aim worker** (regression + admission-remediation). AUTONOMY.md §16–§20 + Appendix A2 committed on `governance/autonomy` (712e641, with origin/main merged in). Disk: 10.2 GB free.
- 2026-09-13 — **Viewer gates on c72b9a9: reviewer FAIL (B1 the per-axis clamp does not preserve aspect though three sites declare it does — at the ceiling a square extent paints non-square; B2 the early return at `main.ts:181` leaves the ratio stale so a resize becomes a zoom); architect PASS-with-notes, the same clamp declaration its one must-fix (ADR-010 rule 6: a declared behaviour must be true); §2's core rule, the fix, the mutation evidence and the ADR-017 contract all clear; the ceiling's viewer-local placement ruled constitutionally right (ADR-025 governs reader ceilings the preflight predicts against; a pixel ceiling in `ceilings.json` would force a kernel change).** Also: test 4 tautological; `MAX_BACKING_STORE_DIM` without a reason clause; the clamp test binds a replica; Amendment 1's post-fix row mislabelled; the line-16 draft must replace the trailing HTML comment too; resident-bytes accounting vs the store's bytes to be declared. Fix round dispatched to the worker (one shared clamp factor; ratio measured before the early return; a pure `clampStoreSize`; Amendment 2). First failed gate — Amendment 1 §A not yet in play.
- 2026-09-13 — **Governance docs landed (worker, reported):** `governance/docs` 57354f2 + d962e38 — AI_DEVELOPMENT.md Amendment 2 §A–§N (flush obligation, docs-only auto-merge, 2× budget, v0.1.1 CI artifacts, evidence archive, public-repo rule, gate defaults, generations/health/drill, what does not change, the quiet-machine harness rule, halt switch, Telegram, the `state/` ledger, the human's repository-settings checklist) + dated pointer bullets in the mechanics sections; PRECEDENTS.md with 33 entries (the three least sure to generalise flagged inside: P-022 Rule-7 accounting, P-020 signing scope, P-021 re-aim wording); RELEASE-DAY-CHECKLIST §4's v0.1.1 step (artifact name bracketed); `scripts/evidence/archive.mjs` (STORE-only zip in Node's standard library, manifest with SHA-256s, upload gated; dry run on the real `e2e/out`: 409 files, 81,989,422 B; round-tripped through `Expand-Archive`); `.github/ISSUE_TEMPLATE/bug-report.yml` + `config.yml`. **Finding (hygiene, delegated):** `residency-harness.mjs`'s `REGEN_FILTER_ZONED` names a generator that does not exist (`generate_the_filter_zoned_fixture`; the real one is `generate_the_filter_fixture`) — folded into the re-aim branch. `kernel/FIXTURES.md` complete for its scope. **Merged into `governance/autonomy`:** plan-seed (b860284) and docs (74f9b34), clean; pushed. Remaining: the tooling branch, then the tooling run on the real plan, the reviewer gate on the whole PR, the live dry runs, the first question set.
- 2026-09-13 — **Polish shell half — architect gate PASS with notes** (mergeable as a unit; FIND′'s discharge the human's): pre-merge items — (1) the `dataPlaneAttach` refusal path in `beginIssue` is unguarded and unlogged (pre-existing; §5 item 1 "anywhere in mintAndStart" not literally met — fix the same way or narrow the claim and queue the residual); (2) the bound is per candidacy, tighter than the declared "per drain" — wording; (3) the requeue pushes without a `MAX_QUEUED_TILES` check — add the guard or declare the overshoot; (4) the lease-substring match is a boundary loan, acceptable only as declared AND temporary — a block-on-sight to remove it when ADR-033's engine half lands (amendment + a REMOVE-WHEN line); (5) an undeclared operator-visible consequence: a requeued tile is outstanding work, so the status may read the paused/stalled suffix where the old drop settled partial — declare it in §2.5 and the PR text; (9) the PR text must quote FIND′'s actual failing assertion (the settled-partial sentence), name the held cause, say this build reports and retries but does not fix entry 87, and note the per-attempt counts are inflated by the retry. Reviewer gate pending; one batched fix round after it.
- 2026-09-13 — **Re-aim piece — harness runs (worker, reported):** regression all PASS (B2'/B3' `engine.crs_undeclared` with the assertion form; ABSENTCRS' `engine.format_default_contradicted`, the form's presence recorded as INFO, not asserted; FIND′ PASS on that branch with its 84 assertion suspended as on main); admission-remediation `BOTHNEEDED'` PASS; **`AXISTRAP'` FAIL — a stale hardcoded message against Brief A P1's `error.rs` wording, the same drift class — folded into the branch (one more run).** The `REGEN_FILTER_ZONED` name fixed (e723430). Pushed: 4ab6e99, 68ac675, cddb54e, e723430. Apps closed by PID each time.
- 2026-09-13 — **Viewer fix round landed (worker, reported):** `viewer/zoom-anchor` 774da00 (B1: `clampStoreSize` with one shared factor, both axes, aspect kept — the reviewer's counterexample now 3462×2423; B2: the ratio measured before the early return; one `lastRatio`) → fef5984 (reason clause on `MAX_BACKING_STORE_DIM`; the resident-bytes vs store-bytes declaration; "exact" reworded as the arithmetic property; the ceiling test binds the shipped function; test 4 rebuilt on the recording context; `ZOOM_ANCHOR_BUNDLE_DIR` override) → 2bac289 (Amendment 2, post-gates, with a fresh three-configuration table). `npm run verify` 67/67; E2E 3/3; mutation fails by name. Re-checks dispatched to the same reviewer and architect.
- 2026-09-13 — **Re-aim branch complete (worker, reported):** `AXISTRAP'` re-aimed to `error.rs:238`'s Display text verbatim, citing 92d87f1 (Brief A P1, ADR-032, ADMISSION-PREREGISTRATION §3 row 3); admission-remediation all PASS (eleven steps), exit 0; app closed by PID. `fix/no-crs-reaim` at 54d8dc0, pushed (five commits: 4ab6e99, 68ac675, cddb54e, e723430, 54d8dc0). Reviewer gate dispatched; the PR opens on PASS (delegated closure, reported).
- 2026-09-13 — **Viewer architect re-check at 2bac289: PASS with notes — the must-fix discharged** (one shared factor; the declaration true; the ceiling test binds the shipped function; the resident-bytes distinction declared). Three new non-blocking notes sent as one closing round: the pixel-ceiling regime still lets a uniform-aspect resize act as a zoom (correct `scale` before the early return + a unit case); the reachability sentence in `MAX_BACKING_STORE_DIM`'s reason clause is wrong by arithmetic (delete it); Amendment 1's tolerance basis superseded by Amendment 2's run (Amendment 3 records the corrected basis; a stray backtick fixed). Reviewer re-check on 2bac289 still running; a delta re-check follows the closing round.
- 2026-09-13 — **Polish shell half — reviewer gate FAIL (first failed gate; §A not in play):** (1) the branch is behind main in five of its eight files and conflicts in `tileViewportStreamManager.ts` + test; post-merge its windowed `currentCoveringKeys` would refuse retries outside the 256×256 window — must yield to 66 (b)'s `latestMembership`; (2) §2.4 (4) violated — the FIND′ entry-84 hard assertion was DELETED, not reinstated, the in-file comment saying the opposite; and a verbatim restore would now be wrong because the requeue re-fires the trace so "unminted = queries − streams" counts retried tiles — the "113 of 193" handed to the human is per-attempt and inflated. Also: `MAX_QUEUED_TILES` overshoot; `dataPlaneAttach` outside the try/catch (a tile stuck `issuing` forever); the substring coupling fragile; dead `records`. Everything else PASS (counts verified mechanically from the session logs; mutation re-run; verify exit 0). **One batched fix round dispatched** (merge main + `latestMembership`; a retry-aware per-tile FIND′ assertion, hard; the room check; `dataPlaneAttach` handled; the REMOVE-WHEN line + block-on-sight; wording; Amendment 2 with both numbers; one regression run).
- 2026-09-13 — **Viewer reviewer re-check at 2bac289: FAIL — B1 verified fixed (three counterexamples), test 4 and the ceiling test PASS, gates PASS; but the B2 remedy regressed the ordinary resize path:** `main.ts:186` divides the old store width by the new client box, so every resize at constant DPR scales the view (measured in Chromium: 1.6822× growth where the invariant demands 1.0; the single-token fix `ratioBefore = lastRatio` reads 1.0000). The architect's note 3 (the pixel-ceiling regime) has the same fix. **Rule-7 reading recorded:** the closing round already in flight addresses this same code path and was dispatched before this verdict arrived — the correction was sent into that round (the same attempt, not a new one); a third reviewer FAIL on this path stops the piece and queues it to the human. The forced-CSS mutation reading 259.19 was the regression's fingerprint; Amendment 3 must say so.
