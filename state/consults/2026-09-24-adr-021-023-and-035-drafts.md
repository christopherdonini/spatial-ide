*Custodian's filing note (2026-09-24): the architect agent's drafts for PLAN node `adr-021-023-b1-notes` (RULED 2026-09-24 — question round 17, item 3) and for ADR-035 (the same round, item 2, the watcher's S1), filed as returned. Everything below the rule is the agent's text. It flagged that the custodian's brief asked for a line-cited quote of the ledger, which rounds 12 (a) and 14 (a′) forbid; it followed the rule, and the drafts carry no quotation of the human. Its ADR-035 Open item 1 (the session reference's wire form) is queued as `DECISIONS-PENDING.md` entry 129.*

---

**Verdict: pass with notes.** The drafts are below. Four things come first:

1. **Your quoting instruction conflicts with a permanent rule. I followed the rule.** You asked me to quote the human by byte-copying from the RULED block with a path:line cite. Round 12 (a) and round 14 (a′) forbid that. The ledger is cited by round and item only, and the gate fails a line cite into `DECISIONS-PENDING.md` by name. The human's words for round 17 exist nowhere except the ledger: `state/questions/round-17.md` holds the questions, not the answers. So every draft below cites "RULED 2026-09-24 — question round 17, item N" and reproduces none of the human's words. Where I restate a ruling, the text is marked as my words, not a quotation. Nothing in the drafts is presented as a quote, and there are no line cites for anyone to hash.

2. **ADR-035 has one stop item: what the "session reference" is.** Rider (b) says the event carries the session reference and never carries a handle. The only thing that identifies an open dataset session on the wire today is the `DatasetHandle`. SKP-V0 §3 calls it a handle, and it still authorises `describe` and `close_dataset` after the session ends. So the rider, read as written, rules it out. The ruling does not say what to use instead. I have left this as a named Open item for the human, with my recommendation marked as mine. The watcher's preregistration cannot fix its payload until the human answers. Queue it; do not let a worker choose.

3. **The ADR index will drift when ADR-035 is added.** `docs/README.md`'s ADR index must be regenerated in the same PR (`npm run adr-index` in `frontends/shell`), or `verify:adr-index` fails. Separately, `HEADER_LINE` in `frontends/shell/scripts/adrIndex.mjs` hard-codes "ADR-014 and ADR-031 today" as the reserved numbers with no file. With ADR-035 filed before ADR-034, that sentence becomes false. Either file ADR-034 first or update the constant.

4. **The number is confirmed.** `docs/adr/` ends at ADR-033. Searching the tree for `ADR-03[4-9]` finds only ADR-034 (reserved by round 17 item 7, in the ledger, `PLAN.yaml`, the MultiPolygon consult and round-17.md) and ADR-035 (this task, in `state/CUT-STATE.md`). ADR-031 (LOD) and ADR-014 are reserved elsewhere. No other reservation exists, so this is ADR-035.

---

## A1 — the dated note for ADR-021

**File:** `C:/dev/spatial-ide/docs/adr/ADR-021-row-filter-on-viewport-query.md`

**Where it goes:** appended at the end of the file, after "Amendment 2026-09-15 — the no-runtime-fetch property is held by configuration, not only by content", with one blank line before it. Nothing above it is edited, including the Status line.

**The refusal code, checked before naming it:**
- ADR-021 decision 8 lists `filter_column_not_filterable` among its eleven codes, and its Consequences name `skp.filter_column_not_filterable`.
- In `kernel/src/skp.rs`, `filter_error_of` maps `FilterError::ColumnNotFilterable { column, reason }` to the code `filter_column_not_filterable`, with fields `column` and `reason`. Its test asserts the code `skp.filter_column_not_filterable`.
- The SKP-V0 §7.5 table carries the same code and fields.
- `engine/src/predicate.rs`'s namespace admission already returns this variant, with a `reason`, for the geometry column and for any type the gate refuses.

**Text:**

```markdown
## Note 2026-09-24 — the filter namespace widens by reference to ADR-023; dictionary-encoded columns are excluded by name

*Appended under the human's ruling of 2026-09-24 (`DECISIONS-PENDING.md`, "RULED 2026-09-24 — question round 17", item 3, its Item 2). The text above is unchanged, the Status line included.*

1. **The widening, stated rather than implicit.** Decision 5 removes from the namespace any column whose type is not admitted for filtering; the type set it refers to is ADR-023 §2's admitted-type list, which is decided by the one admission function (`engine::attributes::admit_attribute_type`, the reference `SKP-V0.md` §7.3 already names), less item 2 below. ADR-023 §2, as amended by the human on 2026-09-14, admits `Float32` as its own type, so **`Float32` columns are filterable**. Against the namespace as it stood before this note, `Float32` is the one type added. The authority for this widening is the ruling named above; the reference to ADR-023 §2 names the type list and does not change ADR-023's Status.
2. **Dictionary-encoded columns are excluded from the namespace by name.** ADR-023 §2 admits a dictionary-encoded column under its value type's rule; this namespace does not take that admission. A predicate that names such a column is refused with the existing `skp.filter_column_not_filterable` (fields `column`, `reason`; decision 8; `SKP-V0.md` §7.5), mapped from `FilterError::ColumnNotFilterable { column, reason }` by `kernel/src/skp.rs`'s `filter_error_of`, with a `reason` stating that the column is dictionary-encoded. No code is added; decision 8's eleven codes stand. The ruling's reason, in this note's words and not a quotation: an ordered comparison on such a column follows the dictionary's declared position (hypothesis H4 of B1's draft preregistration, `state/consults/2026-09-24-b1-engine-prereg-draft.md` — registered, not yet tested), so `<` and `>` would mean something no user expects of a string.
3. Both items are implemented by B1's engine half (PLAN node `b1-engine-kernel-half`), whose preregistration states them as ruled; until that piece lands, the namespace is as decision 5 reads without this note.
```

---

## A2 — the clarifying sentence for ADR-023

**File:** `C:/dev/spatial-ide/docs/adr/ADR-023-attribute-projection-on-viewport-query.md`

**Where it goes:** in Decision, § "2. The admitted-type allowlist", in the paragraph that opens "**One admission function, shared with the publish path**". It is appended to that paragraph, on the same line, after its last sentence: "A second admissible set for the live path would be a second policy over the same question; the decision is that there is exactly one." Nothing else in the file changes, including the Status line and ADR-017.

**Text (one leading space, then):**

```markdown
*(clarified by the human's ruling of 2026-09-24, question round 17, item 3)* — Publishing's check against ADR-017 §4's closed type list is not a second admission policy: admission, which decides whether a type is meaningful, stays this one function, while the bundle check, which decides whether `bundle_version` 1 can carry an admitted column, is owned by the bundle format and enforced at the publish site as a named bundle-format restriction that refuses `Float32` and dictionary-encoded columns at preflight with today's refusal text, byte for byte, until B3's `bundle_version`-2 ADR decides, ADR-017 §4 itself staying byte-identical.
```

The inline mark follows how §2 already marks the 2026-09-14 amendments. §2's italic header line says the port is verbatim "with the two §2 amendments ... marked inline". I left that header unedited, because the ruling allows one sentence, and the inline mark carries the provenance. If a gate calls the header stale, fixing it is a class-3 correction and the human's call. I did not make it.

---

## B — ADR-035

**Proposed filename:** `C:/dev/spatial-ide/docs/adr/ADR-035-dataset-session-ended-control-plane-event.md`

(The complete file text is committed as that file on branch `docs/adr-035-dataset-session-ended`; it is not repeated here.)

**What I changed from my skeleton, and why:**
- **Dropped `detail`, the free text.** Rider (b) allows only the reference and the reason. Engine display text reaching the shell through the event would also be an engine message standing where the owner states consequences.
- **Replaced `code` with `reason`, a closed two-value set.** Rider (b) names the reason itself (observed change vs coverage lost), and the event is not a refusal. Refusal codes stay on later calls, under rider (a).
- **Dropped `skp` and `kind`.** "Only" the two members. The Tauri event's name identifies the kind, and a version mismatch already fails on the first call.
- **Replaced `dataset` (the `DatasetHandle`) with `session`, its form left open.** See stop item 2 above.
- **Restated the skeleton's authority clause as rider (a).** The end is decided and recorded server-side before emission, and a lost or late event cannot leave a client acting on an ended generation.
- **Dropped the skeleton's claim that the P3b and N8 handlers are already idempotent.** No test was named for it; idempotence is kept as a requirement only.

---

## Choices the rulings did not dictate

1. A1's heading reads "Note 2026-09-24 — …". The ruling says "a dated note"; "Note" rather than "Amendment" is my wording.
2. A1 item 1's sentence that the authority is the ruling, and that the reference does not change ADR-023's Status. It is my clarification, because an Accepted ADR is referencing a Proposed one.
3. A1 says the `reason` field states that the column is dictionary-encoded. That is my reading of "by name".
4. A1 qualifies H4 as registered, not yet tested. It is a fact, not a decision, but the ruling does not say it.
5. A1 item 3 names `b1-engine-kernel-half` as the implementing piece and says the namespace is unchanged until it lands.
6. A2's position (the end of §2's first paragraph), its inline-mark convention, and "with today's refusal text, byte for byte" as my rendering of "byte-identical text".
7. ADR-035's filename, its title, and the member names `session` and `reason` with the values `"observed-change"`/`"coverage-lost"`. All are my wording.
8. Carried over from the skeleton the human accepted in round 17 item 2, option (1), not from the riders:
   - Tauri-event delivery with its name as a `protocol/skp` constant;
   - no subscription command;
   - best-effort, at most once;
   - emission only on the watcher's end of a generation, not on pre-check or post-check ends.
9. ADR-035's Open item 1 and its recommendation (ii) are the architect's; the ruling does not settle this.
10. ADR-035 names the literal by merge-order rule, not by number, so that no unmerged branch's number is read as current.
11. I left ADR-035's architect-blockability unstated.
