# Architect consult — the erratum rows for P3b and the LOD, and the reduced RESIDENCY note (2026-09-18)

*Recorded by the custodian verbatim from the architect's report (Opus 5, configured default), under the human's standing directive of 2026-09-18 (points (1), (3), (5)) and round 15's clauses (a)–(b). The four fenced blocks are the amendments a fresh worker byte-copies; the `<COMPUTE: …>` tokens carry their own commands. To be committed under `state/consults/` once #84 merges (its pins name files not yet on main). Nothing here is applied by this file.*

---

ARCHITECT: ERRATA

**Model observed:** `claude-opus-5[1m]` (Opus 5, 1M context) — the configured default (`.claude/agents/architect.md:5`, `model: opus`), no override. Read/Grep/Glob only; every digest below is a `<COMPUTE: …>` placeholder carrying its exact command. Nothing edited.

Read: `C:\dev\spatial-ide\state\directives\2026-09-18-record-cap.md` (whole), `C:\dev\spatial-ide\DECISIONS-PENDING.md` (rounds 11–15 RULED blocks), `C:\dev\spatial-ide\state\consults\2026-09-17-p3b-re-scope.md`, the five gate reports named in the brief, and in the three worktrees: `engine\ADMISSION-PREREGISTRATION.md`, `frontends\shell\OWNER-INVALIDATION-PREREGISTRATION.md`, `engine\LOD-PREREGISTRATION.md`, `engine\tests\lod_tier_builder.rs`, `frontends\shell\RESIDENCY-PREREGISTRATION.md`, plus `C:\dev\spatial-ide\docs\08_Testing.md`.

Form rules obeyed in all three blocks: rows are `id | status | reference`; the reference column carries only a pin (`path:line[-line] @ <12-hex, named> sha256:…`), a round + item cite, or an amendment/item identifier; no sentence restates what a reference proves; no reproduced text; no bare `:line` into the amendment's own file (every self-line is pinned at a commit the closing commit does not create); rulings by round and item (round 12, item 1 (a)); the table is the superseded index (round 12, item 1 (e)); pins at branch commits are named as such where the cited text is not on main (round 15, item 1, clause (e), as the human resolved in this brief).

---

## A. The erratum amendments

### A1 — P3b engine: append to `engine/ADMISSION-PREREGISTRATION.md` (`cut/briefa-p3b` @ 959bd08)

```markdown

---

### Amendment 13 -- erratum rows (2026-09-18, appended; the architect's reduction under the human's standing directive of 2026-09-18, point (1): `state/directives/2026-09-18-record-cap.md:5 @ <COMPUTE: git rev-parse --short=12 6195d5a> sha256:<COMPUTE: git show 6195d5a:state/directives/2026-09-18-record-cap.md | sed -n '5p' | sha256sum>`)

**Written after the gates' attempt-6 findings were seen.** **Read this amendment first.** Classes: **1** for a withdrawal or narrowing row (round 15, item 1, clause (g)); **3** for a pointer row. Every pin names its commit; where the cited text is not on main, the commit named is the branch commit that carries it. This amendment claims nothing about any item it does not list.

**Superseded index** (round 12, item 1 (e)).

| id | status | reference |
| --- | --- | --- |
| Amendment 11's "Classes used" line | corrected: Amendment 12's row on it withdrawn as to the count | `engine/ADMISSION-PREREGISTRATION.md:1534 @ <COMPUTE: git rev-parse --short=12 959bd08> sha256:<COMPUTE: git show 959bd08:engine/ADMISSION-PREREGISTRATION.md | sed -n '1534p' | sha256sum>` |
| Amendment 12, gap 1 | settled | round 14, item 2 |
| Amendment 12, gap 2 | settled | round 15, item 1, clause (g) |
| Amendment 12, gap 3 | settled | round 14, item 2 |

**Read this amendment first.**
```

### A2 — P3b shell: append to `frontends/shell/OWNER-INVALIDATION-PREREGISTRATION.md` (same branch, same commit)

```markdown

---

### Amendment 16 -- erratum rows (2026-09-18, appended; the architect's reduction under the human's standing directive of 2026-09-18, point (1): `state/directives/2026-09-18-record-cap.md:5 @ <COMPUTE: git rev-parse --short=12 6195d5a> sha256:<COMPUTE: git show 6195d5a:state/directives/2026-09-18-record-cap.md | sed -n '5p' | sha256sum>`)

**Written after the gates' attempt-6 findings were seen.** **Read this amendment first.** Classes: **1** for a withdrawal or narrowing row (round 15, item 1, clause (g)); **3** for a pointer row. Every pin names its commit; where the cited text is not on main, the commit named is the branch commit that carries it. This amendment claims nothing about any item it does not list.

**Superseded index** (round 12, item 1 (e)).

| id | status | reference |
| --- | --- | --- |
| Amendment 15's "Scope of the no-line rule" paragraph | narrowed: certifies no cite it does not list | `frontends/shell/OWNER-INVALIDATION-PREREGISTRATION.md:1109 @ <COMPUTE: git rev-parse --short=12 959bd08> sha256:<COMPUTE: git show 959bd08:frontends/shell/OWNER-INVALIDATION-PREREGISTRATION.md | sed -n '1109p' | sha256sum>` |
| Amendment 15's row on Amendment 11 (b)'s missing class number | settled: class 7 | round 14, item 2 |
| Amendment 15, gap 1 | settled | round 14, item 2 |
| Amendment 15, gap 2 | settled | round 15, item 1, clause (g) |
| Amendment 15, gap 3 | settled | round 14, item 2 |
| Amendment 13 (3), and Amendment 8's two driver-output quotes it checks | recorded: evidence, not Authority; the rule dated non-retroactive | round 15, item 4 |

**Read this amendment first.**
```

### A3 — LOD: append to `engine/LOD-PREREGISTRATION.md` (`engine/lod-tier-builder` @ bd8f1f9; em-dash house style, no `---` separator, matching Amendment 13)

```markdown

**Amendment 14 — erratum rows (2026-09-18, appended; the architect's reduction under the human's standing directive of 2026-09-18, point (1): `state/directives/2026-09-18-record-cap.md:5 @ <COMPUTE: git rev-parse --short=12 6195d5a> sha256:<COMPUTE: git show 6195d5a:state/directives/2026-09-18-record-cap.md | sed -n '5p' | sha256sum>`). Written after the attempt-6 gate findings were seen.** **Read this amendment first.** Classes: **1** for a withdrawal or narrowing row (round 15, item 1, clause (g)); **3** for a pointer row. Every pin names its commit; where the cited text is not on main, the commit named is the branch commit that carries it. This amendment claims nothing about any cite it does not list.

**Superseded index** (round 12, item 1 (e)).

| id | status | reference |
| --- | --- | --- |
| Amendment 13, "What this commit does to the tree", its per-span clause | replaced 7→9, 6→4, 2→2; total 1209 = 1209 | `engine/tests/lod_tier_builder.rs:1-1209 @ d6073d85a40a sha256:<COMPUTE: git show d6073d85a40a:engine/tests/lod_tier_builder.rs | sed -n '1,1209p' | sha256sum>`; `engine/tests/lod_tier_builder.rs:1-1209 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/tests/lod_tier_builder.rs | sed -n '1,1209p' | sha256sum>` |
| Amendment 13, the same paragraph's clause that every cite into that file from Amendments 8-12 still resolves | narrowed: no cite into that file is falsified by this commit | `engine/LOD-PREREGISTRATION.md:567 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/LOD-PREREGISTRATION.md | sed -n '567p' | sha256sum>` |
| Amendment 8 (i)2's cite into `engine/tests/lod_tier_builder.rs` (retired form `:169-173`) | retired form; corrected by reference | `engine/tests/lod_tier_builder.rs:322-326 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/tests/lod_tier_builder.rs | sed -n '322,326p' | sha256sum>` |
| Amendment 8 (j)'s two cites into that file (retired forms `:440`, `:503`) | retired form; corrected by reference | `engine/tests/lod_tier_builder.rs:593 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/tests/lod_tier_builder.rs | sed -n '593p' | sha256sum>`; `engine/tests/lod_tier_builder.rs:656 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/tests/lod_tier_builder.rs | sed -n '656p' | sha256sum>` |
| Amendment 8 (l)'s two cites into that file (retired forms `:492`, `:921`) | retired form; corrected by reference | `engine/tests/lod_tier_builder.rs:645 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/tests/lod_tier_builder.rs | sed -n '645p' | sha256sum>`; `engine/tests/lod_tier_builder.rs:1074 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/tests/lod_tier_builder.rs | sed -n '1074p' | sha256sum>` |
| Amendment 8 (r)(ii)'s cite into that file (retired form `:512-517`) | retired form; corrected by reference | `engine/tests/lod_tier_builder.rs:665-670 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/tests/lod_tier_builder.rs | sed -n '665,670p' | sha256sum>` |
| Amendment 11 (d)'s five cites of that file's current text (retired forms `:229`, `:230`, `:143-180`, `:235-238`, `:239-243`) | retired form; corrected by reference | `engine/tests/lod_tier_builder.rs:237-238 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/tests/lod_tier_builder.rs | sed -n '237,238p' | sha256sum>`; `engine/tests/lod_tier_builder.rs:145-182 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/tests/lod_tier_builder.rs | sed -n '145,182p' | sha256sum>`; `engine/tests/lod_tier_builder.rs:243-246 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/tests/lod_tier_builder.rs | sed -n '243,246p' | sha256sum>`; `engine/tests/lod_tier_builder.rs:247-251 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/tests/lod_tier_builder.rs | sed -n '247,251p' | sha256sum>` |
| Amendment 13's row on the nine `sha256:` references | corrected as to the rev claim and the tool claim: checker commit fbf3d55 — seven bound, two reported | `engine/LOD-PREREGISTRATION.md:541 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/LOD-PREREGISTRATION.md | sed -n '541p' | sha256sum>`; `engine/LOD-PREREGISTRATION.md:545 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/LOD-PREREGISTRATION.md | sed -n '545p' | sha256sum>` |
| Amendment 13's `path:line @ rev sha256:` reference in the `RECORDED MUTATION` comment, split across a `//` continuation | recorded; owed | round 15, item 1, clause (d); `engine/tests/lod_tier_builder.rs:190-191 @ <COMPUTE: git rev-parse --short=12 bd8f1f9> sha256:<COMPUTE: git show bd8f1f9:engine/tests/lod_tier_builder.rs | sed -n '190,191p' | sha256sum>` |
| Amendment 13, gap 1 | settled | round 14, item 2 |
| Amendment 13, gap 2 | settled | round 15, item 1, clause (g) |
| Amendment 13, gap 3 | settled | round 14, item 1 |

**Read this amendment first.**
```

### A4 — the RESIDENCY note, reduced: Amendment 28 whole, on a fresh branch cut from `origin/main` (suggested `record/residency-125-note-3`); `record/residency-125-note-2` @ 206b756 is abandoned, not amended

```markdown

**Amendment 28 (2026-09-18, appended; the owed correction ordered by round 14, item 3; reduced to references under the human's standing directive of 2026-09-18, point (1): `state/directives/2026-09-18-record-cap.md:5 @ <COMPUTE: git rev-parse --short=12 6195d5a> sha256:<COMPUTE: git show 6195d5a:state/directives/2026-09-18-record-cap.md | sed -n '5p' | sha256sum>`; not a change to any cell of this closed cut). Class 3.**

| id | status | reference |
| --- | --- | --- |
| §2e's first quoted row | corrected reference | `docs/08_Testing.md:5 @ <COMPUTE: git rev-parse --short=12 origin/main> sha256:<COMPUTE: git show origin/main:docs/08_Testing.md | sed -n '5p' | sha256sum>` |

**Superseded index** (round 12, item 1 (e)): §2e's first quoted row, as to its reference — this amendment.
```

---

## B. Worker steps (fresh default-model worker; no judgment calls; absolute paths; Bash for the POSIX pipelines; STOP and report on any failed assertion, never adjust the text)

### B1 — P3b, worktree `C:\dev\spatial-ide\.claude\worktrees\agent-aa3f47c3b96280c56`, branch `cut/briefa-p3b`

1. **Start state.** `git rev-parse HEAD` = `959bd08…`; `git status --porcelain` empty; `git rev-parse 6195d5a` resolves and `git merge-base --is-ancestor 6195d5a origin/main` rc=0 (the directive pin is on main). Record all three.
2. **Compute the four digests**, pasting each command's output into the report: the directive line at 6195d5a; `engine/ADMISSION-PREREGISTRATION.md:1534 @ 959bd08`; `frontends/shell/OWNER-INVALIDATION-PREREGISTRATION.md:1109 @ 959bd08`; and the two `rev-parse --short=12` values. Assert the directive digest equals `e44168d98d83bff75fc1155dd8b1f94b4ac077e113c8deabacaa93fd2622ef58` (the value the ledger records); any difference: STOP.
3. **Print and read each pinned span.** `:1534` must be the `**Classes used**` line naming `docs/PREREGISTRATION-TEMPLATE.md:101-129` and claiming class 3 throughout; `grep -c 'five\|Five'` over `git show 959bd08:engine/ADMISSION-PREREGISTRATION.md | sed -n '1528,1557p'` must be **0**. `:1109` must be the `**Scope of the no-line rule.**` paragraph. Any mismatch: STOP.
4. **Write the two blocks to temp files** (`$SCRATCH/engine-a13.md`, `$SCRATCH/shell-a16.md`) by byte-copy from this consult — never retyped. Ensure each target file ends in a newline, then `cat "$SCRATCH/engine-a13.md" >> engine/ADMISSION-PREREGISTRATION.md` and `cat "$SCRATCH/shell-a16.md" >> frontends/shell/OWNER-INVALIDATION-PREREGISTRATION.md` (heredoc appends parse-fail on long markdown).
5. **Substitute the `<COMPUTE: …>` tokens by script** with the bare outputs; assert `grep -c '<COMPUTE' ` = 0 in both files.
6. **Self-check, no bare self-line.** `sed -n '/^### Amendment 13 --/,$p' engine/ADMISSION-PREREGISTRATION.md | grep -nE ':[0-9]+'` and the same for `/^### Amendment 16 --/` in the shell file: every hit must sit inside a pinned `@ <12-hex> sha256:` reference. Paste both outputs.
7. **Self-check, append-only and reference form.** `git diff --numstat 959bd08 -- engine/ADMISSION-PREREGISTRATION.md` and `… -- frontends/shell/OWNER-INVALIDATION-PREREGISTRATION.md` each show `N 0`; `git diff --name-only 959bd08` lists exactly the two files; every pinned reference is contiguous on one line (round 15, item 1, clause (d)) — assert no added line contains `sha256:` without `@ ` before it on the same line.
8. **Code freeze.** `git diff --stat fc66f13..HEAD -- kernel frontends/shell/src frontends/shell/src-tauri frontends/shell/e2e protocol engine/src` prints nothing, rc=0.
9. **Mechanical suites, gated on exit code** (print `rc`, continue only on 0): `node scripts/plan/verify-cites.mjs`, `node scripts/plan/verify-test-claims.mjs`, `node scripts/plan/verify-mutation.mjs`. Then the quote checker as prior rounds ran it (scratch copy of `origin/governance/verify-quotes` @ `fbf3d55`, `repoRoot` = the worktree, worktree never written to): assert **zero findings at or above** the two new amendments.
10. **Commit once, both files, signed off** — `git commit -s -F "$SCRATCH/msg.txt"`:
    ```
    docs(p3b): erratum rows -- engine 13, shell 16

    The architect's reduction under the human's standing directive of 2026-09-18,
    point (1): rows of id | status | reference, no prose restating a reference.
    Engine 13 withdraws Amendment 12's count attribution as to Amendment 11's
    "Classes used" line and settles gaps 1-3; shell 16 narrows Amendment 15's
    scope paragraph, settles gaps 1-3 and records round 15, item 4's disposition
    of the untracked e2e reports.

    verify-cites rc=0 | verify-test-claims rc=0 | verify-mutation rc=0 | code
    frozen since fc66f13.

    Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
    Claude-Session: https://claude.ai/code/session_01JeU7h98xrTorztiep3Zgmg
    ```
11. **Push** `cut/briefa-p3b`; report `git status --porcelain` (clean), every digest, every self-check output, the model observed and any override. Open no PR.
12. **The resolution step (round 15, item 1, clause (a)) — before the commit in step 10, not after.** Resolve **each row** of both blocks by recomputing its reference and testing its status word against what the reference shows, and **STOP on a mismatch** instead of committing:
    - *Amendment 11's "Classes used" line / "corrected … withdrawn as to the count"* — the recomputed `:1534` digest matches the text, the printed line makes no count claim, and `grep -c 'five'` over Amendment 11's span is 0.
    - *gap 1 / "settled" / round 14, item 2* — the round-14 RULED block's item 2 is in `DECISIONS-PENDING.md` and its Applied text names class 7 appended to the template's class list.
    - *gap 2 / "settled" / round 15, item 1, clause (g)* — the round-15 RULED block's item 1 Applied text names "class 1 for a withdrawal row" among the seven clauses.
    - *gap 3 / "settled" / round 14, item 2* — the same Applied text names clause (a′).
    - *Amendment 15's scope paragraph / "narrowed: certifies no cite it does not list"* — the recomputed `:1109` digest matches and the printed paragraph is the one whose certification is narrowed.
    - *Amendment 15's row on Amendment 11 (b) / "settled: class 7"* — round 14, item 2 as above.
    - *Amendment 13 (3) and Amendment 8's two quotes / "evidence, not Authority; non-retroactive"* — the round-15 RULED block's item 4 carries the human's dating and the evidence-versus-Authority distinction.
    Any row whose status the reference does not make true: STOP, report, commit nothing.

### B2 — LOD, worktree `C:\dev\spatial-ide\.claude\worktrees\agent-a3073bf516424356e`, branch `engine/lod-tier-builder`

1. **Start state.** `git rev-parse HEAD` = `bd8f1f9…`; `git status --porcelain` empty; `git rev-parse d6073d85a40a` and `git rev-parse 6195d5a` resolve. Record.
2. **Compute every digest in the block**, each with its own command, pasting outputs. Assert the directive digest equals the ledger's `e44168d9…ef58`.
3. **Print and read each pinned span**, asserting what the row needs: `.rs:322-326` is the doc comment naming `LOD_TIER_MAX_RELATIVE_BYTES`; `.rs:593` and `.rs:656` are `// RECORDED MUTATION` lines; `.rs:645` and `.rs:1074` are `disk_cost()` callers; `.rs:665-670` is the "Deliberately not byte-identical" isolation comment and its rings; `.rs:237-238` are `DECLARED_BYTES` / `DECLARED_SHA256`; `.rs:145-182` opens `struct FixtureAside {`; `.rs:243-246` is the two-threads-agree assert and `.rs:247-251` the `DECLARED_SHA256` assert; `.rs:190-191` is the split `//` reference; `.md:567` is "What this commit does to the tree"; `.md:541` and `.md:545` are Amendment 12 (e) and (g).
4. **Resolve the retired cite set, member by member** (the reviewer's mechanism repair). For each of the eleven retired forms listed in the rows — `:169-173`, `:440`, `:503`, `:492`, `:921`, `:512-517`, `:229`, `:230`, `:143-180`, `:235-238`, `:239-243` — print `git show bd8f1f9:engine/tests/lod_tier_builder.rs | sed -n '<span>p'` and confirm it is **not** what its citing item says (the cite is stale), and print the paired current span and confirm it **is**. Paste all twenty-two prints. Any retired form that still resolves, or any current span that does not: STOP.
5. **Resolve the per-span counts.** `git show d6073d85a40a:engine/tests/lod_tier_builder.rs | wc -l` = 1209 and the same at bd8f1f9 = 1209; `git diff d6073d85a40a bd8f1f9 -- engine/tests/lod_tier_builder.rs` shows exactly three replaced spans, 7→9, 6→4 and 2→2. Any other shape: STOP.
6. **Resolve the tool claim** (round 15, item 1, clause (c)). Run the scratch copy of `origin/governance/verify-quotes` @ `fbf3d55` (`repoRoot` = the worktree, nothing written into it) over `engine/LOD-PREREGISTRATION.md` and assert: seven of the nine `sha256:` references bind, and exactly two hash-reference findings are reported for this file, at `.md:541` and `.md:545`. Paste the run. Any other count: STOP.
7. **Append the block** by byte-copy via a temp file (`$SCRATCH/lod-a14.md`, `cat >>`), substitute the `<COMPUTE: …>` tokens by script, assert `grep -c '<COMPUTE'` = 0.
8. **Self-checks.** `sed -n '/^\*\*Amendment 14 —/,$p' engine/LOD-PREREGISTRATION.md | grep -nE ':[0-9]+'` — every hit inside a pin or inside a cell whose id says **retired form**; `git diff --numstat bd8f1f9 -- engine/LOD-PREREGISTRATION.md` = `N 0`; `git diff --name-only bd8f1f9` lists that file only; `git diff --stat 8d182a0..HEAD -- engine/src` empty; no added line carries `sha256:` without `@ ` on the same line.
9. **Mechanical suites, gated on exit code**: `verify-cites`, `verify-test-claims`, `verify-mutation` each rc=0; the checker run of step 6 shows zero findings at or above Amendment 14. Cargo is **not** re-run (no code changed).
10. **Commit once, signed off**, message:
    ```
    docs(lod): erratum rows -- Amendment 14

    The architect's reduction under the human's standing directive of 2026-09-18,
    point (1): rows of id | status | reference. Amendment 13's per-span clause
    corrected (7->9, 6->4, 2->2; 1209 = 1209), its cite certification narrowed to
    what this commit proves, the eight stale cross-file cites in Amendments 8 and
    11 retired by item with their current locations pinned, the tool row corrected
    against checker commit fbf3d55, gaps 1-3 settled.

    verify-cites rc=0 | verify-test-claims rc=0 | verify-mutation rc=0 |
    engine/src untouched since 8d182a0.

    Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
    Claude-Session: https://claude.ai/code/session_01JeU7h98xrTorztiep3Zgmg
    ```
11. **Push** `engine/lod-tier-builder`; report clean status, every digest and every print. No PR.
12. **The resolution step (round 15, item 1, clause (a)) — before the commit in step 10.** Row by row: the per-span row against step 5; the narrowing row against step 5 (line-count neutrality proves only "no cite falsified by this commit"); each of the five retirement rows against its members in step 4; the tool row against step 6; the split-reference row against the `.rs:190-191` print; gap 1 and gap 2 against the round-14 item 2 and round-15 item 1 Applied texts in `DECISIONS-PENDING.md`; gap 3 against round 14, item 1's accepted "its home is the closing amendment's head and foot". **STOP on any mismatch** and commit nothing. If a stale cite appears that no row lists, report it as a finding — do **not** add a row (the amendment claims nothing about cites it does not list).

### B3 — the RESIDENCY note, fresh branch

1. **Fresh worktree and branch** off `origin/main` (suggested `record/residency-125-note-3`). `git rev-parse --short=12 origin/main`, `git status --porcelain` empty. `record/residency-125-note-2` is not merged, not amended, not cited.
2. **Assert the base.** `frontends/shell/RESIDENCY-PREREGISTRATION.md`'s last amendment is 27 and the file ends with one LF; `sed -n '123,126p'` shows §2e's heading and its first quoted row with the `…` elision.
3. **Compute the two digests.** `git show origin/main:docs/08_Testing.md | sed -n '5p' | sha256sum` — assert it equals `289ae3d58d1eba6beb4ec1f3b66574a6ffb7a0818d11fcb5975a40a378086077` (the value both reads recomputed at 79f698c403d8); if it differs, the source line has moved and this STOPS. And the directive line at 6195d5a, asserted against `e44168d9…ef58`.
4. **Print `docs/08_Testing.md:5` whole** and confirm it carries both the qualifier ("the fixed reference hardware profile's own refresh rate") and the negation ("has not met this budget, only relocated the miss") — the pin is the statement, so the line must carry them; nothing is restated in the amendment.
5. **Append the block** by byte-copy via a temp file, substitute the two `<COMPUTE: …>` tokens, assert `grep -c '<COMPUTE'` = 0, assert the pin is contiguous on one line.
6. **Self-checks.** `git diff --numstat origin/main...HEAD` = `N 0`, single hunk at EOF; the added lines' `:NNN` scan returns only `:5` inside the pin; no line cite into `DECISIONS-PENDING.md`; `verify-cites`, `verify-test-claims`, `verify-mutation` each rc=0; the checker run shows no new finding at or above the amendment.
7. **Commit once, signed off** — `docs(residency): Amendment 28 -- the owed correction, reduced to references` with the directive and round 14, item 3 named in one line — and push.
8. **The resolution step (round 15, item 1, clause (a)) — before the commit.** One row: recompute the pin, confirm the digest in the text, confirm the printed line is the source of §2e's first quoted row, and confirm "corrected reference" is what the row does (it changes where the quotation is read, not any claim). Confirm the header's ruling cite resolves: round 14, item 3's RULED text is ":125 owed-correction" and its Applied text names an appended note in this file restating the passage by reference. **STOP on a mismatch.**

**Custodian carries (not the worker's, PR-body lines):** (i) pin-versus-tree authority for P3b's four `@ e53bb97` pins and the LOD's template pins — the pin is authoritative for the proposition each row proves, the tree for the class list in force (seven classes); (ii) the LOD's `engine/LOD-PREREGISTRATION.md:338`/`:340` "five pre-declared classes" — pre-existing, unpinned, uncarried by the cap; (iii) `verify-quotes`'s `HASH_REF_RE` versus the whole-pin-in-one-backtick form (P3b reviewer S-2) belongs to the checker's own round (round 15, item 3); (iv) point (4)'s record-round count: three record-correction rounds for P3b, three for the LOD, two for the note; (v) this consult is committed under `state/consults/` so nothing tracked cites an untracked file.

---

## C. My per-row resolution (round 15, item 1, clause (b)) — one line per row, what I read at the named site

**P3b engine:**
- *Amendment 11's "Classes used" line* — `engine\ADMISSION-PREREGISTRATION.md:1534` reads `**Classes used** (` + `docs/PREREGISTRATION-TEMPLATE.md:101-129`) `: class 3 throughout (cite/reference form fix, mechanical, never changes a claim).` — no count claim anywhere; my grep for `five|Five` over the whole file returns four unrelated hits plus `:1593` itself, none inside Amendment 11 (`:1528-1557`), so "withdrawn as to the count" is true and the class-3 half of `:1593` stands.
- *gaps 1, 3* — `:1607` and `:1609` route class 7 and clause (a′); the round-14 RULED block's item 2 (`DECISIONS-PENDING.md`, "Adopt all three as byte-copied…") has both adopted and applied to the template, so "settled" is true.
- *gap 2* — `:1608` routes the class of a withdrawal; round 15's item 1 Applied text lists "class 1 for a withdrawal row" among the seven adopted clauses, so "settled" is true.

**P3b shell:**
- *Amendment 15's scope paragraph* — `:1109` reads "An earlier self-line cite that still resolves stands as written and is not listed here", which implies the converse the table does not carry (the reviewer's S-3 named `:563`, `:657`, `:878`, `:990-996`, `:1020`, `:950`/`:954`; the architect's nit named Amendment 13 (2)'s `:975`); "narrowed: certifies no cite it does not list" is exactly what the table can carry.
- *Amendment 11 (b)'s missing class number* — `:1127` says "No pre-declared class describes an evidence-driven sight-list addition"; class 7 exists on main by round 14, item 2, so "settled: class 7" is true and the present-tense sentence stops misleading after merge.
- *gaps 1, 2, 3* — `:1149`, `:1150`, `:1151`, resolved as for the engine.
- *the untracked e2e cites* — `:978-982` (Amendment 13 (3)) discloses that `frontends/shell/e2e/out/` is gitignored and that both quoted `note` strings were checked by hand against the reports; Amendment 8's quotes sit at `:775` and `:803` (grep confirms the report paths at `:773`, `:811`, `:835`); round 15, item 4 dates the rule non-retroactive and keeps the evidence-versus-Authority distinction, so "recorded" is true and no correction is owed.

**LOD:**
- *the per-span clause* — `engine\LOD-PREREGISTRATION.md:567` carries both the "each replaced by exactly as many lines as it had" clause and the "every cite … still resolves" clause (the reviewer's `:565` is two short; `:565` is "Why this form, and what it is not"); the architect's B-1 and the reviewer's verification give 184-190→184-192, 198-203→200-203, 2-for-2, `wc -l` 1209 both sides — so "7→9, 6→4, 2→2; total 1209 = 1209" is true and the two whole-file pins are its proof.
- *the certification clause* — same line; line-count neutrality proves only that this commit falsifies no cite, which is exactly the narrowed wording.
- *Amendment 8 (i)2* — `.md:421` cites `:169-173` for "now names … `LOD_TIER_MAX_RELATIVE_BYTES`"; I read `.rs:322-326` and it is that doc comment.
- *Amendment 8 (j)* — `.md:433` cites `:440` and `:503` as mutation-comment pointers; I read `.rs:593` and `.rs:656` and both are `// RECORDED MUTATION` lines naming items (i)3 and (i)9.
- *Amendment 8 (l)* — `.md:449` cites `:492` and `:921` as `disk_cost()` callers; I read `.rs:645` (`assert_eq!(after.disk_cost().total_bytes, on_disk);`) and `.rs:1074` (`let cost = l.set.disk_cost();`).
- *Amendment 8 (r)(ii)* — `.md:461` says the byte-identical-sources hazard was "fixed at `…:512-517`"; I read `.rs:665-670` and it is the "Deliberately not byte-identical to any other test's source" comment with the rings it guards.
- *Amendment 11 (d)* — `.md:513` cites `:229`, `:230`, `:143-180`, `:235-238`, `:239-243`; I read `.rs:237-238` (`DECLARED_BYTES` / `DECLARED_SHA256`), `.rs:145-182` (`struct FixtureAside {` through its `Drop`), `.rs:243-246` (the two-threads-agree assert) and `.rs:247-251` (the `DECLARED_SHA256` assert). **Two of these five ( `:235-238`, `:239-243` ) are not in the reviewer's set of eight** — I surveyed the item whole, which is why the row retires by item and the amendment says it claims nothing about cites it does not list.
- *the tool row* — `.md:592` claims seven-hex revs the grammar "does not match" and that the tool "reported no hash-reference finding about this document"; `.md:545` in fact carries `sha256:2098…` with **no `@ rev` at all** and prose between cite and hash, and `.md:541` carries `:199-204 @ a3f5f2e sha256:…` with no path in its paragraph — the two findings the fbf3d55 run reports; so "corrected as to the rev claim and the tool claim … seven bound, two reported" is true, and naming fbf3d55 satisfies round 15, item 1, clause (c).
- *the split reference* — the new pin in the mutation comment spans `.rs:190-191` across a `//` continuation, which clause (d) now forbids; joining it would change the file's line count and falsify cites at or below, so "recorded; owed" is the only true status.
- *gaps 1, 2, 3* — `.md:608`, `:609`, `:610`; round 14, item 2 adopted class 3's named test-text exception, round 15, item 1 (g) confirms class 1 for a withdrawal row, round 14, item 1 accepted head-and-foot for the pointer — all three "settled".

**RESIDENCY:**
- *§2e's first quoted row* — `frontends\shell\RESIDENCY-PREREGISTRATION.md:123` heads "The existing `docs/08` rows G3/G4/G5 score against, quoted verbatim", and `:125-126` is its first quoted row, elided at "ADR-003 spike M4 …"; `C:\dev\spatial-ide\docs\08_Testing.md:5` carries in full both the qualifier ("'The display's own interval' means the fixed reference hardware profile's own refresh rate … not whatever a runtime happens to negotiate") and the negation ("a renderer that let power-saving mode or variable refresh silently lower the target has not met this budget, only relocated the miss") — so the pin alone is the statement, "corrected reference" is true, and both defects of read 2 disappear because the note now states neither the qualifier nor the exception.
- *the ruling* — round 14, item 3 rules ":125 owed-correction" and its Applied text names an appended note in this file restating the passage by reference; item 2, not item 3, is where the test-text exception lives, and the reduced note invokes no exception at all.
- *the class* — class 3, named without argument, per the directive's point (1).

**Deliberately not carried, by the cap:** the P3b architect's nits (status overlaps, the missing (d)1 row, unpinned first mentions, house style); the LOD's `.md:338`/`:340` pre-existing "five pre-declared classes"; the engine's gap 4 (§12d's pointer at P6, still genuinely the human's); the checker-grammar finding (P3b reviewer S-2), which is the checker piece's under round 15, item 3. I also dropped the house "§12e's rule, honoured in this line" meta-sentence from the P3b headers: it asserts compliance that the declaration itself carries, and point (1) replaces it with nothing.

---

## D. For the human

None. The directive settles the path: point (3) reduces all three records, point (5) lands P3b, the LOD and the checker after the erratum rows, round 15, item 1 (g) settles gap 2 in all three records, and round 14, item 2 settled gaps 1 and 3. Two items are recorded as the human's but are *not* blocked by these rows and need no answer now: the engine record's gap 4 (whether §12d carries a "read the last amendment first" line at P6) and the split `//` reference in the LOD's mutation comment, which is owed to whoever next touches that file under round 15, item 1, clause (d).

No ADR skeleton: these are governance rows landing by ledger ruling and by the standing directive, not by ADR. Nothing in any of the three diffs touches docs/01, docs/02, docs/08, docs/10 or ADR-006; no seam, no `pub` item, no callback, no operator-visible string, no perf claim — P3b's code has been frozen since `fc66f13` and the LOD's `engine/src` since `8d182a0`.

**Paths:** `C:\dev\spatial-ide\state\directives\2026-09-18-record-cap.md`, `C:\dev\spatial-ide\state\consults\2026-09-17-p3b-re-scope.md`, `C:\dev\spatial-ide\DECISIONS-PENDING.md`, `C:\dev\spatial-ide\docs\08_Testing.md`, `C:\dev\spatial-ide\.claude\worktrees\agent-aa3f47c3b96280c56\engine\ADMISSION-PREREGISTRATION.md`, `C:\dev\spatial-ide\.claude\worktrees\agent-aa3f47c3b96280c56\frontends\shell\OWNER-INVALIDATION-PREREGISTRATION.md`, `C:\dev\spatial-ide\.claude\worktrees\agent-a3073bf516424356e\engine\LOD-PREREGISTRATION.md`, `C:\dev\spatial-ide\.claude\worktrees\agent-a3073bf516424356e\engine\tests\lod_tier_builder.rs`, `C:\dev\spatial-ide\frontends\shell\RESIDENCY-PREREGISTRATION.md`.
