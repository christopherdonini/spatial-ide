# Architect consult — the re-scope of the LOD tier builder's fix record (2026-09-17)

*Recorded by the custodian verbatim from the architect's report (Opus 5, configured default), under the human's round 13, item 1, on the model of `state/consults/2026-09-17-p3b-re-scope.md`. The fenced block is the closing amendment a fresh worker byte-copies; the `<COMPUTE: …>` tokens carry their own commands. Nothing here is applied by this file.*

---

ARCHITECT: RE-SCOPE

**Model observed:** `claude-opus-5[1m]` (Opus 5, 1M context), configured default, no override. Read/Grep/Glob only — every hash and every commit expansion below is a `<COMPUTE: …>` placeholder carrying its exact command. Nothing edited.

Read: `C:\dev\spatial-ide\state\consults\2026-09-17-p3b-re-scope.md` (the model, reused not reinvented); the three reports `…\reports\032335-REV-FAIL.md`, `…\reports\031813-REV-FAIL.md`, `…\reports\032186-OTHER.md`; in the worktree `C:\dev\spatial-ide\.claude\worktrees\agent-a3073bf516424356e` — `engine\LOD-PREREGISTRATION.md` §10 whole (`:334-561`), `engine\tests\lod_tier_builder.rs:95-224`, `engine\tests\common\mod.rs:40-97`, `DECISIONS-PENDING.md` rounds 10-13; `C:\dev\spatial-ide\docs\PREREGISTRATION-TEMPLATE.md:88-142`; `C:\dev\spatial-ide\.claude\agents\worker.md:24`.

---

## 1. ROOT CAUSE

This record's instance of the mechanic is the same self-falsification P3b died of, one turn tighter: the fix commit `d6073d8` inserted two lines under §10's header (`:336-337`) — a "read the last amendment first" pointer placed there because clause (e) sends that phrase to a P6 sight list this document does not have — and that insertion moved every amendment below it by two lines, so every bare `:NNN` Amendment 12 wrote into its own file (items (b), (c), (d), (f), (g) and its superseded index, written against the pre-insertion numbering) is stale at its own commit, and the same shift silently staled four cites inside the immutable Amendments 10-11 that the amendment does not record. Around that core sit three defects of one family: an unmarked reproduction of the template's class-4 definition that drops the source's closing condition and renders it as an unconditional alternative (Amendment 12 (g)); a self-disposition of items (c)-(g)'s class attributed to round 13 item 1, which says nothing about classes, where the template's own §10 requires a misfit to be routed to the human; and two index rows that name the corrected span instead of the superseded one. The one governing constraint the closing amendment obeys is the P3b consult's rule, stated in full at `C:\dev\spatial-ide\state\consults\2026-09-17-p3b-re-scope.md` §1 (closing sentence) and not re-typed here: no bare `:line` into a file the same commit edits, self-references by section/amendment/item, and any reference that must carry a line pinned `path:line @ <commit> sha256:<hex>` at a commit the citing commit does not create. Applied here it yields one closing amendment, append-only at EOF with zero deletions in the preregistration, no line cite into this document at all, and no reproduced text — so the verbatim rule, the elision rule and clause (b)'s rider have nothing to bite on.

Two decisions this record forces that P3b did not:

- **The pointer stays where `d6073d8` put it, recorded.** Moving it below the last amendment would delete committed §10 text (append-only forbids it and it forfeits the gate-checkable zero-deletions proof), and it would renumber the section a third time — silently restoring a3f5f2e's numbering under three gate reports that resolved against d6073d8's. The defect is not the pointer's position but cites written by line; Amendment 13 retires those by construction, so the placement stops mattering.
- **The closing commit may touch two spans of `engine/tests/lod_tier_builder.rs`, and only under line-count neutrality.** Test source is ordinary source, expressly outside §10's append-only rule (Amendments 11 and 12 both say so and both edited it), and round 13 item 1 refused in terms to treat the mutation record as an owed nit — so leaving the second pin owed contradicts the ruling that authorized this round. But any insertion into that file moves `:254`, `:205-212`, `:440`, `:492`, `:503`, `:921`, `:512-517` — all cited from Amendments 8 and 12, which cannot be edited. Hence: every span replaced by exactly as many lines as it had, file line count identical before and after, `engine/tests/common/mod.rs` not touched at all.

---

## 2. THE CLOSING AMENDMENT

Byte-copy as-is. House style here is em dash `—`, `§`, straight apostrophes (this document's, unlike P3b's). Nine `<COMPUTE: …>` tokens carry their own commands; each is replaced by the bare output (12 hex for `rev-parse`, 64 hex for `sha256sum` with the trailing ` *-` stripped). Append at EOF of `engine/LOD-PREREGISTRATION.md`; nothing above it moves.

```markdown

**Amendment 13 — the closing state-of-the-record list. Class 3, class 4 and class 1 (Classes used, below). Written 2026-09-17 by a fresh `engine/lod-tier-builder` closing worker on the architect's re-scope under round 13, item 1, AFTER the third re-read's findings were seen.** §10's append-only rule, honoured in this line. **Read this amendment first**: it is the single resolution point for every earlier amendment in this section, and it supersedes Amendment 12's superseded index in whole.

**Why this form, and what it is not.** Round 13, item 1 provides that a further record FAIL holds PR #84 for the architect's re-scope of the record, as for P3b; the third re-read failed on the record (`state/gate-log.json` record 63, on main), and this is that re-scope rather than a fourth correction. The mechanic that failed three re-reads is one — a correction that carries a `:line` into the file its own commit edits is falsified by that commit — so this amendment carries **no line cite into this document at all**: self-references are by amendment and item, rulings by round and item (round 12, item 1 (a)), and every cross-file reference is `path:line @ <commit> sha256:<hex>` at a commit this commit does not create (round 12, item 1 (b)). Where a row must name a defective cite in order to identify it, the cell marks it **retired form**.

**What this commit does to the tree.** It appends this amendment below the last line of §10 and changes no other byte of this document, so no line in it moves and no cite in it is falsified by this commit — proof: `git diff --numstat d6073d8..HEAD -- engine/LOD-PREREGISTRATION.md` shows zero deletions. It also corrects three spans of `engine/tests/lod_tier_builder.rs` (ordinary test source, expressly not subject to this section's append-only rule), each replaced by exactly as many lines as it had, so no line number in that file moves either and every cite into it from Amendments 8-12 still resolves — proof: that file's line count is identical before and after, and its diff shows equal insertions and deletions. No file under `engine/src/` differs from `8d182a0`, `engine/tests/common/mod.rs` is not touched, and no measured figure is written into this document by this amendment.

**Classes used.** **Class 3** for every row that only corrects where a reference points. **Class 4** for the mutation-record row — a mutation record corrected after a gate finding, not re-run and said unit-only, which is the case `docs/PREREGISTRATION-TEMPLATE.md:113-116` @ <COMPUTE: git rev-parse --short=12 d6073d8> sha256:<COMPUTE: git show d6073d8:docs/PREREGISTRATION-TEMPLATE.md | sed -n '113,116p' | sha256sum> provides for. **Class 1** for every row that withdraws, supersedes or narrows a claim, on the stated reading that a gate round's findings are this round's results and such a row records what they invalidate — the reading is stated, not settled, and is routed as gap 2. Three things fit no pre-declared class and are routed to the human rather than forced into one: gaps 1, 2 and 3.

**The fence, applied to this amendment's own words** (round 7, item 1): every "stands" / "corrected" / "withdrawn" / "owed" clause below names the reference, the commit, the amendment item or the gate resolution that proves it.

**Scope of the no-line rule.** It governs from this amendment forward. An earlier self-line cite that still resolves stands as written and is not listed here; a pinned reference is stable under any later insertion and the rule does not touch it.

**The state of the record, by amendment and item.**

| id | status | what governs now |
| --- | --- | --- |
| Amendment 12's "Superseded as of this amendment" index, all eleven rows | **superseded in whole** | Every row carrying a parenthetical line into this document names a line from before the two lines the same commit inserted under §10's header, and rows 1 and 10 name the corrected span rather than the superseded one. This table replaces that index and carries no line into this document. Proof: the insertion and the index are in one commit, d6073d8; the two span corrections are their own rows below. |
| Amendment 12 (b), (c), (d), (f), (g) and its index rows — every bare line cite into this document | **retired form; the item label beside each governs** | Retired forms and what each names: `:491` → Amendment 10 (b); `:493` → Amendment 10 (c); `:501` → Amendment 11's first line; `:507` → Amendment 11 (b), the paragraph carrying the kernel-harness claim; `:511` → Amendment 11 (d). Each is two lines short at its own commit, because that commit inserted two lines under §10's header above every amendment. No count of the occurrences is asserted, and none is needed once the reference is by item. |
| Amendment 11 (a), (b) and (f)'s parenthetical cites of Amendment 10 | **retired form; the item labels govern** | The same insertion staled them inside text this section forbids editing: the retired forms above, plus `:489` → Amendment 10 (a) and `:495` → Amendment 10 (d). Amendment 11 is not edited; this row is the correction. Proof: each retired form's own item label is written beside it there and resolves without a line. |
| The two lines under §10's header, inserted by d6073d8 | **recorded; they stay where they are** | They are an addition, not an edit — no committed byte of any amendment was rewritten — but an insertion mid-section is not an append, which is §10's declared shape, and its whole observable effect is the two-line shift the rows above retire. They stay: removing them would delete committed §10 text, forfeit the zero-deletions proof of append-only, and renumber this section a third time. Proof: `git diff --numstat 77d3408..d6073d8 -- engine/LOD-PREREGISTRATION.md` shows zero deletions — one insertion hunk at the section header and one append. |
| Amendment 12's closing paragraph, its clause about §§1-9 and Amendments 1-11's own text | **stands** | It is true of those bytes: that round rewrote no line of §§1-9 and no line of any amendment. What it does not say is that the same commit inserted two lines under §10's header, which the row above supplies. Proof: that commit's diff of this file, one insertion hunk and one append. |
| Amendment 12 (g)'s reproduction of class 4's definition | **withdrawn as a quotation; the reference governs** | It reproduced the source unmarked and rendered the source's closing condition as an unconditional alternative, which round 11, item 3 forbids and round 12, item 1 (b)'s rider makes unnecessary. What governs is `docs/PREREGISTRATION-TEMPLATE.md:113-116` @ <COMPUTE: git rev-parse --short=12 d6073d8> sha256:<COMPUTE: git show d6073d8:docs/PREREGISTRATION-TEMPLATE.md | sed -n '113,116p' | sha256sum>, unreproduced. (g)'s own finding — that only Amendment 11 (g)(iii) is class-4 work and Amendment 11 is otherwise class 3 — is untouched by this withdrawal and stands. |
| Amendment 12's header, its "per round 13 item 1" attribution for the class disposition | **withdrawn** | Round 13, item 1 names this round's work and says nothing about amendment classes. The class question is routed to the human here as gap 1 instead, which is what `docs/PREREGISTRATION-TEMPLATE.md` §10's opening paragraph requires (paraphrase: a class that fits none is a finding routed to the human, never an invented one). Proof: the gate resolves round 13, item 1 against the ledger's RULED block. |
| Amendment 12's header, its finding that items (c)-(g) fit none of the six pre-declared classes | **stands as a finding; its self-disposition withdrawn** | The third re-read confirmed the finding: (a) is class 4 and (b) is class 3, while (c), (e) and (f) each change a claim in test text or in an operator-facing string, (d) supplies an omission from an earlier count, and (g) corrects a class label — none of which the six at `docs/PREREGISTRATION-TEMPLATE.md:101-129` @ <COMPUTE: git rev-parse --short=12 d6073d8> sha256:<COMPUTE: git show d6073d8:docs/PREREGISTRATION-TEMPLATE.md | sed -n '101,129p' | sha256sum> covers. What is withdrawn is naming them in the amendment instead of routing them; gap 1 carries them to the human. |
| Amendment 12 (d) and (g), their length | **over the ceiling, recorded** | Each runs to four sentences against round 12, item 1 (d)'s three, and neither can be shortened in place under this section's append-only rule. Recorded rather than erased; every row of this amendment holds to the ceiling. Proof: the two items' own text, counted where it stands. |
| Amendment 12 (a)'s mutation record, its second pin | **corrected in the test comment by this commit** | The record pins the mutated line at a3f5f2e but observes the failure at 43a3039, where that line is `engine/tests/common/mod.rs:55` @ <COMPUTE: git rev-parse --short=12 43a3039> sha256:<COMPUTE: git show 43a3039:engine/tests/common/mod.rs | sed -n '55p' | sha256sum>. The `RECORDED MUTATION` comment now carries both pins, each naming its commit in twelve hex, and the comment's line count is unchanged. No mutation was re-run and none is claimed — unit-only; proof: the gate recomputes the pinned span at 43a3039. |
| Amendment 12 (c)'s qualifier, and the `#[ignore]` message beside it | **corrected in the message by this commit** | (c) qualified the `FixtureAside` doc comment to the exit paths the process itself unwinds through while the `#[ignore]` message next to it still told an operator the fixture is restored when done — one claim at two strengths in one file. The message now carries the doc comment's qualifier and keeps its machine-wide precondition word for word; its superseded text is `engine/tests/lod_tier_builder.rs:205-212` @ <COMPUTE: git rev-parse --short=12 d6073d8> sha256:<COMPUTE: git show d6073d8:engine/tests/lod_tier_builder.rs | sed -n '205,212p' | sha256sum>. Proof: that span recomputes to the superseded message, and the qualifier is in the message as committed here. |
| Amendment 12's index row 1 | **corrected by reference** | It names the corrected span rather than the superseded one; the superseded `RECORDED MUTATION` wording is `engine/tests/lod_tier_builder.rs:182-197` @ <COMPUTE: git rev-parse --short=12 a3f5f2e> sha256:<COMPUTE: git show a3f5f2e:engine/tests/lod_tier_builder.rs | sed -n '182,197p' | sha256sum>. Proof: the gate recomputes that span. |
| Amendment 12's index row 10 | **corrected by reference** | It names a span that carries the corrected text at this head; the superseded doc-comment wording is `engine/tests/common/mod.rs:52-53` @ <COMPUTE: git rev-parse --short=12 a3f5f2e> sha256:<COMPUTE: git show a3f5f2e:engine/tests/common/mod.rs | sed -n '52,53p' | sha256sum>, which carries no primary-checkout qualifier. Proof: the gate recomputes that span. |
| The nine `sha256:` references this document carries at d6073d8, all in Amendment 12 | **recorded** | They name their commits in seven hex characters, which the quote checker's hash-reference grammar does not match, so that tool reported no hash-reference finding about this document and proves nothing about it. All nine were recomputed by hand in the third re-read and matched; every reference in this amendment names its commit in twelve hex with `sha256:` next to the cite. Proof: that re-read's recomputation, and round 11, item 2 — the check is a floor, not the proof. |
| The proof test's invisibility to `verify-mutation` | **owed**, 2026-09-17, at a named node | `scripts/plan/verify-mutation.mjs` never resolves `two_concurrent_callers_of_an_absent_fixture_both_get_the_complete_file`, because a multi-line `#[ignore = "…` attribute clears its pending-test state, so its PASS has never covered this record. Unchanged by this round and outside its scope; owed at PLAN node `governance-verify-mutation-multiline-attrs`. Proof: the second re-read's finding, and that node's entry in `PLAN.yaml`. |
| Amendment 8 (a) — the Unix free-space counterpart and a macOS/Linux tier root | **owed**, 2026-09-16, with docs/07's macOS/Linux gates | Unchanged; recorded at that item. |
| Amendment 8 (m) — a fourth miss reason for a present-but-altered tier | **owed**, 2026-09-17, before the first consumer reads a miss reason | Unchanged; recorded at that item. |
| Amendment 8 (n) — the derived path checked against the recorded one on reuse | **owed**, 2026-09-17, to the selection piece or a short-form follow-up | Unchanged; recorded at that item. |
| Amendment 9 (h) — `cancel_quiescent` is not instrumented by this piece | **owed**, 2026-09-17 | Unchanged; recorded at that item. |
| Amendment 11 (g)(iv) — the `common` / `support` test-support naming | **noted, not acted on** | Unchanged; recorded at that item, for whoever next reconciles the two crates' naming. |

**Items that simply stand, by id.** Amendments 1, 2, 3, 4, 5, 6, 7 and 9; Amendment 8 apart from (a), (m) and (n); Amendment 10 in whole, corrected only where a row above names one of its cites; Amendment 11 (a)-(h), corrected only where a row above names one of its cites; Amendment 12 (a), (b), (c), (e) and (f) in substance. Nothing in this amendment touches them; the owed items among them are listed above only to date them, not to change them.

**Superseded earlier and unchanged here:** Amendment 5's `LOD_TIER_SET_MAX_BYTES` and its refusal (withdrawn by Amendment 6 on round 6's ruling); Amendment 10 (a)'s attribution to §3 and Amendment 10 (d)'s §3 half (withdrawn by Amendment 11 (a) and (f)); Amendment 10 (b) and (c)'s "proven" for the atomic-rename half (re-scoped by Amendment 11 (b)); Amendment 11's class-4 label for its non-(g)(iii) content (re-read as class 3 by Amendment 12 (g)).

**Nits not taken, named.** The third re-read's nits 1-3 and the second re-read's nits 1-4 are not taken in this round, and nothing here claims them.

**Gaps routed to the human, named and not forced** (`docs/PREREGISTRATION-TEMPLATE.md` §10's opening paragraph instructs that a class fitting none is a finding routed to the human, never an invented one — paraphrase, not a quotation).

1. **A class for a record correction that changes a claim in test text.** Amendment 12 (c), (e) and (f), and this amendment's mutation-record and `#[ignore]`-message rows, correct claims that live in test comments and in an operator-facing string; class 3 never changes a claim and no other pre-declared class covers them. Nothing is assigned here — whether these are class 3 with a named exception for test text, or a new class, is the human's.
2. **The class of a withdrawal in a record-correction round.** A row that withdraws a count, an attribution or a false clause is not class 3, and it narrows no ruling, so class 5 does not fit; this amendment uses class 1 on the reading stated above, and the reading is the human's to confirm.
3. **Where this document's "read the last amendment first" pointer lives.** Clause (e) sends it to the P6 sight list; this document has none — the only sight list it names is the P6 string-sight list at Amendment 8 (i)7, which is for operator-visible wording. The pointer sits under §10's header where d6073d8 put it, recorded above, and at this amendment's head and foot; whether §9 gains a sight list at P6 is the human's.

**Read this amendment first.** §10's amendments correct each other only by a later one; this is the last, and it is where any earlier amendment's current state is written.
```

---

## 3. WHAT THE WORKER DOES

Fresh default-model worker, no judgment calls. All paths absolute; worktree `C:\dev\spatial-ide\.claude\worktrees\agent-a3073bf516424356e`. Bash tool for the POSIX pipelines. **If any assertion fails, STOP and report — never adjust the amendment text or invent a line number.**

1. **Start state.** `git -C <worktree> rev-parse HEAD` = `d6073d8…`; `git status --porcelain` empty; `git diff --stat 8d182a0..HEAD -- engine/src` prints nothing (rc 0). Record all three.
2. **Expand the three commits, once each, pasting the output:** `git rev-parse --short=12 d6073d8`, `… a3f5f2e`, `… 43a3039`.
3. **Compute the six hashes, once each, pasting each command's output:**
   - `git show d6073d8:docs/PREREGISTRATION-TEMPLATE.md | sed -n '113,116p' | sha256sum`
   - `git show d6073d8:docs/PREREGISTRATION-TEMPLATE.md | sed -n '101,129p' | sha256sum`
   - `git show 43a3039:engine/tests/common/mod.rs | sed -n '55p' | sha256sum`
   - `git show a3f5f2e:engine/tests/lod_tier_builder.rs | sed -n '182,197p' | sha256sum`
   - `git show a3f5f2e:engine/tests/common/mod.rs | sed -n '52,53p' | sha256sum`
   - `git show d6073d8:engine/tests/lod_tier_builder.rs | sed -n '205,212p' | sha256sum`
4. **Verify each hashed span says what the text says** (print and read): `113,116p` is class 4 and its closing sentence is conditional; `101,129p` lists six numbered classes, 1 through 6; `common/mod.rs:55 @ 43a3039` contains `GENERATE.lock().expect("lock")`; `lod_tier_builder.rs:182,197p @ a3f5f2e` starts with `// RECORDED MUTATION:` and contains the quoted lock line; `common/mod.rs:52,53p @ a3f5f2e` does **not** contain `primary checkout`; `lod_tier_builder.rs:205,212p @ d6073d8` contains `restores it when done`. Any mismatch: STOP. **Cross-check:** the `113,116p` digest must equal `20984e79f196cfddd1d63032182f828a92fb66f8e43767c574219c27243dadfb` (Amendment 12 (g)'s value, computed on main); if it differs, STOP and report.
5. **Assert the node exists:** `grep -c 'governance-verify-mutation-multiline-attrs' PLAN.yaml` ≥ 1. If 0, STOP and report.
6. **Anchor the three spans to be replaced** (print each, all must match exactly): `sed -n '184p'` begins `// RECORDED MUTATION: in`; `sed -n '190p'` begins ``// at `engine/tests/lod_tier_builder.rs:187:5```; `sed -n '198p'` begins `// The size and sha256 assertions`; `sed -n '203p'` begins `// Amendment 12(a)); not re-run`; `sed -n '205p'` begins `#[ignore = "moves the one`; `sed -n '206p'` contains `restores it when done`; `sed -n '207p'` is exactly `            machine may read \`; `sed -n '213p'` is `fn two_concurrent_callers_of_an_absent_fixture_both_get_the_complete_file() {`; `sed -n '254p'` is `    assert_eq!(`. Any mismatch: STOP.
7. **Write four temp files by byte-copy from this brief, never retyped:** `$SCRATCH/a13.md` (§2's block), `$SCRATCH/mut-head.txt`, `$SCRATCH/mut-tail.txt`, `$SCRATCH/ignore.txt` (the three below).

   `$SCRATCH/mut-head.txt` — **9 lines**, replaces lines 184-190:
   ```
   // RECORDED MUTATION: in `engine/tests/common/mod.rs`, remove the lock-acquisition line —
   // `engine/tests/common/mod.rs:68` @ <COMPUTE: git rev-parse --short=12 a3f5f2e>
   // sha256:7ab72429b281ae83ac58e4a5b2aaa25bb45a697c076196a775f62fb33848923f — from `polygons_100k`
   // (the temporary-path write, the generation counter and the rename are all left exactly as they
   // are) → two_concurrent_callers_of_an_absent_fixture_both_get_the_complete_file fails on its last
   // assertion. **Observed at commit 43a3039**, fixture deleted first. At that commit the line was
   // `engine/tests/common/mod.rs:55` @ <COMPUTE: git rev-parse --short=12 43a3039>
   // sha256:<COMPUTE: git show 43a3039:engine/tests/common/mod.rs | sed -n '55p' | sha256sum> — the assertion sat at
   // `engine/tests/lod_tier_builder.rs:187:5` (this file's line numbers at that commit):
   ```

   `$SCRATCH/mut-tail.txt` — **4 lines**, replaces lines 198-203:
   ```
   // The size and sha256 assertions above it still passed in that run — on this machine's NTFS two
   // threads writing the *same deterministic* bytes to one temporary name did not corrupt it. That
   // is why this test does not rely on corruption: it is `generations_performed()`'s count, not the
   // file's content, that only the lock keeps at one. Not re-run at this commit; unit-only.
   ```

   `$SCRATCH/ignore.txt` — **2 lines**, replaces lines 206-207 (note the trailing space before each `\`, which is the string's own space; preserve the 12-space indent):
   ```
               suites) shares aside and restores it on exit paths the process unwinds through; no \
               other cargo test anywhere on this machine may read \
   ```
8. **Apply the three replacements bottom-up** (so earlier line numbers stay valid), each by line number with `awk`/`sed`, never by text match: first 206-207 ← `ignore.txt`, then 198-203 ← `mut-tail.txt`, then 184-190 ← `mut-head.txt`.
9. **Append the amendment.** Ensure `engine/LOD-PREREGISTRATION.md` ends in a newline, then `cat "$SCRATCH/a13.md" >> engine/LOD-PREREGISTRATION.md` (heredoc appends parse-fail on long markdown; use the temp-file route). Nothing is inserted above existing text.
10. **Substitute the placeholders by script** — replace each whole `<COMPUTE: …>` token with its bare output (12 hex, or the 64-hex digest with ` *-` stripped) in the preregistration and in `engine/tests/lod_tier_builder.rs`; then assert `grep -c '<COMPUTE' engine/LOD-PREREGISTRATION.md engine/tests/lod_tier_builder.rs` = 0 in both.
11. **Self-check, line neutrality.** `wc -l < engine/tests/lod_tier_builder.rs` equals `git show d6073d8:engine/tests/lod_tier_builder.rs | wc -l`; `sed -n '213p'` and `sed -n '254p'` are unchanged (step 6's values); `sed -n '184p'` begins `// RECORDED MUTATION: in`; `sed -n '203p'` ends `unit-only.`; `git diff --numstat d6073d8 -- engine/tests/lod_tier_builder.rs` shows **equal** insertions and deletions. Paste all outputs.
12. **Self-check, append-only.** `git diff --numstat d6073d8 -- engine/LOD-PREREGISTRATION.md` = `N 0` (zero deletions). `git diff --name-only d6073d8` lists exactly `engine/LOD-PREREGISTRATION.md` and `engine/tests/lod_tier_builder.rs`. `git diff --stat 8d182a0 -- engine/src` prints nothing, rc 0.
13. **Self-check, no bare line cites in the new text.** `sed -n '/^\*\*Amendment 13 —/,$p' engine/LOD-PREREGISTRATION.md | grep -nE ':[0-9]+'` — paste the output. Every hit must be (i) inside a pinned `path:line @ <12-hex> sha256:` reference, or (ii) in a cell that says **retired form**. Anything else: STOP and report.
14. **Self-check, the message renders as intended.** `grep -n 'exit paths the process unwinds through' engine/tests/lod_tier_builder.rs` = 1 hit (the `#[ignore]` string); `grep -n 'exit path the process itself unwinds through' …` = 1 hit (the doc comment); `grep -c 'restores it when done' …` = 0; `grep -c 'anywhere on this machine may read' …` = 1.
15. **Build and run once.** `cargo build --tests -p spatial-engine` → rc 0, **zero warnings**. `cargo test -p spatial-engine --test lod_tier_builder` (no `--ignored`) → rc 0, expect `14 passed; 0 failed; 2 ignored` (~500 s). Run no `#[ignore]`d test.
16. **Mechanical suites, gated on exit code** (print `rc`, continue only on 0): `node scripts/plan/verify-cites.mjs`, `node scripts/plan/verify-test-claims.mjs`, `node scripts/plan/verify-mutation.mjs`.
17. **Quote checker**, as the prior rounds ran it: scratch copy of `origin/governance/verify-quotes`, `repoRoot` = the worktree, worktree never written to, over the two changed files. Assert **zero findings at or above Amendment 13** — the new text reproduces nothing, so any finding there is a defect — and that the only finding in the document remains the pre-existing `engine/LOD-PREREGISTRATION.md:380` one. Delete the scratch copy; `git status --porcelain` must be clean of it.
18. **Fixture integrity after the run:** size 151,812,642, `certutil -hashfile … SHA256` = `9ecd79242ac7d99e09f1989c8c124fd53dcd697689546ec6013949f806ca6043`, and no `.tmp` / `.aside.*` in `C:\dev\spatial-ide\target\fixtures\slice-budgets\`.
19. **Commit once, both files, signed off** — `git commit -s -F "$SCRATCH/msg.txt"`:
    ```
    docs(lod): closing state-of-the-record amendment -- §10 Amendment 13

    The architect's re-scope of the LOD tier builder's fix record under round 13,
    item 1: one closing amendment replacing further correction rounds. No line cite
    into this document, no ledger line cite, no reproduced text; self-references by
    amendment and item, rulings by round and item, everything else pinned
    path:line @ commit sha256 at a commit this commit does not create. The two
    lines d6073d8 inserted under §10's header stay where they are, recorded with
    the two-line shift they caused; every cite they staled is retired by item.
    Two spans of engine/tests/lod_tier_builder.rs are corrected -- the mutation
    record's second pin at 43a3039 and the #[ignore] message's missing qualifier --
    each replaced by exactly as many lines as it had, so no line number moves.

    verify-cites rc=0 | verify-test-claims rc=0 | verify-mutation rc=0 | zero
    deletions in engine/LOD-PREREGISTRATION.md | engine/src unchanged since
    8d182a0 | lod_tier_builder 14 passed, 0 failed, 2 ignored.

    Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
    Claude-Session: https://claude.ai/code/session_01JeU7h98xrTorztiep3Zgmg
    ```
20. **Push** `engine/lod-tier-builder` (PR #84 updates). Report `git status --porcelain` (clean), the three revs, the six hashes, every self-check output, the model observed and any override. **Open no PR.** Nothing else is in scope — no other file, no `engine/src`, no further amendment, no nit.

---

## 4. FOR THE HUMAN'S RULING — only what differs from the P3b consult's questions

1. **Does the pointer's placement stand — recorded where `d6073d8` put it, rather than moved below the last amendment?** Recommended: it stands, because moving it deletes committed §10 text, forfeits the zero-deletions proof of append-only and renumbers the section a third time under three gate reports that resolved against d6073d8.
2. **Where does this document's "read the last amendment first" live, given it has no P6 sight list?** Recommended: at the closing amendment's head and foot plus the recorded §10-header line, with any §9 sight list deferred to P6 as this piece's operator surface is owed to the selection piece (the same answer as P3b's engine gap 4).
3. **May a closing re-scope commit correct test-comment and `#[ignore]`-string text in the same commit, under line-count neutrality, or are those owed?** Recommended: it may, and this one does — round 13 item 1 refused in terms to treat the mutation record as an owed nit, test source is expressly outside §10's append-only rule, and neutrality is what keeps every immutable cite into that file resolving.
4. **What class covers a record correction that changes a claim living in test text or an operator-facing string (gap 1)?** Recommended: rule it class 3 with a named exception for test text, or declare a new class — today it fits none, and Amendment 12's self-disposition of exactly this question is what the third re-read blocked on.

No ADR skeleton: these are governance clauses landing by ledger ruling into `docs/PREREGISTRATION-TEMPLATE.md` and the gate checklists, not by ADR. Nothing in this diff touches docs/01, docs/02, docs/08, docs/10 or ADR-006; no seam, no `pub` item, no perf claim, no CRS assumption; the only operator-visible string changed is a test `#[ignore]` reason, corrected toward the weaker, true claim; `engine/src` has been frozen since `8d182a0`.

Relevant absolute paths: `C:\dev\spatial-ide\.claude\worktrees\agent-a3073bf516424356e\engine\LOD-PREREGISTRATION.md`, `…\engine\tests\lod_tier_builder.rs`, `…\engine\tests\common\mod.rs`, `…\DECISIONS-PENDING.md`, `C:\dev\spatial-ide\docs\PREREGISTRATION-TEMPLATE.md`, `C:\dev\spatial-ide\state\consults\2026-09-17-p3b-re-scope.md`.
