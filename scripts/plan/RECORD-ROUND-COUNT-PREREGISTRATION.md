# Preregistration (five-line short form, `AUTONOMY.md` §21d) — record-round count per piece

Authority: `PLAN.yaml` node `governance-record-round-count`; `state/directives/2026-09-18-record-cap.md:5 @ cc58d933c530 sha256:e44168d98d83bff75fc1155dd8b1f94b4ac077e113c8deabacaa93fd2622ef58` (point 4).
Scope: `scripts/plan/health.mjs`, `scripts/plan/health.test.mjs`, `scripts/plan/site.mjs`, `scripts/plan/site.test.mjs`, `state/gate-log.json` (5 files); §21c figure 135 non-generated lines (insertions + deletions, this file and `site/` excluded), within the <=150/<=8 threshold.
Change: `state/gate-log.json` gains `record: true` on the 21 records that gated a record-correction round (`briefa-p3b-owner-side-invalidation` attempts 2-6, `lod-tier-builder-route-b` attempts 3-6, `governance-verify-quotes` attempts 2-4); `health.mjs`'s `recordRoundCounts` derives, per node, the count of distinct such attempts; `site.mjs` renders a "Record rounds per piece" line beside the gate first-pass line, naming each non-zero node, the total, and the zero target.
Tests+mutation: `recordRoundCounts: an attempt gated twice (both gates record: true) counts once for its node` and `recordRoundCounts: a node whose gate records carry no record: true field counts zero (absent from byNode)` (`scripts/plan/health.test.mjs`); `the record-rounds line lists each non-zero node and the total, with the target stated as zero` (`scripts/plan/site.test.mjs`) — each carries its own `RECORDED MUTATION` comment naming the mutation applied and the failure observed, hand-reverted after. Two existing tests (the key-list assertion in `health.test.mjs`, the absent-gate-log row in `site.test.mjs`) updated to include `record_rounds`; no new mutation, no new behaviour.
Out-of-scope: no ADR, no security posture (ADR-009/020/021), no SKP/MCP wire or MCP-adapter surface, no stated guarantee or invariant (ADR-006/018, copy-minimisation, CRS-is-a-type) touched.
