# Preregistration (five-line short form, `AUTONOMY.md` §21d) — verify:test-claims, superseded claims

Authority: the human's round 14, item 2 (clause (a′) and the root-cause rule — a pinned `path:line @ <commit> sha256:<hex>` reference is a historical pin, never silently read as current); `state/directives/2026-09-18-record-cap.md` (the record cap); `scripts/plan/verify-test-claims.mjs` (the check this piece extends).
Scope: `scripts/plan/verify-test-claims.mjs`, `scripts/plan/verify-test-claims.test.mjs`, `scripts/plan/README.md` (one paragraph), and this file; <= 120 non-generated lines across <= 4 files.
Change: a claimed test name found at line L of file F is reported SUPERSEDED (advisory, never a binding finding) when F itself contains a line that (a) carries a hash reference to `F:L` (or a range containing L) at some `@ <rev>`, (b) contains the word `superseded` outside its backticks, and (c) the hash recomputes over `git show <rev>:F`'s lines a..b, LF bytes — so a marker cannot be forged without the historical bytes; the superseded set is printed under its own heading, the same way the planned set is.
Tests+mutation: (i) a superseded claim with a valid pin is advisory (mutation: drop the superseded check in `runVerifyTestClaims`, routing every such claim to `findings`); (ii) the same row with a wrong hash does NOT exempt (mutation: skip the hash-recompute comparison, so any pinned row exempts regardless of its hash); (iii) a row without the word `superseded` does not exempt (mutation: drop the `containsSupersededOutsideBackticks` condition, exempting on the hash-and-line match alone); (iv) a pin to a different line does not exempt (mutation: drop the line-in-range check, exempting a claim anywhere in the file once any pin verifies) — each mutation run and reverted, the observed failure recorded in a `RECORDED MUTATION` comment above its test.
Out-of-scope: no ADR touched, no security posture, no wire/data-plane change, no stated guarantee changed — the recognizer's existing claim/existence logic is untouched, and no gate verdict is weakened for a landed piece except the three specific historical spans a valid, hash-checked pin names.
Amendment: (class 6, budget deviation, Scope not edited) declared <= 120 non-generated lines across <= 4 files; achieved 231 (226 insertions + 5 deletions, `git diff --stat` over `scripts/plan/verify-test-claims.mjs` + `scripts/plan/verify-test-claims.test.mjs` + `scripts/plan/README.md`) across 3 files. This crosses §21c's own 150-line bound, so per class 6 (`docs/PREREGISTRATION-TEMPLATE.md` §10, class 6): the single-gate route closes and the architect gate is taken in addition to the reviewer; the short form stays, unedited, as this piece's record. Reason for the overrun: the module's own disclosure-heavy comment convention (every new function carries a `verify-quotes.mjs`-style rationale comment, matching the file's existing style) plus four required RECORDED MUTATION tests, each needing its own git-history fixture (a two-commit temp repo, ~35 lines shared by all four) to exercise condition (c)'s `git show`-based hash recomputation — none of which compresses below the threshold without cutting disclosed reasoning the codebase's own convention requires.
Amendment: (class 4, scope statement corrected, one line, written after both gates' attempt-1 findings — architect gate B3) the `Out-of-scope` line's "no gate verdict is weakened for a landed piece except the three specific historical spans a valid, hash-checked pin names" is false. Corrected reading: the check's binding disposition is narrowed generally, for every scanned preregistration or ADR whose text carries a matching pin, not only the three spans this piece's own motivating case happens to exercise; §21a's ADR/security/wire/guarantee categories remain untouched. `Scope` and `Out-of-scope` lines unedited.
Amendment: (class 1, post-result, naming the seven tests by exact name, per §21d "by name" — architect gate S6) `Tests+mutation`'s original four, and this round's three additions: `a_superseded_claim_with_a_valid_pin_is_advisory_not_a_failure`, `a_superseded_pin_with_the_wrong_hash_does_not_exempt`, `a_pin_without_the_word_superseded_does_not_exempt`, `a_pin_to_a_different_line_does_not_exempt`, `a_pin_whose_span_lacks_the_claimed_name_does_not_exempt` (condition (d)), `a_pin_with_no_rev_does_not_exempt` (condition (e)'s rev policy), `a_pin_with_the_word_superseded_only_in_a_double_backtick_span_does_not_exempt` (condition (b)'s double-backtick fix). `Tests+mutation` line itself unedited.
Amendment: (class 2, deviation recorded after results, with the reason — round 15(a)'s resolution step, run against both gate reports in full before this commit) attempt 1 FAILED both gates (`state/gate-log.json`, this node's attempt-1 records): architect B1 (no proof the pinned span named the claim) → condition (d) added, `slice.includes(name)`; architect B2 / reviewer B1 (HEAD-default and non-commit `<rev>` accepted) → `COMMIT_ID_RE` guard, no default; reviewer/architect S3 → condition (e), `git merge-base --is-ancestor <rev> origin/main`, skipped-not-failed when `origin/main` does not resolve; reviewer S1 → `BACKTICK_SPAN_RE` (backreferenced delimiter length) replaces the empty-pair-only regex; reviewer S2 / architect S1 → `HASH_REF_RE` rebuilt from `verify-quotes.mjs`'s own `GAP` as it stands on `governance/verify-quotes` @ 1254cddd4c3b47c9431375874ad327754ef038e9, `linesWithLF`'s `b < a` rejection restored; reviewer S5 → `gitShowFile` memoized by (root, rev, relPath); architect B5 / reviewer S4 → the three helper `export`s dropped (no caller outside the module); reviewer S6 → the unattributed quotation in `runVerifyTestClaims`'s docstring removed; reviewer B2 → the three fabricated RECORDED MUTATION comments replaced with re-captured, actually-observed text, each mutation re-run and reverted; architect B6 → the fixture's appended row reshaped as a real markdown table row, byte-alike in SHAPE to `OWNER-INVALIDATION-PREREGISTRATION.md:1195`. Discharged: every "fixed" item above is proven by its own named test (see the previous Amendment) and by the checks recorded in this node's evidence.
Amendment: (class 6, budget deviation, Scope not edited — this round's own figure, on top of the prior Amendment's 231/3) declared budget unchanged (the Scope line is append-only); achieved, cumulative against `origin/main` (`git diff --stat origin/main -- scripts/plan/verify-test-claims.mjs scripts/plan/verify-test-claims.test.mjs scripts/plan/README.md`): 469 (463 insertions + 6 deletions) across the same 3 files; this correction round's own delta over the prior commit (`3abfa90`) is 416 (326 insertions + 90 deletions). `Out-of-scope` still holds under its corrected reading above. `needs_human` set to `{kind: click, minutes: 2}` (architect gate S5) — the merge is the human's click; `scripts/plan/docsOnly.mjs` does not apply (`*.mjs` code paths touched).
Amendment: (class 1, post-result — round 19, item 2, the round-16-item-4 hold lifted, one round under a fresh count and the record cap; `state/gate-log.json`, this node's attempt-2 architect and reviewer records) reviewer attempt-2's four blockers and architect attempt-2's two residual items, closed: (a) condition (e)'s REFUSED branch had no test — discharged by `a_pin_whose_rev_is_not_an_ancestor_of_origin_main_does_not_exempt` (`scripts/plan/verify-test-claims.test.mjs`), RECORDED MUTATION run and reverted. (b) `42342f5` and `3100766` edited the previous Amendment's committed lines in place — reverted at `6499190` and `107d37c` (round 15(f)'s unmerged-append revert), proved append-only against base commit `59986c7` (the commit that first, unedited, introduced the prior Amendment); the corrected figures are re-carried below, appended, not edited. (c) two cites resolved at no named rev — `verify-test-claims.test.mjs:190` now names `frontends/shell/OWNER-INVALIDATION-PREREGISTRATION.md:1191 @ b14993192c8769113ddd630ab0d49d6c8ec2c897 sha256:72456eb51e3018d88b8583268fdd52b0cf4f4f263470839cf353362377836c6c` on `main` (superseding the prior Amendment's own `:1195` mention, a line that existed only at `cut/briefa-p3b-test-names`'s moving tip, never named a commit); `verify-test-claims.mjs`'s comment on `verify-quotes.mjs`'s `GAP`/`HASH_REF_RE` lines is corrected 553/554 → 554/555, and its `verify-cites.mjs` behaviour statement now names that tool's own commit, `7104cd3a2f961e6e1fa0073c1df77f9d3dea2f7f` on `main` (round 15(c)). (d) architect attempt-2: the fourth Amendment above (the Out-of-scope correction) is mis-classed class 4; corrected reading, class 1 (`docs/PREREGISTRATION-TEMPLATE.md` §10) — its text and class-4 label stand unedited (append-only); this line is the correction. `AUTONOMY.md:163`'s §6a sentence now also names condition (e) (the `<rev>`-ancestor-of-`origin/main` clause), five words appended on the same line, no cited line moved.
Amendment: (class 6, budget deviation, Scope not edited — this round's own figure, on top of the prior Amendment's 469/3) declared budget unchanged; achieved, cumulative against `origin/main` (`git diff --numstat origin/main -- scripts/plan/verify-test-claims.mjs scripts/plan/verify-test-claims.test.mjs scripts/plan/README.md`): 535 insertions + 6 deletions (541) across the same 3 files; this round's own delta over the prior commit (`3100766`) is 76 insertions + 7 deletions (83) across `verify-test-claims.mjs` and `verify-test-claims.test.mjs` (`README.md` unchanged this round). `Out-of-scope` still holds under the corrected reading above. `needs_human` unchanged: `{kind: click, minutes: 2}`.
Amendment: (class 1, post-result — the record cap's point (3), the architect's reduction; `state/gate-log.json` indices 171 and 172, this node's fresh-count attempt-1 architect and reviewer records) Superseded index, read this line first. The second Amendment's class → class 1 (sixth Amendment, item (d)). The third Amendment's condition-(e) label on `a_pin_with_no_rev_does_not_exempt` → the `COMMIT_ID_RE` rev guard in `supersededSpans`; condition (e) → `a_pin_whose_rev_is_not_an_ancestor_of_origin_main_does_not_exempt`. The fourth Amendment's Discharged clause → per item: condition (d) `a_pin_whose_span_lacks_the_claimed_name_does_not_exempt`; the rev guard `a_pin_with_no_rev_does_not_exempt`; condition (e) as above; the backtick span `a_pin_with_the_word_superseded_only_in_a_double_backtick_span_does_not_exempt`; the grammar derivation, the memoization, the dropped exports, the removed docstring quotation and the re-captured mutation comments → `state/gate-log.json` indices 86 and 87; the `b < a` rejection → `linesWithLF` itself. The fourth Amendment's P3b-record line cite → the sixth Amendment, item (c), where "the prior Amendment" reads "the fourth Amendment". The fifth Amendment's figures → the seventh Amendment. The sixth Amendment: item (a) is class 4, its observed failure the RECORDED MUTATION comment above that test; item (b)'s base commit reads 2c69c0b, not 59986c7, and its pairing reads 6499190 reverts 3100766 and 107d37c reverts 42342f5, proof `state/gate-log.json` index 172; item (c) is class 3, its superseded spans the ones `state/gate-log.json` index 87 names; its two bare line cites → `AUTONOMY.md` §6a item 3 and the fixture comment above `SUPERSEDED_DOC` in `scripts/plan/verify-test-claims.test.mjs`; its closing word count is withdrawn, the diff of §6a item 3 is the proof. Re-carried under round 15, item 1, clause (f): the class-3 row 42342f5 appended and 107d37c removed → the comment above `GAP` and `HASH_REF_RE` in `scripts/plan/verify-test-claims.mjs` is that correction and carries its reason; the reason's not-tracked premise is superseded by 6191a7c. Code-comment readings: the fixture comment's `unmerged` for 46cde2c → an ancestor of main, its defect the line only; `entry 133` where the comments name the attempt-2 reviewer gate → `state/gate-log.json` index 87.
