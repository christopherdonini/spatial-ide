# Preregistration (five-line short form, `AUTONOMY.md` §21d) — verify:test-claims, superseded claims

Authority: the human's round 14, item 2 (clause (a′) and the root-cause rule — a pinned `path:line @ <commit> sha256:<hex>` reference is a historical pin, never silently read as current); `state/directives/2026-09-18-record-cap.md` (the record cap); `scripts/plan/verify-test-claims.mjs` (the check this piece extends).
Scope: `scripts/plan/verify-test-claims.mjs`, `scripts/plan/verify-test-claims.test.mjs`, `scripts/plan/README.md` (one paragraph), and this file; <= 120 non-generated lines across <= 4 files.
Change: a claimed test name found at line L of file F is reported SUPERSEDED (advisory, never a binding finding) when F itself contains a line that (a) carries a hash reference to `F:L` (or a range containing L) at some `@ <rev>`, (b) contains the word `superseded` outside its backticks, and (c) the hash recomputes over `git show <rev>:F`'s lines a..b, LF bytes — so a marker cannot be forged without the historical bytes; the superseded set is printed under its own heading, the same way the planned set is.
Tests+mutation: (i) a superseded claim with a valid pin is advisory (mutation: drop the superseded check in `runVerifyTestClaims`, routing every such claim to `findings`); (ii) the same row with a wrong hash does NOT exempt (mutation: skip the hash-recompute comparison, so any pinned row exempts regardless of its hash); (iii) a row without the word `superseded` does not exempt (mutation: drop the `containsSupersededOutsideBackticks` condition, exempting on the hash-and-line match alone); (iv) a pin to a different line does not exempt (mutation: drop the line-in-range check, exempting a claim anywhere in the file once any pin verifies) — each mutation run and reverted, the observed failure recorded in a `RECORDED MUTATION` comment above its test.
Out-of-scope: no ADR touched, no security posture, no wire/data-plane change, no stated guarantee changed — the recognizer's existing claim/existence logic is untouched, and no gate verdict is weakened for a landed piece except the three specific historical spans a valid, hash-checked pin names.
