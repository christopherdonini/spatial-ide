# Preregistration (five-line short form, `AUTONOMY.md` §21d) — verify:test-claims, planned claims

Authority: stop-the-line — `main` red on `verify:test-claims` since 2a939d3 (2026-09-16) — under `PLAN.yaml` node `self-check-false-readings`; `AUTONOMY.md` Appendix A3 (the pre-gate self-checks directive).
Scope: `scripts/plan/verify-test-claims.mjs`, `scripts/plan/verify-test-claims.test.mjs`, `AUTONOMY.md` §6a (one sentence), and this file; <= 120 non-generated lines.
Change: a claimed test name is PLANNED, not binding, when the claiming file is the `gate` of a `PLAN.yaml` node whose `status` is not `done` — such findings print as advisory (`planned — node <id> is <status>`) and do not set exit 1, and the same claim is binding again once the node is `done`.
Tests+mutation: `a_planned_test_in_an_unlanded_preregistration_is_advisory_not_a_failure` (mutation: remove the planned exemption in `runVerifyTestClaims`, so every unmatched claim becomes a binding finding); `a_claim_in_a_done_nodes_gate_file_is_binding` and `plannedGateFiles_ignores_done_nodes_and_gate_none` (mutation: drop the `status` filter in the planned-gate computation, exempting every gate file whatever its node's status) — each mutation run, its observed failure text recorded in a `RECORDED MUTATION` comment above the test, then reverted.
Out-of-scope: no ADR touched, no security posture, no wire/data-plane change, no stated guarantee changed — the check's recognizer is untouched and no gate verdict is weakened for a landed piece.
Amendment: (class 2, deviation with its reason, written 2026-09-16 after the reviewer gate) the declared <= 120 non-generated lines was exceeded — 136 insertions / 7 deletions at the gate, 157 / 7 after the gate-driven additions below — over §21c's 150 on the all-files count (149 code-only; §21c states no counting rule, which is the human's to settle), so the custodian takes the architect gate as well rather than reading the bound in the piece's favour; the excess is the recorded-mutation comments above the three tests and the PLAN.yaml fail-closed branch in main(). The Scope line is not edited to match.
Amendment: (class 5, a scope-narrowing on a gate finding, written 2026-09-16 after the reviewer gate) the reviewer's finding 4, verbatim: "The exemption can reach an ADR — an §21a-category file exempted by a plan edit. `claimFiles` scans preregistrations and `docs/adr/*.md` (`scripts\planerify-test-claims.mjs:148-150`), while the exemption is purely gate-membership. No node sets an ADR as `gate` today … but nothing prevents it. One-line hardening that matches the stated rationale (the before-code rule is a *preregistration* rule): intersect the exempt set with `/PREREGISTRATION.*\.md$/`." Applied: `PLANNABLE_RE` in `plannedGateFiles`, with the test `an_adr_named_as_a_gate_is_never_planned`.
Amendment: (class 5, a scope-narrowing on a gate finding, written 2026-09-16 after the architect gate) the architect's finding 5, verbatim: "The code is a union over nodes: `…erify-test-claims.mjs:157-168` skips `done` nodes but a single non-`done` node naming the same gate keeps the whole file exempt." Applied: binding is sticky — a gate file named by any `done` node is never planned (`plannedGateNotes`), with the test `a_gate_file_named_by_any_done_node_is_never_planned`; the §6a sentence reads "a node naming it".
